"""SC01–SC48 coverage decisions, not execution results.

全部覆盖 means every compatible, applicable value in the global factor catalog,
not its Cartesian product. Each row states exceptions and how to split runs.
The compact records below are explicit authored content; helpers only normalize
the records into the public contract consumed by the Markdown renderer.
"""

FACTOR_CATEGORIES = (
    "环境与功能范围",
    "表结构、存储与物理状态",
    "列类型、属性与非目标列",
    "索引、约束与对象依赖",
    "数据值、规模与分布",
    "ALTER 语法、算法、锁与同句动作",
    "并发、事务、DDL 阶段与 row log",
    "主备、故障、恢复与客户端",
    "结构、数据、索引、性能与交付判定",
)


def _lines(value):
    return [line.strip() for line in value.strip().splitlines() if line.strip()]


def _policy(goal, risk, prerequisites, scopes, observations, acceptance):
    scope_rows = _lines(scopes)
    if len(scope_rows) != len(FACTOR_CATEGORIES):
        raise ValueError("Each scenario must explicitly specify all nine factor categories")
    return {
        "goal": goal,
        "risk": risk,
        "prerequisites": _lines(prerequisites),
        "factor_scopes": {
            category: dict(zip(("requirement", "range", "strategy"), row.split(" | ")))
            for category, row in zip(FACTOR_CATEGORIES, scope_rows)
        },
        "observation_points": [
            dict(zip(("object", "evidence", "decision"), row.split(" | ")))
            for row in _lines(observations)
        ],
        "acceptance_additions": _lines(acceptance),
    }


SCENARIO_POLICIES = {
    "SC01": _policy(
        "在自增主键到达旧整数上限前在线扩宽，证明旧行重建与新范围分配都正确。",
        "聚簇键编码变化可能使二级索引回表错误，客户端取整或计数器持久化错误可能表现为回绕、重复 ID。",
        """
        建立 SIGNED/UNSIGNED 自增 INT 主键源表，计数器接近各自旧上限，源表和多个二级索引已核验。
        保存完整 AUTO_INCREMENT、NOT NULL、默认值及行格式，预备文本或精确整数协议校验器。
        两项功能固定 ON；阿里类型 INSTANT OFF；阿里主机与我方主备版本、SQL 和逻辑数据已对齐。
        插入并发数、QPS、持续时间、资源预算和复制超时运行前写入清单；停止后恢复连接并清理测试表。
        """,
        """
        全部覆盖 | 已有匹配版本和固定 ON 环境；不切换功能开关 | 阿里主机先跑，我方主备逐一核验
        全部覆盖 | 全局兼容自增主键表型、行格式、表空间、历史和物理状态；临时/非 InnoDB 为资格负控 | 各表型独立源表，分区键受限转 SC23，FK 过渡转 SC19
        全部覆盖 | 自增整数符号、合法扩宽边和列属性及非目标列；本场景主线 INT→BIGINT | 保留主线旧上限穿越，其他整数边关联 SC04 的逐边证据
        全部覆盖 | 自增主键及兼容二级索引角色；特殊索引转 SC14–SC18 | 每个旁索引同时做覆盖和回表比对
        全部覆盖 | 自增旧上下界、新范围和精确 64 位值、合法 NULL 旁列与规模分布 | 独立期望生成器记录分配和提交，不要求 ID 连续
        指定覆盖 | 显式 INPLACE/LOCK=NONE，完整重述自增定义 | 保存实际算法和锁证据，不以耗时推断
        边界覆盖 | 插入提交、回滚、显式大 ID，扫描到切换的有界写入 | 记录阶段内至少一次真实提交及事务账本
        专属子运行 | 我方追平副本及重启后继续分配，精确整数客户端 | 主备分别匹配期望，重启在独立恢复夹具执行
        全部覆盖 | 定义、旧新行、ID 唯一性、二级回表和复制位点 | 允许自增空洞，任何重复或已提交丢行判失败
        """,
        """
        自增目标与在线窗口 | 实际算法/锁记录和重建期间插入提交时间 | INPLACE 且允许窗口有写提交才通过；无阶段证据 BLOCKED
        分配器 | 旧上限两侧 ID、回滚分配和显式大 ID 账本 | 不回绕、不重复通过；合法空洞不判失败
        二级引用 | 各索引回表与按业务标识全表读回结果 | 所有已提交行精确一致通过；缺行或错误回表失败
        列元数据 | 前后 SHOW CREATE 和省略自增列的后续插入 | BIGINT 和完整目标属性一致通过；属性丢失失败
        副本与重启分配 | 最终提交位点、主备独立期望及重启后新 ID | 追平且精确一致通过；追平超时 BLOCKED；丢精度失败
        """,
        "新 ID 进入旧范围外且仍精确；允许回滚产生自增空洞。重启结果不能替代在线阶段证据。"),
    "SC02": _policy(
        "验证复合主键首、中、末成员扩宽时，扫描前后改键能形成唯一正确的聚簇和二级记录。",
        "旧新键映射及 row log 回放顺序错误可能留下幽灵旧键、重复新键或指向旧聚簇记录的二级项。",
        """
        三个独立源表分别采用 PK(c,a,b)、PK(a,c,b)、PK(a,b,c)，另设稳定业务标识。
        建立多个旁二级索引和同前导键相邻边界数据；单成员与多成员 ALTER 分开。
        正式扫描阶段观测或同步入口可用；缺失不能假称命中扫描前后。
        改键、删除重插负载写明并发、持续时间、资源、超时和停止条件；保留提交账本，恢复后清理源克隆。
        """,
        """
        全部覆盖 | 兼容固定 ON 环境与我方主备 | 同源逻辑数据先做阿里主机对照
        全部覆盖 | 全局兼容复合主键表型和物理状态；路由键/FK 受限交 SC19–SC23 | 首中末三表分别展开兼容载体
        全部覆盖 | 复合 PK 中已交付整数、字符和二进制安全扩宽及旁列属性 | 每种类型选完整合法值边界，DECIMAL 依冻结范围
        全部覆盖 | 复合 PK 位置与全部兼容旁索引角色；特殊索引单列 SC14–SC18 | 比对聚簇顺序、各旁索引和唯一拒绝
        全部覆盖 | 相同前导键、相邻边界键、空/小/大表和目标完整值域 | 用稳定业务标识建立旧键到新键的独立期望
        指定覆盖 | 单成员和多成员显式 INPLACE/NONE | 每组独立克隆，禁止将换 PK 操作混成扩宽
        边界覆盖 | 未扫描/已扫描改 PK、再更新、删除重插 | 阶段屏障配合 COMMIT 顺序记录，轮数有限
        指定覆盖 | 我方所有副本追平，旧新连接精确复合键读取 | 分别比对事务终态和二级回表
        全部覆盖 | 键唯一性、旧键清除、旁索引、定义、主备 | 以业务标识多重集验收而非只数总行
        """,
        """
        扫描命中 | 正式阶段记录与每次改键事务时间线 | 两时点均命中通过；未证明时点 BLOCKED
        旧新聚簇键 | 业务标识对应的旧键/新键点查和顺序扫描 | 旧键消失且新键唯一通过；幽灵或重复失败
        二级索引引用 | 每个旁索引覆盖及回表与表扫多重集 | 各路径一致通过；只检查一个索引不通过
        算法锁及元数据 | 请求和实际算法、锁、复合键顺序与成员类型 | INPLACE/NONE 和完整目标定义一致通过；静默降级失败
        提交终态与副本 | 删除重插账本、主备逐业务标识结果 | 已提交最终版本准确且副本追平通过；超时 BLOCKED
        """,
        "三种 key part 位置分别给结论；单键路径成功不能覆盖复合键更新路径。"),
    "SC03": _policy(
        "证明无显式主键时，隐式聚簇唯一键与隐藏 row_id 两种组织都能完成唯一索引重建。",
        "错误识别聚簇键或 nullable UNIQUE 的 NULL 语义会误拒合法行、丢行或放过非空重复。",
        """
        分别建立首个 UNIQUE NOT NULL 聚簇源表及仅 nullable UNIQUE 的隐藏 row_id 源表。
        每行加入稳定业务标识但不改变聚簇键选择；保存建表索引顺序和完整列定义。
        NULL 比例为零、混合、全 NULL 的适用组各自铺数，准备重复非空 INSERT/UPDATE 探针。
        固定 ON，精确类型转换期望和我方副本已准备；有界更新设置并发、持续时间、资源预算及超时，停止后恢复并清理夹具。
        """,
        """
        全部覆盖 | 固定 ON 的适用实例；OFF 不在本轮 | 同构数据比较两种聚簇组织
        全部覆盖 | 全局兼容无显式 PK 表型、格式、历史及页状态；要求能合法建表 | 分区要求合格唯一键时验证资格，不冒充隐藏 row_id
        全部覆盖 | 可作为相关唯一键的已交付安全类型、NULL 属性及非目标列 | NULL 与非空类型边界分别装数
        全部覆盖 | 首个非空 UNIQUE、nullable UNIQUE 与兼容旁索引；特殊索引专属 SC14–SC18 | 保留创建顺序并查实际定义
        全部覆盖 | 无/混合/全 NULL、唯一非空端点、相邻值及规模分布 | 独立稳定标识逐行核验
        指定覆盖 | 已有唯一索引载体的显式 INPLACE/NONE 扩宽 | 区分 ADD UNIQUE，不借新增成功替代维护证据
        边界覆盖 | 合法更新、非空重复、NULL↔非 NULL 和回滚 | 记录业务拒绝与 DDL 结果的不同事务
        指定覆盖 | 我方主备和精确整数/字节驱动 | 追平后按业务标识独立读回
        全部覆盖 | 聚簇定位、NULL 约束、元数据、索引及副本 | 重复非空必须拒绝，合法多 NULL 不误报
        """,
        """
        聚簇组织 | 源索引顺序、可用引擎元数据和稳定标识读回 | 两类组织证据充分通过；无法辨别组织 BLOCKED
        NULL 唯一语义 | 多 NULL 插入与 NULL↔非 NULL 事务回执 | 合法 NULL 共存通过；误拒或非空重复被接受失败
        重建在线行为 | 实际 INPLACE/NONE 与期间更新提交 | 正确算法且有提交通过；无窗口证据 BLOCKED
        行与索引定位 | 稳定标识全行、UK 点查和旁索引回表 | 所有路径符合独立期望通过；定位错行失败
        新类型与复制 | 目标定义、新范围值、追平后主备精确结果 | 元数据和每端期望一致通过；副本未追平 BLOCKED
        """,
        "两种聚簇组织均需独立结论，不能用全表行数相等代替稳定标识逐行验证。"),
    "SC04": _policy(
        "用完整类型值域区分安全扩宽与只对当前 0/1 数据看似安全的符号变化或缩窄。",
        "按现存数据而非声明值域决定增强资格会误接纳不安全变更，非严格模式还可能掩盖截断。",
        """
        SIGNED 和 UNSIGNED 的十条同符号上升边逐条列入清单，BIGINT 无更宽整数作为负控。
        每条危险边分别准备仅 0/1 和含合法旧端点的独立源表，源表先成功建成。
        UNSIGNED→更宽 SIGNED 的正式支持范围已冻结；未冻结只登记待决。
        预置 STRICT/非 STRICT 环境、精确十进制整数比较器及结构快照；COPY 控制使用独立克隆。
        各整数边设置并发写数、持续时间、资源预算与 DDL/复制超时；端点负向结束即停止，恢复连接并清理各边克隆。
        """,
        """
        全部覆盖 | 固定功能参数与已有 SQL mode 组 | 不为得到目标结果切换增强开关
        全部覆盖 | 全局兼容整数表型、格式、存储与历史；FK/分区键交专属关系场景 | 各边遍历兼容载体，无效 CREATE 单列
        全部覆盖 | 同符号十条边各 SIGNED/UNSIGNED、跨符号包含边、同宽/逆向、别名和属性 | 资格按规范化定义和完整范围计算
        全部覆盖 | 整数目标的全部兼容索引角色和旁依赖；特殊依赖交 SC14–SC23 | 无索引与每种索引角色均有边界子运行
        全部覆盖 | min/min+1/-1/0/1/max-1/max、新上限、2^53 与 64 位端点 | 仅合法旧值入源表，新值在成功后写入
        全部覆盖 | 本场景适用 MODIFY/CHANGE、INPLACE、DEFAULT 与独立 COPY 入口 | 记录实际算法，显式 INPLACE 结果单列
        指定覆盖 | 成功路径有界在线写、失败前已提交事务 | 写线程有次数/超时，分别保留提交与 DDL 回执
        指定覆盖 | 我方副本精确大整数与文本/预处理读取 | 主备分别匹配整数期望而非相互近似比较
        全部覆盖 | 资格、错误阶段、值转换、属性及失败原子性 | 0/1 与端点组危险边不得因数据巧合改变增强契约
        """,
        """
        值域资格 | 源目标规范化类型和数学范围包含表 | 符合冻结安全范围通过；当前小值导致误接纳失败
        负向原子性 | 两种数据组的 errno/SQLSTATE、阶段和前后结构 | 不安全边准确拒绝且旧态完整通过；部分变更失败
        精确数值 | 字符串整数期望与旧新边界读回 | 全值精确通过；浮点比较不能作为通过证据
        在线算法与属性 | 请求/实际算法锁、期间提交和目标属性 | 正向真实 INPLACE 且符合锁契约通过；COPY 仅控制不替代
        主备边界 | 最终位点和各端 64 位值、新范围写入 | 每端符合预期通过；契约未冻结或追平不足 BLOCKED
        """,
        "显式 INPLACE 危险边、DEFAULT 与 COPY 各自判定；错误必须匹配第一失败阶段。"),
    "SC05": _policy(
        "验证 VARCHAR 最大字节长度跨 255/256 时在线重建正确并保留既有字符前缀索引长度。",
        "把字符数当字节数或将前缀自动扩大，会漏测长度头变化并改变唯一冲突集合。",
        """
        latin1 255→256、utf8mb3 85→86、utf8mb4 63→64 与三种必测行格式组成九个独立夹具。
        源字符集、排序规则、固定前缀和实际字节限额已核验，前缀普通/唯一组分别建表。
        数据含旧最长多字节串、同前缀不同尾部、NULL、空串和新长度目标值。
        预备 CHAR_LENGTH/OCTET_LENGTH/字节期望及主备追平入口；并发读写有时限、QPS 和清理边界。
        九个跨头子运行分别登记持续时间、资源预算、DDL/复制超时和停止阈值；停止后恢复连接及清理最长串夹具。
        """,
        """
        全部覆盖 | 已有固定 ON、三字符集适用环境 | 先阿里主机同 SQL，再我方主备
        全部覆盖 | 全局兼容 VARCHAR 表型/存储/历史；三必测格式不能抽样 | 九组为强制子运行，分区键限制转 SC23
        全部覆盖 | 跨头及同头 VARCHAR 扩长、相关完整属性和非目标列 | 字符集/排序规则保持，同头作为原生基线
        全部覆盖 | 全部兼容前缀与完整 key part 角色；特殊索引转专属场景 | 记录每个 prefix 的字符长度不自动扩展
        全部覆盖 | 最大字节边界、四字节字符、组合字符、NULL/空串及分布 | 同字符数不同字节数成对比较
        指定覆盖 | INPLACE/NONE 仅扩列与同长度头控制 | 保留源前缀定义，显式改前缀交 SC11
        边界覆盖 | 旧定义内在线更新、提交后新长值 | 阶段提交账本区分旧/新定义写入资格
        指定覆盖 | 连接编码和我方主备追平 | 各端按字节及字符长度分别比较
        全部覆盖 | 长度头数据、前缀维护、实际算法锁、结构、副本 | 九组分别判定，不以一种编码成功外推
        """,
        """
        字符与字节边界 | 九组源目标定义、CHAR_LENGTH 与 OCTET_LENGTH | 每组确实跨最大字节头且读回正确通过；未跨头失败覆盖
        固定前缀 | SHOW INDEX、同前缀尾部不同探针 | prefix 不变且冲突集合正确通过；自动扩展失败
        在线重建 | 实际算法锁与 DDL 期间旧定义合法提交 | INPLACE/NONE 且写提交通过；阶段不可证 BLOCKED
        字节和旁列 | 全行字节期望、NULL/空串和新长值读回 | 无截断错位通过；编码替换或旁列变化失败
        副本与访问路径 | 主备追平、表扫与各前缀索引结果 | 每端匹配独立预期通过；未追平 BLOCKED
        """,
        "九个字符集边界与行格式组合逐一留证；同长度头原生成功不计跨头增强通过。"),
    "SC06": _policy(
        "验证 CHAR/VARCHAR 扩长过程中 PAD/NO PAD 与排序规则的唯一等价类和保存点撤销保持一致。",
        "尾空格规范化或回放撤销错误可能把合法提交当重复、把回滚行复活，或改变唯一比较语义。",
        """
        PAD SPACE/NO PAD 与 CHAR/VARCHAR 分别建合法源表，先验证当前排序规则的 a 与 a空格比较结果。
        为 _ci/_cs、_ai/_as 合法排序规则准备相应大小写和重音探针，不能将不支持组合当源夹具。
        每条 SAVEPOINT/ROLLBACK TO/COMMIT 序列有独立期望，重复探针与合法事务分开。
        固定 ON；在线线程有并发数、持续时间、资源限额和超时，停止后结束事务、恢复连接并清理。
        """,
        """
        全部覆盖 | 兼容固定 ON 及已存在 PAD/NO PAD 环境；OFF 范围外 | 每种实际排序规则先建立原定义基线
        全部覆盖 | 全局兼容字符唯一键表型、行格式和历史 | 特殊 FK/路由依赖使用 SC19–SC23 的专属规则
        全部覆盖 | CHAR/VARCHAR 安全扩长、字符属性、默认值和非目标列 | CHAR 返回语义与 VARCHAR 存储字节分开期望
        全部覆盖 | 兼容普通/唯一及复合索引角色；MVI/函数等交 SC14–SC18 | 每个唯一索引单独打等价类探针
        全部覆盖 | a、a空格、b空格、NULL、空串、全空格、大小写/重音边界 | 正反例依据源排序规则，不硬编码等价结论
        指定覆盖 | 保持 charset/collation 的显式 INPLACE/NONE | 改排序规则不并入本安全扩长成功数
        边界覆盖 | 临时占键、保存点撤销与最终合法提交 | 固定事件序列记录每次事务结果
        指定覆盖 | 连接字符编码及我方主备精确比较 | 副本按同规则再执行只读等价判断
        全部覆盖 | 唯一等价、撤销终态、元数据、算法锁和副本 | 业务重复错误不掩盖合法提交丢失
        """,
        """
        比较等价类 | DDL 前后排序规则与成对字符比较、重复回执 | 等价类未改变通过；规则无意漂移失败
        保存点撤销 | SAVEPOINT 账本及最终行多重集 | 撤销行不出现、合法提交保留通过；回滚复活失败
        唯一索引维护 | 每个 UK 的点查、表扫及非空重复探针 | 所有约束按对应规则执行通过；漏约束失败
        在线锁与属性 | 实际算法锁、阶段内提交、charset/collation 定义 | 正确在线路径和属性通过；无证据 BLOCKED
        副本和尾空格 | 每端精确读取与预期的 CHAR/VARCHAR 长度语义 | 各端独立一致通过；驱动裁剪使证据不明 BLOCKED
        """,
        "不以 HEX 始终不变代替 CHAR 目标语义；各排序规则业务重复与回放失败分开记录。"),
    "SC07": _policy(
        "证明 BINARY 扩長对旧值按目标长度补零，同时保持普通与唯一前缀的字节冲突规则。",
        "误用 VARBINARY 的原字节长度不变预期会漏掉定长补零错误或误报正确转换，前缀单位错误会改变约束。",
        """
        准备 BINARY(20)→(40)、(254)→(255) 合法源表和独立 (255)→(256) 定义负向。
        普通前缀与 UNIQUE 前缀各自建表，同前缀尾部不同的唯一组先使用合法源集合。
        旧字节含首中末 0x00、0xFF、空输入，期望按目标 BINARY 长度补零生成。
        三行格式与首中末列位源定义已验证；并发更新、追平与清理设明确任务数和超时。
        二进制更新持续时间与长值资源预算预先登记；停止后回滚未决探针、恢复连接并清理各补零源克隆。
        """,
        """
        全部覆盖 | 固定 ON 且支持 BINARY 定义的环境 | 资格负向不更换算法冒充增强
        全部覆盖 | 全局兼容 BINARY 表型、格式、空间与历史；单侧 FK 扩长交 SC21 | 三行格式和首中末位置分别执行
        全部覆盖 | BINARY 合法长度边、NULL/默认值及非目标类型属性 | 255→256 保留定义拒绝，不能制造非法源表
        全部覆盖 | BINARY 兼容全部索引角色和字节前缀；特殊索引专属 | 固定前缀与完整 key part 各自核验
        全部覆盖 | 零字节、FF、共同前缀、合法端点、NULL 及规模分布 | 生成显式补零期望并与全字节结果比较
        指定覆盖 | 只扩长度 INPLACE/NONE 与超定义上限负向 | 正反组从独立源表执行
        边界覆盖 | 重建中旧长度合法更新与提交后新长度写 | 用事务账本关联转换前后的字节值
        指定覆盖 | 二进制安全协议及我方副本 | 传输参数使用字节，不经字符串编码替换
        全部覆盖 | HEX/LENGTH、前缀约束、旁列、算法锁和主备 | 按目标补零预期判定，不要求旧 HEX 完全不变
        """,
        """
        定长转换 | 前后 HEX/LENGTH 与显式补零期望 | 每个旧值目标长度补零正确通过；截断或不补零失败
        前缀冲突 | SHOW INDEX 及同前缀不同尾部写探针 | 普通允许、唯一依定义拒绝通过；语义混淆失败
        非法目标原子性 | 255→256 错误阶段及旧定义/字节快照 | 准确拒绝且无改动通过；源未建成 BLOCKED
        在线与旁列 | 期间提交、算法锁及全列精确读回 | 真实 INPLACE 且旁列不受损通过；无在线证据 BLOCKED
        主备字节 | 追平位点及各端新旧范围 HEX | 各端符合补零期望通过；任一字节错配失败
        """,
        "父子 BINARY 外键单侧扩长限制由 SC21 单独证明，不作为普通二进制扩长失败归因。"),
    "SC08": _policy(
        "验证 VARBINARY 跨长度头及页外长值扩容保持实际字节，并在本表真实 Lmax 外原子拒绝。",
        "宽行与页外引用变化可能造成截断或错链，把 65535 误当每表可用上限会产生无效夹具。",
        """
        根据实际行格式、页大小及旁列计算可建表 Lmax，准备 Lmax-1/Lmax/Lmax+1 目标组。
        跨 255→256 和页外长值是独立夹具，源表全部可创建并已读回验证。
        准备空字节串、NULL、长共同前缀和不同尾部，期望保留字节及实际长度。
        长值替换线程限定并发、持续时间、内存/临时空间资源和超时；停止后结束事务、恢复连接并清理测试空间。
        """,
        """
        全部覆盖 | 固定 ON、现成页大小/表空间环境 | 每个配置单独计算有效边界
        全部覆盖 | 全局兼容宽表、页内/页外、格式、历史与表型；路由/FK 交专属 | 不把单一 Lmax 复用于其他结构
        全部覆盖 | VARBINARY 合法扩长和跨头、NULL/默认值及全部旁列 | 区别 BINARY 补零语义
        全部覆盖 | 全部兼容字节前缀、完整及复合索引角色；特殊索引专属 | 校验每个前缀单位与总字节限额
        全部覆盖 | 空串/NULL/最长值、Lmax 邻域、页内外阈值和分布 | 合法旧值装源表，超界只用于目标或写入负向
        边界覆盖 | INPLACE/NONE 合法目标和超界一档 | 记录准备、资格或执行第一失败阶段
        边界覆盖 | 扫描和回放期间长值替换、删除重插 | 提交账本生成最终完整字节期望
        指定覆盖 | 二进制协议与我方副本追平 | 各端读取页外长值并完整比较
        全部覆盖 | 实际长度、页外读回、索引、原子拒绝及清理 | 超界失败不能丢已提交业务
        """,
        """
        Lmax 资格 | 实际页/行格式、旁列和成功源定义及目标错误 | 合法边界成功或按冻结能力判定通过；无有效源 BLOCKED
        变长字节 | 全量 HEX/LENGTH 与独立字节期望 | 不补零不截断通过；长度或字节改变失败
        页外访问 | 多次完整长值读回、旁索引回表和可用存储校验证据 | 页外引用可读且结果一致通过；错链/读错失败
        在线原子性 | 算法锁、长值提交账本及超界前后 schema | 在线合法提交保留、失败旧定义完整通过；部分 DDL 失败
        副本与空间 | 追平后各端长值及任务后临时空间基线 | 每端正确且清理完成通过；残留或丢行失败
        """,
        "合法/超界结论绑定具体完整源结构；页外读取证据不能用总行数或短摘要替代。"),
    "SC09": _policy(
        "按已交付范围验证 DECIMAL 的 M 增而 D 不变，跨整数编码组后复合唯一键仍精确。",
        "编码组重排或浮点期望会掩盖小数舍入、符号损坏及索引顺序错误，未冻结增强不能凭执行成功承诺。",
        """
        正式冻结 DECIMAL 精度增强范围和 INPLACE 资格；缺失时相关运行 BLOCKED。
        建立整数部分跨 9 位组及残余位档位夹具，每组 ALTER 前后 D 固定，覆盖 D=0/1/2/9。
        十进制字符串生成正负极值、零、最小非零小数、相邻值和旧界外新合法值。
        复合普通/唯一索引和默认值已预校验；有界在线写、复制超时及临时表清理策略已登记。
        十进制在线写限定并发、持续时间、资源和停止阈值；恢复连接后先对账精确提交再清理各精度组。
        """,
        """
        全部覆盖 | 固定 ON 且 DECIMAL 增强正式交付的配置 | 未冻结配置 BLOCKED，不外推到未交付精度
        全部覆盖 | 全局兼容 DECIMAL 表型、行格式、存储和历史；关系受限转专属 | 各载体保留精度编码边界子运行
        全部覆盖 | M 增/D 固定、残余档位、合法属性与旁列；改 D/非法 M 负向 | 以十进制精确算术构建目标期望
        全部覆盖 | DECIMAL 全部兼容索引角色，重点复合 UK 各位置；特殊依赖专属 | 每个索引做等值/范围/排序和重复探针
        全部覆盖 | 正负端点、负零规范化、相邻小数、NULL、新范围及规模分布 | 禁止浮点作为输入期望或比较中间态
        边界覆盖 | INPLACE/NONE 精度扩展，D 改变和非法精度负向 | COPY 另克隆控制，保留第一错误阶段
        边界覆盖 | DDL 内旧范围提交、完成后新范围提交与回滚 | 账本记录字符串金额及实际事务结果
        指定覆盖 | 精确 DECIMAL 协议和我方主备 | 各端按十进制预期逐行校验
        全部覆盖 | 精度/标度元数据、数值等价、索引、在线性及副本 | 未冻结门槛不计增强 PASS
        """,
        """
        交付与编码边 | 正式契约、M/D 定义及跨组输入清单 | 范围已冻结且边界命中通过；契约缺失 BLOCKED
        精确十进制 | 字符串期望与旧新值、负零规范化结果 | 无舍入损失且符号正确通过；浮点近似不作通过
        复合唯一访问 | 各 key part 点查、范围、排序和重复回执 | 所有路径与唯一关系正确通过；错误顺序或漏约束失败
        在线及负向阶段 | 实际算法锁、阶段提交和改 D/非法精度错误 | 合法路径符合契约且负向原子拒绝通过；部分变更失败
        元数据和副本 | 精度/标度/默认值与追平后每端精确数据 | 各端匹配独立目标通过；复制精度损失失败
        """,
        "每条 ALTER 保持 D 不变；改 D 的 COPY 转换不能计入本增强成功。"),
    "SC10": _policy(
        "证明扩宽叠加属性重述、CHANGE 改名和 FIRST/AFTER 调序后，元数据与全列实际读写准确一致。",
        "遗漏属性被错误当作自动保留、列位置错位或旧句柄字段信息过期，会使结构表面正确但默认写入和读回错误。",
        """
        建立完整重述和故意遗漏属性两组源克隆，独立计算每条目标定义的原生属性语义。
        源表含 NOT NULL、常量/表达式 DEFAULT、中文转义 COMMENT、INVISIBLE 及非目标特殊列。
        首中末位置、合法 CHANGE/FIRST/AFTER 与非法默认值括号语法有独立 SQL。
        旧长连接与新连接、显式列投影和省略列插入均已准备；有界写入与连接/复制超时、清理计划就绪。
        旧新连接并发、持续时间及句柄资源上限固定；异常停止后核实目标字段，恢复会话并清理长连接与探针。
        """,
        """
        全部覆盖 | 固定 ON 和既有 SQL mode/连接编码环境 | 相同规范化目标定义先做阿里主机对照
        全部覆盖 | 全局兼容宽/窄及历史表型、格式和物理状态；特殊关系专属 | 首中末列位置逐组验证全列对齐
        全部覆盖 | 已交付安全类型及 NULL/DEFAULT/COMMENT/INVISIBLE/自增、全部旁列 | 重述、遗漏、显式改属性三个分支分别计算预期
        全部覆盖 | 全部兼容索引和依赖角色；表达式/函数索引/FK 专属 SC16–SC21 | 改名后的依赖使用各自正式规则
        全部覆盖 | 完整类型边界、默认值、NULL、中文引号转义及分布 | 显式写和省略列写分别验收
        全部覆盖 | 本场景适用 MODIFY/CHANGE、改名、FIRST/AFTER 和属性语法 | 非法位置和缺括号分别保留第一错误阶段
        边界覆盖 | 旧/新定义切换前后有界写与旧连接读 | 按提交边界判字段名称和位置，记录锁行为
        指定覆盖 | 旧长连接、新连接、prepared 元信息及我方副本 | 旧句柄刷新机制按客户端契约，不假设静默刷新
        全部覆盖 | 精确列序、默认写、不可见投影、索引与副本 | SELECT * 不出现不能免除不可见列核验
        """,
        """
        完整目标定义 | 前后 SHOW CREATE、字段序号、NULL/DEFAULT/COMMENT | 与该语句完整目标一致通过；误保留或误丢属性失败
        列顺序和旁列 | 显式全列投影、FIRST/AFTER 后稳定行期望 | 所有列值不串位通过；旁列变化失败
        默认与不可见列 | 省略列 INSERT、显式不可见投影和 SELECT * | 各入口符合目标语义通过；仅星号查询证据不足 BLOCKED
        算法与错误阶段 | 实际算法锁、切换窗口提交、非法位置/括号回执 | 合法组合交集正确及负向原子拒绝通过；部分成功失败
        客户端与副本元数据 | 旧新句柄字段类型长度及追平后每端全行 | 元信息和数据匹配通过；旧字段误解码失败
        """,
        "CHANGE 目标定义必须完整解释；不假定表达式、视图或触发器文本自动改名。"),
    "SC11": _policy(
        "对照仅扩列与同句显式扩大复合前缀，验证最终索引定义及冲突集合分别符合目标。",
        "列变长被误当作索引前缀变长，或字符/字节单位混用，会错误放行、拒绝唯一键。",
        """
        两组独立克隆分别保留前缀与 DROP/ADD 显式改前缀，不复用已变更的源表。
        目标列在复合索引首中末位置分别建表，其他 key part 数据真实不同。
        CHAR/VARCHAR 和 BINARY/VARBINARY 分别按字符与字节准备 NULL、短值及相同前缀不同尾部。
        普通与 UNIQUE 组源数据均合法；已核算行格式/页大小索引上限并准备各路径查询。
        前缀探针登记并发、持续时间、资源、事务超时与停止条件；恢复事务终态后清理保持/扩大两组源表。
        """,
        """
        指定覆盖 | 固定 ON 与实际字符集/行格式配置 | 记录算法资格所依赖的环境
        全部覆盖 | 全局兼容前缀索引载体及物理状态 | 不兼容 FK 前缀作为 CREATE 负向交 SC21
        全部覆盖 | CHAR/VARCHAR/BINARY/VARBINARY 安全扩长及旁列属性 | 四类类型按实际转换规则生成期望
        全部覆盖 | 全部兼容普通/唯一、复合首中末、可见性/方向及旁索引；特殊索引 SC14–SC18 专属 | 逐索引检查前缀与查询
        全部覆盖 | 短于前缀、等长、同前缀异尾、NULL/空值和长度边界 | 其他 key part 决定的冲突也入清单
        指定覆盖 | 仅 MODIFY 与 MODIFY 加 DROP/ADD INDEX 两组 INPLACE 入口 | 独立验证整句算法锁交集
        边界覆盖 | 合法更新、前缀重复 INSERT/UPDATE 和回滚 | 有界事务中区分业务冲突和 DDL 失败
        指定覆盖 | 我方主备和字符/二进制精确客户端 | 追平后重放只读访问路径
        全部覆盖 | 前缀定义、冲突集合、访问路径、依赖与原子性 | 各组使用自己的目标预期
        """,
        """
        前缀元数据 | SHOW INDEX 的 SUB_PART 与完整目标 DDL | 保持组不变、扩大组显式改变通过；自动漂移失败
        单位边界 | 多字节字符、二进制长度与实际索引定义 | 字符/字节单位正确通过；用字符数代字节限额失败
        复合唯一约束 | 同前缀不同其他 key part 的 INSERT/UPDATE 回执 | 仅真实目标重复拒绝通过；误拒或放过失败
        访问路径 | 每个复合索引点查/范围/回表与表扫 | 全部兼容索引结果相同通过；缺项失败
        算法与原子依赖 | 实际算法锁、索引列表、失败快照及主备位点 | 整句交集正确且无残留依赖通过；未冻结能力 BLOCKED
        """,
        "保持前缀与扩大前缀必须分别结论，不能共享同一冲突期望。"),
    "SC12": _policy(
        "在真实索引字节限额两侧验证扩宽及同句动作的整句原子性。",
        "统一套用 3072 字节或只算单 key part 会遗漏旧行格式、页大小和复合总长越界。",
        """
        读取实例页大小、表行格式及源索引定义，按实际配置计算每组合法限额。
        单 key part 与复合总长各准备合法上界、超过一字节目标，源结构必须成功创建。
        多字节字符采用真实字节占用的边界值，不仅写 ASCII 短值。
        同句增加另一合法动作并保存完整源快照，普通/唯一及各访问路径预校验。
        边界组设置并发业务、持续时间、临时空间资源和超时；错误后停止该组，恢复原结构可用性并清理克隆。
        """,
        """
        全部覆盖 | 已有页大小与固定 ON 配置 | 配置不符单列 BLOCKED，不临时改实例参数
        全部覆盖 | 全局兼容索引表型及格式；DYNAMIC 与 COMPACT/REDUNDANT 分开 | 每类重新计算限额
        指定覆盖 | 多字节字符及二进制扩长、旁列行宽影响 | 保持字符集并精确计算长度
        全部覆盖 | 全部兼容普通/唯一/复合/前缀/方向/可见性索引；特殊索引专属 SC14–SC18 | 分别检查成员和总字节上限
        边界覆盖 | 合法上界与超一字节，旧最长及新最长值 | 源值合法与目标定义超界分离
        指定覆盖 | 扩宽加合法附加动作、INPLACE 与独立 COPY 控制 | 两种子句顺序检查整句回滚
        指定覆盖 | 失败前后有界合法提交 | 业务提交账本不随 DDL 回滚删除
        指定覆盖 | 我方主备与元数据查询 | 每端对照源/目标快照
        全部覆盖 | 错误阶段、结构/索引原子性、约束与可用性 | 源 CREATE 失败不算 ALTER 负向通过
        """,
        """
        实际字节限额 | 页大小、行格式、全部 key part 计算表 | 计算绑定真实结构通过；套用统一上限失败覆盖
        合法边界访问 | 目标定义、索引与表扫、最大值写入 | 合法目标按冻结能力完成且结果正确通过
        超界错误 | 第一错误阶段、errno/SQLSTATE 和原始 SQL | 指定定义越界准确拒绝通过；无效源 BLOCKED
        同句原子性 | 附加动作及源结构/索引前后快照 | 所有 DDL 子动作均未留下通过；部分成功失败
        约束与副本 | 后续重复探针、业务账本和追平结果 | 原约束有效且各端已提交行保留通过；差异失败
        """,
        "合法限额随实际配置记录；源准备失败单列，不能降低目标长度后覆盖原失败。"),
    "SC13": _policy(
        "验证降序、不可见和覆盖索引在扩宽后维护正确，并用真实不同访问路径交叉核验。",
        "所有查询实际走同一路径会掩盖坏索引，不可见索引被误当不维护会漏掉唯一约束缺陷。",
        """
        建立 ASC/DESC 混合及可见/不可见的多个源索引，目标列首中末位置各有夹具。
        预备表扫、覆盖、回表和排序查询，并确认各查询实际执行计划。
        不可见索引的访问资格按已允许会话条件处理；不能用无效 FORCE INDEX 证明维护。
        并发更新限定任务数与超时；ANALYZE 前后独立留证，保存源数据与索引元信息。
        查询与更新持续时间、连接/临时空间资源预算及停止阈值登记；恢复事务后清理探针与独立索引夹具。
        """,
        """
        指定覆盖 | 固定 ON 与允许的优化器会话条件 | 记录计划条件，不把计划变化当数据差异
        全部覆盖 | 全局兼容多索引表型、行格式和历史 | 每个载体保留访问路径资格证据
        指定覆盖 | 已交付安全扩宽类型及有序边界、旁列 | 精确值支持范围和排序核验
        全部覆盖 | 全部兼容方向、可见性、覆盖/回表、复合角色及旁索引；特殊索引 SC14–SC18 专属 | 逐索引核验维护与元信息
        全部覆盖 | 相邻值、重复非唯一值、NULL、选择性和分布 | 比较结果多重集，排序另用明确 ORDER BY
        指定覆盖 | INPLACE 扩宽和 ANALYZE 前后观测 | 维护动作不混入计时窗口
        边界覆盖 | 重建前后与期间有界索引键更新 | 提交账本提供一致读回终态
        指定覆盖 | 我方副本与多连接查询 | 追平后按同逻辑查询独立比较
        全部覆盖 | 实际计划、结果、排序、可见性与约束 | 计划不同解释原因，结果不同判失败
        """,
        """
        访问路径身份 | EXPLAIN 及实际可用执行证据 | 表扫/覆盖/回表确实不同通过；未区分路径 BLOCKED
        降序顺序 | 明确 ORDER BY 的 ASC/DESC 结果和精确期望 | 单向及混合顺序正确通过；倒序或遗漏失败
        不可见约束 | SHOW INDEX 可见性和唯一重复回执 | 不可见性保留且约束仍有效通过；漏约束失败
        索引维护 | 各路径终态多重集和并发提交账本 | 所有合法提交一致通过；缺行/多行失败
        依赖元数据 | 类型、key part、方向、索引名和主备快照 | 定义及每端结果一致通过；计划证据缺失 BLOCKED
        """,
        "ANALYZE 引起计划改变不自动判 BUG；不可见唯一索引仍须实际重复探针。"),
    "SC14": _policy(
        "用唯一 MVI 的四行最小夹具验证既有索引重建及异常后的恢复可读性。",
        "空数组/SQL NULL 多值索引重建曾触发原生崩溃，恢复成功容易被误用来抹去运行失败。",
        """
        在独立可恢复实例建成唯一 MVI 四行夹具，分别含 []、SQL NULL、[3]、[4]，先证明源查询正确。
        正式冻结已有 MVI 重建的功能/锁资格；没有契约时功能结论 BLOCKED。
        仅执行一次 ENGINE=InnoDB、INPLACE/LOCK=NONE；并发 DDL=1，持续到该次终态，不自动重试。
        运行前登记资源预算、DDL/恢复超时和停止条件；日志先留存再恢复，只清理专属测试表和临时资产。
        """,
        """
        专属子运行 | 固定 ON 的隔离实例，阿里只作必要主机对照 | 崩溃回归与常规批次隔离
        指定覆盖 | 已核验的四行 InnoDB MVI 载体 | 不乘全表型矩阵，失败后使用快照恢复
        指定覆盖 | JSON 数组列和兼容普通旁列 | 不擅自修改未承诺的数组结果类型
        专属子运行 | 唯一 MVI；其他兼容索引维护关联 SC11–SC13，其他特殊索引 SC15–SC18 | 既有重建与 ADD MVI 资格分账
        指定覆盖 | []、SQL NULL、[3]、[4] 四行最小数据 | 按原最小结构保留确定重现条件
        指定覆盖 | 单次 ENGINE=InnoDB、INPLACE/NONE | 不修改锁或算法使原请求假通过
        边界覆盖 | 单 DDL 重建，异常退出与完成两个终态 | 不叠加随机业务干扰最小复现
        专属子运行 | 崩溃日志、正式恢复入口、恢复后只读 | 恢复不覆盖首轮异常记录
        全部覆盖 | 存活、MVI 查询、定义、约束和恢复读回 | 任何断言退出或崩溃均失败
        """,
        """
        源 MVI 资格 | CREATE、SHOW INDEX 与四行数组查询 | 真实源夹具成立通过；源构建失败 BLOCKED
        重建请求 | 原始 SQL、实际算法锁和资格错误 | 成功或准确拒绝按契约通过；契约缺失 BLOCKED
        服务存活 | 进程状态、错误日志、连接断开时间 | 正常存活通过；断言/崩溃无条件失败
        数组访问与唯一性 | 恢复前可用查询及重复数组元素探针 | 结果和约束符合源期望通过；漏项/漏约束失败
        恢复资产 | 正式恢复记录、四行读回及临时对象清单 | 恢复可读只说明恢复通过；首轮崩溃仍失败
        """,
        "阿里异常只保存对照证据，不扩展排查；已有 MVI 重建不能证明新增 MVI 在线资格。"),
    "SC15": _policy(
        "区分既有 FULLTEXT/SPATIAL 载体重建与同句新增特殊索引的资格、锁和查询维护。",
        "将新增限制套到既有重建或把 SHARED 当作可并发写，会形成错误支持结论。",
        """
        FULLTEXT 与 SPATIAL 独立建表，既有索引和同句 ADD 各用独立源克隆。
        源全文词项、空间 SRID/非空资格及可见隐藏 FTS_DOC_ID 元信息预先核验。
        正式冻结每类完整动作的算法/锁要求，准备 NONE 负向与合法锁正向。
        SELECT/写探针设置并发数、持续时间、资源、超时与停止条件；释放后恢复并清理探针事务。
        """,
        """
        指定覆盖 | 固定 ON 与已启用的全文/空间引擎环境 | 资格差异绑定实际构建
        全部覆盖 | 全局兼容全文/空间载体；不支持表型显式资格负向 | 已有与新增独立克隆
        指定覆盖 | 普通已交付扩宽列及全文/空间旁列属性 | 特殊列不执行未承诺类型扩宽
        专属子运行 | FULLTEXT/SPATIAL 已有与 ADD；全部兼容旁索引一并维护，MVI/生成/函数索引另专属 | 每类维护和依赖分别判定
        全部覆盖 | 合法词项、边界空间对象、SRID 与相关普通列完整值边界 | 用确定性词项/空间谓词建立预期
        全部覆盖 | 本场景已有重建/同句 ADD 的适用算法锁入口 | NONE 拒绝和合法锁成功分别结论
        边界覆盖 | 锁持有时 SELECT、写等待和释放后提交 | 有界探针以真实等待链证明行为
        指定覆盖 | 我方副本和特殊查询客户端 | 追平后重新验证词项与空间谓词
        全部覆盖 | 检索结果、SRID/隐藏列、锁、元数据和约束 | 不用普通 BTREE 结论替代特殊索引
        """,
        """
        已有与新增资格 | 两套 SQL、第一拒绝阶段及实际算法锁 | 按各自动作契约正确通过；契约缺失 BLOCKED
        全文维护 | 确定词项查询、源目标行集合和隐藏列元数据 | 词项与对象定义正确通过；丢词项失败
        空间维护 | SRID/非空定义及空间谓词结果 | 路径结果和约束正确通过；错误路由/丢对象失败
        读写锁 | 持锁阶段 SELECT、写等待及释放回执 | 符合实际锁契约通过；SHARED 记写不停失败表述
        旁索引与副本 | 全部兼容旁索引结果和追平后的各端查询 | 每条路径独立一致通过；未追平 BLOCKED
        """,
        "特殊查询正确、资格正确和在线程度分项记录；既有成功不外推 ADD。"),
    "SC16": _policy(
        "验证有索引且表达式不变的虚拟列自身扩宽，在正式 SHARED 契约下允许读并阻塞写。",
        "误把虚拟列自身与基列扩宽混同，或未实际持锁就发探针，会错误声称 LOCK=NONE 在线写支持。",
        """
        正式冻结索引虚拟列整数/字符返回类型扩宽范围；命名虚拟列与函数隐藏列分开。
        源虚拟列确有普通/唯一索引且表达式保持不变，准备 NULL、边界返回值和源求值预期。
        NONE 负向与 SHARED 正向独立克隆，正式锁观测入口可用，否则锁结论 BLOCKED。
        读写并发及持续时间、资源预算、锁/事务超时预先登记；停止后释放事务，恢复连接并清理等待探针。
        """,
        """
        指定覆盖 | 固定 ON 且虚拟列增强已交付配置 | 缺契约 BLOCKED
        全部覆盖 | 全局兼容有索引虚拟列表型与物理状态 | 无索引虚拟列单列资格负向
        全部覆盖 | 已交付整数/字符返回类型安全扩宽、表达式不变和旁列 | 结果类型与表达式分别比对
        专属子运行 | 命名虚拟列全部兼容索引角色；函数隐藏列 SC18，MVI/FULLTEXT/SPATIAL SC14–SC15 | 每个索引做访问和约束检查
        全部覆盖 | 旧返回边界、NULL、新可表示范围与重复结果 | 基列输入合法且由表达式独立求值
        指定覆盖 | 显式 INPLACE/NONE 拒绝，INPLACE/SHARED 正向 | 原 NONE 不降级重报通过
        边界覆盖 | 确认持 SHARED 后 SELECT 与有界写等待、释放后提交 | 每个探针有事务 ID 和唯一提交记录
        指定覆盖 | 多连接读写和我方副本 | 追平后核验返回值及表达式定义
        全部覆盖 | 锁契约、表达式、返回类型、索引/约束和副本 | SHARED 不套在线写无阻塞门槛
        """,
        """
        NONE 负向 | 原请求回执和源定义/索引快照 | 按契约拒绝且无变化通过；自动改锁不计通过
        SHARED 锁窗口 | 真实持锁证据、SELECT 回执与写等待链 | 允许读且阻塞写通过；未命中持锁窗口 BLOCKED
        释放后事务 | 探针事务 ID、释放时间和最终行 | 写恰好提交一次通过；丢失或重复失败
        虚拟求值 | 表达式、结果类型与独立求值结果 | 表达式不变且新返回范围精确通过；计算差异失败
        索引依赖 | 每个索引访问/重复探针及追平副本元数据 | 索引约束和各端结果正确通过；契约未冻结 BLOCKED
        """,
        "无索引、表达式变化和函数隐藏列不能用本命名虚拟列 SHARED 成功代替其资格验证。"),
    "SC17": _policy(
        "用生成结果 INT/BIGINT 成对夹具区分基列扩宽成功与后续生成结果溢出的合法业务拒绝。",
        "新基列范围被错误套到窄生成结果，可能产生截断或部分写；把合法溢出误报扩宽失败会污染结论。",
        """
        两组基列均 INT→BIGINT，仅生成结果 INT/BIGINT 不同，源表达式和业务数据相同。
        VIRTUAL/STORED 及链式依赖源结构真实成立，每个结果列及索引有独立求值期望。
        准备使窄结果溢出但宽结果可表示的精确新值，旧值和 NULL 也保留。
        INSERT 显式列出可写基列，直接赋生成列独立负向；写事务和副本追平有超时。
        成对生成列组限定并发、持续时间及资源；溢出后按事务终态停止探针，恢复可用连接并清理依赖链夹具。
        """,
        """
        指定覆盖 | 固定 ON 与既有 SQL mode | 溢出错误按实际模式和契约判断
        全部覆盖 | 全局兼容生成依赖表型、格式、历史 | 不合法生成链归源准备负向
        全部覆盖 | 基列安全扩宽、窄/宽生成结果、VIRTUAL/STORED 和旁列 | 按每层声明类型独立计算
        专属子运行 | 生成列全部兼容索引与 CHECK 依赖；函数/MVI/全文/空间另专属 | 每层访问路径和约束都检查
        全部覆盖 | 旧边界、窄溢出宽合法、NULL、重复结果及分布 | 成对输入同值，不共用错误预期
        指定覆盖 | 基列 INPLACE 扩宽与非法生成列直接赋值 | 生成列自身类型变更不混入本组
        边界覆盖 | 新值 INSERT/UPDATE 的提交或整事务回滚 | 账本分开记录两种结果类型组
        指定覆盖 | 我方主备精确表达式结果 | 每端对照独立生成期望
        全部覆盖 | 依赖类型、求值、回滚原子性、索引及副本 | 窄结果正确拒绝可通过业务约束验证
        """,
        """
        基列定义 | 前后 SHOW CREATE 和实际算法 | 基列扩宽且生成类型保持通过；擅改结果类型失败
        窄结果拒绝 | 新值溢出错误、事务前后完整行 | 正确拒绝且无部分写通过；静默截断失败
        宽结果成功 | 同输入的 BIGINT 精确求值和提交回执 | 可表示值精确提交通过；错误拒绝失败
        依赖链维护 | 每层生成值、CHECK 与全部相关索引访问 | 逐层符合声明语义通过；只末层相同不足通过
        写规则和副本 | 直接赋值负向、主备各端行与元数据 | 合法规则和每端期望一致通过；追平超时 BLOCKED
        """,
        "基列扩宽结果和后续业务可表示性分开记；窄结果溢出不是自动判功能 BUG。"),
    "SC18": _policy(
        "验证函数索引表达式依赖在扩类型、改表达式、改基列名三种独立动作后的更新或准确拒绝。",
        "隐藏生成列与文本对象依赖更新规则不同，统一假设自动改名会留下悬空引用和错误计算。",
        """
        三个源克隆分别只扩类型、修改表达式、重命名基列，源函数索引及查询已通过。
        构造 CAST 返回类型、整数/字符表达式和普通/唯一函数索引，表达式资格规则已冻结。
        另建命名生成列、视图、触发器或程序文本依赖，并记录每类允许的改名行为。
        非法表达式与依赖错误有原始 SQL；源元数据和全行快照用于证明整句原子性。
        依赖调用并发、持续时间、资源、事务超时和停止规则登记；恢复旧/新定义一致状态后清理专属依赖对象。
        """,
        """
        指定覆盖 | 固定 ON 及已支持表达式语法 | 未冻结依赖自动更新规则 BLOCKED
        全部覆盖 | 全局兼容函数索引载体、历史和格式 | 各动作使用独立源克隆
        全部覆盖 | CAST 结果、整数/字符基列和已交付安全扩宽属性 | 表达式结果与基列类型分别检查
        专属子运行 | 函数索引全部兼容普通/唯一角色及生成/文本依赖；MVI/全文/空间另专属 | 各依赖按自己的正式规则验收
        全部覆盖 | 表达式旧新边界、NULL、相同结果不同输入及字符比较 | 建立独立表达式期望
        全部覆盖 | 本场景适用 CHANGE/RENAME、类型变更、表达式变更及非法表达式入口 | 第一错误阶段和整句资格逐条记录
        指定覆盖 | 变更前后有界写入及依赖对象调用 | 失败的依赖调用保留原始错误
        指定覆盖 | 我方副本和旧新客户端 | 重连检查字段与依赖名称
        全部覆盖 | 隐藏/命名依赖元数据、访问路径、约束、错误原子性 | 不假定所有文本对象自动改名
        """,
        """
        表达式元数据 | 函数 key part、CAST 返回类型和源目标定义 | 按动作正确保留或更新通过；悬空引用失败
        函数访问 | 表扫计算与实际函数索引路径结果 | 精确结果一致通过；路径未证实 BLOCKED
        唯一表达式 | 相同结果不同输入的重复探针 | 唯一等价规则保持通过；漏约束失败
        文本依赖传播 | 视图/触发器/程序定义及调用回执 | 按正式更新或拒绝规则通过；契约缺失 BLOCKED
        负向与复制 | 非法表达式阶段、完整回滚快照和副本定义 | 原子拒绝或完整成功且各端正确通过；半更新失败
        """,
        "命名生成列、函数隐藏列和文本依赖分别留证，不能以一个对象可用替代整条依赖链。"),
    "SC19": _policy(
        "验证整数外键父先扩宽的每个提交中间态，以及子侧第二次 DDL 失败后的关系恢复。",
        "将两次独立 DDL 当作原子操作会掩盖父侧提交后的不兼容结构、孤儿及第二侧失败遗留。",
        """
        正式冻结父先过渡的类型/符号兼容规则；缺失则过渡结果 BLOCKED。
        父 PK/非主唯一键和子显式/隐式 FK 索引的源关系已真实建成并无孤儿。
        父侧提交后立即验证，再执行子侧；子侧权限或超时失败使用正式受控入口。
        并发引用的线程数、持续时间、资源和超时有限；停止后恢复父子可用关系并清理专属表。
        """,
        """
        全部覆盖 | 已冻结整数 FK 过渡的固定 ON 环境 | 不关闭 foreign_key_checks 凑条件
        全部覆盖 | 全局兼容父/子/自引用表型、物理状态及关系图；原生分区 FK 无效源单列 | 每个图均保留父先中间态
        全部覆盖 | 适用整数扩宽/符号保持、复合 FK 首中末和旁列 | 每侧按完整目标定义核验
        全部覆盖 | 兼容父 PK/UK、子显式/隐式索引、级联及多表关系 | 受影响全部关系逐条检查
        全部覆盖 | 合法引用、NULL、孤儿负向、旧新边界与分布 | 中间态新范围写入按兼容契约判定
        指定覆盖 | 父先子后两个独立 INPLACE 请求和第二侧失败 | 每次提交保存完整结构
        边界覆盖 | 两次 DDL 间父子增删改、级联、超时失败 | 有界提交账本按表记录
        指定覆盖 | 我方主备及正式第二侧恢复流程 | 每个提交位点单独追平验证
        全部覆盖 | 中间兼容、无孤儿、级联、索引依赖和失败原子性 | 最终一致不能替代中间态正确
        """,
        """
        父侧提交态 | 父子类型/符号和正式过渡规则 | 每次可见态兼容通过；规则缺失 BLOCKED
        引用约束 | 合法/孤儿探针及全关系反连接查询 | 无孤儿且负向拒绝通过；漏约束失败
        第二侧失败 | 子侧 errno/阶段与两表前后定义 | 子侧原子失败且父侧已提交保持通过；半更新失败
        级联与依赖 | 各动作提交账本、全部关系表和 FK 索引元数据 | 不漏级联且依赖有效通过；遗漏表失败覆盖
        恢复和副本 | 恢复步骤、逐提交位点主备独立期望 | 每端每中间态正确通过；追平不足 BLOCKED
        """,
        "父侧已提交不可当作子侧失败应回滚的部分；每侧提交状态分别验收。"),
    "SC20": _policy(
        "验证子先扩宽在自引用、三层链、多子扇出和双父表关系中的过渡及级联完整性。",
        "只校验最初父子对会漏掉链尾孤儿、扇出遗漏与双父约束失效，级联锁顺序还可能阻塞后续 DDL。",
        """
        正式冻结子先父后的类型兼容及失败恢复规则，保持外键检查配置不变。
        自引用、三层链、16 子表扇出和双父表源图均实际创建，逐边验证初始关系。
        复合 FK 首中末、显式/隐式支撑索引及合法 CASCADE/SET NULL/RESTRICT 组独立准备。
        级联并发、持续时间、资源、锁超时与停止阈值已限定；恢复顺序与全图清理顺序已登记。
        """,
        """
        全部覆盖 | 子先契约已冻结的固定 ON 环境 | 缺契约只保留待决证据
        全部覆盖 | 全局兼容父子/自引用及完整关系图、存储与历史 | 每条边真实创建，原生不支持分区 FK 单列负向
        全部覆盖 | 适用整数安全边、复合成员位置、可空子列与旁列 | 每侧类型与符号逐步核验
        全部覆盖 | 全部兼容父键、子索引、级联动作、扇出/链/双父依赖 | 不抽掉受影响关系表
        全部覆盖 | 根 NULL、多个子引用、边界整数、孤儿负向和倾斜 | 每个关系节点有独立期望
        指定覆盖 | 子先父后的独立 INPLACE 及不兼容拒绝 | 每步执行后立即验证图
        边界覆盖 | 中间态增删父子与级联，锁等待及提交/回滚 | 有界事件序列覆盖不同关系动作
        指定覆盖 | 我方所有副本和关系恢复入口 | 追平后全图对照账本
        全部覆盖 | 每边约束、中间态、路由关系、级联和原子性 | 单表正确不替代全图正确
        """,
        """
        子先中间态 | 逐步父子定义和正式兼容表 | 每步兼容或准确原子拒绝通过；规则缺失 BLOCKED
        全图引用 | 每条 FK 反连接和合法/孤儿写探针 | 全图无孤儿通过；遗漏节点不足通过
        级联传播 | 删除/更新账本与链尾、全部子表、双父引用结果 | 所有受影响行按规则改变通过；漏级联失败
        索引与锁队列 | FK 支撑索引及锁等待/事务终态 | 依赖仍有效、队列有界恢复通过；悬挂锁失败
        副本关系 | 各提交位点的主备全图独立期望 | 每端一致通过；只验最终父表不足通过
        """,
        "关系图全部适用节点和边必须可追溯，扇出负载不能无界扩大。"),
    "SC21": _policy(
        "证明 BINARY 外键父侧/子侧单独扩长准确拒绝，并将 VARBINARY 的不同兼容契约分开判断。",
        "定长补零会改变父子字节匹配；把变长规则和定长规则混用可错误放行不兼容关系。",
        """
        BINARY 父侧和子侧各独立源克隆，单列/复合 FK 均先确认字节匹配和约束有效。
        数据含首中末 0x00 和合法 NULL 引用，保存 HEX、长度及父子关系快照。
        另一普通列合法扩宽混入同句，用于证明受限外键动作导致整句拒绝。
        VARBINARY 正式兼容规则冻结后才判其扩長结果；前缀 FK 无效定义保留在 CREATE 负向。
        引用探针限定并发、持续时间、资源与锁超时；拒绝后停止探针，恢复源关系可用性并按依赖顺序清理测试表。
        """,
        """
        全部覆盖 | 固定 ON 与已冻结二进制 FK 契约环境 | VARBINARY 未冻结 BLOCKED
        全部覆盖 | 全局兼容二进制父子表型、关系图、格式与历史 | 先保证完整源关系图可建
        全部覆盖 | BINARY/VARBINARY 合法扩长、复合首中末、旁列 | 定长补零与变长保持分开期望
        全部覆盖 | 兼容父引用键、子支撑索引与级联关系；前缀 FK 源负向 | 核验每条依赖元数据
        全部覆盖 | 含零字节、共同前缀、NULL、最大合法长度与关系分布 | 全字节关系比较不经文本裁剪
        指定覆盖 | 单侧 INPLACE 加普通安全列、交换子句顺序 | BINARY 两侧各自原子拒绝
        指定覆盖 | 拒绝前后有界合法引用和孤儿探针 | 不关闭约束绕过准备
        指定覆盖 | 我方主备精确二进制读取 | 每端验证原关系和旁列
        全部覆盖 | 字节匹配、中间约束、错误阶段、整句原子性 | 不将 CREATE 失败计为 ALTER 通过
        """,
        """
        BINARY 两侧拒绝 | 父/子独立 SQL、errno/阶段与完整结构 | 两侧按限制原子拒绝通过；一侧静默放行失败
        原字节关系 | 父子 HEX/LENGTH 与关系反连接 | 源值和关系保持通过；补零半应用失败
        旁列原子性 | 同句普通列前后定义及两种子句顺序 | 旁列也未变更通过；局部成功失败
        VARBINARY 资格 | 正式规则、目标定义、字节匹配和约束探针 | 符合独立契约通过；未冻结 BLOCKED
        依赖与副本 | FK/支撑索引元数据和各端关系读回 | 全部关系正确通过；源前缀 FK 无效属范围外 ALTER
        """,
        "BINARY 的明确限制不外推 VARBINARY；每个单侧中间态单独留证。"),
    "SC22": _policy(
        "验证非分区列扩宽遍及所有实际叶分区，期间跨叶更新最终只落到正确路由位置。",
        "只检查全表行数可能掩盖重复叶记录、漏重建空叶及分区剪枝错误。",
        """
        一级各合法方式及原生 16 个二级组合完整 CREATE 已验证，目标列不参与直接或间接路由。
        主键/唯一键满足分区限制，源表每个叶的数据、空叶和边界路由已有独立期望。
        准备整数/BINARY/VARBINARY 范围内扩宽与热点/均匀分布，旁列和索引先核验。
        跨叶更新限定并发、持续时间、资源与超时；停止后按提交账本恢复验证并清理分区夹具。
        """,
        """
        全部覆盖 | 固定 ON 与已配置合法分区能力 | 内部扩展无完整入口转 SC24 BLOCKED
        全部覆盖 | 全局兼容分区/子分区表型、全部实际叶、格式和历史 | 一级方式与 16 二级组合按适用性逐项执行
        全部覆盖 | 非路由已交付类型、属性和旁列；路由键扩宽 SC23 | 显式解析间接依赖排除伪非分区列
        全部覆盖 | 全部兼容分区 PK/UK、旁索引及合法依赖图 | 原生 FK 限制归源准备负向
        全部覆盖 | 各叶边界/MAXVALUE、空叶/热点叶、跨叶迁移和完整类型边界 | 逐叶期望总和与全表对应
        指定覆盖 | 非分区列 INPLACE/NONE，维护混句交 SC24 | 确认所有叶参与正确变更
        边界覆盖 | DDL 扫描/回放中跨叶路由键更新与回滚 | 阶段证据加业务标识账本
        指定覆盖 | 我方主备逐叶校验及分区查询客户端 | 追平后各端独立验路由
        全部覆盖 | 路由、剪枝、叶数据、索引、在线与复制 | 全表一致不能替代逐叶正确
        """,
        """
        非路由资格 | 分区表达式/隐式 KEY 和生成依赖元数据 | 目标确不参与路由通过；无法排除间接依赖 BLOCKED
        叶覆盖 | 全部叶列表、目标定义和逐叶数据 | 每个实际叶均正确通过；空叶被漏检失败覆盖
        跨叶提交 | 业务账本和每个标识实际所在叶 | 已提交行只在正确叶一次通过；重复/错叶失败
        剪枝与索引 | 真实执行计划、分区谓词和全表/索引结果 | 剪枝与结果符合预期通过；路径未证实 BLOCKED
        在线与主备 | 算法锁、阶段内提交、追平后逐叶期望 | INPLACE 契约和各端数据正确通过；副本超时 BLOCKED
        """,
        "所有实际叶有明确证据；非分区列正向不能因同表存在路由键而整体排除。"),
    "SC23": _policy(
        "验证一级、二级及间接分区键扩宽受限时整句拒绝，同时保留非路由列正向对照。",
        "只字符串搜索顶层分区表达式会漏掉 s_key、隐式 KEY 和生成依赖，混句可能部分修改安全列。",
        """
        p_key、s_key、普通 c 使用独立源克隆，源主键/唯一键满足实际分区限制。
        直接、生成表达式及隐式 KEY 路由依赖从元数据确认，不只匹配列名。
        安全 c 加受限键同句以及子句顺序交换已准备，完整源定义和逐叶数据已保存。
        每个实际叶和关系依赖有期望，普通 c 正向使用已交付安全类型与合法算法锁。
        路由写探针限定并发、持续时间、资源及超时；受限键拒绝后停止该组，恢复事务并清理独立叶图夹具。
        """,
        """
        全部覆盖 | 固定 ON 的原生/已交付扩展分区配置 | 不对未映射扩展臆造资格
        全部覆盖 | 全局兼容一级/二级分区表型、叶图、存储和历史 | 每种路由结构独立源表
        全部覆盖 | p_key/s_key/间接基列及安全 c 的适用类型属性 | 三种角色分别结论
        全部覆盖 | 分区表达式、生成链、隐式 KEY、兼容 PK/UK 和依赖图 | 按实际元数据建立依赖闭包
        全部覆盖 | 路由边界、空叶、热点叶及旧值域边界 | 拒绝后逐叶值与源一致
        指定覆盖 | 受限键 INPLACE 和安全 c 混句两种顺序 | 所有 DDL 子动作原子拒绝
        指定覆盖 | 拒绝后有界路由更新与正常写入 | 证明约束及路由仍可用
        指定覆盖 | 我方副本逐叶和元数据 | 追平后检查源定义保留
        全部覆盖 | 资格、错误阶段、中间约束、路由与原子性 | 正向 c 不因受限键负向被省略
        """,
        """
        依赖识别 | p/s/生成链/KEY 隐含列清单与元数据 | 所有路由目标可追溯通过；只搜名称不足通过
        键资格拒绝 | 每类受限键的原 SQL、错误阶段和算法 | 准确拒绝增强通过；误接纳失败
        混句原子性 | 两种顺序的全部列和分区定义快照 | 安全 c 也未被部分修改通过；半成功失败
        普通列对照 | c 单独扩宽结果、算法锁及目标数据 | 非路由合法路径正确通过；漏跑不计通过
        路由与副本 | 拒绝后逐叶查询、写入探针和各端数据 | 路由/约束/每端源态完整通过；差异失败
        """,
        "分区键限制与分区表载体能力分别记录，拒绝后仍核验完整叶图。"),
    "SC24": _policy(
        "把分区组合的 CREATE 语法资格、内部扩展正向与后续 ALTER 结果分开追溯。",
        "无法建成的原生组合被计作类型 ALTER 覆盖，或缺少完整扩展 SQL 就推断增强，会夸大覆盖率。",
        """
        64 组合的原生 16 合法/48 负向索引与 320 个整数/二进制定义保留原 ID。
        内部扩展必须具有正式完整 CREATE 和契约，缺失则扩展运行 BLOCKED。
        分区维护 ADD/DROP/REORGANIZE/EXCHANGE/REMOVE 与 MODIFY 的合法混句入口逐项核验。
        合法源叶分布、兼容主键/唯一键及关系图已经建立；类型 INSTANT 分支标本轮范围外。
        各扩展/维护运行登记并发、持续时间、资源和 DDL 超时；停止后按正式流程恢复原叶图再清理测试分区资产。
        """,
        """
        全部覆盖 | 原生 8.0.45 与已有内部扩展固定 ON 环境 | 两平台能力差异按完整源资格分账
        全部覆盖 | 全局兼容一级/二级分区表型和实际叶图；48 不合法组合为准备负向 | 320 定义逐项筛选不做笛卡尔积
        全部覆盖 | 定义目录内适用整数/二进制安全扩宽、属性与旁列 | 类型 INSTANT 交 SC48 不计本轮
        全部覆盖 | 兼容 PK/UK、分区依赖和关系图；原生 FK 不支持单列 | 真实建成后再验证 ALTER 依赖
        指定覆盖 | 每个合法扩展叶的边界、空叶与路由代表值 | 足以证明全图路由与转换正确
        全部覆盖 | 本场景适用 CREATE 分区组合、维护与 MODIFY 入口 | 不合法混句不自动拆改为原 SQL 成功
        指定覆盖 | 维护前后有界读写及提交顺序 | 无正式并发承诺按实际锁资格验收
        指定覆盖 | 阿里主机对照与我方主备扩展验证 | 阿里拒绝后我方仍须独立全量正确
        全部覆盖 | CREATE/ALTER 第一失败阶段、路由、约束与原子性 | 准备拒绝不得计 ALTER PASS
        """,
        """
        定义追溯 | 64 组合/320 定义 ID 与适用筛选表 | 原始定义无遗漏通过；类型 INSTANT 为范围外
        原生语法阶段 | 原始 CREATE、errno/SQLSTATE 与执行阶段 | 48 负向准确归准备拒绝通过；不得计 ALTER 通过
        扩展源资格 | 正式契约、完整 CREATE 与所有叶元数据 | 源真实建成通过；入口缺失 BLOCKED
        ALTER 和维护 | 真实 SQL、算法锁、列/分区前后快照 | 合法动作完整成功或准确原子拒绝通过；部分变更失败
        路由关系与副本 | 全叶数据、兼容约束及我方各端独立期望 | 全部正确才可记增强通过；仅阿里拒绝不足通过
        """,
        "CREATE、ALTER 和扩展验收独立计数，所有原始分区定义保留追溯。"),
    "SC25": _policy(
        "验证多列安全扩宽与增删、改名、调序及默认值动作在一条 ALTER 中完整切换。",
        "动作顺序或逻辑列映射错误会导致默认值串列、旁列错位和仅部分结构生效。",
        """
        同构源克隆包含两/三目标列、普通旁列及合法生成依赖，完整定义和数据已保存。
        每种类型单独扩宽已验证，混句目标定义按每个 ADD/DROP/CHANGE/FIRST/AFTER 明确计算。
        ADD 括号列清单和非法 MODIFY 列表分开，默认常量与表达式有省略列写入预期。
        子句顺序变体各独立执行，实际算法/锁观测与第一错误阶段记录入口就绪。
        切换探针并发、持续时间、资源、DDL/事务超时预先限定；异常停止后核实全列真实态，恢复连接并清理克隆。
        """,
        """
        指定覆盖 | 固定 ON 的已交付安全类型配置 | 相同目标定义先阿里主机后我方
        全部覆盖 | 全局兼容多列表型及首中末/历史列布局 | 非法源先归准备错误
        全部覆盖 | 两/三列已交付类型及全部适用属性、非目标特殊列 | 每列独立目标期望
        指定覆盖 | 混句涉及的全部兼容索引、生成依赖和约束 | 特殊索引动作交 SC27/专属场景
        全部覆盖 | 各类型边界、NULL、默认值及旁列标识 | 明确全列投影避免位置误读
        全部覆盖 | 适用 MODIFY/CHANGE、ADD 列清单、DROP、改名调序、默认值及算法锁入口 | 按同句动作类和顺序逐项展开，不做全组合
        边界覆盖 | 切换前后有界读写与事务终态 | 合法并发依整句锁交集判定
        指定覆盖 | 旧新连接及我方副本 | 追平后验证列顺序和省略列插入
        全部覆盖 | 解析/准备/资格/执行阶段及结构数据原子性 | 每条混句单独结论
        """,
        """
        动作覆盖 | 全部适用入口/同句动作类与实际 SQL 对照 | 每类可追溯通过；合法入口漏项不计完整通过
        全列映射 | 目标列序、全列数据及稳定旁列标识 | 无错位、各列符合目标通过；默认值串列失败
        默认写入 | 省略不同列的 INSERT 及实际默认结果 | 按最终定义生成正确值通过；遗漏属性误判失败
        算法锁交集 | 请求与实际算法锁及完整动作列表 | 满足整句契约通过；单动作资格替代整句失败
        阶段与原子切换 | 第一错误阶段、前后结构及副本目标 | 完整成功或准确整句拒绝通过；局部生效失败
        """,
        "同句多目标列逐列给出数据和元数据期望，不能只检查扩宽的一列。"),
    "SC26": _policy(
        "证明安全扩宽混入不安全类型、真实压缩列或表达式修改后，资格失败使整个 DDL 原子撤销。",
        "无效压缩列夹具或失败后误回滚业务账本，会把未测受限能力和丢失合法提交伪装成正确拒绝。",
        """
        缩窄、真实产品压缩列、虚拟表达式变化各使用独立合法源表。
        压缩列必须来自正式 SQL 映射和真实资产；普通 COLUMN_FORMAT 不替代，入口缺失 BLOCKED。
        安全与受限子句两种顺序保存原始 SQL 和完整源结构快照。
        并发合法写设定线程、持续时间、资源、超时与停止条件；提交账本独立保存，恢复后清理克隆。
        """,
        """
        指定覆盖 | 固定 ON 与真实受限资产配置 | 准备缺失不冒充资格拒绝
        全部覆盖 | 全局兼容受限动作源表与历史状态 | 源 CREATE 与目标 ALTER 分阶段归类
        全部覆盖 | 安全扩宽加缩窄/压缩属性/表达式变化及旁列 | 每个受限机制分别运行
        指定覆盖 | 受影响全部索引/生成依赖与约束 | 拒绝后验证原依赖仍有效
        边界覆盖 | 仅小值与完整旧边界、压缩数据和表达式边界 | 不因数据可转换而放宽声明资格
        全部覆盖 | 适用安全/受限混句动作类、入口和两种顺序、显式算法锁 | 原 SQL 保留，不自动删受限动作
        边界覆盖 | 失败前合法提交与失败后读写 | 有界账本区分 DDL 与业务事务
        指定覆盖 | 我方主备和正式源恢复 | 只重置测试克隆，不丢业务证据
        全部覆盖 | 准备/资格/执行阶段与整句原子性、提交保留 | 不能以第二次修正 SQL 成功覆盖首错
        """,
        """
        真实受限资产 | 源 CREATE、正式压缩映射和表达式元数据 | 源真实成立通过；无正式入口 BLOCKED
        资格错误阶段 | 两种子句顺序原 SQL、errno/SQLSTATE | 受限组合准确拒绝通过；语法失败不能冒充执行通过
        全定义原子性 | 所有列/索引/表达式前后快照 | 安全子句也未生效通过；部分修改失败
        已提交业务 | DDL 外提交账本和恢复后完整行 | 合法提交保留通过；随 DDL 误回滚失败
        依赖与后续可用 | 原索引查询、约束探针和主备独立结果 | 原依赖可用且各端正确通过；残留半态失败
        """,
        "每个受限类型和动作独立有效源夹具；DDL 原子回滚不意味着业务提交被撤销。"),
    "SC27": _policy(
        "验证扩宽同句增删重命名索引及选项后，最终索引集合与物理记录一起原子生效。",
        "重命名冲突或超限后残留半套索引，可能使查询走错路径或不可见唯一索引停止约束。",
        """
        普通/唯一/复合索引及固定/改前缀源克隆独立，当前索引名和定义已保存。
        ADD/DROP/RENAME KEY、ASC/DESC、VISIBLE/INVISIBLE 等适用动作类及合法入口列入清单。
        重名、缺失索引和超字节上限各有独立负向，源结构全部合法。
        每个最终索引预备点查/范围/回表和重复探针，实际计划及第一错误阶段可记录。
        索引写探针限定并发、持续时间、资源及超时；错误即停止该组，恢复原/目标索引可用性并清理测试源表。
        """,
        """
        指定覆盖 | 固定 ON 与实际索引限额环境 | 行格式和页大小绑定边界计算
        全部覆盖 | 全局兼容索引表型与历史 | 各混句独立源克隆
        指定覆盖 | 已交付安全扩宽类型和索引长度属性 | 改列与改前缀分别定义期望
        全部覆盖 | 全部兼容普通/唯一/复合/前缀/方向/可见性角色；特殊 ADD 专属 SC14–SC18 | 每个最终索引独立核验
        全部覆盖 | key part 边界、共同前缀、NULL、重复探针和分布 | 普通与唯一合法源数据分开
        全部覆盖 | 适用 MODIFY 加 ADD/DROP/RENAME KEY 及选项、算法锁、子句顺序 | 覆盖完整动作类和错误入口
        指定覆盖 | DDL 前后有界索引键写与回滚 | 提交终态用于索引核验
        指定覆盖 | 我方主备与实际访问路径客户端 | 新索引名称不能只做元数据检查
        全部覆盖 | 解析/资格/执行错误、索引原子性与约束 | 原索引保留或目标集合完整，不允许半套
        """,
        """
        最终索引集合 | SHOW INDEX 与目标名字/顺序/前缀/方向 | 每个定义精确符合目标通过；残留旧名失败
        访问路径维护 | 实际计划、表扫与每个最终索引结果 | 路径真实且结果一致通过；无路径证据 BLOCKED
        不可见唯一性 | 不可见 UNIQUE 重复 INSERT/UPDATE 回执 | 仍执行约束通过；重复被接纳失败
        错误阶段 | 重名/缺失/超限原 SQL 及第一错误回执 | 对应阶段准确拒绝通过；错误改写不计通过
        整句原子性 | 扩宽列与全部索引前后快照和副本结果 | 完整成功或源态完整通过；部分 ADD/DROP 失败
        """,
        "DROP/ADD 改前缀与仅扩列保持前缀分开；每个最终索引需实际访问证据。"),
    "SC28": _policy(
        "验证扩宽加 UNIQUE/CHECK/FK 时真实最终违规准确使整句失败，合法分支约束持续生效。",
        "唯一冲突优化错误吞掉真实重复或只应用一部分约束，会使最终结构宣称有效但数据已违规。",
        """
        UNIQUE、CHECK、FK 各独立合法源克隆，历史违规和并发最终违规分别设计。
        正式扫描/回放阶段入口可观测，新增 UK 最终重复事件链可精确证明；缺入口 BLOCKED。
        ENFORCED/NOT ENFORCED、DROP/ADD、同名约束及级联动作按合法语法列清单。
        并发数、持续时间、资源预算、超时和停止规则登记；恢复后保留业务账本再清理测试关系。
        """,
        """
        指定覆盖 | 固定 ON 与已冻结约束增强范围 | 不因 ON 推断所有 ADD 都受优化
        全部覆盖 | 全局兼容新增约束载体和父子关系 | 无效源归准备错误
        指定覆盖 | 已交付扩宽类型及 CHECK/FK 依赖属性 | 约束表达式与类型变更分别核验
        全部覆盖 | 本场景兼容 UNIQUE/CHECK/FK、支撑索引和级联类 | 约束逐个独立源夹具
        全部覆盖 | 历史/最终违规、NULL、合法引用与唯一边界 | 真实违规与临时回放冲突分离
        全部覆盖 | 适用 ADD/DROP/ENFORCED/NOT ENFORCED、同名负向及算法锁入口 | 整句动作交集独立判断
        边界覆盖 | 扫描/回放间提交最终重复，合法提交与回滚 | 正式阶段证据加终态账本
        指定覆盖 | 我方主备及关系恢复 | 合法业务提交不能随失败消失
        全部覆盖 | 准备/资格/执行拒绝、完整约束、原子性与持续写 | 最终违规绝不能被静默跳过
        """,
        """
        最终违规身份 | 历史数据或阶段提交账本、重复/违规独立查询 | 确为最终违规通过夹具验证；未命中 BLOCKED
        约束失败阶段 | 每类原 SQL、errno/SQLSTATE、第一失败阶段 | 准确拒绝真实违规通过；优化吞错失败
        全定义回滚 | 扩宽列和全部新增/删除约束前后快照 | 整句撤销通过；仅部分约束生效失败
        业务提交保留 | DDL 外合法事务账本和失败后全行 | 合法提交保留通过；误删业务失败
        成功约束与副本 | 合法分支后重复/CHECK/FK 探针及各端定义 | 按 enforcement 语义持续有效通过；主备错配失败
        """,
        "NOT ENFORCED 按其真实语义判定；合法业务错误、回放冲突与 DDL 最终失败分别记录。"),
    "SC29": _policy(
        "厘清扩宽混入表选项、强制重建或字符集动作后实际转换范围和整句资格。",
        "仅改表默认字符集被误当转换已有列，或表压缩/页压缩被误当产品压缩列，会导致错误能力归因。",
        """
        FORCE/ENGINE/ROW_FORMAT/COMMENT/STATS/CHARSET 及维护选项按源卡列出合法/受限组。
        仅表默认 charset 与 CONVERT 实际列转换各自独立源克隆，保存列级 charset/collation。
        已有表空间和压缩环境先核验；不修改全局功能开关或伪造压缩列。
        同源 COPY 控制独立运行，各动作目标定义、字节期望和实际算法证据已准备。
        选项混句的并发探针、持续时间、资源和 DDL 超时有限；停止后恢复正式可用状态并清理临时转换资产。
        """,
        """
        全部覆盖 | 已有固定 ON、空间/压缩配置 | 不可用配置单列 BLOCKED
        全部覆盖 | 全局兼容表空间、行格式、压缩形式和历史表 | 表压缩、页压缩、压缩列保持区分
        全部覆盖 | 适用字符集默认/转换、类型属性和全部旁列 | 按动作计算哪些列应实际变化
        指定覆盖 | 受字符语义或重建影响的全部兼容索引/约束 | 改比较语义后的唯一性独立验证
        全部覆盖 | 多字节、不可转换值、行/索引边界和默认值 | 真正内容转换采用独立字节预期
        全部覆盖 | 适用 FORCE/ENGINE/ROW_FORMAT/COMMENT/STATS/charset、ORDER BY/ENABLE KEYS 等完整入口类 | 解析接受、忽略和有效执行分开
        指定覆盖 | 整句锁契约内的有界读写 | 不将普通重建自动记为类型增强
        指定覆盖 | 我方副本及字符编码客户端 | 每端核验列级内容与表选项
        全部覆盖 | 解析/准备/资格/执行阶段、实际转换和原子性 | COPY 独立控制不能替代 INPLACE
        """,
        """
        转换范围 | 表默认及每列 charset/collation 前后快照 | 仅预期列变化通过；仅改默认却声称全列转换失败
        物理选项 | 实际行格式/空间/压缩元数据 | 按实际生效/忽略契约通过；名称冒充失败
        算法锁 | 完整动作和实际算法、锁证据 | 符合整句交集通过；静默替换显式要求失败
        错误原子性 | 不合法组合第一阶段与全定义/数据快照 | 准确整句拒绝通过；部分选项生效失败
        数据约束与副本 | 转换字节、唯一比较及各端独立期望 | 实际内容和依赖一致通过；无法冻结转换规则 BLOCKED
        """,
        "表选项成功不直接计类型增强；目标的实际列转换范围必须明确。"),
    "SC30": _policy(
        "逐项核对适用少见语法及直接/预处理入口，将解析成功、准备成功和引擎执行结果分开。",
        "自动修正错误 SQL 或为 PREPARE 已失败的语句伪造 EXECUTE 结果，会把语法覆盖和能力结论混淆。",
        """
        155 项语法索引保留 ID、合法/非法 SQL 和本轮适用状态，类型 INSTANT/开关项标范围外。
        相同目标数据和独立源克隆用于直接执行与 PREPARE/EXECUTE，源结构先验证。
        别名规范化、DEFAULT 括号、MODIFY 列表、其他产品语法、ENGINE_ATTRIBUTE JSON 和 VALIDATION 已列入清单。
        errno/SQLSTATE、第一失败阶段、连接健康及前后结构记录入口就绪，不自动重写 SQL。
        语法执行并发、持续时间、最大语句数、资源和语句超时登记；异常停止后恢复连接并清理 prepared 句柄和专属夹具。
        """,
        """
        全部覆盖 | 已有固定 ON 与适用 SQL mode/语法版本 | 记录原 SQL 的实际解析环境
        指定覆盖 | 覆盖语法所需的最小合法表型及同名对象负向 | 准备失败与 ALTER 资格严格分离
        全部覆盖 | 适用类型别名、属性、默认值和旁列 | 按规范化真实含义比较而非文本同义
        指定覆盖 | 语法涉及的索引/约束/依赖最小合法资产 | 准备有效后才判断入口能力
        边界覆盖 | 同目标数据、非法默认/JSON/表达式边界 | 一次只改变被测语法要素
        全部覆盖 | 155 索引内适用 ALTER 入口、算法锁与同句动作类；INSTANT/开关范围外 | 每条原 SQL 直接与预处理分别记录
        指定覆盖 | PREPARE 成功后才 EXECUTE，有限次数运行 | 句柄释放和事务清理有超时
        指定覆盖 | 直接协议、prepared 句柄及我方副本 | 元信息和错误传播与客户端阶段对应
        全部覆盖 | 词法/解析/准备/资格/执行错误与整句原子性 | 不修改失败原文后登记原项通过
        """,
        """
        索引完整性 | 155 语法 ID 与适用状态/原 SQL | 每项可追溯通过；范围外不计当前通过
        第一失败阶段 | 直接/PREPARE/EXECUTE 分阶段回执 | 准确定位首错通过；PREPARE 失败后虚构执行失败
        规范化定义 | 别名及属性对应的实际列/索引元数据 | 语义正确通过；解析接受不等于功能通过
        负向原子性 | 原 SQL、错误码与完整结构/数据前后快照 | 无部分修改通过；自动改写不计原项通过
        服务和句柄 | 错误后 SELECT/合法写、句柄释放及副本定义 | 连接/服务可用且状态一致通过；崩溃失败
        """,
        "语法有效与引擎支持分别统计；未执行的阶段必须明确未到达。"),
    "SC31": _policy(
        "以扫描前后同键重插和保存点事件序列证明 row log 重放产生唯一准确的提交终态。",
        "DELETE/INSERT/改键/撤销顺序失真可能复活旧行、重复新行或把保存点回滚写入新表。",
        """
        正式扫描前/后同步或观测入口可用，事件链每次执行都保存阶段命中证据；缺失 BLOCKED。
        PK/UK 迁移、相同 PK 重插及同 UK 临时占用释放各用独立源克隆和稳定业务标识。
        COMMIT/ROLLBACK/ROLLBACK TO 顺序和独立终态计算器已冻结，不按最终行数倒推。
        并发线程、持续时间、事件数、资源预算和阶段/事务超时登记；触发停止后释放屏障，恢复事务并清理夹具。
        """,
        """
        指定覆盖 | 固定 ON 和正式扫描证据入口 | 同源先阿里主机，定点能力不足单列
        指定覆盖 | 已通过的 PK/UK 代表载体与删除历史状态 | 不乘全物理矩阵
        指定覆盖 | 已交付扩宽类型及稳定标识旁列 | 保证能精确区分旧新版本
        边界覆盖 | PK/UK 迁移和兼容旁索引 | 每个受影响键独立查询
        边界覆盖 | 同键重插、相邻键、最终合法 UK 与撤销值 | 用事务事件账本求终态
        指定覆盖 | 触发重建的 INPLACE/NONE | 实际算法与完整目标定义留证
        边界覆盖 | 未扫描/已扫描事件链、SAVEPOINT、提交/回滚和回放 | 两阶段分别确定命中，不以概率代替
        指定覆盖 | 我方副本及恢复连接 | 追平后重算每端终态
        全部覆盖 | 阶段、账本、索引、队列/锁与确定终态 | 未命中目标时点 BLOCKED
        """,
        """
        扫描屏障 | 正式阶段事件与事务起止时间 | 前后两窗口各命中通过；无证据 BLOCKED
        提交撤销账本 | 全事件及 COMMIT/ROLLBACK TO 回执 | 可独立计算唯一终态通过；不明事务 BLOCKED
        旧新键重放 | 旧键/新键点查及稳定标识全行 | 旧行不复活、新行唯一通过；漏重放失败
        队列和锁释放 | 等待链、屏障释放与事务终态 | 有界结束无遗留锁通过；悬挂失败
        索引与副本 | 各路径及主备精确终态 | 每端匹配账本通过；只总行数相等不足通过
        """,
        "阶段命中与数据正确必须同时满足；保存点撤销和整个事务回滚分别记录。"),
    "SC32": _policy(
        "在非唯一目标列重建时命中既有唯一键的临时回放冲突，验证优化保留合法最终提交。",
        "业务真实重复、普通成功重建或未命中回放冲突被误记为优化通过，会掩盖冲突处理缺陷。",
        """
        目标为非索引普通列，源表确有既有 UNIQUE，类型扩宽与约束资格已经验证。
        正式回放同步/冲突证据入口和适用优化契约已提供；缺失 BLOCKED，不编造内部同步点。
        INSERT/UPDATE/DELETE/REPLACE/IODKU 各合法事件链和最终无重复期望预先确定。
        并发数、持续时间、最大事件、资源、超时与停止阈值登记；停止后释放同步，恢复事务并清理独立源表。
        """,
        """
        指定覆盖 | 两项固定 ON 且有正式冲突证据的实例 | OFF 不执行
        指定覆盖 | 已通过非索引目标列加既有 UK 的代表表 | 保持源夹具便于重现
        指定覆盖 | 非唯一目标列已交付安全扩宽及精确键旁列 | 不混入修改唯一列自身
        边界覆盖 | 既有 UK 的临时占用/释放及旁索引 | 真实重复探针独立事务
        边界覆盖 | 回放临时冲突但最终无重复的确定性值集 | 固定种子与事件 ID
        指定覆盖 | 原始 INPLACE/NONE 重建 | 不改动作避开目标冲突
        边界覆盖 | 各 DML 事件链、回放冲突、最终 COMMIT/ROLLBACK | 命中冲突后核验终态
        指定覆盖 | 我方副本、客户端错误来源与恢复连接 | 区分 DML 错误和 DDL 回执
        全部覆盖 | 阶段命中、提交账本、锁队列和唯一终态 | 没有冲突证据不能判优化通过
        """,
        """
        临时回放冲突 | 正式冲突/阶段记录与事件时间线 | 确实命中适用冲突通过；仅重建成功 BLOCKED
        业务合法性 | 原表提交回执和最终无重复计算 | 事件链合法终态无重复通过；真实重复不属本正向
        DDL 终态 | 原 SQL、实际算法及最终任务/数据库状态 | 适用优化下完整成功通过；错误中止失败
        事务与锁 | 账本、等待队列及释放记录 | 无丢提交且队列有界恢复通过；悬挂失败
        唯一维护与副本 | 全行/各索引、重复探针和每端期望 | 约束仍有效且数据精确通过；漏约束失败
        """,
        "未命中冲突的成功只记基础重建证据；不计本优化目标 PASS。"),
    "SC33": _policy(
        "验证唯一列自身扩宽与多 UNIQUE 冲突交织时，每个索引保持各自比较和约束语义。",
        "只核验目标唯一索引会漏掉旁唯一索引损坏，REPLACE/IODKU 的多个冲突候选容易造成错误删除或定位。",
        """
        源表具有目标 UK 及至少一个旁 UK，单列/复合/合法 16 key part 夹具实际建成。
        nullable、PAD/NO PAD、字符/二进制前缀各独立准备，预先核定每个 UK 的等价类。
        REPLACE/IODKU 与改目标/旁键事件链有真实业务结果期望，正式回放证据缺失 BLOCKED。
        并发数、持续时间、资源、事件上限及超时登记；停止后释放等待、恢复事务并清理多索引夹具。
        """,
        """
        指定覆盖 | 固定 ON 和相应排序规则/正式回放入口 | 不执行 OFF 归因轮
        指定覆盖 | 已通过的多 UK 高风险代表表 | 每种冲突机制独立克隆
        指定覆盖 | 字符/二进制唯一列安全扩宽及 NULL 属性 | 精确按补零/变长/比较规则求期望
        边界覆盖 | 单列、复合、16 key part、多 UK、nullable/前缀 | 每个 UK 都查维护和约束
        边界覆盖 | NULL、PAD 等价、共同前缀、不同冲突候选 | 终态按实际 DML 语义计算
        指定覆盖 | 唯一目标列 INPLACE 重建 | 不以旁列重建替代本路径
        边界覆盖 | 目标/旁 UK 更新、REPLACE/IODKU、临时回放冲突和业务拒绝 | 正式阶段加每次事务回执
        指定覆盖 | 我方副本与精确客户端 | 各端逐 UK 和完整行对照
        全部覆盖 | 阶段、账本、每个约束、队列/锁、终态 | 一个 UK 成功不能替代其他 UK
        """,
        """
        各 UK 身份 | 全部定义、key part、NULL/前缀/排序规则 | 每个实际约束明确通过；遗漏索引不足通过
        冲突来源 | DML/DDL 独立回执与正式回放记录 | 业务拒绝与临时冲突准确区分通过；无证据 BLOCKED
        多候选终态 | REPLACE/IODKU 账本及独立最终行 | 删除/更新对象符合真实语义通过；错误删行失败
        锁队列终态 | 各事务等待和提交/回滚记录 | 全部确定结束且锁释放通过；不明提交 BLOCKED
        逐索引与副本 | 每 UK 探针、表扫/回表和各端结果 | 所有约束及数据正确通过；任一坏索引失败
        """,
        "16 key part 使用不同真实成员；不能用重复引用同列伪造复合边界。"),
    "SC34": _policy(
        "验证真实重复和优化排除动作仍准确拒绝，失败后原定义、合法提交及后续业务保持可用。",
        "固定 ON 被当作全部 PK/特殊唯一索引均支持，或真实重复被优化吞掉，会破坏约束和原子性。",
        """
        ADD/替换 PK、ADD UK、虚拟 UK/MVI 各独立冻结资格；未知动作契约 BLOCKED。
        合法源准备后分别构造预检、扫描和回放最终重复，不用无法 CREATE 的重复源结构。
        每种动作的原定义、独立业务账本及后续合法读写/重复探针已备妥。
        并发、持续时间、最大重复事件、资源和超时限定；停止后恢复连接/事务并清理专属特殊索引资产。
        """,
        """
        指定覆盖 | 固定 ON 且排除范围已冻结 | 不依据开关猜测特殊索引支持
        指定覆盖 | 各排除动作合法源代表夹具 | 特殊崩溃载体隔离执行
        指定覆盖 | PK/UK 对应已交付类型及生成/MVI 结果资格 | 特殊类型不外推普通键结论
        专属子运行 | ADD/替换 PK、ADD UK、虚拟 UK/MVI 各动作 | 维护与新增优化资格分开
        边界覆盖 | 预检、扫描、回放最终真实重复和合法对照 | 最终违规有独立证据
        边界覆盖 | 动作对应算法锁和独立 COPY 控制 | 原请求拒绝不改 SQL 冒充通过
        边界覆盖 | 最终违规 COMMIT、业务拒绝、DDL 原子回滚和后续重试 | 先核验真实表态再重试
        指定覆盖 | 我方副本和错误后连接可用性 | 恢复记录保留首轮错误
        全部覆盖 | 错误阶段、账本、锁/队列、原定义与唯一终态 | 真实违规不得被静默跳过
        """,
        """
        排除契约 | 每动作正式支持/排除说明与源定义 | 对应规则明确通过；未冻结 BLOCKED
        真实重复 | 最终键集合和各阶段提交证据 | 违规确实存在通过夹具验证；临时冲突不属此负向
        失败阶段与原子性 | errno/SQLSTATE、完整旧结构和索引快照 | 准确拒绝且旧态完整通过；吞错或半态失败
        账本及锁释放 | 合法提交终态、队列/锁和任务状态 | 提交保留且无悬挂通过；丢提交失败
        后续业务和副本 | 合法读写、重复探针、各端独立结果 | 原约束可用且每端正确通过；异常恢复不抹首轮失败
        """,
        "先冻结排除项再判定；特殊索引 COPY 控制不计对应 INPLACE 支持。"),
    "SC35": _policy(
        "证明长事务持有 MDL 时 DDL 与新业务排队关系可观测，取消后队列和事务恢复到确定终态。",
        "取消回执与实际 DDL 状态不一致或队列残留会造成持续阻塞，旧 read view 首次访问分支还可能被误判。",
        """
        已访问/未访问目标表的旧 read view、未提交写事务各独立建立，RC/RR 使用既有配置组。
        按持事务→发 DDL→发新业务顺序构造等待链，真实 MDL 观测入口已确认。
        正式取消入口及其任务终态规则已明确；原生首次访问错误按源卡记录。
        并发连接数、持续时间、资源预算、锁/取消超时和停止条件登记；停止时释放原事务，恢复队列并清理连接。
        """,
        """
        指定覆盖 | 固定 ON、已有 RC/RR 和正式取消入口 | 不切功能参数制造锁行为
        指定覆盖 | 可稳定持有 MDL/read view 的已通过表 | 最小结构保持等待链可解释
        指定覆盖 | 已交付类型与事务读写列 | 初次访问与已访问区别记录
        指定覆盖 | 原有兼容索引/约束 | 取消后保持依赖可用
        边界覆盖 | 旧快照值、未提交值及取消后新提交 | 按实际隔离规则构建预期
        指定覆盖 | 等 MDL 的原 INPLACE 请求及正式取消 | 重试前核查实际 schema
        边界覆盖 | read view 两类、持锁、排队、超时/取消、释放与恢复 | 真实等待边与事务终态逐一关联
        指定覆盖 | 多连接和任务取消/恢复 | 取消客户端回执与数据库独立对账
        全部覆盖 | 阶段、提交账本、MDL 队列、取消终态和后续业务 | 原生可见性分支不笼统要求相同结果
        """,
        """
        等待链 | MDL 锁对象/拥有者/等待者与发起时间 | 真实三段队列命中通过；仅耗时猜锁 BLOCKED
        取消终态 | 正式取消回执、任务状态和实际 schema | 终态一致通过；客户端取消但任务不明 BLOCKED
        旧快照分支 | 已/未访问事务首次读回或原生错误 | 按隔离及源卡语义通过；非法外推失败
        队列恢复 | 释放时间、排队业务提交账本和残留锁清单 | 有界恢复且无遗留锁通过；持续阻塞失败
        数据可用性 | 取消后合法读写与完整行期望 | 合法提交保持且原表可用通过；丢数据失败
        """,
        "必须有真实等待链；取消只由回执确认不足以判断 DDL 已回滚。"),
    "SC36": _policy(
        "验证 XA PREPARED 跨断连/重启后提交或回滚与 DDL 等待、并发 DDL 串行化产生唯一终态。",
        "不明 XA 被重复提交或未核表态便重试 DDL，可能造成重复动作、悬挂锁和不可恢复事务。",
        """
        使用专属 XA 标识，预备 PREPARED 后断连与隔离实例重启两个独立夹具。
        正式 XA 恢复、重启和 MDL 观测入口可用；保存事务业务期望与原 schema。
        XA COMMIT/ROLLBACK、同表/不同表并发 DDL 分开序列，重试前核验表态。
        并发连接、持续时间、资源、最大重试和超时有上限；停止后按账本恢复 XA，清理确认终结的测试事务。
        """,
        """
        指定覆盖 | 固定 ON 和现成 XA/恢复环境 | 正式重启入口缺失 BLOCKED
        指定覆盖 | XA 可持锁的代表表与同表/异表 DDL 对 | 不扩展为全表型压力
        指定覆盖 | 已通过安全扩宽类型及事务列 | schema 版本记录在账本
        指定覆盖 | 原有兼容主键/唯一约束 | 恢复后重复探针核验
        边界覆盖 | XA 内未决值与最终提交/回滚值 | 每个 XA ID 唯一期望
        指定覆盖 | 等待及重试的原 INPLACE、并发 DDL 入口 | 不盲目重复未确认动作
        边界覆盖 | PREPARED、断连/重启、COMMIT/ROLLBACK、DDL 串行化 | 逐事务与 DDL 记录最终状态
        专属子运行 | 断连与正式重启恢复 | 恢复日志和 XA 列表共同留证
        全部覆盖 | XA/DDL 阶段、提交账本、MDL 队列和确定终态 | 没有悬挂或重复提交
        """,
        """
        XA 持久身份 | PREPARED 回执、恢复事务列表和专属 XA ID | 同事务恢复可追溯通过；身份不明 BLOCKED
        MDL 等待 | XA 持锁与 DDL 等待链 | 因果链明确通过；凭耗时推断 BLOCKED
        XA 最终数据 | COMMIT/ROLLBACK 回执与业务终态 | 仅一次提交或完整回滚通过；重复提交失败
        并发 DDL 序列 | 每任务起止、实际 schema 和串行顺序 | 同表合法串行化且终态唯一通过；盲重试失败
        清理可用性 | 残留 XA/锁列表及后续合法 DDL/DML | 无悬挂且可继续通过；超时未恢复失败或待恢复 BLOCKED
        """,
        "只清理专属且身份已确认的测试 XA；未决事务不应凭客户端断开判回滚。"),
    "SC37": _policy(
        "在不改日志容量参数的条件下验证持续回放追赶和当前 row log 容量越限的准确失败。",
        "普通重复错误被误记日志越限、概率负载未命中目标阶段，或停止后资源未清理会产生虚假覆盖。",
        """
        只读记录当前正式 row log 容量配置及可用阶段/越限证据；缺映射或证据入口 BLOCKED。
        固定种子 ON 四轮确定性序列与概率压力分开，OFF 轮明确不执行。
        容量内/越限通过负载规模控制，热点数据和独立提交账本可重建终态。
        并发、QPS 梯度、持续时间、资源预算、事件上限和超时登记；阈值触发停止，恢复容量可用后清理并独立重试。
        """,
        """
        指定覆盖 | 固定 ON 和当前容量配置 | 只读记录，不调日志参数
        指定覆盖 | 可稳定生成回放压力的已通过代表表 | 热点与多索引负载单列
        指定覆盖 | 已交付安全类型及热点写列 | 值合法以免混入转换错误
        边界覆盖 | 原有 UK 与兼容旁索引 | 普通 1062 和日志越限分账
        边界覆盖 | 容量内/越限工作量和热点分布 | 每组保留提交事件与实际资源曲线
        指定覆盖 | INPLACE 重建及清理后一次独立合法重试 | 不覆盖首轮失败
        边界覆盖 | 多轮回放、追赶、越限、ON 四轮和概率压力 | 阶段命中与重复轮次分别记录
        指定覆盖 | 我方副本、客户端错误传播和恢复 | 追平以已提交业务为准
        全部覆盖 | 阶段/容量证据、账本、锁队列、资源清理及终态 | 未命中越限不能判越限通过
        """,
        """
        当前容量与命中 | 只读配置、正式阶段/越限记录及负载时间线 | 目标越限有证据通过；普通 1062 不计通过
        容量内追赶 | 回放阶段序列、提交账本和完成时间 | 有界追赶且终态正确通过；无阶段证据 BLOCKED
        越限原子性 | 指定错误阶段、源结构与全部合法提交 | 准确失败且业务保留通过；丢提交失败
        队列和资源 | 停止后锁、row log/临时空间和任务状态 | 回归清理基线通过；持续残留失败
        重试与重复轮次 | 四轮独立结果及恢复后新 DDL/主备终态 | 各轮正确且重试可运行通过；不得平均隐藏单轮失败
        """,
        "确定性命中与概率压力分开报告，日志容量参数全程不变。"),
    "SC38": _policy(
        "在构建、回放、切换和持久提交边界定点崩溃，验证恢复后 schema 与已提交业务对应同一合法终态。",
        "响应丢失被误判回滚、持久提交被恢复覆盖或字典/数据半新半旧，会导致提交确认与真实状态不一致。",
        """
        五组阶段分别独立克隆：构建、首次回放、切换前、提交后响应前、已返回成功后。
        正式同步/故障入口与持久化证据规则已冻结；缺入口或精确位置证据则 BLOCKED。
        选已通过的自增/复合 UK/边界字节代表夹具，备份、提交账本及主备恢复流程预先验证。
        并发业务、持续时间、资源预算、单轮故障次数、恢复超时和停止规则登记；恢复核验后清理专属克隆与临时资产。
        """,
        """
        专属子运行 | 固定 ON 的隔离恢复环境 | 只使用正式授权故障入口
        指定覆盖 | 已通过自增/复合 UK/页外边界代表表 | 不乘全表型和全物理矩阵
        指定覆盖 | 已交付高风险整数/字节类型与旁列 | 完整值期望用于恢复判断
        指定覆盖 | 代表 PK/多 UK/旁索引依赖 | 恢复后逐个查询和约束探针
        边界覆盖 | 故障前已提交、新范围及未决事务 | 按持久提交证据裁定合法终态
        指定覆盖 | 原 INPLACE 请求和真实目标定义 | 原请求回执保存不可覆盖
        边界覆盖 | 五个持久化边界与有界业务提交 | 每阶段独立故障，不靠延时猜命中
        专属子运行 | 正式崩溃/恢复与我方主备，阿里只必要主机对照 | 每轮恢复到可验证基线
        全部覆盖 | 持久边界、恢复、任务/客户端、复制与清理 | 已返回成功必须保留新态
        """,
        """
        故障位置 | 正式同步记录、故障时间和持久化证据 | 精确命中通过；仅 sleep 定位 BLOCKED
        DDL 持久终态 | 成功回执、提交证据、恢复 schema | 已确认成功保留新态通过；无确认按正式证据裁定，丢确认提交失败
        业务事务 | 故障前提交账本和恢复后全行 | 已提交不丢且未决按证据处理通过；丢行失败
        索引与复制 | 各索引/约束及追平后每端期望 | 主备各自正确通过；半新半旧失败
        恢复和清理 | 恢复日志、任务/客户端对账、临时对象基线 | 有界恢复且无残留通过；缺正式恢复入口 BLOCKED
        """,
        "未确认响应不能单凭客户端报错判旧态；每一故障轮保留原始结论。"),
    "SC39": _policy(
        "在 DDL 恢复回滚过程中第二次崩溃，验证重复恢复及中间资产清理幂等。",
        "二次恢复可能重复删除仍需文件、残留字典对象或恢复出半态，使下一次合法 DDL 无法执行。",
        """
        选已通过且有多索引/页外数据的代表源表，原定义与合法提交账本已保存。
        正式入口能证明首次恢复处于回滚阶段，并能在指定边界二次故障；缺失 BLOCKED。
        每轮只两次故障，恢复日志、数据/字典/临时索引资产清单和正式恢复流程就绪。
        并发业务、持续时间、资源和每次故障/恢复超时有限；触发停止后完成恢复，核验清理再独立合法扩宽。
        """,
        """
        专属子运行 | 固定 ON 的独立恢复实例 | 首次和二次故障分开记录
        指定覆盖 | 已通过多索引/页外数据代表载体 | 不扩展全功能矩阵
        指定覆盖 | 已交付安全类型及完整旁列 | 原态精确期望用于回滚验收
        指定覆盖 | 代表主/二级索引和约束 | 记录重建中临时资产身份
        边界覆盖 | 首次故障前已提交与恢复中无新增不明写 | 业务账本保持唯一
        指定覆盖 | 首次 INPLACE 和最终独立重试 | 重试前确认原态与资源基线
        边界覆盖 | 首故障→恢复回滚→二次故障→最终恢复 | 每次阶段都要求正式证据
        专属子运行 | 两次崩溃、重复恢复、任务对账与资产清理 | 不通过盲删文件修复
        全部覆盖 | 原态一致、提交保持、清理幂等和后续可用 | 恢复成功不抹早期异常
        """,
        """
        二次故障命中 | 恢复回滚阶段与第二故障正式记录 | 精确位置可证通过；入口缺失 BLOCKED
        字典与数据态 | 恢复 schema、完整数据和提交账本 | 原态完整且无半态通过；字典数据错配失败
        清理幂等 | 两次恢复日志及临时表/索引/文件清单 | 无误删和残留通过；重复清理破坏源资产失败
        任务客户端 | 每次异常回执、最终任务状态和连接读写 | 状态一致且可用通过；客户端回执不明需对账 BLOCKED
        独立重试 | 基线确认后的合法扩宽和索引检查 | 后续完整成功通过；被残留阻塞失败
        """,
        "二次故障次数固定；不得为恢复测试删除身份不明的数据库文件。"),
    "SC40": _policy(
        "分别验证排序临时文件空间不足、中间表分配和回放内存分配失败的精确错误与原子恢复。",
        "全盘满或进程 OOM 替代目标分配错误会改变测试机制，未命中调用的异常可能被错误归因。",
        """
        三类故障均有正式目标 DDL 定点入口及命中证据，缺任一入口对应分支 BLOCKED。
        采用已通过宽行/多索引代表夹具，源快照、提交账本和正式资源恢复方法就绪。
        故障资源限制在专属测试配额或正式注入范围，不混用全盘满、进程 OOM 或任意文件破坏。
        并发 DML、持续时间、资源上限、注入次数和超时登记；阈值触发停止，恢复资源并清理临时资产后重试。
        """,
        """
        专属子运行 | 固定 ON 且有正式目标分配注入环境 | 未提供接口不编造参数或 API
        指定覆盖 | 宽行/排序索引/回放代表源表 | 一种失败机制一组克隆
        指定覆盖 | 已交付类型和长值旁列 | 转换预期不受资源异常改写
        指定覆盖 | 代表排序索引与唯一约束 | 故障后核验原依赖
        边界覆盖 | 合法提交和目标资源分配边界 | 不用数据违规触发替代错误
        指定覆盖 | 原始 INPLACE 请求与恢复后独立重试 | 首轮失败单独保留
        边界覆盖 | 排序/中间表/回放各阶段注入及 DML 提交 | 同步命中和账本关联
        专属子运行 | 三类正式故障、恢复、客户端错误和清理 | 不外推全实例 OOM 行为
        全部覆盖 | 命中调用、错误传播、原子性、可用性和资源回收 | 无命中证据 BLOCKED
        """,
        """
        目标调用命中 | 正式注入回执与阶段/调用证据 | 正确分配点命中通过；普通 OOM/全盘满不足通过
        错误传播 | errno/SQLSTATE、任务状态及客户端原错误 | 对应错误准确传播通过；吞错/错报成功失败
        原子业务态 | 原 schema、索引和合法提交账本读回 | 原态完整且提交保留通过；半态或丢行失败
        资源恢复清理 | 配额恢复记录、临时资产与内存/空间基线 | 可用且无持续泄漏通过；持久不可用失败
        后续运行与副本 | 恢复后独立 DDL/DML 及我方副本期望 | 重新可运行且各端正确通过；恢复入口缺失 BLOCKED
        """,
        "三种目标分配失败独立判定，不能用同一泛化资源故障结果覆盖。"),
    "SC41": _policy(
        "按已有日志格式配置验证 INPLACE DDL 与新范围事务复制顺序及主备精确值。",
        "仅主备互相一致会掩盖双方共同截断，未追平便查询又可能把延迟误报或误判一致。",
        """
        ROW/STATEMENT/MIXED 与 ROW FULL/MINIMAL 仅使用现成已配置实例，功能开关保持固定。
        选已通过 BIGINT/DECIMAL/二进制代表夹具，精确独立期望不使用浮点。
        复制位点/GTID 与正式状态查询入口可用，确定性业务保存最后确认提交位置。
        并发更新、持续时间、资源、复制超时和停止阈值登记；停止写入后恢复追平并清理专属源表。
        """,
        """
        全部覆盖 | 已有 ROW/STATEMENT/MIXED、FULL/MINIMAL 固定配置 | 缺配置单列 BLOCKED，不切换参数凑组
        指定覆盖 | 已通过的大值和热点代表表 | 不乘全表型矩阵
        指定覆盖 | BIGINT/已交付 DECIMAL/二进制边界 | 主备分别对独立精确期望
        指定覆盖 | 代表复合 UK 与旁索引 | 复制后约束与访问路径检查
        边界覆盖 | 64 位/十进制/零字节新旧边界、长事务与热点 | 每条业务保存精确值和提交位点
        指定覆盖 | 原 INPLACE DDL 后的新范围 DML | 顺序绑定真实提交边界
        边界覆盖 | 长事务/并行复制/热点交织的有限序列 | 停写后确定最终比较位点
        全部覆盖 | 我方主备、复制格式、追平/错误、精确客户端 | 阿里仅主机，无其备机检查
        全部覆盖 | 日志顺序、持久提交、每端正确和清理 | 追平超时不默认通过
        """,
        """
        配置与日志顺序 | 只读格式配置、DDL/DML 提交位点 | 组别真实且顺序合法通过；无位点证据 BLOCKED
        复制追平 | 最后确认位点及每个副本执行状态 | 全部追平通过；超时 BLOCKED，复制错误失败
        精确大值 | 每端独立 BIGINT/DECIMAL/HEX 比较 | 双方各自符合预期通过；共同截断仍失败
        索引与结构 | 每端目标定义、各索引结果及约束 | 复制后完整正确通过；只主备相同不足通过
        客户端和清理 | 精确协议结果、任务终态、停止后资源状态 | 无解码损失且任务清理通过；句柄误读失败
        """,
        "类型 INSTANT 专属复制证据不纳入本轮；延迟超时保持未完成。"),
    "SC42": _policy(
        "在 DDL 提交前及确认成功后切换我方主备，验证新主继续新范围业务及原主重新追平。",
        "切换时丢失已确认 DDL/业务提交或把未确认任务盲目重试，可能造成结构错配和自增重复。",
        """
        我方正式切换、回归节点追平和任务对账入口已验证；阿里不执行主备切换。
        自增整数、复合 UK、合法 FK 关系各选择已通过代表夹具，保存独立期望。
        提交前与已确认成功后分两个独立轮次，切换点需要正式持久化/阶段证据。
        并发业务、持续时间、资源、切换/追平超时和停止条件登记；按正式流程恢复拓扑并清理测试资产。
        """,
        """
        专属子运行 | 固定 ON 的我方主备切换环境 | 正式入口缺失 BLOCKED
        指定覆盖 | 自增/复合 UK/FK 已通过高风险载体 | 不乘所有表型
        指定覆盖 | 新整数范围和关系兼容类型 | 切换后写值依据实际 schema
        指定覆盖 | 代表 PK/UK/FK 依赖图 | 逐约束验证新主和回归节点
        边界覆盖 | 切换前确认值、未决值和新主新范围值 | 用提交账本区分承诺边界
        指定覆盖 | 原 INPLACE 任务和必要合法后续动作 | 不明状态先对账再重试
        边界覆盖 | 提交前/成功后两边界与持续有界业务 | 阶段证据和客户端回执对齐
        专属子运行 | 我方切换、新主续写、原主回归追平 | 阿里仅主机对照
        全部覆盖 | 持久终态、任务/客户端、约束、拓扑恢复与清理 | 已确认提交不丢
        """,
        """
        切换边界 | 正式阶段/持久证据、成功回执和切换记录 | 两类位置准确命中通过；仅时间猜测 BLOCKED
        原任务终态 | 新主 schema、任务 ID 和提交账本 | 已确认新态保留通过；未确认按证据定态，盲重试失败
        新主业务 | 新范围 INSERT/UPDATE、自增和约束回执 | 精确一次提交且约束有效通过；回绕/重复失败
        原主回归 | 追平位置、全行及依赖图独立预期 | 每端正确通过；追平超时 BLOCKED
        恢复清理 | 正式拓扑恢复记录、遗留任务/连接/临时对象 | 拓扑稳定且清理完成通过；状态分叉失败
        """,
        "新主允许的新范围以已核实 schema 为准；任务终态不明必须先对账。"),
    "SC43": _policy(
        "验证 DDL 前、DDL 后和新范围事务后备份/PITR 恢复点的结构与数据事务边界对应。",
        "用不匹配的备份、日志或物理资产恢复可能产生新 schema 配旧数据，表面可读但索引和约束不一致。",
        """
        正式逻辑/物理/PITR/导入导出入口与产品支持范围已冻结，缺入口的子运行 BLOCKED。
        选择已通过扩宽与索引约束代表表，备份资产身份、时间和提交位点可追溯。
        DDL 前、后及新范围事务后三个独立目标点有各自 schema/数据期望，恢复到专属实例。
        备份/业务并发、持续时间、资源预算、恢复超时和停止规则登记；恢复验证后按资产清单清理，不覆盖源实例。
        """,
        """
        专属子运行 | 固定 ON 与现成正式备份恢复能力 | 入口不全对应分支 BLOCKED
        指定覆盖 | 已通过索引/约束及物理资产代表表 | 不乘全存储矩阵
        指定覆盖 | 已交付扩宽类型与新范围值 | 各恢复点使用对应声明类型
        指定覆盖 | 代表 PK/UK/FK 或生成依赖 | 恢复后逐项验证
        边界覆盖 | 三个恢复点的已提交/未到达事务 | 点位预期不能共用最终数据
        指定覆盖 | INPLACE DDL 前后历史 | 类型 INSTANT 混合代际恢复范围外
        边界覆盖 | 备份期间有限提交及恢复点事务截断 | 提交账本绑定恢复目标
        专属子运行 | 逻辑/物理/PITR/导入导出正式支持路径 | 每条路径独立资产与恢复目标
        全部覆盖 | 资产身份、持久边界、恢复、读写和清理 | 无源位点证据不计通过
        """,
        """
        恢复资产身份 | 备份清单、源位点、日志范围和恢复目标 | 资产与点位一致通过；身份不明 BLOCKED
        点位结构 | 三个恢复目标 SHOW CREATE 与各点位期望 | 各点 schema 正确通过；套用最终定义失败
        点位数据 | 独立事务账本与恢复全行 | 只含目标点应有提交通过；丢已提交/混入未来事务失败
        索引及后续业务 | 访问路径、约束探针和合法新写 | 恢复可读写且依赖正确通过；仅启动成功不足通过
        任务和清理 | 恢复任务终态、错误日志和专属资产清理表 | 有界恢复并可追溯清理通过；入口缺失 BLOCKED
        """,
        "恢复到专属目标；类型 INSTANT 专属代际恢复不计本轮通过。"),
    "SC44": _policy(
        "区分提交前取消与持久提交后响应丢失，验证客户端重连、旧预处理句柄和管控重试的终态一致性。",
        "把超时当回滚并直接重发可重复执行，旧句柄类型/长度缓存还会截断扩宽字段或误解列顺序。",
        """
        正式提交前取消和提交后丢响应入口可用，任务 ID/幂等键及重试契约已冻结；缺失 BLOCKED。
        已通过改名/调序/可见性代表夹具与旧 prepared 句柄、连接池及新连接已备妥。
        独立保存精确大整数/二进制/字段顺序期望，重连第一步查询真实 schema 与任务状态。
        连接/重试并发、持续时间、资源、最大重试和超时有限；停止后恢复连接状态并清理句柄、任务与专属夹具。
        """,
        """
        指定覆盖 | 固定 ON、正式任务/协议和现成旧构建环境 | 不编造管控幂等 API
        指定覆盖 | 已通过改名/调序/可见性高风险代表表 | 不乘全表型
        指定覆盖 | 大整数/变长字段及列属性 | 精确解码期望事先生成
        指定覆盖 | 代表主键/索引及字段依赖 | 重试后约束持续验证
        边界覆盖 | 旧新字段长度、顺序、新范围和未确认提交 | 依据实际持久证据求终态
        指定覆盖 | 原 INPLACE SQL 与同幂等键重试 | 先对账不盲目重放
        边界覆盖 | 提交前取消/持久后丢响应、旧句柄与新连接 | 两个时点独立执行
        全部覆盖 | 本场景适用直接协议/连接池/prepared/任务对账及我方副本 | 每种客户端保留字段元信息
        全部覆盖 | 持久状态、幂等、元信息刷新、恢复与清理 | 超时不直接等同回滚
        """,
        """
        丢响应位置 | 正式阶段证据、提交记录与客户端超时 | 提交前后准确分组通过；位置未证 BLOCKED
        对账终态 | 任务 ID、幂等键、数据库 schema 与回执 | 一致且唯一终态通过；任务/数据库矛盾失败
        重试次数 | 真实执行记录和业务提交账本 | 契约范围内不重复执行/提交通过；盲重试重复失败
        旧句柄元信息 | 旧新句柄字段类型/长度/顺序及精确读写 | 按客户端契约刷新或准确报错通过；静默截断失败
        恢复与副本 | 连接池恢复、各端期望及残留句柄任务清单 | 每端正确且清理完成通过；契约缺失 BLOCKED
        """,
        "同幂等键重试只按正式管控契约验证；数据库没有该入口时保留 BLOCKED，不杜撰。"),
    "SC45": _policy(
        "在已通过功能正确性的类型代表夹具上测大表扩容和共享业务负载，取得可比较的性能资源证据。",
        "缓存、数据规模或基线不一致会造成虚假提升，SHARED 虚拟列被套写不停门槛会误报性能。",
        """
        整数、字符、二进制及已交付 DECIMAL/虚拟列均先有小数据正确性通过的代表运行 ID。
        按源卡选择 10M～200M+ 行或至少 10GiB 的适用规模，实际资源预算不足的目标规模 BLOCKED，不静默缩小。
        正式冻结性能门槛、预热、缓存状态、无 DDL 配对、重复轮次和计时区间；未冻结不判性能 PASS。
        并发数、QPS、持续时间、资源上限、复制/DDL 超时和停止阈值登记；停止后恢复业务基线，计时外校验并清理资产。
        """,
        """
        指定覆盖 | 固定 ON 与已冻结门槛的同构测量环境 | 记录 CPU/内存/存储和共享干扰
        指定覆盖 | 已通过的各交付类型代表载体，大表/工作集超 BP | 不乘所有表型和功能矩阵
        全部覆盖 | 已交付类型族各有已通过代表；未交付范围除外 | 虚拟列 SHARED 使用对应独立门槛
        指定覆盖 | 每类型代表多索引/高风险依赖 | 选型绑定先前正确性证据
        边界覆盖 | 源卡规模、热点/均匀、冷热缓存和超 BP 工作集 | 实际行数/字节和预热方法留证
        指定覆盖 | 相同 INPLACE 动作和无 DDL 配对基线 | COPY 不混作同算法基线
        边界覆盖 | 阶段计时、高读写共享负载和固定 ON 重复 | 限定线程/QPS/时长并记录节流
        指定覆盖 | 我方副本追赶、客户端延迟与资源恢复 | 超时或中止保留不完整结果
        全部覆盖 | 吞吐/延迟/耗时/空间/CPU/内存/复制、正确性和缺陷证据 | 正式门槛未冻结 BLOCKED
        """,
        """
        基线可比性 | 正确性运行 ID、硬件/规模/缓存和无 DDL 配对记录 | 条件可比通过；差异未控制 BLOCKED
        阶段性能 | 阶段耗时、吞吐和延迟分布原始采样 | 满足冻结门槛通过；无门槛 BLOCKED，越线失败
        资源与复制 | CPU/内存/临时空间曲线及副本延迟 | 在预算和门槛内通过；触发停止阈值记录未完成/失败原因
        类型锁差异 | 虚拟列 SHARED 锁证据与对应基线 | 使用匹配门槛通过；套写不停指标失败判定
        计时外正确性 | 全行/索引/主备独立期望及可复现异常包 | 全部正确才能性能通过；数据错误优先失败
        """,
        "报告原始分布及重复轮次，不只平均值；计时窗口外完成完整数据验证。"),
    "SC46": _policy(
        "以固定 ON 的连续独立克隆轮次观察成功/负向 DDL 交替后的资源稳定趋势和清理完整性。",
        "重复在已扩宽表上运行会失去真实工作量，平均指标又可能掩盖单轮失败和累积泄漏。",
        """
        已交付类型族各选已通过代表夹具，每轮重建源结构与数据，不能在目标态重复空操作。
        合法扩宽与已验证原子负向交替，NEW_ON_NO_DDL/NEW_ON_POST_DDL 的测量方法事先固定。
        连接、锁、任务、中间对象和临时空间资源基线与稳定趋势门槛已冻结，主备核验频率确定。
        并发、持续总时长、最大轮次、资源、单轮超时和停止条件登记；停止后恢复基线、核验提交并清理所有专属轮次资产。
        """,
        """
        指定覆盖 | 固定 ON 与相同运行环境 | 旧构建/OFF 四组归因本轮不执行
        指定覆盖 | 已通过各类型族代表源克隆 | 每轮源结构真实重建
        全部覆盖 | 已交付类型族的已通过代表 | 未交付类型不混入稳定性通过
        指定覆盖 | 代表多索引/约束以观察清理 | 索引资产按每轮 ID 关联
        边界覆盖 | 固定种子数据和成功/负向交替 | 每轮完整期望独立保存
        指定覆盖 | 合法 INPLACE 与原子负向两个动作 | 记录每轮实际执行而非空改
        边界覆盖 | 有界轮次、共享业务和成功/失败终态 | 重试不覆盖原轮结论
        指定覆盖 | 我方主备、连接恢复与任务清理 | 定期全量比较并末轮收尾
        全部覆盖 | 资源曲线、稳定趋势、逐轮数据、清理及复现包 | 单轮失败不可被均值掩盖
        """,
        """
        真实轮次 | 每轮源定义、数据种子、实际 SQL 和结果 | 每轮确实执行目标动作通过；空操作不足通过
        资源趋势 | 连接/内存/空间/锁/任务时间序列和冻结基线 | 无持续累积且满足门槛通过；门槛缺失 BLOCKED
        逐轮正确性 | 成功目标态、失败原态和提交账本 | 每轮精确符合期望通过；任一半态失败
        清理完整性 | 轮次临时对象/文件/锁/任务清单 | 结束回归可解释基线通过；累积残留失败
        主备与缺陷包 | 定期/末轮全量比较和首错结构/SQL/时序 | 各端正确且异常可复现通过交付；平均掩错失败
        """,
        "报告逐轮最坏值和趋势；固定 ON 稳定性不能充当 OFF/旧构建因果归因。"),
    "SC47": _policy(
        "将跨厂商差异收敛为可复现的我方缺陷、已验证增强或明确无效对照，并形成完整交付证据。",
        "版本、开关、准备失败和实际算法不一致被误判能力差异，或双方相同错误被误记通过，会污染缺陷清单。",
        """
        已交付类型族选择已通过代表夹具和待收敛异常，双方逻辑数据、目标 SQL 与显式算法一致。
        阿里主机先执行，仅记录对照；我方主备各自对照独立期望，不排查阿里 BUG。
        版本/构建、固定参数、权限、源准备与实际算法证据齐全，源 ID 和子运行 ID 可追溯。
        复现并发、最大轮次、持续时间、资源、超时和停止条件明确；复现后恢复并清理专属夹具，首错原始证据保留。
        """,
        """
        全部覆盖 | 参与比较的实际固定 ON 环境差异 | 先排除版本/参数/权限/准备/算法不可比
        指定覆盖 | 每个已交付类型族已通过代表及异常最小源表 | 缩减保持原错误机制和完整依赖
        全部覆盖 | 已交付类型族及异常相关属性 | 未交付类型不能判当前增强通过
        指定覆盖 | 复现所必需索引/约束依赖 | 不删掉触发缺陷的隐含依赖
        指定覆盖 | 最小重现数据与原异常边界 | 保持精确原值和种子
        全部覆盖 | 比较项的原 SQL、请求/实际算法锁和首错阶段 | 修正 SQL 单开运行不覆盖原项
        边界覆盖 | 缺陷所需确定性事件序列和有限重试 | 固定线程与提交顺序保留复现性
        全部覆盖 | 阿里主机对照、我方主备独立预期、客户端错误 | 阿里备机和故障排查不在范围
        全部覆盖 | 能力分类、性能/资源佐证、稳定趋势和可复现缺陷包 | 未经主备正确验证只能候选增强
        """,
        """
        对照有效性 | 版本/参数/权限/源准备和实际算法证据 | 可比才通过对照资格；证据缺失 BLOCKED
        能力差异 | 双方原 SQL、阶段及回执 | 阿里支持我方不支持且环境已排除则我方失败/BUG
        候选增强 | 我方每端独立结构/数据/索引预期 | 主备均正确才增强通过；仅阿里拒绝不足通过
        缺陷复现包 | 最小完整 CREATE/数据/SQL/种子/时序/错误日志 | 有界可复现且机制保留通过交付；缺关键证据 BLOCKED
        逐项交付状态 | 每子运行 MD、首错/重试、性能资源或趋势附件 | 状态与证据一致通过；未执行不得 PASS，阿里查错范围外
        """,
        "双方都拒绝不自动代表通过；必须对照原场景正负向契约。每个子运行完成即更新记录。"),
    "SC48": _policy(
        "保留类型 INSTANT、累计代际和 OFF/ON 开关矩阵的后续追溯与交接，不执行本轮范围外测试。",
        "把后续定义、普通 INSTANT ADD/DROP 历史或未实测开关矩阵混入当前 PASS，会虚增交付能力。",
        """
        原始 1602 定义及对应 INSTANT/开关/恢复分支 ID 保留，源引用可追溯。
        本轮两项功能固定 ON，阿里类型 INSTANT 增强 OFF，我方类型 INSTANT 尚未转测。
        累计代际、OFF/ON 切换、关闭开销和专属恢复列出后续所需正式入口与待冻结契约。
        当前仅整理依赖与交接，不执行 SQL、不更改参数；后续运行需重新核定版本、范围及验收证据。
        """,
        """
        不适用 | 类型 INSTANT 与开关矩阵为本轮范围外 | 保留后续版本/参数依赖，不执行
        不适用 | 后续累计代际及类型 INSTANT 专属表历史 | 普通 INSTANT ADD/DROP 历史仍归当前适用场景
        不适用 | 尚未转测的类型 INSTANT 类型/属性范围 | 保留定义 ID，待正式交付再展开
        不适用 | 类型 INSTANT 专属索引和依赖组合 | 不以 INPLACE 结果代替
        不适用 | 后续代际数据和新旧格式边界 | 仅保留源定义追溯
        不适用 | 类型 INSTANT 与 OFF/ON 动作矩阵 | 不执行、不调整固定开关
        不适用 | 类型 INSTANT 专属并发/事务阶段 | 不计当前阶段命中或在线性
        不适用 | 类型 INSTANT 专属复制、恢复、开关客户端矩阵 | 后续需正式入口和恢复契约
        不适用 | 后续性能/关闭开销与功能验收 | 全部标范围外，不计当前 PASS
        """,
        """
        源定义追溯 | 原始定义 ID 与后续分支引用清单 | 定义保留且全部标范围外；不生成当前通过结论
        固定开关边界 | 本轮只读配置与执行清单 | 未执行切换符合范围外规则；擅改参数失败
        INSTANT 历史区分 | 普通 ADD/DROP 历史与类型增强分账 | 专属类型增强范围外；普通历史不被误删
        后续入口依赖 | 版本/正式参数/同步/恢复契约待办 | 未冻结保持范围外，不凭猜测标通过
        当前计数 | 本轮 PASS 汇总与 SC48 状态 | SC48 为范围外且不计当前 PASS；混入通过计数失败
        """,
        """不计当前 PASS；范围外不等于删除原始定义。
        后续转测时重新确认版本、参数、正式入口和验收契约；本轮不执行类型 INSTANT 或开关切换。"""),
}
