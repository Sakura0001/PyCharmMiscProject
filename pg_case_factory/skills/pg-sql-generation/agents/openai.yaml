interface:
  display_name: "PG SQL Generation"
  short_description: "PostgreSQL SQL test generation skill"
  default_prompt: "Use $pg-sql-generation to design PostgreSQL SQL generation coverage, lifecycle plans, and SQL cases with bundled base-object templates."
policy:
  allow_implicit_invocation: true
