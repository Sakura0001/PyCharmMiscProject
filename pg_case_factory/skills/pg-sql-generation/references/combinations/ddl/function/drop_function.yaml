schema_version: 1
kind: statement_combination_matrix
statement:
  key: drop_function
  name: DROP FUNCTION
  category: ddl
  domain: function
  source_reference: references/statements/ddl/function/drop_function.md
  official_source: https://www.postgresql.org/docs/16/sql-dropfunction.html
  aliases:
  - DROP FUNCTION
  - drop function
  - drop_function
execution_contract:
  required_matrix_is_baseline: true
  no_inference_before_required_coverage_passes: true
  runner_must_complete_required_matrix_first: true
  allow_post_coverage_extension_inference: true
  extension_combinations_must_be_marked: true
  extension_combinations_must_record_derivation: true
  extension_combinations_must_not_replace_required_coverage: true
  success_and_failure_both_allowed: true
  all_success_and_failure_reasons_must_be_declared: true
  required_coverage_sql_templates_must_come_from_combination_groups: true
  extension_sql_templates_must_be_recorded_in_artifacts: true
post_coverage_extension_policy:
  enabled: true
  allowed_after_audit_verdict: required_coverage_passed
  output_location: artifacts/intermediates/<task_slug>/derived_extension_combinations.yaml
  required_fields:
  - id
  - title
  - derived_from_combination_group
  - derivation_reason
  - factors
  - expected_status_policy
  - compatibility
  - sql_shape
  - verification
  - cleanup
  guardrails:
  - Required matrix coverage must pass before any derived extension is emitted.
  - Derived extensions may add stress, boundary, dependency, environment, or cross-factor combinations after baseline audit
    passes.
  - Derived extensions must not be counted as satisfying required factor, object, routine-signature, relation, table, or column-type
    coverage.
  - Derived extensions must use declared factors and declared factor values.
  - Derived extensions must preserve success or failure attribution.
coverage_scope:
  target_object_coverage:
    required: true
    coverage_mode: explicit
    inventory_source: references/combinations/_shared/coverage_inventory.yaml
    required_object_kinds:
    - function
    decision_reason: DROP_FUNCTION directly targets a PostgreSQL function object. Baseline coverage is scoped to routine lifecycle
      state, signature resolution, language/body attributes, privileges, and dependency behavior.
  target_relation_coverage:
    required: false
    coverage_mode: not_applicable
    inventory_source: references/combinations/_shared/coverage_inventory.yaml
    decision_reason: DROP_FUNCTION does not target a pg_class table, index, view, sequence, or materialized-view relation;
      routine catalog dependencies are represented as object and signature factors.
  table_coverage:
    required: false
    coverage_mode: not_applicable
    decision_reason: DROP_FUNCTION operates on routine catalog objects and signatures rather than table variants. Required
      baseline coverage does not depend on regular, temporary, partitioned, inherited, or foreign table forms.
  column_type_coverage:
    required: false
    coverage_mode: not_applicable
    decision_reason: DROP_FUNCTION requires argument and return type coverage as routine signature/type coverage through factor_contract
      and dynamic_inputs. Those types identify or define routine signatures; they are not table column definitions, so shared
      table column_type_coverage is not applicable.
factor_contract:
  source_reference_must_define_all_factors: true
  matrix_must_cover_required_factor_values: true
  factors:
    statement_branch:
      tier: T1
      coverage_role: main_axis
      required_values: &id001
      - branch_1
      - branch_2
      coverage_requirement: all_declared_values
    object_state:
      tier: T1
      coverage_role: main_axis
      required_values: &id002
      - exists
      - not_exists
      - different_signature_exists
      coverage_requirement: all_declared_values
    expected_status:
      tier: T1
      coverage_role: main_axis
      required_values: &id003
      - success
      - failure
      coverage_requirement: all_declared_values
    if_exists_clause:
      tier: T2
      coverage_role: behavior_axis
      required_values: &id004
      - present
      - absent
      coverage_requirement: all_declared_values
    cascade_restrict_clause:
      tier: T2
      coverage_role: behavior_axis
      required_values: &id005
      - CASCADE
      - RESTRICT
      - absent
      coverage_requirement: all_declared_values
    argtype_specification:
      tier: T2
      coverage_role: routine_signature_type_axis
      required_values: &id006
      - with_full_signature
      - without_signature_single
      - without_signature_multiple
      coverage_requirement: all_declared_values
    multiple_objects:
      tier: T2
      coverage_role: behavior_axis
      required_values: &id007
      - single_function
      - multiple_functions
      coverage_requirement: all_declared_values
    function_name_shape:
      tier: T3
      coverage_role: input_shape_axis
      required_values: &id008
      - simple
      - quoted
      - reserved_word
      - schema_qualified
      coverage_requirement: all_declared_values
    privilege_level:
      tier: T4
      coverage_role: dependency_or_environment_axis
      required_values: &id009
      - superuser
      - function_owner
      - non_owner_no_privilege
      coverage_requirement: all_declared_values
    dependent_objects:
      tier: T4
      coverage_role: dependency_or_environment_axis
      required_values: &id010
      - no_dependencies
      - has_dependents_restrict_blocks
      - has_dependents_cascade_removes
      coverage_requirement: all_declared_values
    schema_dependency:
      tier: T4
      coverage_role: dependency_or_environment_axis
      required_values: &id011
      - schema_exists
      - schema_not_exists
      coverage_requirement: all_declared_values
    target_function_not_exists:
      tier: T5
      coverage_role: negative_boundary_axis
      required_values: &id012
      - without_IF_EXISTS_error
      - with_IF_EXISTS_noop
      coverage_requirement: all_declared_values
    target_function_different_type:
      tier: T5
      coverage_role: negative_boundary_axis
      required_values: &id013
      - same_name_is_procedure
      - same_name_is_aggregate
      coverage_requirement: all_declared_values
    permission_insufficient:
      tier: T5
      coverage_role: negative_boundary_axis
      required_values: &id014
      - not_owner
      coverage_requirement: all_declared_values
    cascade_destroys_dependents:
      tier: T5
      coverage_role: negative_boundary_axis
      required_values: &id015
      - cascade_removes_trigger
      - cascade_removes_view
      coverage_requirement: all_declared_values
    identifier_length_exceeded:
      tier: T5
      coverage_role: negative_boundary_axis
      required_values: &id016
      - over_63_chars
      coverage_requirement: all_declared_values
    verification_mode:
      tier: T6
      coverage_role: verification_cleanup_axis
      required_values: &id017
      - pg_proc_catalog_query
      - information_schema_routines
      coverage_requirement: all_declared_values
    cleanup_mode:
      tier: T6
      coverage_role: verification_cleanup_axis
      required_values: &id018
      - DROP_FUNCTION_cascade
      - DROP_FUNCTION_if_exists_cascade
      - no_cleanup_needed
      coverage_requirement: all_declared_values
dynamic_inputs:
  routine_signature_resolution_manifest:
    required: true
    source: factor_contract.argtype_specification and combination_groups.expansion
    required_fields:
    - function_name
    - argument_signature
    - if_exists_clause
    - cascade_restrict_clause
    - dependency_state
    - expected_status
combination_groups:
- id: drop_function_required_baseline_factor_space
  title: DROP FUNCTION required baseline factor space
  lifecycle_role: target_statement
  expected_status_policy: per_factor_binding
  default_expected_status: success
  expected_failure_reasons:
  - expected_statement_failure
  - routine_resolution_or_privilege_failure
  - invalid_signature_or_type_binding
  - dependency_or_environment_failure
  factors:
    statement_branch: branch_1
    object_state: exists
    expected_status: success
    if_exists_clause: present
    cascade_restrict_clause: CASCADE
    argtype_specification: with_full_signature
    multiple_objects: single_function
    function_name_shape: simple
    privilege_level: superuser
    dependent_objects: no_dependencies
    schema_dependency: schema_exists
    target_function_not_exists: without_IF_EXISTS_error
    target_function_different_type: same_name_is_procedure
    permission_insufficient: not_owner
    cascade_destroys_dependents: cascade_removes_trigger
    identifier_length_exceeded: over_63_chars
    verification_mode: pg_proc_catalog_query
    cleanup_mode: DROP_FUNCTION_cascade
  expansion:
    statement_branch:
      mode: exhaustive
      values: *id001
    object_state:
      mode: exhaustive
      values: *id002
    expected_status:
      mode: exhaustive
      values: *id003
    if_exists_clause:
      mode: exhaustive
      values: *id004
    cascade_restrict_clause:
      mode: exhaustive
      values: *id005
    argtype_specification:
      mode: exhaustive
      values: *id006
    multiple_objects:
      mode: exhaustive
      values: *id007
    function_name_shape:
      mode: representative
      values: *id008
    privilege_level:
      mode: representative
      values: *id009
    dependent_objects:
      mode: representative
      values: *id010
    schema_dependency:
      mode: representative
      values: *id011
    target_function_not_exists:
      mode: representative
      values: *id012
    target_function_different_type:
      mode: representative
      values: *id013
    permission_insufficient:
      mode: representative
      values: *id014
    cascade_destroys_dependents:
      mode: representative
      values: *id015
    identifier_length_exceeded:
      mode: representative
      values: *id016
    verification_mode:
      mode: representative
      values: *id017
    cleanup_mode:
      mode: representative
      values: *id018
  compatibility:
    resolver: drop_function_factor_binding_resolver
    success_when:
    - expected_status=success and selected object, signature, privilege, and dependency bindings are valid
    - statement branch and routine kind support the selected type, language, body, or aggregate option binding
    failure_when:
    - condition: expected_status=failure or a declared negative boundary factor is selected
      reason: expected_statement_failure
    - condition: target routine object, signature, owner, schema, language, extension, support function, or privilege is missing
        or incompatible
      reason: routine_resolution_or_privilege_failure
    - condition: argument, return, aggregate signature, or support function type binding is invalid for the selected branch
      reason: invalid_signature_or_type_binding
    default_failure_reason: expected_statement_failure
  sql_shape:
    template: DROP FUNCTION [IF EXISTS] {function_name}({argument_signature}) [CASCADE|RESTRICT];
  verification:
    required: true
    mode: catalog_query
    sql: SELECT oid FROM pg_proc WHERE proname = '{routine_name}'
  cleanup:
    required: true
    steps:
    - sql: DROP FUNCTION IF EXISTS {function_name}({argument_signature}) CASCADE
audit_rules:
  require_all_required_top_level_keys: true
  require_declared_coverage_scope: true
  require_declared_factor_values: true
  require_expected_failure_reasons: true
  require_post_coverage_extension_policy: true
  forbid_extension_before_required_coverage_passes: true
  forbid_extension_counting_toward_required_coverage: true
