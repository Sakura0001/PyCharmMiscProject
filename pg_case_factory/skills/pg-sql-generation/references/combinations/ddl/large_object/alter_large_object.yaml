schema_version: 1
kind: statement_combination_matrix
statement:
  key: alter_large_object
  name: ALTER LARGE OBJECT
  category: ddl
  domain: large_object
  source_reference: references/statements/ddl/large_object/alter_large_object.md
  official_source: https://www.postgresql.org/docs/16/sql-alterlargeobject.html
execution_contract:
  required_matrix_is_baseline: true
  no_inference_before_required_coverage_passes: true
  runner_must_complete_required_matrix_first: true
  allow_post_coverage_extension_inference: true
  extension_combinations_must_be_marked: true
  extension_combinations_must_record_derivation: true
  extension_combinations_must_not_replace_required_coverage: true
  success_and_failure_both_allowed: true
  all_success_and_failure_reasons_must_be_declared: true
  required_coverage_sql_templates_must_come_from_combination_groups: true
  extension_sql_templates_must_be_recorded_in_artifacts: true
post_coverage_extension_policy:
  enabled: true
  allowed_after_audit_verdict: required_coverage_passed
  output_location: artifacts/intermediates/<task_slug>/derived_extension_combinations.yaml
  required_fields:
  - id
  - title
  - derived_from_combination_group
  - derivation_reason
  - factors
  - expected_status_policy
  - compatibility
  - sql_shape
  - verification
  - cleanup
  guardrails:
  - Required matrix coverage must pass before any derived extension is emitted.
  - Derived extensions must not be counted as satisfying required factor or coverage scope requirements.
coverage_scope:
  target_object_coverage:
    required: true
    coverage_mode: explicit
    inventory_source: references/combinations/_shared/coverage_inventory.yaml
    required_object_kinds:
    - large_object
    decision_reason: large_object statement coverage is tracked by explicit PostgreSQL object kind.
  target_relation_coverage:
    required: false
    coverage_mode: not_applicable
    decision_reason: large_object statements do not target relation objects directly.
  table_coverage:
    required: false
    coverage_mode: not_applicable
    decision_reason: large_object statements do not directly depend on table instance coverage.
  column_type_coverage:
    required: false
    coverage_mode: not_applicable
    decision_reason: large_object statements do not vary by table column data type.
factor_contract:
  source_reference_must_define_all_factors: true
  matrix_must_cover_required_factor_values: true
  factors:
    statement_branch:
      tier: T1
      coverage_role: main_axis
      required_values: &id001
      - branch_owner
      coverage_requirement: all_values
    target_object_state:
      tier: T1
      coverage_role: main_axis
      required_values: &id002
      - exists
      - missing
      coverage_requirement: all_values
    expected_status:
      tier: T1
      coverage_role: main_axis
      required_values: &id003
      - success
      - failure
      coverage_requirement: all_values
    new_owner_shape:
      tier: T2
      coverage_role: main_axis
      required_values: &id004
      - plain_role
      - current_role
      - current_user
      - session_user
      - missing_role
      coverage_requirement: all_values
    privilege_context:
      tier: T2
      coverage_role: main_axis
      required_values: &id005
      - superuser
      - owner
      - non_owner
      - insufficient_privilege
      coverage_requirement: all_values
    oid_shape:
      tier: T3
      coverage_role: rotate_attach
      required_values: &id006
      - valid_oid
      - nonexistent_oid
      coverage_requirement: all_values
    dependency_state:
      tier: T4
      coverage_role: rotate_attach
      required_values: &id007
      - ready
      - missing_dependency
      coverage_requirement: all_values
    invalid_combination:
      tier: T5
      coverage_role: rotate_attach
      required_values: &id008
      - none
      - syntax_valid_semantic_error
      coverage_requirement: all_values
    ownership_boundary:
      tier: T5
      coverage_role: rotate_attach
      required_values: &id009
      - superuser
      - owner
      - non_owner
      coverage_requirement: all_values
    verification_mode:
      tier: T6
      coverage_role: rotate_attach
      required_values: &id010
      - catalog_query
      - effect_query
      - error_assertion
      coverage_requirement: all_values
    cleanup_mode:
      tier: T6
      coverage_role: rotate_attach
      required_values: &id011
      - drop_objects
      - reset_state
      coverage_requirement: all_values
dynamic_inputs:
  object_manifest:
    required: true
    source: lifecycle_plan
    required_fields:
    - object_name
    - object_kind
    - expected_status
combination_groups:
- id: alter_large_object_declared_factor_baseline
  title: ALTER LARGE OBJECT declared factor baseline
  lifecycle_role: target_statement
  expected_status_policy: per_factor_binding
  default_expected_status: success
  expected_failure_reasons:
  - alter_large_object_declared_failure
  factors:
    statement_branch: branch_owner
    target_object_state: exists
    expected_status: success
    new_owner_shape: plain_role
    privilege_context: superuser
    oid_shape: valid_oid
    dependency_state: ready
    invalid_combination: none
    ownership_boundary: superuser
    verification_mode: catalog_query
    cleanup_mode: drop_objects
  expansion:
    statement_branch:
      mode: explicit
      values: *id001
    target_object_state:
      mode: explicit
      values: *id002
    expected_status:
      mode: explicit
      values: *id003
    new_owner_shape:
      mode: explicit
      values: *id004
    privilege_context:
      mode: explicit
      values: *id005
    oid_shape:
      mode: explicit
      values: *id006
    dependency_state:
      mode: explicit
      values: *id007
    invalid_combination:
      mode: explicit
      values: *id008
    ownership_boundary:
      mode: explicit
      values: *id009
    verification_mode:
      mode: explicit
      values: *id010
    cleanup_mode:
      mode: explicit
      values: *id011
  compatibility:
    resolver: declared_matrix
    success_when:
    - expected_status == success
    failure_when:
    - condition: expected_status == failure
      reason: alter_large_object_declared_failure
    default_failure_reason: alter_large_object_declared_failure
  sql_shape:
    template: ALTER LARGE OBJECT {object_name};
  verification:
    required: true
    mode: catalog_or_error_assertion
    sql: SELECT 1;
  cleanup:
    required: true
    steps:
    - sql: -- cleanup for alter_large_object
audit_rules:
  require_all_required_top_level_keys: true
  require_declared_coverage_scope: true
  require_declared_factor_values: true
  require_expected_failure_reasons: true
  require_post_coverage_extension_policy: true
  forbid_extension_before_required_coverage_passes: true
  forbid_extension_counting_toward_required_coverage: true
