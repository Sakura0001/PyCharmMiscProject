schema_version: 1
kind: statement_combination_matrix
statement:
  key: drop_publication
  name: DROP PUBLICATION
  category: ddl
  domain: publication
  source_reference: references/statements/ddl/publication/drop_publication.md
  official_source: https://www.postgresql.org/docs/16/sql-droppublication.html
execution_contract:
  required_matrix_is_baseline: true
  no_inference_before_required_coverage_passes: true
  runner_must_complete_required_matrix_first: true
  allow_post_coverage_extension_inference: true
  extension_combinations_must_be_marked: true
  extension_combinations_must_record_derivation: true
  extension_combinations_must_not_replace_required_coverage: true
  success_and_failure_both_allowed: true
  all_success_and_failure_reasons_must_be_declared: true
  required_coverage_sql_templates_must_come_from_combination_groups: true
  extension_sql_templates_must_be_recorded_in_artifacts: true
post_coverage_extension_policy:
  enabled: true
  allowed_after_audit_verdict: required_coverage_passed
  output_location: artifacts/intermediates/<task_slug>/derived_extension_combinations.yaml
  required_fields:
  - id
  - title
  - derived_from_combination_group
  - derivation_reason
  - factors
  - expected_status_policy
  - compatibility
  - sql_shape
  - verification
  - cleanup
  guardrails:
  - Required matrix coverage must pass before any derived extension is emitted.
  - Derived extensions must not be counted as satisfying required factor or coverage scope requirements.
coverage_scope:
  target_object_coverage:
    required: true
    coverage_mode: explicit
    inventory_source: references/combinations/_shared/coverage_inventory.yaml
    required_object_kinds:
    - publication
    decision_reason: publication statement coverage is tracked by explicit PostgreSQL object kind.
  target_relation_coverage:
    required: true
    coverage_mode: representative
    inventory_source: references/combinations/_shared/coverage_inventory.yaml
    required_relation_kinds:
    - regular_table
    - partitioned_table_parent
    decision_reason: Publication statements can target table relations, so representative relation coverage is required.
  table_coverage:
    required: true
    coverage_mode: representative
    inventory_source: references/combinations/_shared/coverage_inventory.yaml
    required_table_kinds:
    - regular_table
    - partitioned_table_parent
    decision_reason: Publication table membership depends on table object form.
  column_type_coverage:
    required: true
    coverage_mode: conditional
    inventory_source: references/common/pg16_type_catalog.md
    required_type_set: all_pg16_column_types
    expansion_mode: publication_column_list_or_predicate
    require_each_type_success_or_failure: true
    decision_reason: Publication column lists and row filters can reference table columns; type coverage is conditional on
      those branches.
factor_contract:
  source_reference_must_define_all_factors: true
  matrix_must_cover_required_factor_values: true
  factors:
    statement_branch:
      tier: T1
      coverage_role: main_axis
      required_values: &id001
      - branch_drop_publication
      - branch_drop_publication_if_exists
      - branch_drop_publication_cascade
      - branch_drop_publication_restrict
      coverage_requirement: all_values
    publication_existence:
      tier: T1
      coverage_role: main_axis
      required_values: &id002
      - publication_exists
      - publication_not_exists
      coverage_requirement: all_values
    expected_status:
      tier: T1
      coverage_role: main_axis
      required_values: &id003
      - success
      - failure
      coverage_requirement: all_values
    if_exists_clause:
      tier: T2
      coverage_role: main_axis
      required_values: &id004
      - without_if_exists
      - with_if_exists
      coverage_requirement: all_values
    cascade_restrict_clause:
      tier: T2
      coverage_role: main_axis
      required_values: &id005
      - no_clause_default_restrict
      - cascade
      - restrict
      coverage_requirement: all_values
    privilege_context:
      tier: T2
      coverage_role: main_axis
      required_values: &id006
      - superuser
      - owner_of_publication
      - non_owner_no_privilege
      coverage_requirement: all_values
    multi_target:
      tier: T2
      coverage_role: main_axis
      required_values: &id007
      - single_target
      - multi_target_all_exist
      - multi_target_some_not_exist
      coverage_requirement: all_values
    publication_name_shape:
      tier: T3
      coverage_role: rotate_attach
      required_values: &id008
      - simple_name
      - quoted_name
      - reserved_word_name
      - non_existing_name
      coverage_requirement: all_values
    executor_privilege:
      tier: T4
      coverage_role: rotate_attach
      required_values: &id009
      - superuser
      - owner
      - non_owner_no_privilege
      coverage_requirement: all_values
    subscription_dependency:
      tier: T4
      coverage_role: rotate_attach
      required_values: &id010
      - no_subscription_dependency
      - has_subscription_dependency
      coverage_requirement: all_values
    nonexistent_publication:
      tier: T5
      coverage_role: rotate_attach
      required_values: &id011
      - publication_does_not_exist
      coverage_requirement: all_values
    privilege_insufficient:
      tier: T5
      coverage_role: rotate_attach
      required_values: &id012
      - non_owner_dropping_publication
      - non_superuser_dropping_other_publication
      coverage_requirement: all_values
    dependent_subscription:
      tier: T5
      coverage_role: rotate_attach
      required_values: &id013
      - restrict_with_subscription_fails
      - cascade_with_subscription_succeeds
      coverage_requirement: all_values
    verification_mode:
      tier: T6
      coverage_role: rotate_attach
      required_values: &id014
      - pg_publication_catalog
      - error_assertion
      coverage_requirement: all_values
    cleanup_mode:
      tier: T6
      coverage_role: rotate_attach
      required_values: &id015
      - drop_publication_cascade
      - drop_subscription_then_drop_publication
      coverage_requirement: all_values
dynamic_inputs:
  object_manifest:
    required: true
    source: lifecycle_plan
    required_fields:
    - object_name
    - object_kind
    - expected_status
combination_groups:
- id: drop_publication_declared_factor_baseline
  title: DROP PUBLICATION declared factor baseline
  lifecycle_role: target_statement
  expected_status_policy: per_factor_binding
  default_expected_status: success
  expected_failure_reasons:
  - drop_publication_declared_failure
  factors:
    statement_branch: branch_drop_publication
    publication_existence: publication_exists
    expected_status: success
    if_exists_clause: without_if_exists
    cascade_restrict_clause: no_clause_default_restrict
    privilege_context: superuser
    multi_target: single_target
    publication_name_shape: simple_name
    executor_privilege: superuser
    subscription_dependency: no_subscription_dependency
    nonexistent_publication: publication_does_not_exist
    privilege_insufficient: non_owner_dropping_publication
    dependent_subscription: restrict_with_subscription_fails
    verification_mode: pg_publication_catalog
    cleanup_mode: drop_publication_cascade
  expansion:
    statement_branch:
      mode: explicit
      values: *id001
    publication_existence:
      mode: explicit
      values: *id002
    expected_status:
      mode: explicit
      values: *id003
    if_exists_clause:
      mode: explicit
      values: *id004
    cascade_restrict_clause:
      mode: explicit
      values: *id005
    privilege_context:
      mode: explicit
      values: *id006
    multi_target:
      mode: explicit
      values: *id007
    publication_name_shape:
      mode: explicit
      values: *id008
    executor_privilege:
      mode: explicit
      values: *id009
    subscription_dependency:
      mode: explicit
      values: *id010
    nonexistent_publication:
      mode: explicit
      values: *id011
    privilege_insufficient:
      mode: explicit
      values: *id012
    dependent_subscription:
      mode: explicit
      values: *id013
    verification_mode:
      mode: explicit
      values: *id014
    cleanup_mode:
      mode: explicit
      values: *id015
  compatibility:
    resolver: declared_matrix
    success_when:
    - expected_status == success
    failure_when:
    - condition: expected_status == failure
      reason: drop_publication_declared_failure
    default_failure_reason: drop_publication_declared_failure
  sql_shape:
    template: DROP PUBLICATION {object_name};
  verification:
    required: true
    mode: catalog_or_error_assertion
    sql: SELECT 1;
  cleanup:
    required: true
    steps:
    - sql: -- cleanup for drop_publication
audit_rules:
  require_all_required_top_level_keys: true
  require_declared_coverage_scope: true
  require_declared_factor_values: true
  require_expected_failure_reasons: true
  require_post_coverage_extension_policy: true
  forbid_extension_before_required_coverage_passes: true
  forbid_extension_counting_toward_required_coverage: true
