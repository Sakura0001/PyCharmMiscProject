schema_version: 1
kind: statement_combination_matrix
statement:
  key: drop_domain
  name: DROP DOMAIN
  category: ddl
  domain: domain
  source_reference: references/statements/ddl/domain/drop_domain.md
  official_source: https://www.postgresql.org/docs/16/sql-dropdomain.html
execution_contract:
  required_matrix_is_baseline: true
  no_inference_before_required_coverage_passes: true
  runner_must_complete_required_matrix_first: true
  allow_post_coverage_extension_inference: true
  extension_combinations_must_be_marked: true
  extension_combinations_must_record_derivation: true
  extension_combinations_must_not_replace_required_coverage: true
  success_and_failure_both_allowed: true
  all_success_and_failure_reasons_must_be_declared: true
  required_coverage_sql_templates_must_come_from_combination_groups: true
  extension_sql_templates_must_be_recorded_in_artifacts: true
post_coverage_extension_policy:
  enabled: true
  allowed_after_audit_verdict: required_coverage_passed
  output_location: artifacts/intermediates/<task_slug>/derived_extension_combinations.yaml
  required_fields:
  - id
  - title
  - derived_from_combination_group
  - derivation_reason
  - factors
  - expected_status_policy
  - compatibility
  - sql_shape
  - verification
  - cleanup
  guardrails:
  - Required matrix coverage must pass before any derived extension is emitted.
  - Derived extensions must not be counted as satisfying required factor or coverage scope requirements.
coverage_scope:
  target_object_coverage:
    required: true
    coverage_mode: explicit
    inventory_source: references/combinations/_shared/coverage_inventory.yaml
    required_object_kinds:
    - domain
    decision_reason: domain statements operate on PostgreSQL domain objects; object coverage is explicit for create, alter,
      and drop paths.
  target_relation_coverage:
    required: false
    coverage_mode: not_applicable
    decision_reason: domain statements do not target relation objects directly.
  table_coverage:
    required: false
    coverage_mode: not_applicable
    decision_reason: domain statements define catalog objects and do not directly operate on table instances.
  column_type_coverage:
    required: true
    coverage_mode: representative
    inventory_source: references/common/pg16_type_catalog.md
    required_type_set: all_pg16_column_types
    expansion_mode: representative_type_families
    require_each_type_success_or_failure: true
    decision_reason: domain validity depends on PostgreSQL data type bindings, so representative PG16 type coverage is required.
factor_contract:
  source_reference_must_define_all_factors: true
  matrix_must_cover_required_factor_values: true
  factors:
    statement_branch:
      tier: T1
      coverage_role: main_axis
      required_values: &id001
      - branch_drop_domain
      - branch_drop_domain_if_exists
      coverage_requirement: all_values
    object_state:
      tier: T1
      coverage_role: main_axis
      required_values: &id002
      - exists
      - absent
      - partially_exists_multi
      coverage_requirement: all_values
    expected_status:
      tier: T1
      coverage_role: main_axis
      required_values: &id003
      - success
      - failure
      coverage_requirement: all_values
    if_exists_clause:
      tier: T2
      coverage_role: main_axis
      required_values: &id004
      - present
      - absent
      coverage_requirement: all_values
    cascade_restrict:
      tier: T2
      coverage_role: main_axis
      required_values: &id005
      - cascade
      - restrict
      - omitted_default_restrict
      coverage_requirement: all_values
    multi_domain:
      tier: T2
      coverage_role: main_axis
      required_values: &id006
      - single_domain
      - multiple_domains
      coverage_requirement: all_values
    dependency_status:
      tier: T2
      coverage_role: main_axis
      required_values: &id007
      - no_dependents
      - has_table_column_dependent
      - has_other_dependent
      coverage_requirement: all_values
    domain_name_shape:
      tier: T3
      coverage_role: rotate_attach
      required_values: &id008
      - simple_id
      - quoted_id
      - schema_qualified
      - reserved_word_as_name
      - nonexistent_name
      - existing_name
      coverage_requirement: all_values
    privilege_level:
      tier: T4
      coverage_role: rotate_attach
      required_values: &id009
      - superuser
      - domain_owner
      - non_owner
      coverage_requirement: all_values
    dependency_context:
      tier: T4
      coverage_role: rotate_attach
      required_values: &id010
      - no_dependencies
      - column_using_domain
      - other_type_dependency
      coverage_requirement: all_values
    nonexistent_domain:
      tier: T5
      coverage_role: rotate_attach
      required_values: &id011
      - domain_exists
      - domain_missing_without_if_exists
      coverage_requirement: all_values
    has_dependents_restrict:
      tier: T5
      coverage_role: rotate_attach
      required_values: &id012
      - no_dependents_safe
      - dependents_block_restrict
      coverage_requirement: all_values
    privilege_denied:
      tier: T5
      coverage_role: rotate_attach
      required_values: &id013
      - owner_execution
      - non_owner_denied
      - superuser_execution
      coverage_requirement: all_values
    verification_mode:
      tier: T6
      coverage_role: rotate_attach
      required_values: &id014
      - catalog_query_pg_type
      - error_assertion
      - notice_assertion
      coverage_requirement: all_values
    cleanup_mode:
      tier: T6
      coverage_role: rotate_attach
      required_values: &id015
      - drop_domain
      - cascade_cleanup
      - role_cleanup
      coverage_requirement: all_values
dynamic_inputs:
  object_manifest:
    required: true
    source: lifecycle_plan
    required_fields:
    - object_name
    - schema_name
    - expected_status
  dependency_manifest:
    required: true
    source: statement_reference_dependencies
    required_fields:
    - dependency_name
    - dependency_kind
    - setup_sql
combination_groups:
- id: drop_domain_declared_factor_baseline
  title: DROP DOMAIN declared factor baseline
  lifecycle_role: target_statement
  expected_status_policy: per_factor_binding
  default_expected_status: success
  expected_failure_reasons:
  - drop_domain_declared_failure
  factors:
    statement_branch: branch_drop_domain
    object_state: exists
    expected_status: success
    if_exists_clause: present
    cascade_restrict: cascade
    multi_domain: single_domain
    dependency_status: no_dependents
    domain_name_shape: simple_id
    privilege_level: superuser
    dependency_context: no_dependencies
    nonexistent_domain: domain_exists
    has_dependents_restrict: no_dependents_safe
    privilege_denied: owner_execution
    verification_mode: catalog_query_pg_type
    cleanup_mode: drop_domain
  expansion:
    statement_branch:
      mode: explicit
      values: *id001
    object_state:
      mode: explicit
      values: *id002
    expected_status:
      mode: explicit
      values: *id003
    if_exists_clause:
      mode: explicit
      values: *id004
    cascade_restrict:
      mode: explicit
      values: *id005
    multi_domain:
      mode: explicit
      values: *id006
    dependency_status:
      mode: explicit
      values: *id007
    domain_name_shape:
      mode: explicit
      values: *id008
    privilege_level:
      mode: explicit
      values: *id009
    dependency_context:
      mode: explicit
      values: *id010
    nonexistent_domain:
      mode: explicit
      values: *id011
    has_dependents_restrict:
      mode: explicit
      values: *id012
    privilege_denied:
      mode: explicit
      values: *id013
    verification_mode:
      mode: explicit
      values: *id014
    cleanup_mode:
      mode: explicit
      values: *id015
  compatibility:
    resolver: declared_matrix
    success_when:
    - expected_status == success
    failure_when:
    - condition: expected_status == failure
      reason: drop_domain_declared_failure
    default_failure_reason: drop_domain_declared_failure
  sql_shape:
    template: DROP DOMAIN {object_name};
  verification:
    required: true
    mode: catalog_or_error_assertion
    sql: SELECT 1;
  cleanup:
    required: true
    steps:
    - sql: -- cleanup for drop_domain
audit_rules:
  require_all_required_top_level_keys: true
  require_declared_coverage_scope: true
  require_declared_factor_values: true
  require_expected_failure_reasons: true
  require_post_coverage_extension_policy: true
  forbid_extension_before_required_coverage_passes: true
  forbid_extension_counting_toward_required_coverage: true
