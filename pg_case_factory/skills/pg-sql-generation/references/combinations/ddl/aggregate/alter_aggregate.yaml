schema_version: 1
kind: statement_combination_matrix
statement:
  key: alter_aggregate
  name: ALTER AGGREGATE
  category: ddl
  domain: aggregate
  source_reference: references/statements/ddl/aggregate/alter_aggregate.md
  official_source: https://www.postgresql.org/docs/16/sql-alteraggregate.html
  aliases:
  - ALTER AGGREGATE
  - alter aggregate
  - alter_aggregate
execution_contract:
  required_matrix_is_baseline: true
  no_inference_before_required_coverage_passes: true
  runner_must_complete_required_matrix_first: true
  allow_post_coverage_extension_inference: true
  extension_combinations_must_be_marked: true
  extension_combinations_must_record_derivation: true
  extension_combinations_must_not_replace_required_coverage: true
  success_and_failure_both_allowed: true
  all_success_and_failure_reasons_must_be_declared: true
  required_coverage_sql_templates_must_come_from_combination_groups: true
  extension_sql_templates_must_be_recorded_in_artifacts: true
post_coverage_extension_policy:
  enabled: true
  allowed_after_audit_verdict: required_coverage_passed
  output_location: artifacts/intermediates/<task_slug>/derived_extension_combinations.yaml
  required_fields:
  - id
  - title
  - derived_from_combination_group
  - derivation_reason
  - factors
  - expected_status_policy
  - compatibility
  - sql_shape
  - verification
  - cleanup
  guardrails:
  - Required matrix coverage must pass before any derived extension is emitted.
  - Derived extensions may add stress, boundary, dependency, environment, or cross-factor combinations after baseline audit
    passes.
  - Derived extensions must not be counted as satisfying required factor, object, routine-signature, relation, table, or column-type
    coverage.
  - Derived extensions must use declared factors and declared factor values.
  - Derived extensions must preserve success or failure attribution.
coverage_scope:
  target_object_coverage:
    required: true
    coverage_mode: explicit
    inventory_source: references/combinations/_shared/coverage_inventory.yaml
    required_object_kinds:
    - aggregate
    decision_reason: ALTER_AGGREGATE directly targets PostgreSQL aggregate routine objects and aggregate catalog metadata.
      Baseline coverage is scoped to aggregate signatures, forms, support-function dependencies, privileges, and lifecycle
      state.
  target_relation_coverage:
    required: false
    coverage_mode: not_applicable
    inventory_source: references/combinations/_shared/coverage_inventory.yaml
    decision_reason: ALTER_AGGREGATE does not target a pg_class table, index, view, sequence, or materialized-view relation;
      routine catalog dependencies are represented as object and signature factors.
  table_coverage:
    required: false
    coverage_mode: not_applicable
    decision_reason: ALTER_AGGREGATE operates on routine catalog objects and signatures rather than table variants. Required
      baseline coverage does not depend on regular, temporary, partitioned, inherited, or foreign table forms.
  column_type_coverage:
    required: false
    coverage_mode: not_applicable
    decision_reason: ALTER_AGGREGATE uses aggregate input or state types as routine signature/type coverage. The statement
      does not create, alter, scan, or validate table columns, so pg16 table column_type_coverage is not applicable.
factor_contract:
  source_reference_must_define_all_factors: true
  matrix_must_cover_required_factor_values: true
  factors:
    statement_branch:
      tier: T1
      coverage_role: main_axis
      required_values: &id001
      - branch_rename
      - branch_owner
      - branch_set_schema
      coverage_requirement: all_declared_values
    aggregate_signature:
      tier: T1
      coverage_role: routine_signature_type_axis
      required_values: &id002
      - star_zero_arg
      - single_argtype
      - multi_argtype
      - ordered_set_signature
      - abbreviated_ordered_set
      coverage_requirement: all_declared_values
    expected_status:
      tier: T1
      coverage_role: main_axis
      required_values: &id003
      - success
      - failure
      coverage_requirement: all_declared_values
    aggregate_form:
      tier: T2
      coverage_role: behavior_axis
      required_values: &id004
      - single_arg
      - multi_arg
      - zero_arg
      - ordered_set
      coverage_requirement: all_declared_values
    target_state:
      tier: T2
      coverage_role: main_axis
      required_values: &id005
      - new_name_available
      - new_name_conflict
      - new_owner_available
      - new_owner_unavailable
      - new_schema_available
      - new_schema_conflict
      coverage_requirement: all_declared_values
    aggregate_name_shape:
      tier: T3
      coverage_role: input_shape_axis
      required_values: &id006
      - plain_identifier
      - quoted_identifier
      - schema_qualified
      coverage_requirement: all_declared_values
    new_name_shape:
      tier: T3
      coverage_role: input_shape_axis
      required_values: &id007
      - plain_identifier
      - quoted_identifier
      - reserved_word
      coverage_requirement: all_declared_values
    new_owner_shape:
      tier: T3
      coverage_role: input_shape_axis
      required_values: &id008
      - plain_role
      - CURRENT_ROLE
      - CURRENT_USER
      - SESSION_USER
      coverage_requirement: all_declared_values
    new_schema_shape:
      tier: T3
      coverage_role: input_shape_axis
      required_values: &id009
      - existing_schema
      - nonexistent_schema
      coverage_requirement: all_declared_values
    privilege_level:
      tier: T4
      coverage_role: dependency_or_environment_axis
      required_values: &id010
      - superuser
      - aggregate_owner
      - non_owner
      coverage_requirement: all_declared_values
    owner_membership:
      tier: T4
      coverage_role: dependency_or_environment_axis
      required_values: &id011
      - member_of_new_owner
      - not_member_of_new_owner
      coverage_requirement: all_declared_values
    aggregate_not_exists:
      tier: T5
      coverage_role: negative_boundary_axis
      required_values: &id012
      - not_exists
      coverage_requirement: all_declared_values
    signature_mismatch:
      tier: T5
      coverage_role: negative_boundary_axis
      required_values: &id013
      - wrong_arg_count
      - wrong_arg_type
      coverage_requirement: all_declared_values
    new_name_conflict:
      tier: T5
      coverage_role: negative_boundary_axis
      required_values: &id014
      - same_signature_conflict
      coverage_requirement: all_declared_values
    new_owner_not_exists:
      tier: T5
      coverage_role: negative_boundary_axis
      required_values: &id015
      - nonexistent_role
      coverage_requirement: all_declared_values
    new_schema_not_exists:
      tier: T5
      coverage_role: negative_boundary_axis
      required_values: &id016
      - nonexistent_schema
      coverage_requirement: all_declared_values
    insufficient_privilege:
      tier: T5
      coverage_role: negative_boundary_axis
      required_values: &id017
      - non_owner
      - no_create_on_schema
      coverage_requirement: all_declared_values
    star_mismatch:
      tier: T5
      coverage_role: negative_boundary_axis
      required_values: &id018
      - star_for_nonzero_arg
      coverage_requirement: all_declared_values
    verification_mode:
      tier: T6
      coverage_role: verification_cleanup_axis
      required_values: &id019
      - pg_aggregate_catalog_query
      - pg_proc_query
      - pg_aggregate_actual_execution
      coverage_requirement: all_declared_values
    cleanup_mode:
      tier: T6
      coverage_role: verification_cleanup_axis
      required_values: &id020
      - DROP_AGGREGATE
      - DROP_AGGREGATE_IF_EXISTS
      - DROP_AGGREGATE_CASCADE
      coverage_requirement: all_declared_values
dynamic_inputs:
  aggregate_signature_resolution_manifest:
    required: true
    source: factor_contract.aggregate_signature and combination_groups.expansion
    required_fields:
    - aggregate_name
    - aggregate_signature
    - aggregate_form
    - alter_branch
    - target_state
    - privilege_context
    - expected_status
combination_groups:
- id: alter_aggregate_required_baseline_factor_space
  title: ALTER AGGREGATE required baseline factor space
  lifecycle_role: target_statement
  expected_status_policy: per_factor_binding
  default_expected_status: success
  expected_failure_reasons:
  - expected_statement_failure
  - routine_resolution_or_privilege_failure
  - invalid_signature_or_type_binding
  - dependency_or_environment_failure
  factors:
    statement_branch: branch_rename
    aggregate_signature: star_zero_arg
    expected_status: success
    aggregate_form: single_arg
    target_state: new_name_available
    aggregate_name_shape: plain_identifier
    new_name_shape: plain_identifier
    new_owner_shape: plain_role
    new_schema_shape: existing_schema
    privilege_level: superuser
    owner_membership: member_of_new_owner
    aggregate_not_exists: not_exists
    signature_mismatch: wrong_arg_count
    new_name_conflict: same_signature_conflict
    new_owner_not_exists: nonexistent_role
    new_schema_not_exists: nonexistent_schema
    insufficient_privilege: non_owner
    star_mismatch: star_for_nonzero_arg
    verification_mode: pg_aggregate_catalog_query
    cleanup_mode: DROP_AGGREGATE
  expansion:
    statement_branch:
      mode: exhaustive
      values: *id001
    aggregate_signature:
      mode: exhaustive
      values: *id002
    expected_status:
      mode: exhaustive
      values: *id003
    aggregate_form:
      mode: exhaustive
      values: *id004
    target_state:
      mode: exhaustive
      values: *id005
    aggregate_name_shape:
      mode: representative
      values: *id006
    new_name_shape:
      mode: representative
      values: *id007
    new_owner_shape:
      mode: representative
      values: *id008
    new_schema_shape:
      mode: representative
      values: *id009
    privilege_level:
      mode: representative
      values: *id010
    owner_membership:
      mode: representative
      values: *id011
    aggregate_not_exists:
      mode: representative
      values: *id012
    signature_mismatch:
      mode: representative
      values: *id013
    new_name_conflict:
      mode: representative
      values: *id014
    new_owner_not_exists:
      mode: representative
      values: *id015
    new_schema_not_exists:
      mode: representative
      values: *id016
    insufficient_privilege:
      mode: representative
      values: *id017
    star_mismatch:
      mode: representative
      values: *id018
    verification_mode:
      mode: representative
      values: *id019
    cleanup_mode:
      mode: representative
      values: *id020
  compatibility:
    resolver: alter_aggregate_factor_binding_resolver
    success_when:
    - expected_status=success and selected object, signature, privilege, and dependency bindings are valid
    - statement branch and routine kind support the selected type, language, body, or aggregate option binding
    failure_when:
    - condition: expected_status=failure or a declared negative boundary factor is selected
      reason: expected_statement_failure
    - condition: target routine object, signature, owner, schema, language, extension, support function, or privilege is missing
        or incompatible
      reason: routine_resolution_or_privilege_failure
    - condition: argument, return, aggregate signature, or support function type binding is invalid for the selected branch
      reason: invalid_signature_or_type_binding
    default_failure_reason: expected_statement_failure
  sql_shape:
    template: ALTER AGGREGATE {aggregate_name}({aggregate_signature}) {alter_aggregate_action};
  verification:
    required: true
    mode: catalog_query
    sql: SELECT p.oid FROM pg_proc p JOIN pg_aggregate a ON a.aggfnoid = p.oid WHERE p.proname = '{aggregate_name}'
  cleanup:
    required: true
    steps:
    - sql: DROP AGGREGATE IF EXISTS {aggregate_name}({aggregate_signature}) CASCADE
audit_rules:
  require_all_required_top_level_keys: true
  require_declared_coverage_scope: true
  require_declared_factor_values: true
  require_expected_failure_reasons: true
  require_post_coverage_extension_policy: true
  forbid_extension_before_required_coverage_passes: true
  forbid_extension_counting_toward_required_coverage: true
