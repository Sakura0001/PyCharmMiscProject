"""Evidence-backed regress generation for the remaining statement cycle.

This module is intentionally registry-driven.  There is no generic SQL
fallback: a statement is generatable only after an explicit, reviewed case
design and renderer have been registered for it.  The first registered design
is PostgreSQL 18.4 ``ABORT``.
"""

from __future__ import annotations

import hashlib
import importlib.util
import itertools
import json
import os
import re
import shutil
import tempfile
import sys
from collections import Counter
from dataclasses import dataclass
from functools import lru_cache
from pathlib import Path
from typing import Any, Callable, Mapping

from .regression_style import (
    HuaweiSqlHeader,
    audit_catalog_observability,
    audit_complete_table_script,
    contains_create_table_statement,
    render_huawei_sql_header,
    validate_huawei_sql_header,
)
from .statement_factor_cycle import (
    StatementCycleEntry,
    StatementFactorCycleSnapshot,
)


_CREATE_DATE = "2026-08-10"
_VERSION = "1.0"
_AUTHOR = "pg-case-factory Codex"
_FE = "PG18-STATEMENT-FACTOR-LOOP"
_DISPOSITIONS = frozenset({"covered", "expected_failure", "justified_na"})


class RemainingStatementRegressError(ValueError):
    """Raised when a statement design or generated package is incomplete."""


@dataclass(frozen=True)
class FactorValueDecision:
    row_id: str
    factor: str
    value: str
    disposition: str
    reason: str | None
    case_ids: tuple[str, ...]

    def __post_init__(self) -> None:
        if self.disposition not in _DISPOSITIONS:
            raise RemainingStatementRegressError(
                f"unsupported factor disposition {self.disposition!r}"
            )
        if self.disposition == "justified_na":
            if not self.reason or self.case_ids:
                raise RemainingStatementRegressError(
                    f"justified N/A {self.factor}={self.value} needs a reason and no cases"
                )
        elif not self.case_ids:
            raise RemainingStatementRegressError(
                f"{self.factor}={self.value} must be witnessed by a case"
            )

    def to_dict(self) -> dict[str, Any]:
        return {
            "row_id": self.row_id,
            "factor": self.factor,
            "value": self.value,
            "disposition": self.disposition,
            "reason": self.reason,
            "case_ids": list(self.case_ids),
        }


@dataclass(frozen=True)
class StatementRegressCase:
    ordinal: int
    case_id: str
    sql_filename: str
    object_prefix: str
    case_group: str
    case_type: str
    outcome: str
    execution_profile: str
    derived_axes: Mapping[str, str]
    factor_values: tuple[str, ...]
    combination_strategy: str
    description: str
    expected_anchor: str

    def to_dict(self) -> dict[str, Any]:
        return {
            "ordinal": self.ordinal,
            "case_id": self.case_id,
            "sql_filename": self.sql_filename,
            "object_prefix": self.object_prefix,
            "case_group": self.case_group,
            "case_type": self.case_type,
            "outcome": self.outcome,
            "execution_profile": self.execution_profile,
            "derived_axes": dict(self.derived_axes),
            "factor_values": list(self.factor_values),
            "combination_strategy": self.combination_strategy,
            "description": self.description,
            "expected_anchor": self.expected_anchor,
        }


@dataclass(frozen=True)
class StatementRegressPlan:
    statement_key: str
    file_prefix: str
    cycle_fingerprint: str
    matrix_path: str
    matrix_sha256: str
    reference_path: str
    reference_sha256: str
    universe_semantic_sha256: str
    official_source: str
    cases: tuple[StatementRegressCase, ...]
    factor_decisions: tuple[FactorValueDecision, ...]

    @property
    def case_group_counts(self) -> dict[str, int]:
        return dict(sorted(Counter(case.case_group for case in self.cases).items()))

    @property
    def factor_disposition_counts(self) -> dict[str, int]:
        counts = Counter(decision.disposition for decision in self.factor_decisions)
        return {name: counts.get(name, 0) for name in sorted(_DISPOSITIONS)}

    @property
    def external_cases(self) -> tuple[StatementRegressCase, ...]:
        return tuple(
            case for case in self.cases if case.execution_profile == "external_isolated"
        )

    @property
    def serial_cases(self) -> tuple[StatementRegressCase, ...]:
        return tuple(
            case
            for case in self.cases
            if case.execution_profile == "same_session_multiphase"
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            "schema_version": 1,
            "statement_key": self.statement_key,
            "file_prefix": self.file_prefix,
            "cycle_fingerprint": self.cycle_fingerprint,
            "matrix_path": self.matrix_path,
            "matrix_sha256": self.matrix_sha256,
            "reference_path": self.reference_path,
            "reference_sha256": self.reference_sha256,
            "universe_semantic_sha256": self.universe_semantic_sha256,
            "official_source": self.official_source,
            "case_count": len(self.cases),
            "case_group_counts": self.case_group_counts,
            "factor_value_count": len(self.factor_decisions),
            "factor_disposition_counts": self.factor_disposition_counts,
            "cases": [case.to_dict() for case in self.cases],
        }


@dataclass(frozen=True)
class StatementRegressValidation:
    statement_key: str
    cycle_fingerprint: str
    passed: bool
    issues: tuple[str, ...]
    sql_file_count: int
    factor_value_count: int
    sql_sha256: Mapping[str, str]
    input_sha256: Mapping[str, str]
    package_sha256: str

    def to_dict(self) -> dict[str, Any]:
        return {
            "schema_version": 1,
            "statement_key": self.statement_key,
            "cycle_fingerprint": self.cycle_fingerprint,
            "passed": self.passed,
            "issues": list(self.issues),
            "sql_file_count": self.sql_file_count,
            "factor_value_count": self.factor_value_count,
            "sql_sha256": dict(self.sql_sha256),
            "input_sha256": dict(self.input_sha256),
            "package_sha256": self.package_sha256,
            "runtime_status": "not_run_static_sql_only",
        }


def _entry(snapshot: StatementFactorCycleSnapshot, statement_key: str) -> StatementCycleEntry:
    matches = [entry for entry in snapshot.entries if entry.statement_key == statement_key]
    if len(matches) != 1:
        raise RemainingStatementRegressError(
            f"expected one cycle entry for {statement_key}, found {len(matches)}"
        )
    entry = matches[0]
    if entry.is_retained:
        raise RemainingStatementRegressError(
            f"statement {statement_key} is retained and must not be regenerated here"
        )
    return entry


def _case(
    ordinal: int,
    *,
    group: str,
    case_type: str,
    outcome: str,
    execution_profile: str,
    axes: Mapping[str, str],
    factor_values: tuple[str, ...],
    combination_strategy: str,
    description: str,
    expected_anchor: str,
) -> StatementRegressCase:
    number = f"{ordinal:05d}"
    return StatementRegressCase(
        ordinal=ordinal,
        case_id=f"ABORT{number}",
        sql_filename=f"ABORT{number}.sql",
        object_prefix=f"abort_{number}_",
        case_group=group,
        case_type=case_type,
        outcome=outcome,
        execution_profile=execution_profile,
        derived_axes=dict(axes),
        factor_values=factor_values,
        combination_strategy=combination_strategy,
        description=description,
        expected_anchor=expected_anchor,
    )


def _build_abort_cases() -> tuple[StatementRegressCase, ...]:
    cases: list[StatementRegressCase] = []

    optional_keywords = ("omitted", "WORK", "TRANSACTION")
    chain_clauses = ("omitted", "AND CHAIN", "AND NO CHAIN")
    states = (
        "active_clean",
        "active_failed",
        "active_with_nested_savepoints",
        "idle",
    )
    for optional_keyword, chain_clause, transaction_state in itertools.product(
        optional_keywords, chain_clauses, states
    ):
        target_failure = transaction_state == "idle" and chain_clause == "AND CHAIN"
        outcome = "expected_failure" if target_failure else "success"
        state_factor = {
            "active_clean": "transaction_state=inside_transaction",
            "active_failed": "transaction_state=inside_transaction",
            "active_with_nested_savepoints": "transaction_state=savepoint_exists",
            "idle": "transaction_state=outside_transaction",
        }[transaction_state]
        factors = (
            "statement_branch=branch_1",
            f"expected_status={'failure' if target_failure else 'success'}",
            state_factor,
            "transaction_mode=default",
            f"chain_behavior={_chain_factor_value(chain_clause)}",
            "environment_context=normal_session",
            (
                "environment_context=outside_transaction_required"
                if transaction_state == "idle"
                else "environment_context=transaction_block"
            ),
            "framework_context=no_outer_transaction",
            (
                "invalid_combination=syntax_valid_semantic_error"
                if target_failure
                else "invalid_combination=none"
            ),
            (
                "state_boundary=no_open_transaction"
                if transaction_state == "idle"
                else (
                    "state_boundary=nested_savepoint"
                    if transaction_state == "active_with_nested_savepoints"
                    else "verification_mode=effect_query"
                )
            ),
            (
                "verification_mode=error_assertion"
                if target_failure
                else "verification_mode=effect_query"
            ),
            (
                "cleanup_mode=rollback"
                if chain_clause == "AND CHAIN"
                else "cleanup_mode=drop_objects"
            ),
        )
        if transaction_state == "active_with_nested_savepoints":
            factors += (
                "savepoint_name_shape=simple_name",
                "savepoint_name_shape=quoted_name",
                "savepoint_name_shape=released_name",
            )
        syntax_label = _abort_sql(optional_keyword, chain_clause).removesuffix(";")
        description = (
            f"Verify PostgreSQL 18.4 {syntax_label} in {transaction_state} state"
        )
        anchor = (
            "target SQLSTATE 25P01 and a usable idle session"
            if target_failure
            else (
                "old transaction changes are absent and the chained transaction is active"
                if chain_clause == "AND CHAIN"
                else "old transaction changes are absent and no chained transaction remains"
            )
        )
        cases.append(
            _case(
                len(cases) + 1,
                group="legal_syntax_state_product",
                case_type="negative" if target_failure else "state_transition",
                outcome=outcome,
                execution_profile="same_session_multiphase",
                axes={
                    "optional_keyword": optional_keyword,
                    "chain_clause": chain_clause,
                    "transaction_state": transaction_state,
                },
                factor_values=tuple(dict.fromkeys(factors)),
                combination_strategy="full-cartesian 3 optional keywords × 3 chain clauses × 4 transaction states",
                description=description,
                expected_anchor=anchor,
            )
        )

    isolations = (
        "READ COMMITTED",
        "READ UNCOMMITTED",
        "REPEATABLE READ",
        "SERIALIZABLE",
    )
    access_modes = ("READ WRITE", "READ ONLY")
    deferrabilities = ("DEFERRABLE", "NOT DEFERRABLE")
    for isolation, access_mode, deferrability in itertools.product(
        isolations, access_modes, deferrabilities
    ):
        factors = (
            "statement_branch=branch_1",
            "expected_status=success",
            "transaction_state=inside_transaction",
            "transaction_mode=isolation_level",
            (
                "transaction_mode=read_write"
                if access_mode == "READ WRITE"
                else "transaction_mode=read_only"
            ),
            "chain_behavior=and_chain",
            "environment_context=transaction_block",
            "framework_context=no_outer_transaction",
            "invalid_combination=none",
            "verification_mode=effect_query",
            "cleanup_mode=rollback",
        )
        if deferrability == "DEFERRABLE":
            factors += ("transaction_mode=deferrable",)
        cases.append(
            _case(
                len(cases) + 1,
                group="chain_characteristics_product",
                case_type="state_transition",
                outcome="success",
                execution_profile="same_session_multiphase",
                axes={
                    "isolation": isolation,
                    "access_mode": access_mode,
                    "deferrability": deferrability,
                },
                factor_values=tuple(dict.fromkeys(factors)),
                combination_strategy="full-cartesian 4 isolation levels × 2 access modes × 2 deferrability modes",
                description=(
                    "Verify ABORT AND CHAIN preserves "
                    f"{isolation}, {access_mode}, {deferrability}"
                ),
                expected_anchor="all three transaction characteristics are preserved and the chained transaction is cleaned",
            )
        )

    cases.append(
        _case(
            len(cases) + 1,
            group="transactional_ddl",
            case_type="catalog",
            outcome="success",
            execution_profile="same_session_multiphase",
            axes={"catalog_effect": "create_table_rolled_back"},
            factor_values=(
                "statement_branch=branch_1",
                "expected_status=success",
                "transaction_state=inside_transaction",
                "transaction_mode=default",
                "chain_behavior=none",
                "environment_context=transaction_block",
                "framework_context=no_outer_transaction",
                "invalid_combination=none",
                "verification_mode=catalog_query",
                "cleanup_mode=drop_objects",
            ),
            combination_strategy="dedicated catalog-effect witness",
            description="Verify ABORT discards a table created in the current transaction",
            expected_anchor="to_regclass is NULL after ABORT",
        )
    )

    syntax_failures = (
        ("missing_and", "ABORT CHAIN;"),
        ("missing_chain", "ABORT AND;"),
        ("incomplete_no_chain", "ABORT AND NO;"),
        ("duplicate_optional_keyword", "ABORT WORK TRANSACTION;"),
        ("target_operand", "{target_operand}"),
    )
    for syntax_error, _ in syntax_failures:
        cases.append(
            _case(
                len(cases) + 1,
                group="syntax_boundary",
                case_type="negative",
                outcome="expected_failure",
                execution_profile="same_session_multiphase",
                axes={"syntax_error": syntax_error},
                factor_values=(
                    "statement_branch=branch_1",
                    "expected_status=failure",
                    "framework_context=no_outer_transaction",
                    "verification_mode=error_assertion",
                    "cleanup_mode=reset_state",
                ),
                combination_strategy="one independent parser boundary per file",
                description=f"Verify ABORT rejects the {syntax_error} syntax boundary",
                expected_anchor="target SQLSTATE 42601 and a usable session",
            )
        )

    for chain_clause in chain_clauses:
        target_failure = chain_clause == "AND CHAIN"
        cases.append(
            _case(
                len(cases) + 1,
                group="prepared_transaction_noninterference",
                case_type="external_state",
                outcome="expected_failure" if target_failure else "success",
                execution_profile="external_isolated",
                axes={
                    "chain_clause": chain_clause,
                    "required_setting": "max_prepared_transactions_gt_0",
                },
                factor_values=(
                    "statement_branch=branch_1",
                    f"expected_status={'failure' if target_failure else 'success'}",
                    "transaction_state=prepared_transaction_exists",
                    "transaction_mode=default",
                    f"chain_behavior={_chain_factor_value(chain_clause)}",
                    "environment_context=external_resource_required",
                    "framework_context=no_outer_transaction",
                    (
                        "invalid_combination=syntax_valid_semantic_error"
                        if target_failure
                        else "invalid_combination=none"
                    ),
                    "state_boundary=prepared_transaction_leftover",
                    (
                        "verification_mode=error_assertion"
                        if target_failure
                        else "verification_mode=catalog_query"
                    ),
                    "cleanup_mode=rollback",
                ),
                combination_strategy="external prepared-transaction state × all chain clauses",
                description=(
                    f"Verify {_abort_sql('omitted', chain_clause).removesuffix(';')} "
                    "does not consume an existing prepared transaction"
                ),
                expected_anchor=(
                    "target SQLSTATE 25P01 and the prepared transaction remains present"
                    if target_failure
                    else "the prepared transaction remains present until explicit cleanup"
                ),
            )
        )

    return tuple(cases)


def _chain_factor_value(chain_clause: str) -> str:
    return {
        "omitted": "none",
        "AND CHAIN": "and_chain",
        "AND NO CHAIN": "and_no_chain",
    }[chain_clause]


def _abort_sql(optional_keyword: str, chain_clause: str) -> str:
    parts = ["ABORT"]
    if optional_keyword != "omitted":
        parts.append(optional_keyword)
    if chain_clause != "omitted":
        parts.extend(chain_clause.split())
    return " ".join(parts) + ";"


def _ids(cases: tuple[StatementRegressCase, ...], predicate: Callable[[StatementRegressCase], bool]) -> tuple[str, ...]:
    return tuple(case.case_id for case in cases if predicate(case))


def _build_abort_factor_decisions(
    entry: StatementCycleEntry,
    cases: tuple[StatementRegressCase, ...],
) -> tuple[FactorValueDecision, ...]:
    na_reasons = {
        ("transaction_id_shape", "simple_id"): "ABORT has no transaction identifier operand.",
        ("transaction_id_shape", "quoted_id"): "ABORT has no transaction identifier operand, quoted or otherwise.",
        ("transaction_id_shape", "missing_id"): "ABORT does not require or accept a transaction identifier.",
        ("transaction_id_shape", "duplicate_id"): "ABORT cannot contain duplicate transaction identifiers because it accepts none.",
        ("savepoint_name_shape", "missing_name"): "ABORT rolls back the top-level transaction and never resolves a savepoint name.",
        ("transaction_state", "missing_required_state"): "This value duplicates the explicitly covered outside_transaction idle state and has no distinct official ABORT state.",
        ("framework_context", "outer_transaction_present"): "A hidden harness transaction would be terminated by ABORT; the file contract therefore forbids an implicit outer transaction.",
        ("invalid_combination", "object_type_mismatch"): "ABORT has no target object whose type can mismatch.",
        ("verification_mode", "returned_rows"): "ABORT returns a command status, not a result-row set.",
    }
    expected_failure_values = {
        ("expected_status", "failure"),
        ("invalid_combination", "syntax_valid_semantic_error"),
        ("verification_mode", "error_assertion"),
    }

    def witnesses(factor: str, value: str) -> tuple[str, ...]:
        token = f"{factor}={value}"
        return _ids(cases, lambda case: token in case.factor_values)

    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            pair = (factor.name, value)
            if pair in na_reasons:
                disposition = "justified_na"
                reason = na_reasons[pair]
                case_ids: tuple[str, ...] = ()
            else:
                disposition = (
                    "expected_failure" if pair in expected_failure_values else "covered"
                )
                reason = (
                    "The value is exercised only by a single-cause expected-failure target."
                    if disposition == "expected_failure"
                    else None
                )
                case_ids = witnesses(factor.name, value)
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=disposition,
                    reason=reason,
                    case_ids=case_ids,
                )
            )
    return tuple(decisions)


def _build_abort_plan(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    cases = _build_abort_cases()
    if tuple(case.ordinal for case in cases) != tuple(range(1, len(cases) + 1)):
        raise RemainingStatementRegressError("ABORT case ordinals are not contiguous")
    decisions = _build_abort_factor_decisions(entry, cases)
    return StatementRegressPlan(
        statement_key="abort",
        file_prefix="ABORT",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=decisions,
    )


def _build_alter_aggregate_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    # Keep statement-specific fixture and SQL logic out of this already large
    # cycle/publication module.  The lazy import avoids a module cycle because
    # the design module reuses the immutable plan dataclasses above.
    from .alter_aggregate_regress import build_alter_aggregate_plan

    return build_alter_aggregate_plan(snapshot, entry)


def _build_alter_collation_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    from .alter_collation_regress import build_alter_collation_plan

    return build_alter_collation_plan(snapshot, entry)


def _build_alter_conversion_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    from .alter_conversion_regress import build_alter_conversion_plan

    return build_alter_conversion_plan(snapshot, entry)


def _build_alter_database_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    from .alter_database_regress import build_alter_database_plan

    return build_alter_database_plan(snapshot, entry)


def _build_alter_default_privileges_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    from .alter_default_privileges_regress import (
        build_alter_default_privileges_plan,
    )

    return build_alter_default_privileges_plan(snapshot, entry)


def _build_alter_domain_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    from .alter_domain_regress import build_alter_domain_plan

    return build_alter_domain_plan(snapshot, entry)


def _build_alter_event_trigger_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    from .alter_event_trigger_regress import build_alter_event_trigger_plan

    return build_alter_event_trigger_plan(snapshot, entry)


def _build_alter_extension_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    from .alter_extension_regress import build_alter_extension_plan

    return build_alter_extension_plan(snapshot, entry)


def _build_alter_foreign_data_wrapper_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    from .alter_foreign_data_wrapper_regress import (
        build_alter_foreign_data_wrapper_plan,
    )

    return build_alter_foreign_data_wrapper_plan(snapshot, entry)


@lru_cache(maxsize=4)
def _alter_foreign_table_factor_plan(repository_root: str):
    from .alter_foreign_table_factor_loop import (
        build_alter_foreign_table_factor_loop_plan,
    )

    return build_alter_foreign_table_factor_loop_plan(Path(repository_root))


def _build_alter_foreign_table_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_foreign_table_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    cases = tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER FOREIGN TABLE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )
    standard_case_by_id = {case.case_id: case for case in cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER FOREIGN TABLE canonical factor mapping is not unique: "
                    f"{factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return StatementRegressPlan(
        statement_key="alter_foreign_table",
        file_prefix="ALTERFOREIGNTABLE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


@lru_cache(maxsize=4)
def _alter_function_factor_plan(repository_root: str):
    from .alter_function_factor_loop import (
        build_alter_function_factor_loop_plan,
    )

    return build_alter_function_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_function_extension_plan(repository_root: str):
    from .alter_function_factor_extension import (
        build_alter_function_factor_extension_plan,
    )

    return build_alter_function_factor_extension_plan(Path(repository_root))


def _alter_function_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER FUNCTION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_function_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_function_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER FUNCTION canonical factor mapping is not unique: "
                    f"{factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_function_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_function_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_function_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_function_baseline_cases(factor_plan)
    extension_cases = _alter_function_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_function_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_function",
        file_prefix="ALTERFUNCTION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


@lru_cache(maxsize=4)
def _alter_language_factor_plan(repository_root: str):
    from .alter_language_factor_loop import (
        build_alter_language_factor_loop_plan,
    )

    return build_alter_language_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_language_extension_plan(repository_root: str):
    from .alter_language_factor_extension import (
        build_alter_language_factor_extension_plan,
    )

    return build_alter_language_factor_extension_plan(Path(repository_root))


def _alter_language_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER LANGUAGE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_language_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_language_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER LANGUAGE canonical factor mapping is not unique: "
                    f"{factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_language_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_language_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_language_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_language_baseline_cases(factor_plan)
    extension_cases = _alter_language_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_language_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_language",
        file_prefix="ALTERLANGUAGE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


@lru_cache(maxsize=4)
def _alter_large_object_factor_plan(repository_root: str):
    from .alter_large_object_factor_loop import (
        build_alter_large_object_factor_loop_plan,
    )

    return build_alter_large_object_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_large_object_extension_plan(repository_root: str):
    from .alter_large_object_factor_extension import (
        build_alter_large_object_factor_extension_plan,
    )

    return build_alter_large_object_factor_extension_plan(Path(repository_root))


def _alter_large_object_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER LARGE OBJECT witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_large_object_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_large_object_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER LARGE OBJECT canonical factor mapping is not unique: "
                    f"{factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_large_object_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_large_object_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_large_object_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_large_object_baseline_cases(factor_plan)
    extension_cases = _alter_large_object_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_large_object_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_large_object",
        file_prefix="ALTERLARGEOBJECT",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


@lru_cache(maxsize=4)
def _alter_materialized_view_factor_plan(repository_root: str):
    from .alter_materialized_view_factor_loop import (
        build_alter_materialized_view_factor_loop_plan,
    )

    return build_alter_materialized_view_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_materialized_view_extension_plan(repository_root: str):
    from .alter_materialized_view_factor_extension import (
        build_alter_materialized_view_factor_extension_plan,
    )

    return build_alter_materialized_view_factor_extension_plan(
        Path(repository_root)
    )


def _alter_materialized_view_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER MATERIALIZED VIEW witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_materialized_view_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_materialized_view_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER MATERIALIZED VIEW canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_materialized_view_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_materialized_view_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_materialized_view_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_materialized_view_baseline_cases(factor_plan)
    extension_cases = _alter_materialized_view_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_materialized_view_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_materialized_view",
        file_prefix="ALTERMATERIALIZEDVIEW",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


@lru_cache(maxsize=4)
def _alter_policy_factor_plan(repository_root: str):
    from .alter_policy_factor_loop import (
        build_alter_policy_factor_loop_plan,
    )

    return build_alter_policy_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_policy_extension_plan(repository_root: str):
    from .alter_policy_factor_extension import (
        build_alter_policy_factor_extension_plan,
    )

    return build_alter_policy_factor_extension_plan(
        Path(repository_root)
    )


def _alter_policy_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER POLICY witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_policy_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_policy_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER POLICY canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_policy_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_policy_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_policy_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_policy_baseline_cases(factor_plan)
    extension_cases = _alter_policy_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_policy_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_policy",
        file_prefix="ALTERPOLICY",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


@lru_cache(maxsize=4)
def _alter_procedure_factor_plan(repository_root: str):
    from .alter_procedure_factor_loop import (
        build_alter_procedure_factor_loop_plan,
    )

    return build_alter_procedure_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_procedure_extension_plan(repository_root: str):
    from .alter_procedure_factor_extension import (
        build_alter_procedure_factor_extension_plan,
    )

    return build_alter_procedure_factor_extension_plan(
        Path(repository_root)
    )


def _alter_procedure_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER PROCEDURE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_procedure_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_procedure_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER PROCEDURE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_procedure_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_procedure_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_procedure_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_procedure_baseline_cases(factor_plan)
    extension_cases = _alter_procedure_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_procedure_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_procedure",
        file_prefix="ALTERPROCEDURE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


@lru_cache(maxsize=4)
def _alter_routine_factor_plan(repository_root: str):
    from .alter_routine_factor_loop import (
        build_alter_routine_factor_loop_plan,
    )

    return build_alter_routine_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_routine_extension_plan(repository_root: str):
    from .alter_routine_factor_extension import (
        build_alter_routine_factor_extension_plan,
    )

    return build_alter_routine_factor_extension_plan(
        Path(repository_root)
    )


def _alter_routine_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER ROUTINE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_routine_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_routine_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER ROUTINE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_routine_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_routine_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_routine_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_routine_baseline_cases(factor_plan)
    extension_cases = _alter_routine_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_routine_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_routine",
        file_prefix="ALTERROUTINE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )




@lru_cache(maxsize=4)
def _alter_rule_factor_plan(repository_root: str):
    from .alter_rule_factor_loop import (
        build_alter_rule_factor_loop_plan,
    )

    return build_alter_rule_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_rule_extension_plan(repository_root: str):
    from .alter_rule_factor_extension import (
        build_alter_rule_factor_extension_plan,
    )

    return build_alter_rule_factor_extension_plan(
        Path(repository_root)
    )


def _alter_rule_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER RULE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_rule_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_rule_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER RULE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_rule_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_rule_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_rule_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_rule_baseline_cases(factor_plan)
    extension_cases = _alter_rule_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_rule_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_rule",
        file_prefix="ALTERRULE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


@lru_cache(maxsize=4)
def _alter_schema_factor_plan(repository_root: str):
    from .alter_schema_factor_loop import (
        build_alter_schema_factor_loop_plan,
    )

    return build_alter_schema_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_schema_extension_plan(repository_root: str):
    from .alter_schema_factor_extension import (
        build_alter_schema_factor_extension_plan,
    )

    return build_alter_schema_factor_extension_plan(
        Path(repository_root)
    )


def _alter_schema_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER SCHEMA witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_schema_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_schema_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER SCHEMA canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_schema_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_schema_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_schema_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_schema_baseline_cases(factor_plan)
    extension_cases = _alter_schema_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_schema_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_schema",
        file_prefix="ALTERSCHEMA",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


@lru_cache(maxsize=4)
def _alter_server_factor_plan(repository_root: str):
    from .alter_server_factor_loop import (
        build_alter_server_factor_loop_plan,
    )

    return build_alter_server_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_server_extension_plan(repository_root: str):
    from .alter_server_factor_extension import (
        build_alter_server_factor_extension_plan,
    )

    return build_alter_server_factor_extension_plan(
        Path(repository_root)
    )


def _alter_server_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER SERVER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_server_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_server_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER SERVER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_server_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_server_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_server_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_server_baseline_cases(factor_plan)
    extension_cases = _alter_server_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_server_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_server",
        file_prefix="ALTERSERVER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _alter_statistics_factor_plan(repository_root: str):
    from .alter_statistics_factor_loop import (
        build_alter_statistics_factor_loop_plan,
    )

    return build_alter_statistics_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _alter_statistics_extension_plan(repository_root: str):
    from .alter_statistics_factor_extension import (
        build_alter_statistics_factor_extension_plan,
    )

    return build_alter_statistics_factor_extension_plan(
        Path(repository_root)
    )


def _alter_statistics_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy=(
                "one_primary_factor_value_with_legal_baselines"
            ),
            description=(
                "Independent ALTER STATISTICS witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; "
                "actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_statistics_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": (
                    case.derived_from_combination_group
                ),
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}"
                for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; "
                "actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_statistics_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {
        case.case_id: case for case in factor_plan.cases
    }
    factor_case_by_obligation = {
        case.primary_obligation_id: case
        for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER STATISTICS canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[
                obligation.obligation_id
            ]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_statistics_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_statistics_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_statistics_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_statistics_baseline_cases(factor_plan)
    extension_cases = _alter_statistics_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_statistics_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_statistics",
        file_prefix="ALTERSTATISTICS",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _alter_subscription_factor_plan(repository_root: str):
    from .alter_subscription_factor_loop import (
        build_alter_subscription_factor_loop_plan,
    )

    return build_alter_subscription_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_subscription_extension_plan(repository_root: str):
    from .alter_subscription_factor_extension import (
        build_alter_subscription_factor_extension_plan,
    )

    return build_alter_subscription_factor_extension_plan(
        Path(repository_root)
    )


def _alter_subscription_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER SUBSCRIPTION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_subscription_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_subscription_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER SUBSCRIPTION canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_subscription_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_subscription_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_subscription_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_subscription_baseline_cases(factor_plan)
    extension_cases = _alter_subscription_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_subscription_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_subscription",
        file_prefix="ALTERSUBSCRIPTION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _alter_system_factor_plan(repository_root: str):
    from .alter_system_factor_loop import (
        build_alter_system_factor_loop_plan,
    )

    return build_alter_system_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_system_extension_plan(repository_root: str):
    from .alter_system_factor_extension import (
        build_alter_system_factor_extension_plan,
    )

    return build_alter_system_factor_extension_plan(
        Path(repository_root)
    )


def _alter_system_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER SYSTEM witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_system_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_system_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER SYSTEM canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_system_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_system_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_system_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_system_baseline_cases(factor_plan)
    extension_cases = _alter_system_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_system_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_system",
        file_prefix="ALTERSYSTEM",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _alter_tablespace_factor_plan(repository_root: str):
    from .alter_tablespace_factor_loop import (
        build_alter_tablespace_factor_loop_plan,
    )

    return build_alter_tablespace_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_tablespace_extension_plan(repository_root: str):
    from .alter_tablespace_factor_extension import (
        build_alter_tablespace_factor_extension_plan,
    )

    return build_alter_tablespace_factor_extension_plan(
        Path(repository_root)
    )


def _alter_tablespace_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER TABLESPACE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_tablespace_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_tablespace_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER TABLESPACE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_tablespace_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_tablespace_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_tablespace_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_tablespace_baseline_cases(factor_plan)
    extension_cases = _alter_tablespace_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_tablespace_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_tablespace",
        file_prefix="ALTERTABLESPACE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _alter_text_search_configuration_factor_plan(repository_root: str):
    from .alter_text_search_configuration_factor_loop import (
        build_alter_text_search_configuration_factor_loop_plan,
    )

    return build_alter_text_search_configuration_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_text_search_configuration_extension_plan(repository_root: str):
    from .alter_text_search_configuration_factor_extension import (
        build_alter_text_search_configuration_factor_extension_plan,
    )

    return build_alter_text_search_configuration_factor_extension_plan(
        Path(repository_root)
    )


def _alter_text_search_configuration_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER TEXT SEARCH CONFIGURATION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_text_search_configuration_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_text_search_configuration_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER TEXT SEARCH CONFIGURATION canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_text_search_configuration_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_text_search_configuration_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_text_search_configuration_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_text_search_configuration_baseline_cases(factor_plan)
    extension_cases = _alter_text_search_configuration_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_text_search_configuration_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_text_search_configuration",
        file_prefix="ALTERTEXTSEARCHCONFIGURATION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _alter_text_search_dictionary_factor_plan(repository_root: str):
    from .alter_text_search_dictionary_factor_loop import (
        build_alter_text_search_dictionary_factor_loop_plan,
    )

    return build_alter_text_search_dictionary_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_text_search_dictionary_extension_plan(repository_root: str):
    from .alter_text_search_dictionary_factor_extension import (
        build_alter_text_search_dictionary_factor_extension_plan,
    )

    return build_alter_text_search_dictionary_factor_extension_plan(
        Path(repository_root)
    )


def _alter_text_search_dictionary_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER TEXT SEARCH DICTIONARY witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_text_search_dictionary_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_text_search_dictionary_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER TEXT SEARCH DICTIONARY canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_text_search_dictionary_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_text_search_dictionary_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_text_search_dictionary_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_text_search_dictionary_baseline_cases(factor_plan)
    extension_cases = _alter_text_search_dictionary_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_text_search_dictionary_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_text_search_dictionary",
        file_prefix="ALTERTEXTSEARCHDICTIONARY",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _alter_text_search_parser_factor_plan(repository_root: str):
    from .alter_text_search_parser_factor_loop import (
        build_alter_text_search_parser_factor_loop_plan,
    )

    return build_alter_text_search_parser_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_text_search_parser_extension_plan(repository_root: str):
    from .alter_text_search_parser_factor_extension import (
        build_alter_text_search_parser_factor_extension_plan,
    )

    return build_alter_text_search_parser_factor_extension_plan(
        Path(repository_root)
    )


def _alter_text_search_parser_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER TEXT SEARCH PARSER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_text_search_parser_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_text_search_parser_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER TEXT SEARCH PARSER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_text_search_parser_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_text_search_parser_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_text_search_parser_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_text_search_parser_baseline_cases(factor_plan)
    extension_cases = _alter_text_search_parser_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_text_search_parser_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_text_search_parser",
        file_prefix="ALTERTEXTSEARCHPARSER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _alter_text_search_template_factor_plan(repository_root: str):
    from .alter_text_search_template_factor_loop import (
        build_alter_text_search_template_factor_loop_plan,
    )

    return build_alter_text_search_template_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _alter_text_search_template_extension_plan(repository_root: str):
    from .alter_text_search_template_factor_extension import (
        build_alter_text_search_template_factor_extension_plan,
    )

    return build_alter_text_search_template_factor_extension_plan(
        Path(repository_root)
    )


def _alter_text_search_template_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER TEXT SEARCH TEMPLATE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_text_search_template_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_text_search_template_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER TEXT SEARCH TEMPLATE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_text_search_template_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_text_search_template_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_text_search_template_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_text_search_template_baseline_cases(factor_plan)
    extension_cases = _alter_text_search_template_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_text_search_template_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_text_search_template",
        file_prefix="ALTERTEXTSEARCHTEMPLATE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _alter_trigger_factor_plan(repository_root: str):
    from .alter_trigger_factor_loop import (
        build_alter_trigger_factor_loop_plan,
    )

    return build_alter_trigger_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_trigger_extension_plan(repository_root: str):
    from .alter_trigger_factor_extension import (
        build_alter_trigger_factor_extension_plan,
    )

    return build_alter_trigger_factor_extension_plan(
        Path(repository_root)
    )


def _alter_trigger_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER TRIGGER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_trigger_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_trigger_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER TRIGGER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_trigger_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_trigger_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_trigger_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_trigger_baseline_cases(factor_plan)
    extension_cases = _alter_trigger_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_trigger_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_trigger",
        file_prefix="ALTERTRIGGER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _alter_type_factor_plan(repository_root: str):
    from .alter_type_factor_loop import (
        build_alter_type_factor_loop_plan,
    )

    return build_alter_type_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_type_extension_plan(repository_root: str):
    from .alter_type_factor_extension import (
        build_alter_type_factor_extension_plan,
    )

    return build_alter_type_factor_extension_plan(
        Path(repository_root)
    )


def _alter_type_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER TYPE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_type_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_type_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER TYPE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_type_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_type_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_type_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_type_baseline_cases(factor_plan)
    extension_cases = _alter_type_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_type_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_type",
        file_prefix="ALTERTYPE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _alter_user_factor_plan(repository_root: str):
    from .alter_user_factor_loop import (
        build_alter_user_factor_loop_plan,
    )

    return build_alter_user_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_user_extension_plan(repository_root: str):
    from .alter_user_factor_extension import (
        build_alter_user_factor_extension_plan,
    )

    return build_alter_user_factor_extension_plan(
        Path(repository_root)
    )


def _alter_user_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER USER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_user_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_user_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER USER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_user_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_user_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_user_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_user_baseline_cases(factor_plan)
    extension_cases = _alter_user_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_user_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_user",
        file_prefix="ALTERUSER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _alter_user_mapping_factor_plan(repository_root: str):
    from .alter_user_mapping_factor_loop import (
        build_alter_user_mapping_factor_loop_plan,
    )

    return build_alter_user_mapping_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_user_mapping_extension_plan(repository_root: str):
    from .alter_user_mapping_factor_extension import (
        build_alter_user_mapping_factor_extension_plan,
    )

    return build_alter_user_mapping_factor_extension_plan(
        Path(repository_root)
    )


def _alter_user_mapping_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER USER MAPPING witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_user_mapping_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_user_mapping_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER USER MAPPING canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_user_mapping_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_user_mapping_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_user_mapping_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_user_mapping_baseline_cases(factor_plan)
    extension_cases = _alter_user_mapping_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_user_mapping_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_user_mapping",
        file_prefix="ALTERUSERMAPPING",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _alter_view_factor_plan(repository_root: str):
    from .alter_view_factor_loop import (
        build_alter_view_factor_loop_plan,
    )

    return build_alter_view_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_view_extension_plan(repository_root: str):
    from .alter_view_factor_extension import (
        build_alter_view_factor_extension_plan,
    )

    return build_alter_view_factor_extension_plan(
        Path(repository_root)
    )


def _alter_view_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER VIEW witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_view_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_view_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER VIEW canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_view_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_view_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_view_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_view_baseline_cases(factor_plan)
    extension_cases = _alter_view_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_view_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_view",
        file_prefix="ALTERVIEW",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _analyze_factor_plan(repository_root: str):
    from .analyze_factor_loop import (
        build_analyze_factor_loop_plan,
    )

    return build_analyze_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _analyze_extension_plan(repository_root: str):
    from .analyze_factor_extension import (
        build_analyze_factor_extension_plan,
    )

    return build_analyze_factor_extension_plan(
        Path(repository_root)
    )


def _analyze_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ANALYZE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _analyze_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _analyze_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ANALYZE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_analyze_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _analyze_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _analyze_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _analyze_baseline_cases(factor_plan)
    extension_cases = _analyze_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _analyze_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="analyze",
        file_prefix="ANALYZE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _begin_factor_plan(repository_root: str):
    from .begin_factor_loop import (
        build_begin_factor_loop_plan,
    )

    return build_begin_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _begin_extension_plan(repository_root: str):
    from .begin_factor_extension import (
        build_begin_factor_extension_plan,
    )

    return build_begin_factor_extension_plan(
        Path(repository_root)
    )


def _begin_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent BEGIN witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _begin_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _begin_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "BEGIN canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_begin_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _begin_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _begin_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _begin_baseline_cases(factor_plan)
    extension_cases = _begin_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _begin_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="begin",
        file_prefix="BEGIN",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _call_factor_plan(repository_root: str):
    from .call_factor_loop import (
        build_call_factor_loop_plan,
    )

    return build_call_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _call_extension_plan(repository_root: str):
    from .call_factor_extension import (
        build_call_factor_extension_plan,
    )

    return build_call_factor_extension_plan(
        Path(repository_root)
    )


def _call_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CALL witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _call_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _call_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CALL canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_call_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _call_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _call_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _call_baseline_cases(factor_plan)
    extension_cases = _call_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _call_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="call",
        file_prefix="CALL",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _checkpoint_factor_plan(repository_root: str):
    from .checkpoint_factor_loop import (
        build_checkpoint_factor_loop_plan,
    )

    return build_checkpoint_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _checkpoint_extension_plan(repository_root: str):
    from .checkpoint_factor_extension import (
        build_checkpoint_factor_extension_plan,
    )

    return build_checkpoint_factor_extension_plan(
        Path(repository_root)
    )


def _checkpoint_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CHECKPOINT witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _checkpoint_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _checkpoint_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CHECKPOINT canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_checkpoint_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _checkpoint_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _checkpoint_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _checkpoint_baseline_cases(factor_plan)
    extension_cases = _checkpoint_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _checkpoint_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="checkpoint",
        file_prefix="CHECKPOINT",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _cluster_factor_plan(repository_root: str):
    from .cluster_factor_loop import (
        build_cluster_factor_loop_plan,
    )

    return build_cluster_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _cluster_extension_plan(repository_root: str):
    from .cluster_factor_extension import (
        build_cluster_factor_extension_plan,
    )

    return build_cluster_factor_extension_plan(
        Path(repository_root)
    )


def _cluster_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CLUSTER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _cluster_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _cluster_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CLUSTER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_cluster_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _cluster_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _cluster_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _cluster_baseline_cases(factor_plan)
    extension_cases = _cluster_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _cluster_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="cluster",
        file_prefix="CLUSTER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _comment_factor_plan(repository_root: str):
    from .comment_factor_loop import (
        build_comment_factor_loop_plan,
    )

    return build_comment_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _comment_extension_plan(repository_root: str):
    from .comment_factor_extension import (
        build_comment_factor_extension_plan,
    )

    return build_comment_factor_extension_plan(
        Path(repository_root)
    )


def _comment_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent COMMENT ON witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _comment_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _comment_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "COMMENT canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_comment_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _comment_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _comment_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _comment_baseline_cases(factor_plan)
    extension_cases = _comment_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _comment_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="comment",
        file_prefix="COMMENT",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _commit_factor_plan(repository_root: str):
    from .commit_factor_loop import (
        build_commit_factor_loop_plan,
    )

    return build_commit_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _commit_extension_plan(repository_root: str):
    from .commit_factor_extension import (
        build_commit_factor_extension_plan,
    )

    return build_commit_factor_extension_plan(
        Path(repository_root)
    )


def _commit_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent COMMIT witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _commit_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _commit_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "COMMIT canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_commit_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _commit_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _commit_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _commit_baseline_cases(factor_plan)
    extension_cases = _commit_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _commit_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="commit",
        file_prefix="COMMIT",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _commit_prepared_factor_plan(repository_root: str):
    from .commit_prepared_factor_loop import (
        build_commit_prepared_factor_loop_plan,
    )

    return build_commit_prepared_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _commit_prepared_extension_plan(repository_root: str):
    from .commit_prepared_factor_extension import (
        build_commit_prepared_factor_extension_plan,
    )

    return build_commit_prepared_factor_extension_plan(
        Path(repository_root)
    )


def _commit_prepared_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent COMMIT PREPARED witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _commit_prepared_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _commit_prepared_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "COMMIT PREPARED canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_commit_prepared_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _commit_prepared_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _commit_prepared_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _commit_prepared_baseline_cases(factor_plan)
    extension_cases = _commit_prepared_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _commit_prepared_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="commit_prepared",
        file_prefix="COMMITPREPARED",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _copy_factor_plan(repository_root: str):
    from .copy_factor_loop import (
        build_copy_factor_loop_plan,
    )

    return build_copy_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _copy_extension_plan(repository_root: str):
    from .copy_factor_extension import (
        build_copy_factor_extension_plan,
    )

    return build_copy_factor_extension_plan(
        Path(repository_root)
    )


def _copy_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent COPY witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _copy_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _copy_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "COPY canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_copy_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _copy_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _copy_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _copy_baseline_cases(factor_plan)
    extension_cases = _copy_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _copy_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="copy",
        file_prefix="COPY",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_access_method_factor_plan(repository_root: str):
    from .create_access_method_factor_loop import (
        build_create_access_method_factor_loop_plan,
    )

    return build_create_access_method_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_access_method_extension_plan(repository_root: str):
    from .create_access_method_factor_extension import (
        build_create_access_method_factor_extension_plan,
    )

    return build_create_access_method_factor_extension_plan(
        Path(repository_root)
    )


def _create_access_method_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE ACCESS METHOD witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_access_method_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_access_method_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE ACCESS METHOD canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_access_method_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_access_method_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_access_method_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_access_method_baseline_cases(factor_plan)
    extension_cases = _create_access_method_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_access_method_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_access_method",
        file_prefix="CREATEACCESSMETHOD",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_aggregate_factor_plan(repository_root: str):
    from .create_aggregate_factor_loop import (
        build_create_aggregate_factor_loop_plan,
    )

    return build_create_aggregate_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_aggregate_extension_plan(repository_root: str):
    from .create_aggregate_factor_extension import (
        build_create_aggregate_factor_extension_plan,
    )

    return build_create_aggregate_factor_extension_plan(
        Path(repository_root)
    )


def _create_aggregate_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE AGGREGATE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_aggregate_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_aggregate_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE AGGREGATE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_aggregate_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_aggregate_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_aggregate_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_aggregate_baseline_cases(factor_plan)
    extension_cases = _create_aggregate_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_aggregate_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_aggregate",
        file_prefix="CREATEAGGREGATE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_cast_factor_plan(repository_root: str):
    from .create_cast_factor_loop import (
        build_create_cast_factor_loop_plan,
    )

    return build_create_cast_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_cast_extension_plan(repository_root: str):
    from .create_cast_factor_extension import (
        build_create_cast_factor_extension_plan,
    )

    return build_create_cast_factor_extension_plan(
        Path(repository_root)
    )


def _create_cast_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE CAST witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_cast_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_cast_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE CAST canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_cast_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_cast_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_cast_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_cast_baseline_cases(factor_plan)
    extension_cases = _create_cast_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_cast_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_cast",
        file_prefix="CREATECAST",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_collation_factor_plan(repository_root: str):
    from .create_collation_factor_loop import (
        build_create_collation_factor_loop_plan,
    )

    return build_create_collation_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_collation_extension_plan(repository_root: str):
    from .create_collation_factor_extension import (
        build_create_collation_factor_extension_plan,
    )

    return build_create_collation_factor_extension_plan(
        Path(repository_root)
    )


def _create_collation_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE COLLATION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_collation_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_collation_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE COLLATION canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_collation_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_collation_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_collation_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_collation_baseline_cases(factor_plan)
    extension_cases = _create_collation_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_collation_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_collation",
        file_prefix="CREATECOLLATION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_conversion_factor_plan(repository_root: str):
    from .create_conversion_factor_loop import (
        build_create_conversion_factor_loop_plan,
    )

    return build_create_conversion_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_conversion_extension_plan(repository_root: str):
    from .create_conversion_factor_extension import (
        build_create_conversion_factor_extension_plan,
    )

    return build_create_conversion_factor_extension_plan(
        Path(repository_root)
    )


def _create_conversion_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE CONVERSION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_conversion_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_conversion_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE CONVERSION canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_conversion_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_conversion_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_conversion_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_conversion_baseline_cases(factor_plan)
    extension_cases = _create_conversion_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_conversion_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_conversion",
        file_prefix="CREATECONVERSION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_database_factor_plan(repository_root: str):
    from .create_database_factor_loop import (
        build_create_database_factor_loop_plan,
    )

    return build_create_database_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_database_extension_plan(repository_root: str):
    from .create_database_factor_extension import (
        build_create_database_factor_extension_plan,
    )

    return build_create_database_factor_extension_plan(
        Path(repository_root)
    )


def _create_database_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE DATABASE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_database_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_database_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE DATABASE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_database_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_database_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_database_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_database_baseline_cases(factor_plan)
    extension_cases = _create_database_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_database_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_database",
        file_prefix="CREATEDATABASE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_domain_factor_plan(repository_root: str):
    from .create_domain_factor_loop import (
        build_create_domain_factor_loop_plan,
    )

    return build_create_domain_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_domain_extension_plan(repository_root: str):
    from .create_domain_factor_extension import (
        build_create_domain_factor_extension_plan,
    )

    return build_create_domain_factor_extension_plan(
        Path(repository_root)
    )


def _create_domain_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE DOMAIN witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_domain_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_domain_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE DOMAIN canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_domain_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_domain_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_domain_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_domain_baseline_cases(factor_plan)
    extension_cases = _create_domain_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_domain_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_domain",
        file_prefix="CREATEDOMAIN",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_event_trigger_factor_plan(repository_root: str):
    from .create_event_trigger_factor_loop import (
        build_create_event_trigger_factor_loop_plan,
    )

    return build_create_event_trigger_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_event_trigger_extension_plan(repository_root: str):
    from .create_event_trigger_factor_extension import (
        build_create_event_trigger_factor_extension_plan,
    )

    return build_create_event_trigger_factor_extension_plan(
        Path(repository_root)
    )


def _create_event_trigger_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(
                f"{case.factor_key}={case.factor_value}",
            ),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE EVENT TRIGGER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_event_trigger_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_event_trigger_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE EVENT TRIGGER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_event_trigger_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_event_trigger_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_event_trigger_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_event_trigger_baseline_cases(factor_plan)
    extension_cases = _create_event_trigger_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_event_trigger_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_event_trigger",
        file_prefix="CREATEEVENTTRIGGER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_extension_factor_plan(repository_root: str):
    from .create_extension_factor_loop import (
        build_create_extension_factor_loop_plan,
    )

    return build_create_extension_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_extension_extension_plan(repository_root: str):
    from .create_extension_factor_extension import (
        build_create_extension_factor_extension_plan,
    )

    return build_create_extension_factor_extension_plan(
        Path(repository_root)
    )


def _create_extension_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE EXTENSION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_extension_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_extension_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE EXTENSION canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_extension_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_extension_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_extension_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_extension_baseline_cases(factor_plan)
    extension_cases = _create_extension_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_extension_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_extension",
        file_prefix="CREATEEXTENSION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_foreign_data_wrapper_factor_plan(repository_root: str):
    from .create_foreign_data_wrapper_factor_loop import (
        build_create_foreign_data_wrapper_factor_loop_plan,
    )

    return build_create_foreign_data_wrapper_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_foreign_data_wrapper_extension_plan(repository_root: str):
    from .create_foreign_data_wrapper_factor_extension import (
        build_create_foreign_data_wrapper_factor_extension_plan,
    )

    return build_create_foreign_data_wrapper_factor_extension_plan(
        Path(repository_root)
    )


def _create_foreign_data_wrapper_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE FOREIGN DATA WRAPPER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_foreign_data_wrapper_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_foreign_data_wrapper_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE FOREIGN DATA WRAPPER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_foreign_data_wrapper_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_foreign_data_wrapper_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_foreign_data_wrapper_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_foreign_data_wrapper_baseline_cases(factor_plan)
    extension_cases = _create_foreign_data_wrapper_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_foreign_data_wrapper_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_foreign_data_wrapper",
        file_prefix="CREATEFOREIGNDATAWRAPPER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_foreign_table_factor_plan(repository_root: str):
    from .create_foreign_table_factor_loop import (
        build_create_foreign_table_factor_loop_plan,
    )

    return build_create_foreign_table_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_foreign_table_extension_plan(repository_root: str):
    from .create_foreign_table_factor_extension import (
        build_create_foreign_table_factor_extension_plan,
    )

    return build_create_foreign_table_factor_extension_plan(
        Path(repository_root)
    )


def _create_foreign_table_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(
                f"{case.factor_key}={case.factor_value}",
            ),
            combination_strategy=(
                "one_primary_factor_value_with_legal_baselines"
            ),
            description=(
                "Independent CREATE FOREIGN TABLE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; "
                "actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_foreign_table_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": (
                    case.derived_from_combination_group
                ),
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}"
                for key, value in case.factor_assignment
            ),
            combination_strategy=(
                "bounded_post_coverage_extension"
            ),
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; "
                "actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_foreign_table_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {
        case.case_id: case for case in factor_plan.cases
    }
    factor_case_by_obligation = {
        case.primary_obligation_id: case
        for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(
            factor.values, factor.row_ids
        ):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE FOREIGN TABLE canonical factor "
                    f"mapping is not unique: "
                    f"{factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[
                obligation.obligation_id
            ]
            standard_case = standard_case_by_id[
                factor_case.case_id
            ]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition
                        == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_foreign_table_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_foreign_table_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_foreign_table_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_foreign_table_baseline_cases(
        factor_plan
    )
    extension_cases = _create_foreign_table_extension_cases(
        extension_plan
    )
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_foreign_table_factor_decisions(
        factor_plan, entry
    )
    return StatementRegressPlan(
        statement_key="create_foreign_table",
        file_prefix="CREATEFOREIGNTABLE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=(
            snapshot.universe_semantic_sha256
        ),
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_function_factor_plan(repository_root: str):
    from .create_function_factor_loop import (
        build_create_function_factor_loop_plan,
    )

    return build_create_function_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_function_extension_plan(repository_root: str):
    from .create_function_factor_extension import (
        build_create_function_factor_extension_plan,
    )

    return build_create_function_factor_extension_plan(
        Path(repository_root)
    )


def _create_function_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE FUNCTION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_function_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_function_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE FUNCTION canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_function_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_function_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_function_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_function_baseline_cases(factor_plan)
    extension_cases = _create_function_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_function_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_function",
        file_prefix="CREATEFUNCTION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_group_factor_plan(repository_root: str):
    from .create_group_factor_loop import (
        build_create_group_factor_loop_plan,
    )

    return build_create_group_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_group_extension_plan(repository_root: str):
    from .create_group_factor_extension import (
        build_create_group_factor_extension_plan,
    )

    return build_create_group_factor_extension_plan(
        Path(repository_root)
    )


def _create_group_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE GROUP witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_group_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_group_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE GROUP canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_group_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_group_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_group_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_group_baseline_cases(factor_plan)
    extension_cases = _create_group_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_group_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_group",
        file_prefix="CREATEGROUP",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_index_factor_plan(repository_root: str):
    from .create_index_factor_loop import (
        build_create_index_factor_loop_plan,
    )

    return build_create_index_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_index_extension_plan(repository_root: str):
    from .create_index_factor_extension import (
        build_create_index_factor_extension_plan,
    )

    return build_create_index_factor_extension_plan(
        Path(repository_root)
    )


def _create_index_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE INDEX witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_index_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_index_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE INDEX canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_index_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_index_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_index_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_index_baseline_cases(factor_plan)
    extension_cases = _create_index_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_index_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_index",
        file_prefix="CREATEINDEX",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_language_factor_plan(repository_root: str):
    from .create_language_factor_loop import (
        build_create_language_factor_loop_plan,
    )

    return build_create_language_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_language_extension_plan(repository_root: str):
    from .create_language_factor_extension import (
        build_create_language_factor_extension_plan,
    )

    return build_create_language_factor_extension_plan(
        Path(repository_root)
    )


def _create_language_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE LANGUAGE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_language_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_language_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE LANGUAGE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_language_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_language_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_language_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_language_baseline_cases(factor_plan)
    extension_cases = _create_language_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_language_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_language",
        file_prefix="CREATELANGUAGE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_materialized_view_factor_plan(repository_root: str):
    from .create_materialized_view_factor_loop import (
        build_create_materialized_view_factor_loop_plan,
    )

    return build_create_materialized_view_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_materialized_view_extension_plan(repository_root: str):
    from .create_materialized_view_factor_extension import (
        build_create_materialized_view_factor_extension_plan,
    )

    return build_create_materialized_view_factor_extension_plan(
        Path(repository_root)
    )


def _create_materialized_view_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE MATERIALIZED VIEW witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_materialized_view_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_materialized_view_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {
        case.case_id: case for case in factor_plan.cases
    }
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE MATERIALIZED VIEW canonical factor mapping "
                    f"is not unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[
                obligation.obligation_id
            ]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_materialized_view_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_materialized_view_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_materialized_view_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_materialized_view_baseline_cases(factor_plan)
    extension_cases = _create_materialized_view_extension_cases(
        extension_plan
    )
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_materialized_view_factor_decisions(
        factor_plan, entry
    )
    return StatementRegressPlan(
        statement_key="create_materialized_view",
        file_prefix="CREATEMATERIALIZEDVIEW",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_operator_factor_plan(repository_root: str):
    from .create_operator_factor_loop import (
        build_create_operator_factor_loop_plan,
    )

    return build_create_operator_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_operator_extension_plan(repository_root: str):
    from .create_operator_factor_extension import (
        build_create_operator_factor_extension_plan,
    )

    return build_create_operator_factor_extension_plan(
        Path(repository_root)
    )


def _create_operator_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE OPERATOR witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_operator_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_operator_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE OPERATOR canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_operator_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_operator_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_operator_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_operator_baseline_cases(factor_plan)
    extension_cases = _create_operator_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_operator_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_operator",
        file_prefix="CREATEOPERATOR",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_operator_class_factor_plan(repository_root: str):
    from .create_operator_class_factor_loop import (
        build_create_operator_class_factor_loop_plan,
    )

    return build_create_operator_class_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_operator_class_extension_plan(repository_root: str):
    from .create_operator_class_factor_extension import (
        build_create_operator_class_factor_extension_plan,
    )

    return build_create_operator_class_factor_extension_plan(
        Path(repository_root)
    )


def _create_operator_class_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(
                f"{case.factor_key}={case.factor_value}",
            ),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE OPERATOR CLASS witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_operator_class_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}"
                for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_operator_class_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {
        case.case_id: case for case in factor_plan.cases
    }
    factor_case_by_obligation = {
        case.primary_obligation_id: case
        for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE OPERATOR CLASS canonical factor mapping "
                    f"is not unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[
                obligation.obligation_id
            ]
            standard_case = standard_case_by_id[
                factor_case.case_id
            ]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition
                        == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_operator_class_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_operator_class_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_operator_class_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_operator_class_baseline_cases(
        factor_plan
    )
    extension_cases = _create_operator_class_extension_cases(
        extension_plan
    )
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_operator_class_factor_decisions(
        factor_plan, entry
    )
    return StatementRegressPlan(
        statement_key="create_operator_class",
        file_prefix="CREATEOPERATORCLASS",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_operator_family_factor_plan(repository_root: str):
    from .create_operator_family_factor_loop import (
        build_create_operator_family_factor_loop_plan,
    )

    return build_create_operator_family_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_operator_family_extension_plan(repository_root: str):
    from .create_operator_family_factor_extension import (
        build_create_operator_family_factor_extension_plan,
    )

    return build_create_operator_family_factor_extension_plan(
        Path(repository_root)
    )


def _create_operator_family_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE OPERATOR FAMILY witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_operator_family_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_operator_family_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE OPERATOR FAMILY canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_operator_family_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_operator_family_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_operator_family_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_operator_family_baseline_cases(factor_plan)
    extension_cases = _create_operator_family_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_operator_family_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_operator_family",
        file_prefix="CREATEOPERATORFAMILY",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_policy_factor_plan(repository_root: str):
    from .create_policy_factor_loop import (
        build_create_policy_factor_loop_plan,
    )

    return build_create_policy_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_policy_extension_plan(repository_root: str):
    from .create_policy_factor_extension import (
        build_create_policy_factor_extension_plan,
    )

    return build_create_policy_factor_extension_plan(
        Path(repository_root)
    )


def _create_policy_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE POLICY witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_policy_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_policy_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE POLICY canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_policy_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_policy_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_policy_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_policy_baseline_cases(factor_plan)
    extension_cases = _create_policy_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_policy_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_policy",
        file_prefix="CREATEPOLICY",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_procedure_factor_plan(repository_root: str):
    from .create_procedure_factor_loop import (
        build_create_procedure_factor_loop_plan,
    )

    return build_create_procedure_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_procedure_extension_plan(repository_root: str):
    from .create_procedure_factor_extension import (
        build_create_procedure_factor_extension_plan,
    )

    return build_create_procedure_factor_extension_plan(
        Path(repository_root)
    )


def _create_procedure_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE PROCEDURE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_procedure_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_procedure_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE PROCEDURE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_procedure_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_procedure_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_procedure_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_procedure_baseline_cases(factor_plan)
    extension_cases = _create_procedure_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_procedure_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_procedure",
        file_prefix="CREATEPROCEDURE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_publication_factor_plan(repository_root: str):
    from .create_publication_factor_loop import (
        build_create_publication_factor_loop_plan,
    )

    return build_create_publication_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_publication_extension_plan(repository_root: str):
    from .create_publication_factor_extension import (
        build_create_publication_factor_extension_plan,
    )

    return build_create_publication_factor_extension_plan(
        Path(repository_root)
    )


def _create_publication_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE PUBLICATION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_publication_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_publication_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE PUBLICATION canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_publication_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_publication_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_publication_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_publication_baseline_cases(factor_plan)
    extension_cases = _create_publication_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_publication_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_publication",
        file_prefix="CREATEPUBLICATION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_role_factor_plan(repository_root: str):
    from .create_role_factor_loop import (
        build_create_role_factor_loop_plan,
    )

    return build_create_role_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_role_extension_plan(repository_root: str):
    from .create_role_factor_extension import (
        build_create_role_factor_extension_plan,
    )

    return build_create_role_factor_extension_plan(
        Path(repository_root)
    )


def _create_role_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE ROLE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_role_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_role_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE ROLE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_role_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_role_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_role_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_role_baseline_cases(factor_plan)
    extension_cases = _create_role_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_role_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_role",
        file_prefix="CREATEROLE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_rule_factor_plan(repository_root: str):
    from .create_rule_factor_loop import (
        build_create_rule_factor_loop_plan,
    )

    return build_create_rule_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_rule_extension_plan(repository_root: str):
    from .create_rule_factor_extension import (
        build_create_rule_factor_extension_plan,
    )

    return build_create_rule_factor_extension_plan(
        Path(repository_root)
    )


def _create_rule_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE RULE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_rule_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_rule_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE RULE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_rule_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_rule_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_rule_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_rule_baseline_cases(factor_plan)
    extension_cases = _create_rule_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_rule_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_rule",
        file_prefix="CREATERULE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_schema_factor_plan(repository_root: str):
    from .create_schema_factor_loop import (
        build_create_schema_factor_loop_plan,
    )

    return build_create_schema_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_schema_extension_plan(repository_root: str):
    from .create_schema_factor_extension import (
        build_create_schema_factor_extension_plan,
    )

    return build_create_schema_factor_extension_plan(
        Path(repository_root)
    )


def _create_schema_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE SCHEMA witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_schema_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_schema_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE SCHEMA canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_schema_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_schema_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_schema_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_schema_baseline_cases(factor_plan)
    extension_cases = _create_schema_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_schema_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_schema",
        file_prefix="CREATESCHEMA",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_sequence_factor_plan(repository_root: str):
    from .create_sequence_factor_loop import (
        build_create_sequence_factor_loop_plan,
    )

    return build_create_sequence_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_sequence_extension_plan(repository_root: str):
    from .create_sequence_factor_extension import (
        build_create_sequence_factor_extension_plan,
    )

    return build_create_sequence_factor_extension_plan(
        Path(repository_root)
    )


def _create_sequence_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE SEQUENCE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_sequence_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_sequence_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE SEQUENCE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_sequence_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_sequence_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_sequence_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_sequence_baseline_cases(factor_plan)
    extension_cases = _create_sequence_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_sequence_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_sequence",
        file_prefix="CREATESEQUENCE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_server_factor_plan(repository_root: str):
    from .create_server_factor_loop import (
        build_create_server_factor_loop_plan,
    )

    return build_create_server_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_server_extension_plan(repository_root: str):
    from .create_server_factor_extension import (
        build_create_server_factor_extension_plan,
    )

    return build_create_server_factor_extension_plan(
        Path(repository_root)
    )


def _create_server_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE SERVER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_server_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_server_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE SERVER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_server_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_server_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_server_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_server_baseline_cases(factor_plan)
    extension_cases = _create_server_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_server_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_server",
        file_prefix="CREATESERVER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_statistics_factor_plan(repository_root: str):
    from .create_statistics_factor_loop import (
        build_create_statistics_factor_loop_plan,
    )

    return build_create_statistics_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_statistics_extension_plan(repository_root: str):
    from .create_statistics_factor_extension import (
        build_create_statistics_factor_extension_plan,
    )

    return build_create_statistics_factor_extension_plan(
        Path(repository_root)
    )


def _create_statistics_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE STATISTICS witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_statistics_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_statistics_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE STATISTICS canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_statistics_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_statistics_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_statistics_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_statistics_baseline_cases(factor_plan)
    extension_cases = _create_statistics_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_statistics_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_statistics",
        file_prefix="CREATESTATISTICS",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_subscription_factor_plan(repository_root: str):
    from .create_subscription_factor_loop import (
        build_create_subscription_factor_loop_plan,
    )

    return build_create_subscription_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_subscription_extension_plan(repository_root: str):
    from .create_subscription_factor_extension import (
        build_create_subscription_factor_extension_plan,
    )

    return build_create_subscription_factor_extension_plan(
        Path(repository_root)
    )


def _create_subscription_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE SUBSCRIPTION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_subscription_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_subscription_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE SUBSCRIPTION canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_subscription_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_subscription_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_subscription_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_subscription_baseline_cases(factor_plan)
    extension_cases = _create_subscription_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_subscription_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_subscription",
        file_prefix="CREATESUBSCRIPTION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_table_factor_plan(repository_root: str):
    from .create_table_factor_loop import (
        build_create_table_factor_loop_plan,
    )

    return build_create_table_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_table_extension_plan(repository_root: str):
    from .create_table_factor_extension import (
        build_create_table_factor_extension_plan,
    )

    return build_create_table_factor_extension_plan(
        Path(repository_root)
    )


def _create_table_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE TABLE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_table_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_table_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE TABLE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_table_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_table_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_table_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_table_baseline_cases(factor_plan)
    extension_cases = _create_table_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_table_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_table",
        file_prefix="CREATETABLE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_tablespace_factor_plan(repository_root: str):
    from .create_tablespace_factor_loop import (
        build_create_tablespace_factor_loop_plan,
    )

    return build_create_tablespace_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_tablespace_extension_plan(repository_root: str):
    from .create_tablespace_factor_extension import (
        build_create_tablespace_factor_extension_plan,
    )

    return build_create_tablespace_factor_extension_plan(
        Path(repository_root)
    )


def _create_tablespace_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE TABLESPACE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_tablespace_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_tablespace_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE TABLESPACE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_tablespace_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_tablespace_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_tablespace_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_tablespace_baseline_cases(factor_plan)
    extension_cases = _create_tablespace_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_tablespace_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_tablespace",
        file_prefix="CREATETABLESPACE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_text_search_configuration_factor_plan(
    repository_root: str,
):
    from .create_text_search_configuration_factor_loop import (
        build_create_text_search_configuration_factor_loop_plan,
    )

    return build_create_text_search_configuration_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_text_search_configuration_extension_plan(
    repository_root: str,
):
    from .create_text_search_configuration_factor_extension import (
        build_create_text_search_configuration_factor_extension_plan,
    )

    return (
        build_create_text_search_configuration_factor_extension_plan(
            Path(repository_root)
        )
    )


def _create_text_search_configuration_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(
                f"{case.factor_key}={case.factor_value}",
            ),
            combination_strategy=(
                "one_primary_factor_value_with_legal_baselines"
            ),
            description=(
                "Independent CREATE TEXT SEARCH CONFIGURATION "
                f"witness for {case.factor_key}="
                f"{case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; "
                "actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_text_search_configuration_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": (
                    case.derived_from_combination_group
                ),
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}"
                for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; "
                "actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_text_search_configuration_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {
        case.case_id: case for case in factor_plan.cases
    }
    factor_case_by_obligation = {
        case.primary_obligation_id: case
        for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(
            factor.values, factor.row_ids
        ):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE TEXT SEARCH CONFIGURATION canonical "
                    "factor mapping is not unique: "
                    f"{factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[
                obligation.obligation_id
            ]
            standard_case = standard_case_by_id[
                factor_case.case_id
            ]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition
                        == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_text_search_configuration_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_text_search_configuration_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = (
        _create_text_search_configuration_extension_plan(
            str(snapshot.repository_root.resolve())
        )
    )
    baseline_cases = (
        _create_text_search_configuration_baseline_cases(
            factor_plan
        )
    )
    extension_cases = (
        _create_text_search_configuration_extension_cases(
            extension_plan
        )
    )
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = (
        _create_text_search_configuration_factor_decisions(
            factor_plan, entry
        )
    )
    return StatementRegressPlan(
        statement_key="create_text_search_configuration",
        file_prefix="CREATETEXTSEARCHCONFIGURATION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=(
            snapshot.universe_semantic_sha256
        ),
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_text_search_dictionary_factor_plan(repository_root: str):
    from .create_text_search_dictionary_factor_loop import (
        build_create_text_search_dictionary_factor_loop_plan,
    )

    return build_create_text_search_dictionary_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_text_search_dictionary_extension_plan(repository_root: str):
    from .create_text_search_dictionary_factor_extension import (
        build_create_text_search_dictionary_factor_extension_plan,
    )

    return build_create_text_search_dictionary_factor_extension_plan(
        Path(repository_root)
    )


def _create_text_search_dictionary_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE TEXT SEARCH DICTIONARY witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_text_search_dictionary_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_text_search_dictionary_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE TEXT SEARCH DICTIONARY canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_text_search_dictionary_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_text_search_dictionary_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_text_search_dictionary_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_text_search_dictionary_baseline_cases(factor_plan)
    extension_cases = _create_text_search_dictionary_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_text_search_dictionary_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_text_search_dictionary",
        file_prefix="CREATETEXTSEARCHDICTIONARY",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_text_search_parser_factor_plan(repository_root: str):
    from .create_text_search_parser_factor_loop import (
        build_create_text_search_parser_factor_loop_plan,
    )

    return build_create_text_search_parser_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_text_search_parser_extension_plan(repository_root: str):
    from .create_text_search_parser_factor_extension import (
        build_create_text_search_parser_factor_extension_plan,
    )

    return build_create_text_search_parser_factor_extension_plan(
        Path(repository_root)
    )


def _create_text_search_parser_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE TEXT SEARCH PARSER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_text_search_parser_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_text_search_parser_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE TEXT SEARCH PARSER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_text_search_parser_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_text_search_parser_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_text_search_parser_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_text_search_parser_baseline_cases(factor_plan)
    extension_cases = _create_text_search_parser_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_text_search_parser_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_text_search_parser",
        file_prefix="CREATETEXTSEARCHPARSER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_table_as_factor_plan(repository_root: str):
    from .create_table_as_factor_loop import (
        build_create_table_as_factor_loop_plan,
    )

    return build_create_table_as_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _create_table_as_extension_plan(repository_root: str):
    from .create_table_as_factor_extension import (
        build_create_table_as_factor_extension_plan,
    )

    return build_create_table_as_factor_extension_plan(
        Path(repository_root)
    )


def _create_table_as_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE TABLE AS witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_table_as_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_table_as_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE TABLE AS canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_table_as_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_table_as_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_table_as_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_table_as_baseline_cases(factor_plan)
    extension_cases = _create_table_as_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_table_as_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_table_as",
        file_prefix="CREATETABLEAS",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_text_search_template_factor_plan(repository_root: str):
    from .create_text_search_template_factor_loop import (
        build_create_text_search_template_factor_loop_plan,
    )

    return build_create_text_search_template_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_text_search_template_extension_plan(repository_root: str):
    from .create_text_search_template_factor_extension import (
        build_create_text_search_template_factor_extension_plan,
    )

    return build_create_text_search_template_factor_extension_plan(
        Path(repository_root)
    )


def _create_text_search_template_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE TEXT SEARCH TEMPLATE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_text_search_template_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_text_search_template_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE TEXT SEARCH TEMPLATE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_text_search_template_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_text_search_template_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_text_search_template_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_text_search_template_baseline_cases(factor_plan)
    extension_cases = _create_text_search_template_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_text_search_template_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_text_search_template",
        file_prefix="CREATETEXTSEARCHTEMPLATE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_transform_factor_plan(repository_root: str):
    from .create_transform_factor_loop import (
        build_create_transform_factor_loop_plan,
    )

    return build_create_transform_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_transform_extension_plan(repository_root: str):
    from .create_transform_factor_extension import (
        build_create_transform_factor_extension_plan,
    )

    return build_create_transform_factor_extension_plan(
        Path(repository_root)
    )


def _create_transform_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE TRANSFORM witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_transform_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_transform_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE TRANSFORM canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_transform_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_transform_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_transform_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_transform_baseline_cases(factor_plan)
    extension_cases = _create_transform_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_transform_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_transform",
        file_prefix="CREATETRANSFORM",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_trigger_factor_plan(repository_root: str):
    from .create_trigger_factor_loop import (
        build_create_trigger_factor_loop_plan,
    )

    return build_create_trigger_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_trigger_extension_plan(repository_root: str):
    from .create_trigger_factor_extension import (
        build_create_trigger_factor_extension_plan,
    )

    return build_create_trigger_factor_extension_plan(
        Path(repository_root)
    )


def _create_trigger_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE TRIGGER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_trigger_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_trigger_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE TRIGGER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_trigger_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_trigger_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_trigger_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_trigger_baseline_cases(factor_plan)
    extension_cases = _create_trigger_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_trigger_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_trigger",
        file_prefix="CREATETRIGGER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_type_factor_plan(repository_root: str):
    from .create_type_factor_loop import (
        build_create_type_factor_loop_plan,
    )

    return build_create_type_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_type_extension_plan(repository_root: str):
    from .create_type_factor_extension import (
        build_create_type_factor_extension_plan,
    )

    return build_create_type_factor_extension_plan(
        Path(repository_root)
    )


def _create_type_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE TYPE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_type_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_type_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE TYPE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_type_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_type_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_type_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_type_baseline_cases(factor_plan)
    extension_cases = _create_type_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_type_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_type",
        file_prefix="CREATETYPE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_user_factor_plan(repository_root: str):
    from .create_user_factor_loop import (
        build_create_user_factor_loop_plan,
    )

    return build_create_user_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_user_extension_plan(repository_root: str):
    from .create_user_factor_extension import (
        build_create_user_factor_extension_plan,
    )

    return build_create_user_factor_extension_plan(
        Path(repository_root)
    )


def _create_user_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE USER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_user_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_user_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE USER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_user_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_user_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_user_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_user_baseline_cases(factor_plan)
    extension_cases = _create_user_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_user_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_user",
        file_prefix="CREATEUSER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_user_mapping_factor_plan(repository_root: str):
    from .create_user_mapping_factor_loop import (
        build_create_user_mapping_factor_loop_plan,
    )

    return build_create_user_mapping_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_user_mapping_extension_plan(repository_root: str):
    from .create_user_mapping_factor_extension import (
        build_create_user_mapping_factor_extension_plan,
    )

    return build_create_user_mapping_factor_extension_plan(
        Path(repository_root)
    )


def _create_user_mapping_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE USER MAPPING witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_user_mapping_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_user_mapping_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE USER MAPPING canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_user_mapping_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_user_mapping_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_user_mapping_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_user_mapping_baseline_cases(factor_plan)
    extension_cases = _create_user_mapping_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_user_mapping_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_user_mapping",
        file_prefix="CREATEUSERMAPPING",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _create_view_factor_plan(repository_root: str):
    from .create_view_factor_loop import (
        build_create_view_factor_loop_plan,
    )

    return build_create_view_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _create_view_extension_plan(repository_root: str):
    from .create_view_factor_extension import (
        build_create_view_factor_extension_plan,
    )

    return build_create_view_factor_extension_plan(
        Path(repository_root)
    )


def _create_view_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent CREATE VIEW witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _create_view_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _create_view_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "CREATE VIEW canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_create_view_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _create_view_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _create_view_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _create_view_baseline_cases(factor_plan)
    extension_cases = _create_view_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _create_view_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="create_view",
        file_prefix="CREATEVIEW",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _deallocate_factor_plan(repository_root: str):
    from .deallocate_factor_loop import (
        build_deallocate_factor_loop_plan,
    )

    return build_deallocate_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _deallocate_extension_plan(repository_root: str):
    from .deallocate_factor_extension import (
        build_deallocate_factor_extension_plan,
    )

    return build_deallocate_factor_extension_plan(
        Path(repository_root)
    )


def _deallocate_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DEALLOCATE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _deallocate_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _deallocate_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DEALLOCATE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_deallocate_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _deallocate_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _deallocate_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _deallocate_baseline_cases(factor_plan)
    extension_cases = _deallocate_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _deallocate_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="deallocate",
        file_prefix="DEALLOCATE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _delete_factor_plan(repository_root: str):
    from .delete_factor_loop import (
        build_delete_factor_loop_plan,
    )

    return build_delete_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _delete_extension_plan(repository_root: str):
    from .delete_factor_extension import (
        build_delete_factor_extension_plan,
    )

    return build_delete_factor_extension_plan(
        Path(repository_root)
    )


def _delete_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DELETE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _delete_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _delete_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DELETE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_delete_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _delete_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _delete_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _delete_baseline_cases(factor_plan)
    extension_cases = _delete_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _delete_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="delete",
        file_prefix="DELETE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _discard_factor_plan(repository_root: str):
    from .discard_factor_loop import (
        build_discard_factor_loop_plan,
    )

    return build_discard_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _discard_extension_plan(repository_root: str):
    from .discard_factor_extension import (
        build_discard_factor_extension_plan,
    )

    return build_discard_factor_extension_plan(
        Path(repository_root)
    )


def _discard_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DISCARD witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _discard_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _discard_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DISCARD canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_discard_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _discard_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _discard_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _discard_baseline_cases(factor_plan)
    extension_cases = _discard_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _discard_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="discard",
        file_prefix="DISCARD",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _do_factor_plan(repository_root: str):
    from .do_factor_loop import (
        build_do_factor_loop_plan,
    )

    return build_do_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _do_extension_plan(repository_root: str):
    from .do_factor_extension import (
        build_do_factor_extension_plan,
    )

    return build_do_factor_extension_plan(
        Path(repository_root)
    )


def _do_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DO witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _do_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _do_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DO canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_do_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _do_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _do_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _do_baseline_cases(factor_plan)
    extension_cases = _do_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _do_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="do",
        file_prefix="DO",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_access_method_factor_plan(repository_root: str):
    from .drop_access_method_factor_loop import (
        build_drop_access_method_factor_loop_plan,
    )

    return build_drop_access_method_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_access_method_extension_plan(repository_root: str):
    from .drop_access_method_factor_extension import (
        build_drop_access_method_factor_extension_plan,
    )

    return build_drop_access_method_factor_extension_plan(
        Path(repository_root)
    )


def _drop_access_method_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP ACCESS METHOD witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_access_method_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_access_method_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP ACCESS METHOD canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_access_method_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_access_method_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_access_method_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_access_method_baseline_cases(factor_plan)
    extension_cases = _drop_access_method_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_access_method_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_access_method",
        file_prefix="DROPACCESSMETHOD",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_aggregate_factor_plan(repository_root: str):
    from .drop_aggregate_factor_loop import (
        build_drop_aggregate_factor_loop_plan,
    )

    return build_drop_aggregate_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_aggregate_extension_plan(repository_root: str):
    from .drop_aggregate_factor_extension import (
        build_drop_aggregate_factor_extension_plan,
    )

    return build_drop_aggregate_factor_extension_plan(
        Path(repository_root)
    )


def _drop_aggregate_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP AGGREGATE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_aggregate_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_aggregate_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP AGGREGATE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_aggregate_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_aggregate_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_aggregate_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_aggregate_baseline_cases(factor_plan)
    extension_cases = _drop_aggregate_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_aggregate_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_aggregate",
        file_prefix="DROPAGGREGATE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_cast_factor_plan(repository_root: str):
    from .drop_cast_factor_loop import (
        build_drop_cast_factor_loop_plan,
    )

    return build_drop_cast_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_cast_extension_plan(repository_root: str):
    from .drop_cast_factor_extension import (
        build_drop_cast_factor_extension_plan,
    )

    return build_drop_cast_factor_extension_plan(
        Path(repository_root)
    )


def _drop_cast_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP CAST witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_cast_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_cast_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP CAST canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_cast_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_cast_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_cast_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_cast_baseline_cases(factor_plan)
    extension_cases = _drop_cast_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_cast_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_cast",
        file_prefix="DROPCAST",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_conversion_factor_plan(repository_root: str):
    from .drop_conversion_factor_loop import (
        build_drop_conversion_factor_loop_plan,
    )

    return build_drop_conversion_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_conversion_extension_plan(repository_root: str):
    from .drop_conversion_factor_extension import (
        build_drop_conversion_factor_extension_plan,
    )

    return build_drop_conversion_factor_extension_plan(
        Path(repository_root)
    )


def _drop_conversion_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP CONVERSION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_conversion_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_conversion_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP CONVERSION canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_conversion_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_conversion_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_conversion_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_conversion_baseline_cases(factor_plan)
    extension_cases = _drop_conversion_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_conversion_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_conversion",
        file_prefix="DROPCONVERSION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_database_factor_plan(repository_root: str):
    from .drop_database_factor_loop import (
        build_drop_database_factor_loop_plan,
    )

    return build_drop_database_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_database_extension_plan(repository_root: str):
    from .drop_database_factor_extension import (
        build_drop_database_factor_extension_plan,
    )

    return build_drop_database_factor_extension_plan(
        Path(repository_root)
    )


def _drop_database_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP DATABASE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_database_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_database_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP DATABASE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_database_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_database_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_database_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_database_baseline_cases(factor_plan)
    extension_cases = _drop_database_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_database_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_database",
        file_prefix="DROPDATABASE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_domain_factor_plan(repository_root: str):
    from .drop_domain_factor_loop import (
        build_drop_domain_factor_loop_plan,
    )

    return build_drop_domain_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_domain_extension_plan(repository_root: str):
    from .drop_domain_factor_extension import (
        build_drop_domain_factor_extension_plan,
    )

    return build_drop_domain_factor_extension_plan(
        Path(repository_root)
    )


def _drop_domain_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP DOMAIN witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_domain_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_domain_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP DOMAIN canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_domain_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_domain_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_domain_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_domain_baseline_cases(factor_plan)
    extension_cases = _drop_domain_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_domain_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_domain",
        file_prefix="DROPDOMAIN",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_event_trigger_factor_plan(repository_root: str):
    from .drop_event_trigger_factor_loop import (
        build_drop_event_trigger_factor_loop_plan,
    )

    return build_drop_event_trigger_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_event_trigger_extension_plan(repository_root: str):
    from .drop_event_trigger_factor_extension import (
        build_drop_event_trigger_factor_extension_plan,
    )

    return build_drop_event_trigger_factor_extension_plan(
        Path(repository_root)
    )


def _drop_event_trigger_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP EVENT TRIGGER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_event_trigger_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_event_trigger_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP EVENT TRIGGER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_event_trigger_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_event_trigger_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_event_trigger_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_event_trigger_baseline_cases(factor_plan)
    extension_cases = _drop_event_trigger_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_event_trigger_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_event_trigger",
        file_prefix="DROPEVENTTRIGGER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_extension_factor_plan(repository_root: str):
    from .drop_extension_factor_loop import (
        build_drop_extension_factor_loop_plan,
    )

    return build_drop_extension_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_extension_extension_plan(repository_root: str):
    from .drop_extension_factor_extension import (
        build_drop_extension_factor_extension_plan,
    )

    return build_drop_extension_factor_extension_plan(
        Path(repository_root)
    )


def _drop_extension_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP EXTENSION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_extension_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_extension_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP EXTENSION canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_extension_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_extension_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_extension_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_extension_baseline_cases(factor_plan)
    extension_cases = _drop_extension_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_extension_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_extension",
        file_prefix="DROPEXTENSION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_foreign_data_wrapper_factor_plan(repository_root: str):
    from .drop_foreign_data_wrapper_factor_loop import (
        build_drop_foreign_data_wrapper_factor_loop_plan,
    )

    return build_drop_foreign_data_wrapper_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_foreign_data_wrapper_extension_plan(repository_root: str):
    from .drop_foreign_data_wrapper_factor_extension import (
        build_drop_foreign_data_wrapper_factor_extension_plan,
    )

    return build_drop_foreign_data_wrapper_factor_extension_plan(
        Path(repository_root)
    )


def _drop_foreign_data_wrapper_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP FOREIGN DATA WRAPPER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_foreign_data_wrapper_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_foreign_data_wrapper_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP FOREIGN DATA WRAPPER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_foreign_data_wrapper_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_foreign_data_wrapper_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_foreign_data_wrapper_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_foreign_data_wrapper_baseline_cases(factor_plan)
    extension_cases = _drop_foreign_data_wrapper_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_foreign_data_wrapper_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_foreign_data_wrapper",
        file_prefix="DROPFOREIGNDATAWRAPPER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_foreign_table_factor_plan(repository_root: str):
    from .drop_foreign_table_factor_loop import (
        build_drop_foreign_table_factor_loop_plan,
    )

    return build_drop_foreign_table_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_foreign_table_extension_plan(repository_root: str):
    from .drop_foreign_table_factor_extension import (
        build_drop_foreign_table_factor_extension_plan,
    )

    return build_drop_foreign_table_factor_extension_plan(
        Path(repository_root)
    )


def _drop_foreign_table_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP FOREIGN TABLE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_foreign_table_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_foreign_table_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP FOREIGN TABLE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_foreign_table_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_foreign_table_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_foreign_table_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_foreign_table_baseline_cases(factor_plan)
    extension_cases = _drop_foreign_table_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_foreign_table_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_foreign_table",
        file_prefix="DROPFOREIGNTABLE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_function_factor_plan(repository_root: str):
    from .drop_function_factor_loop import (
        build_drop_function_factor_loop_plan,
    )

    return build_drop_function_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_function_extension_plan(repository_root: str):
    from .drop_function_factor_extension import (
        build_drop_function_factor_extension_plan,
    )

    return build_drop_function_factor_extension_plan(
        Path(repository_root)
    )


def _drop_function_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP FUNCTION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_function_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_function_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP FUNCTION canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_function_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_function_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_function_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_function_baseline_cases(factor_plan)
    extension_cases = _drop_function_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_function_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_function",
        file_prefix="DROPFUNCTION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_group_factor_plan(repository_root: str):
    from .drop_group_factor_loop import (
        build_drop_group_factor_loop_plan,
    )

    return build_drop_group_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_group_extension_plan(repository_root: str):
    from .drop_group_factor_extension import (
        build_drop_group_factor_extension_plan,
    )

    return build_drop_group_factor_extension_plan(
        Path(repository_root)
    )


def _drop_group_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP GROUP witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_group_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_group_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP GROUP canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_group_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_group_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_group_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_group_baseline_cases(factor_plan)
    extension_cases = _drop_group_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_group_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_group",
        file_prefix="DROPGROUP",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_index_factor_plan(repository_root: str):
    from .drop_index_factor_loop import (
        build_drop_index_factor_loop_plan,
    )

    return build_drop_index_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_index_extension_plan(repository_root: str):
    from .drop_index_factor_extension import (
        build_drop_index_factor_extension_plan,
    )

    return build_drop_index_factor_extension_plan(
        Path(repository_root)
    )


def _drop_index_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP INDEX witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_index_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_index_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP INDEX canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_index_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_index_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_index_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_index_baseline_cases(factor_plan)
    extension_cases = _drop_index_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_index_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_index",
        file_prefix="DROPINDEX",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_language_factor_plan(repository_root: str):
    from .drop_language_factor_loop import (
        build_drop_language_factor_loop_plan,
    )

    return build_drop_language_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_language_extension_plan(repository_root: str):
    from .drop_language_factor_extension import (
        build_drop_language_factor_extension_plan,
    )

    return build_drop_language_factor_extension_plan(
        Path(repository_root)
    )


def _drop_language_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP LANGUAGE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_language_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_language_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP LANGUAGE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_language_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_language_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_language_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_language_baseline_cases(factor_plan)
    extension_cases = _drop_language_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_language_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_language",
        file_prefix="DROPLANGUAGE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_materialized_view_factor_plan(repository_root: str):
    from .drop_materialized_view_factor_loop import (
        build_drop_materialized_view_factor_loop_plan,
    )

    return build_drop_materialized_view_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_materialized_view_extension_plan(repository_root: str):
    from .drop_materialized_view_factor_extension import (
        build_drop_materialized_view_factor_extension_plan,
    )

    return build_drop_materialized_view_factor_extension_plan(
        Path(repository_root)
    )


def _drop_materialized_view_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP MATERIALIZED VIEW witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_materialized_view_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_materialized_view_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP MATERIALIZED VIEW canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_materialized_view_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_materialized_view_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_materialized_view_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_materialized_view_baseline_cases(factor_plan)
    extension_cases = _drop_materialized_view_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_materialized_view_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_materialized_view",
        file_prefix="DROPMATERIALIZEDVIEW",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_operator_factor_plan(repository_root: str):
    from .drop_operator_factor_loop import (
        build_drop_operator_factor_loop_plan,
    )

    return build_drop_operator_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_operator_extension_plan(repository_root: str):
    from .drop_operator_factor_extension import (
        build_drop_operator_factor_extension_plan,
    )

    return build_drop_operator_factor_extension_plan(
        Path(repository_root)
    )


def _drop_operator_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP OPERATOR witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_operator_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_operator_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP OPERATOR canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_operator_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_operator_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_operator_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_operator_baseline_cases(factor_plan)
    extension_cases = _drop_operator_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_operator_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_operator",
        file_prefix="DROPOPERATOR",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_operator_class_factor_plan(repository_root: str):
    from .drop_operator_class_factor_loop import (
        build_drop_operator_class_factor_loop_plan,
    )

    return build_drop_operator_class_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_operator_class_extension_plan(repository_root: str):
    from .drop_operator_class_factor_extension import (
        build_drop_operator_class_factor_extension_plan,
    )

    return build_drop_operator_class_factor_extension_plan(
        Path(repository_root)
    )


def _drop_operator_class_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP OPERATOR CLASS witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_operator_class_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_operator_class_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP OPERATOR CLASS canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_operator_class_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_operator_class_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_operator_class_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_operator_class_baseline_cases(factor_plan)
    extension_cases = _drop_operator_class_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_operator_class_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_operator_class",
        file_prefix="DROPOPERATORCLASS",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_operator_family_factor_plan(repository_root: str):
    from .drop_operator_family_factor_loop import (
        build_drop_operator_family_factor_loop_plan,
    )

    return build_drop_operator_family_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_operator_family_extension_plan(repository_root: str):
    from .drop_operator_family_factor_extension import (
        build_drop_operator_family_factor_extension_plan,
    )

    return build_drop_operator_family_factor_extension_plan(
        Path(repository_root)
    )


def _drop_operator_family_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP OPERATOR FAMILY witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_operator_family_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_operator_family_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP OPERATOR FAMILY canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_operator_family_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_operator_family_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_operator_family_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_operator_family_baseline_cases(factor_plan)
    extension_cases = _drop_operator_family_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_operator_family_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_operator_family",
        file_prefix="DROPOPERATORFAMILY",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_owned_factor_plan(repository_root: str):
    from .drop_owned_factor_loop import (
        build_drop_owned_factor_loop_plan,
    )

    return build_drop_owned_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_owned_extension_plan(repository_root: str):
    from .drop_owned_factor_extension import (
        build_drop_owned_factor_extension_plan,
    )

    return build_drop_owned_factor_extension_plan(
        Path(repository_root)
    )


def _drop_owned_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP OWNED witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_owned_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_owned_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP OWNED canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_owned_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_owned_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_owned_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_owned_baseline_cases(factor_plan)
    extension_cases = _drop_owned_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_owned_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_owned",
        file_prefix="DROPOWNED",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_policy_factor_plan(repository_root: str):
    from .drop_policy_factor_loop import (
        build_drop_policy_factor_loop_plan,
    )

    return build_drop_policy_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_policy_extension_plan(repository_root: str):
    from .drop_policy_factor_extension import (
        build_drop_policy_factor_extension_plan,
    )

    return build_drop_policy_factor_extension_plan(
        Path(repository_root)
    )


def _drop_policy_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP POLICY witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_policy_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_policy_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP POLICY canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_policy_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_policy_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_policy_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_policy_baseline_cases(factor_plan)
    extension_cases = _drop_policy_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_policy_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_policy",
        file_prefix="DROPPOLICY",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_procedure_factor_plan(repository_root: str):
    from .drop_procedure_factor_loop import (
        build_drop_procedure_factor_loop_plan,
    )

    return build_drop_procedure_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_procedure_extension_plan(repository_root: str):
    from .drop_procedure_factor_extension import (
        build_drop_procedure_factor_extension_plan,
    )

    return build_drop_procedure_factor_extension_plan(
        Path(repository_root)
    )


def _drop_procedure_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP PROCEDURE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_procedure_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_procedure_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP PROCEDURE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_procedure_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_procedure_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_procedure_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_procedure_baseline_cases(factor_plan)
    extension_cases = _drop_procedure_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_procedure_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_procedure",
        file_prefix="DROPPROCEDURE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_publication_factor_plan(repository_root: str):
    from .drop_publication_factor_loop import (
        build_drop_publication_factor_loop_plan,
    )

    return build_drop_publication_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_publication_extension_plan(repository_root: str):
    from .drop_publication_factor_extension import (
        build_drop_publication_factor_extension_plan,
    )

    return build_drop_publication_factor_extension_plan(
        Path(repository_root)
    )


def _drop_publication_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP PUBLICATION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_publication_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_publication_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP PUBLICATION canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_publication_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_publication_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_publication_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_publication_baseline_cases(factor_plan)
    extension_cases = _drop_publication_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_publication_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_publication",
        file_prefix="DROPPUBLICATION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_role_factor_plan(repository_root: str):
    from .drop_role_factor_loop import (
        build_drop_role_factor_loop_plan,
    )

    return build_drop_role_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_role_extension_plan(repository_root: str):
    from .drop_role_factor_extension import (
        build_drop_role_factor_extension_plan,
    )

    return build_drop_role_factor_extension_plan(
        Path(repository_root)
    )


def _drop_role_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP ROLE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_role_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_role_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP ROLE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_role_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_role_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_role_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_role_baseline_cases(factor_plan)
    extension_cases = _drop_role_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_role_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_role",
        file_prefix="DROPROLE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_routine_factor_plan(repository_root: str):
    from .drop_routine_factor_loop import (
        build_drop_routine_factor_loop_plan,
    )

    return build_drop_routine_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_routine_extension_plan(repository_root: str):
    from .drop_routine_factor_extension import (
        build_drop_routine_factor_extension_plan,
    )

    return build_drop_routine_factor_extension_plan(
        Path(repository_root)
    )


def _drop_routine_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP ROUTINE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_routine_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_routine_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP ROUTINE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_routine_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_routine_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_routine_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_routine_baseline_cases(factor_plan)
    extension_cases = _drop_routine_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_routine_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_routine",
        file_prefix="DROPROUTINE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_rule_factor_plan(repository_root: str):
    from .drop_rule_factor_loop import (
        build_drop_rule_factor_loop_plan,
    )

    return build_drop_rule_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_rule_extension_plan(repository_root: str):
    from .drop_rule_factor_extension import (
        build_drop_rule_factor_extension_plan,
    )

    return build_drop_rule_factor_extension_plan(
        Path(repository_root)
    )


def _drop_rule_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP RULE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_rule_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_rule_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP RULE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_rule_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_rule_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_rule_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_rule_baseline_cases(factor_plan)
    extension_cases = _drop_rule_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_rule_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_rule",
        file_prefix="DROPRULE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_schema_factor_plan(repository_root: str):
    from .drop_schema_factor_loop import (
        build_drop_schema_factor_loop_plan,
    )

    return build_drop_schema_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_schema_extension_plan(repository_root: str):
    from .drop_schema_factor_extension import (
        build_drop_schema_factor_extension_plan,
    )

    return build_drop_schema_factor_extension_plan(
        Path(repository_root)
    )


def _drop_schema_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP SCHEMA witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_schema_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_schema_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP SCHEMA canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_schema_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_schema_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_schema_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_schema_baseline_cases(factor_plan)
    extension_cases = _drop_schema_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_schema_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_schema",
        file_prefix="DROPSCHEMA",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_sequence_factor_plan(repository_root: str):
    from .drop_sequence_factor_loop import (
        build_drop_sequence_factor_loop_plan,
    )

    return build_drop_sequence_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_sequence_extension_plan(repository_root: str):
    from .drop_sequence_factor_extension import (
        build_drop_sequence_factor_extension_plan,
    )

    return build_drop_sequence_factor_extension_plan(
        Path(repository_root)
    )


def _drop_sequence_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP SEQUENCE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_sequence_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_sequence_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP SEQUENCE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_sequence_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_sequence_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_sequence_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_sequence_baseline_cases(factor_plan)
    extension_cases = _drop_sequence_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_sequence_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_sequence",
        file_prefix="DROPSEQUENCE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_server_factor_plan(repository_root: str):
    from .drop_server_factor_loop import (
        build_drop_server_factor_loop_plan,
    )

    return build_drop_server_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_server_extension_plan(repository_root: str):
    from .drop_server_factor_extension import (
        build_drop_server_factor_extension_plan,
    )

    return build_drop_server_factor_extension_plan(
        Path(repository_root)
    )


def _drop_server_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP SERVER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_server_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_server_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP SERVER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_server_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_server_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_server_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_server_baseline_cases(factor_plan)
    extension_cases = _drop_server_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_server_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_server",
        file_prefix="DROPSERVER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_statistics_factor_plan(repository_root: str):
    from .drop_statistics_factor_loop import (
        build_drop_statistics_factor_loop_plan,
    )

    return build_drop_statistics_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_statistics_extension_plan(repository_root: str):
    from .drop_statistics_factor_extension import (
        build_drop_statistics_factor_extension_plan,
    )

    return build_drop_statistics_factor_extension_plan(
        Path(repository_root)
    )


def _drop_statistics_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP STATISTICS witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_statistics_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_statistics_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP STATISTICS canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_statistics_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_statistics_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_statistics_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_statistics_baseline_cases(factor_plan)
    extension_cases = _drop_statistics_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_statistics_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_statistics",
        file_prefix="DROPSTATISTICS",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_subscription_factor_plan(repository_root: str):
    from .drop_subscription_factor_loop import (
        build_drop_subscription_factor_loop_plan,
    )

    return build_drop_subscription_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_subscription_extension_plan(repository_root: str):
    from .drop_subscription_factor_extension import (
        build_drop_subscription_factor_extension_plan,
    )

    return build_drop_subscription_factor_extension_plan(
        Path(repository_root)
    )


def _drop_subscription_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP SUBSCRIPTION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_subscription_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_subscription_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP SUBSCRIPTION canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_subscription_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_subscription_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_subscription_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_subscription_baseline_cases(factor_plan)
    extension_cases = _drop_subscription_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_subscription_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_subscription",
        file_prefix="DROPSUBSCRIPTION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_table_factor_plan(repository_root: str):
    from .drop_table_factor_loop import (
        build_drop_table_factor_loop_plan,
    )

    return build_drop_table_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_table_extension_plan(repository_root: str):
    from .drop_table_factor_extension import (
        build_drop_table_factor_extension_plan,
    )

    return build_drop_table_factor_extension_plan(
        Path(repository_root)
    )


def _drop_table_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP TABLE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_table_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_table_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP TABLE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_table_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_table_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_table_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_table_baseline_cases(factor_plan)
    extension_cases = _drop_table_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_table_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_table",
        file_prefix="DROPTABLE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_tablespace_factor_plan(repository_root: str):
    from .drop_tablespace_factor_loop import (
        build_drop_tablespace_factor_loop_plan,
    )

    return build_drop_tablespace_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_tablespace_extension_plan(repository_root: str):
    from .drop_tablespace_factor_extension import (
        build_drop_tablespace_factor_extension_plan,
    )

    return build_drop_tablespace_factor_extension_plan(
        Path(repository_root)
    )


def _drop_tablespace_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP TABLESPACE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_tablespace_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_tablespace_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP TABLESPACE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_tablespace_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_tablespace_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_tablespace_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_tablespace_baseline_cases(factor_plan)
    extension_cases = _drop_tablespace_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_tablespace_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_tablespace",
        file_prefix="DROPTABLESPACE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_text_search_configuration_factor_plan(repository_root: str):
    from .drop_text_search_configuration_factor_loop import (
        build_drop_text_search_configuration_factor_loop_plan,
    )

    return build_drop_text_search_configuration_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_text_search_configuration_extension_plan(repository_root: str):
    from .drop_text_search_configuration_factor_extension import (
        build_drop_text_search_configuration_factor_extension_plan,
    )

    return build_drop_text_search_configuration_factor_extension_plan(
        Path(repository_root)
    )


def _drop_text_search_configuration_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP TEXT SEARCH CONFIGURATION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_text_search_configuration_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_text_search_configuration_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP TEXT SEARCH CONFIGURATION canonical factor mapping "
                    f"is not unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_text_search_configuration_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_text_search_configuration_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_text_search_configuration_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_text_search_configuration_baseline_cases(factor_plan)
    extension_cases = _drop_text_search_configuration_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_text_search_configuration_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_text_search_configuration",
        file_prefix="DROPTEXTSEARCHCONFIGURATION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_text_search_dictionary_factor_plan(repository_root: str):
    from .drop_text_search_dictionary_factor_loop import (
        build_drop_text_search_dictionary_factor_loop_plan,
    )

    return build_drop_text_search_dictionary_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_text_search_dictionary_extension_plan(repository_root: str):
    from .drop_text_search_dictionary_factor_extension import (
        build_drop_text_search_dictionary_factor_extension_plan,
    )

    return build_drop_text_search_dictionary_factor_extension_plan(
        Path(repository_root)
    )


def _drop_text_search_dictionary_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP TEXT SEARCH DICTIONARY witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_text_search_dictionary_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_text_search_dictionary_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP TEXT SEARCH DICTIONARY canonical factor mapping is "
                    f"not unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_text_search_dictionary_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_text_search_dictionary_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_text_search_dictionary_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_text_search_dictionary_baseline_cases(factor_plan)
    extension_cases = _drop_text_search_dictionary_extension_cases(
        extension_plan
    )
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_text_search_dictionary_factor_decisions(
        factor_plan, entry
    )
    return StatementRegressPlan(
        statement_key="drop_text_search_dictionary",
        file_prefix="DROPTEXTSEARCHDICTIONARY",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_text_search_parser_factor_plan(repository_root: str):
    from .drop_text_search_parser_factor_loop import (
        build_drop_text_search_parser_factor_loop_plan,
    )

    return build_drop_text_search_parser_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_text_search_parser_extension_plan(repository_root: str):
    from .drop_text_search_parser_factor_extension import (
        build_drop_text_search_parser_factor_extension_plan,
    )

    return build_drop_text_search_parser_factor_extension_plan(
        Path(repository_root)
    )


def _drop_text_search_parser_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP TEXT SEARCH PARSER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_text_search_parser_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_text_search_parser_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP TEXT SEARCH PARSER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_text_search_parser_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_text_search_parser_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_text_search_parser_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_text_search_parser_baseline_cases(factor_plan)
    extension_cases = _drop_text_search_parser_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_text_search_parser_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_text_search_parser",
        file_prefix="DROPTEXTSEARCHPARSER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_text_search_template_factor_plan(repository_root: str):
    from .drop_text_search_template_factor_loop import (
        build_drop_text_search_template_factor_loop_plan,
    )

    return build_drop_text_search_template_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_text_search_template_extension_plan(repository_root: str):
    from .drop_text_search_template_factor_extension import (
        build_drop_text_search_template_factor_extension_plan,
    )

    return build_drop_text_search_template_factor_extension_plan(
        Path(repository_root)
    )


def _drop_text_search_template_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP TEXT SEARCH TEMPLATE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_text_search_template_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_text_search_template_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP TEXT SEARCH TEMPLATE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_text_search_template_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_text_search_template_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_text_search_template_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_text_search_template_baseline_cases(factor_plan)
    extension_cases = _drop_text_search_template_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_text_search_template_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_text_search_template",
        file_prefix="DROPTEXTSEARCHTEMPLATE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_transform_factor_plan(repository_root: str):
    from .drop_transform_factor_loop import (
        build_drop_transform_factor_loop_plan,
    )

    return build_drop_transform_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_transform_extension_plan(repository_root: str):
    from .drop_transform_factor_extension import (
        build_drop_transform_factor_extension_plan,
    )

    return build_drop_transform_factor_extension_plan(
        Path(repository_root)
    )


def _drop_transform_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP TRANSFORM witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_transform_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_transform_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP TRANSFORM canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_transform_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_transform_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_transform_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_transform_baseline_cases(factor_plan)
    extension_cases = _drop_transform_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_transform_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_transform",
        file_prefix="DROPTRANSFORM",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_trigger_factor_plan(repository_root: str):
    from .drop_trigger_factor_loop import (
        build_drop_trigger_factor_loop_plan,
    )

    return build_drop_trigger_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_trigger_extension_plan(repository_root: str):
    from .drop_trigger_factor_extension import (
        build_drop_trigger_factor_extension_plan,
    )

    return build_drop_trigger_factor_extension_plan(
        Path(repository_root)
    )


def _drop_trigger_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP TRIGGER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_trigger_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_trigger_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP TRIGGER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_trigger_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_trigger_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_trigger_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_trigger_baseline_cases(factor_plan)
    extension_cases = _drop_trigger_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_trigger_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_trigger",
        file_prefix="DROPTRIGGER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_type_factor_plan(repository_root: str):
    from .drop_type_factor_loop import (
        build_drop_type_factor_loop_plan,
    )

    return build_drop_type_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_type_extension_plan(repository_root: str):
    from .drop_type_factor_extension import (
        build_drop_type_factor_extension_plan,
    )

    return build_drop_type_factor_extension_plan(
        Path(repository_root)
    )


def _drop_type_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP TYPE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_type_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_type_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP TYPE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_type_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_type_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_type_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_type_baseline_cases(factor_plan)
    extension_cases = _drop_type_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_type_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_type",
        file_prefix="DROPTYPE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _drop_user_factor_plan(repository_root: str):
    from .drop_user_factor_loop import (
        build_drop_user_factor_loop_plan,
    )

    return build_drop_user_factor_loop_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _drop_user_extension_plan(repository_root: str):
    from .drop_user_factor_extension import (
        build_drop_user_factor_extension_plan,
    )

    return build_drop_user_factor_extension_plan(
        Path(repository_root)
    )


def _drop_user_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP USER witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_user_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_user_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP USER canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_user_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_user_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_user_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_user_baseline_cases(factor_plan)
    extension_cases = _drop_user_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_user_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_user",
        file_prefix="DROPUSER",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )

@lru_cache(maxsize=4)
def _alter_publication_factor_plan(repository_root: str):
    from .alter_publication_factor_loop import (
        build_alter_publication_factor_loop_plan,
    )

    return build_alter_publication_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_sequence_factor_plan(repository_root: str):
    from .alter_sequence_factor_loop import (
        build_alter_sequence_factor_loop_plan,
    )

    return build_alter_sequence_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_publication_extension_plan(repository_root: str):
    from .alter_publication_factor_extension import (
        build_alter_publication_factor_extension_plan,
    )

    return build_alter_publication_factor_extension_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _alter_sequence_extension_plan(repository_root: str):
    from .alter_sequence_factor_extension import (
        build_alter_sequence_factor_extension_plan,
    )

    return build_alter_sequence_factor_extension_plan(
        Path(repository_root)
    )


@lru_cache(maxsize=4)
def _alter_table_factor_plan(repository_root: str):
    from .alter_table_factor_loop import (
        build_alter_table_factor_loop_plan,
    )

    return build_alter_table_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_table_extension_plan(repository_root: str):
    from .alter_table_factor_extension import (
        build_alter_table_factor_extension_plan,
    )

    return build_alter_table_factor_extension_plan(
        Path(repository_root)
    )


def _alter_publication_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER PUBLICATION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_sequence_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER SEQUENCE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_publication_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_sequence_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_table_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER TABLE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_table_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_publication_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER PUBLICATION canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _alter_sequence_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER SEQUENCE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _alter_table_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER TABLE canonical factor mapping is not "
                    f"unique: {factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_publication_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_publication_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_publication_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_publication_baseline_cases(factor_plan)
    extension_cases = _alter_publication_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_publication_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_publication",
        file_prefix="ALTERPUBLICATION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


def _build_alter_sequence_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_sequence_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_sequence_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_sequence_baseline_cases(factor_plan)
    extension_cases = _alter_sequence_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_sequence_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_sequence",
        file_prefix="ALTERSEQUENCE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


def _build_alter_table_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_table_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_table_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_table_baseline_cases(factor_plan)
    extension_cases = _alter_table_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_table_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_table",
        file_prefix="ALTERTABLE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )



@lru_cache(maxsize=4)
def _alter_operator_factor_plan(repository_root: str):
    from .alter_operator_factor_loop import (
        build_alter_operator_factor_loop_plan,
    )

    return build_alter_operator_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_operator_extension_plan(repository_root: str):
    from .alter_operator_factor_extension import (
        build_alter_operator_factor_extension_plan,
    )

    return build_alter_operator_factor_extension_plan(Path(repository_root))


def _alter_operator_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER OPERATOR witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_operator_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_operator_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER OPERATOR canonical factor mapping is not unique: "
                    f"{factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_operator_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_operator_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_operator_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_operator_baseline_cases(factor_plan)
    extension_cases = _alter_operator_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_operator_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_operator",
        file_prefix="ALTEROPERATOR",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )



@lru_cache(maxsize=4)
def _alter_operator_class_factor_plan(repository_root: str):
    from .alter_operator_class_factor_loop import (
        build_alter_operator_class_factor_loop_plan,
    )

    return build_alter_operator_class_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_operator_class_extension_plan(repository_root: str):
    from .alter_operator_class_factor_extension import (
        build_alter_operator_class_factor_extension_plan,
    )

    return build_alter_operator_class_factor_extension_plan(Path(repository_root))


def _alter_operator_class_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER OPERATOR CLASS witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_operator_class_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_operator_class_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER OPERATOR CLASS canonical factor mapping is not unique: "
                    f"{factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_operator_class_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_operator_class_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_operator_class_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_operator_class_baseline_cases(factor_plan)
    extension_cases = _alter_operator_class_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_operator_class_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_operator_class",
        file_prefix="ALTEROPERATORCLASS",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


@lru_cache(maxsize=4)
def _alter_operator_family_factor_plan(repository_root: str):
    from .alter_operator_family_factor_loop import (
        build_alter_operator_family_factor_loop_plan,
    )

    return build_alter_operator_family_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_operator_family_extension_plan(repository_root: str):
    from .alter_operator_family_factor_extension import (
        build_alter_operator_family_factor_extension_plan,
    )

    return build_alter_operator_family_factor_extension_plan(Path(repository_root))


def _alter_operator_family_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER OPERATOR FAMILY witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_operator_family_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_operator_family_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER OPERATOR FAMILY canonical factor mapping is not unique: "
                    f"{factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_operator_family_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_operator_family_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_operator_family_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_operator_family_baseline_cases(factor_plan)
    extension_cases = _alter_operator_family_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_operator_family_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_operator_family",
        file_prefix="ALTEROPERATORFAMILY",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


def _alter_operator_family_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_operator_family_factor_validate import (
        validate_alter_operator_family_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_operator_family_factor_plan(str(root))
    extension_plan = _alter_operator_family_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_operator_family_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_operator_family_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_operator_family",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_operator_family_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report



@lru_cache(maxsize=4)
def _alter_group_factor_plan(repository_root: str):
    from .alter_group_factor_loop import build_alter_group_factor_loop_plan

    return build_alter_group_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_group_extension_plan(repository_root: str):
    from .alter_group_factor_extension import (
        build_alter_group_factor_extension_plan,
    )

    return build_alter_group_factor_extension_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_role_factor_plan(repository_root: str):
    from .alter_role_factor_loop import build_alter_role_factor_loop_plan

    return build_alter_role_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_role_extension_plan(repository_root: str):
    from .alter_role_factor_extension import (
        build_alter_role_factor_extension_plan,
    )

    return build_alter_role_factor_extension_plan(Path(repository_root))


def _alter_group_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="serial_sql",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER GROUP witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_group_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="serial_sql",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_group_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER GROUP canonical factor mapping is not unique: "
                    f"{factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_group_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_group_factor_plan(str(snapshot.repository_root.resolve()))
    extension_plan = _alter_group_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_group_baseline_cases(factor_plan)
    extension_cases = _alter_group_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_group_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_group",
        file_prefix="ALTERGROUP",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


def _alter_role_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="serial_sql",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER ROLE witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_role_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="serial_sql",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_role_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER ROLE canonical factor mapping is not unique: "
                    f"{factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_role_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_role_factor_plan(str(snapshot.repository_root.resolve()))
    extension_plan = _alter_role_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_role_baseline_cases(factor_plan)
    extension_cases = _alter_role_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_role_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_role",
        file_prefix="ALTERROLE",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


@lru_cache(maxsize=4)
def _alter_index_factor_plan(repository_root: str):
    from .alter_index_factor_loop import (
        build_alter_index_factor_loop_plan,
    )

    return build_alter_index_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _alter_index_extension_plan(repository_root: str):
    from .alter_index_factor_extension import (
        build_alter_index_factor_extension_plan,
    )

    return build_alter_index_factor_extension_plan(Path(repository_root))


def _alter_index_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent ALTER INDEX witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _alter_index_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="same_session_multiphase",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _alter_index_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "ALTER INDEX canonical factor mapping is not unique: "
                    f"{factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_alter_index_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _alter_index_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _alter_index_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _alter_index_baseline_cases(factor_plan)
    extension_cases = _alter_index_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _alter_index_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="alter_index",
        file_prefix="ALTERINDEX",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


@lru_cache(maxsize=4)
def _drop_collation_factor_plan(repository_root: str):
    from .drop_collation_factor_loop import (
        build_drop_collation_factor_loop_plan,
    )

    return build_drop_collation_factor_loop_plan(Path(repository_root))


@lru_cache(maxsize=4)
def _drop_collation_extension_plan(repository_root: str):
    from .drop_collation_factor_extension import (
        build_drop_collation_factor_extension_plan,
    )

    return build_drop_collation_factor_extension_plan(Path(repository_root))


def _drop_collation_baseline_cases(
    factor_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group=f"factor_value_loop_{case.kind.lower()}",
            case_type="factor_value_independent_witness",
            outcome=case.outcome,
            execution_profile="serial_sql",
            derived_axes={
                "primary_obligation_id": case.primary_obligation_id,
                "kind": case.kind,
                "factor_key": case.factor_key,
                "factor_value": case.factor_value,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
            },
            factor_values=(f"{case.factor_key}={case.factor_value}",),
            combination_strategy="one_primary_factor_value_with_legal_baselines",
            description=(
                "Independent DROP COLLATION witness for "
                f"{case.factor_key}={case.factor_value}"
            ),
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in factor_plan.cases
    )


def _drop_collation_extension_cases(
    extension_plan: object,
) -> tuple[StatementRegressCase, ...]:
    return tuple(
        StatementRegressCase(
            ordinal=case.ordinal,
            case_id=case.case_id,
            sql_filename=case.sql_filename,
            object_prefix=case.object_prefix,
            case_group="factor_extension_cross",
            case_type="bounded_post_coverage_extension",
            outcome=case.outcome,
            execution_profile="serial_sql",
            derived_axes={
                "derivation_id": case.derivation_id,
                "combination_group": case.derived_from_combination_group,
                "consumer_action_id": case.consumer_action_id,
                "expected_sqlstate": case.expected_sqlstate,
                "derivation_reason": case.derivation_reason,
            },
            factor_values=tuple(
                f"{key}={value}" for key, value in case.factor_assignment
            ),
            combination_strategy="bounded_post_coverage_extension",
            description=case.derivation_reason,
            expected_anchor=(
                f"SQLSTATE {case.expected_sqlstate}; actual-byte semantic locus"
            ),
        )
        for case in extension_plan.cases
    )


def _drop_collation_factor_decisions(
    factor_plan: object,
    entry: StatementCycleEntry,
) -> list[FactorValueDecision]:
    standard_case_by_id = {case.case_id: case for case in factor_plan.cases}
    factor_case_by_obligation = {
        case.primary_obligation_id: case for case in factor_plan.cases
    }
    decisions: list[FactorValueDecision] = []
    for factor in entry.factors:
        for value, row_id in zip(factor.values, factor.row_ids):
            matches = [
                obligation
                for obligation in factor_plan.obligations
                if obligation.kind == "SFV"
                and obligation.factor_key == factor.name
                and obligation.value == value
                and f"|{row_id}|" in obligation.obligation_id
            ]
            if len(matches) != 1:
                raise RemainingStatementRegressError(
                    "DROP COLLATION canonical factor mapping is not unique: "
                    f"{factor.name}={value}"
                )
            obligation = matches[0]
            factor_case = factor_case_by_obligation[obligation.obligation_id]
            standard_case = standard_case_by_id[factor_case.case_id]
            decisions.append(
                FactorValueDecision(
                    row_id=row_id,
                    factor=factor.name,
                    value=value,
                    disposition=obligation.disposition,
                    reason=(
                        factor_case.expected_failure_reason
                        if obligation.disposition == "expected_failure"
                        else None
                    ),
                    case_ids=(standard_case.case_id,),
                )
            )
    return decisions


def _build_drop_collation_plan_lazily(
    snapshot: StatementFactorCycleSnapshot,
    entry: StatementCycleEntry,
) -> StatementRegressPlan:
    factor_plan = _drop_collation_factor_plan(
        str(snapshot.repository_root.resolve())
    )
    extension_plan = _drop_collation_extension_plan(
        str(snapshot.repository_root.resolve())
    )
    baseline_cases = _drop_collation_baseline_cases(factor_plan)
    extension_cases = _drop_collation_extension_cases(extension_plan)
    cases = tuple(
        sorted(
            (*baseline_cases, *extension_cases),
            key=lambda case: case.ordinal,
        )
    )
    decisions = _drop_collation_factor_decisions(factor_plan, entry)
    return StatementRegressPlan(
        statement_key="drop_collation",
        file_prefix="DROPCOLLATION",
        cycle_fingerprint=snapshot.fingerprint,
        matrix_path=entry.matrix_path.as_posix(),
        matrix_sha256=entry.matrix_sha256,
        reference_path=entry.reference_path.as_posix(),
        reference_sha256=entry.reference_sha256,
        universe_semantic_sha256=snapshot.universe_semantic_sha256,
        official_source=entry.official_source,
        cases=cases,
        factor_decisions=tuple(decisions),
    )


_PLAN_BUILDERS: Mapping[
    str,
    Callable[[StatementFactorCycleSnapshot, StatementCycleEntry], StatementRegressPlan],
] = {
    "abort": _build_abort_plan,
    "alter_aggregate": _build_alter_aggregate_plan_lazily,
    "alter_collation": _build_alter_collation_plan_lazily,
    "alter_conversion": _build_alter_conversion_plan_lazily,
    "alter_database": _build_alter_database_plan_lazily,
    "alter_default_privileges": _build_alter_default_privileges_plan_lazily,
    "alter_domain": _build_alter_domain_plan_lazily,
    "alter_event_trigger": _build_alter_event_trigger_plan_lazily,
    "alter_extension": _build_alter_extension_plan_lazily,
    "alter_foreign_data_wrapper": _build_alter_foreign_data_wrapper_plan_lazily,
    "alter_foreign_table": _build_alter_foreign_table_plan_lazily,
    "alter_function": _build_alter_function_plan_lazily,
    "alter_language": _build_alter_language_plan_lazily,
    "alter_large_object": _build_alter_large_object_plan_lazily,
    "alter_materialized_view": _build_alter_materialized_view_plan_lazily,
    "alter_operator": _build_alter_operator_plan_lazily,
    "alter_operator_class": _build_alter_operator_class_plan_lazily,
    "alter_operator_family": _build_alter_operator_family_plan_lazily,
    "alter_policy": _build_alter_policy_plan_lazily,
    "alter_procedure": _build_alter_procedure_plan_lazily,
    "alter_routine": _build_alter_routine_plan_lazily,
    "alter_rule": _build_alter_rule_plan_lazily,
    "alter_schema": _build_alter_schema_plan_lazily,
    "drop_user": _build_drop_user_plan_lazily,
    "drop_type": _build_drop_type_plan_lazily,
    "drop_trigger": _build_drop_trigger_plan_lazily,
    "drop_transform": _build_drop_transform_plan_lazily,
    "drop_text_search_template": _build_drop_text_search_template_plan_lazily,
    "drop_text_search_parser": _build_drop_text_search_parser_plan_lazily,
    "drop_text_search_dictionary": _build_drop_text_search_dictionary_plan_lazily,
    "drop_text_search_configuration": _build_drop_text_search_configuration_plan_lazily,
    "drop_tablespace": _build_drop_tablespace_plan_lazily,
    "drop_table": _build_drop_table_plan_lazily,
    "drop_subscription": _build_drop_subscription_plan_lazily,
    "drop_statistics": _build_drop_statistics_plan_lazily,
    "drop_server": _build_drop_server_plan_lazily,
    "drop_sequence": _build_drop_sequence_plan_lazily,
    "drop_schema": _build_drop_schema_plan_lazily,
    "drop_rule": _build_drop_rule_plan_lazily,
    "drop_routine": _build_drop_routine_plan_lazily,
    "drop_role": _build_drop_role_plan_lazily,
    "drop_publication": _build_drop_publication_plan_lazily,
    "drop_procedure": _build_drop_procedure_plan_lazily,
    "drop_policy": _build_drop_policy_plan_lazily,
    "drop_owned": _build_drop_owned_plan_lazily,
    "drop_operator_family": _build_drop_operator_family_plan_lazily,
    "drop_operator_class": _build_drop_operator_class_plan_lazily,
    "drop_operator": _build_drop_operator_plan_lazily,
    "drop_materialized_view": _build_drop_materialized_view_plan_lazily,
    "drop_language": _build_drop_language_plan_lazily,
    "drop_index": _build_drop_index_plan_lazily,
    "drop_group": _build_drop_group_plan_lazily,
    "drop_function": _build_drop_function_plan_lazily,
    "drop_foreign_table": _build_drop_foreign_table_plan_lazily,
    "drop_foreign_data_wrapper": _build_drop_foreign_data_wrapper_plan_lazily,
    "drop_extension": _build_drop_extension_plan_lazily,
    "drop_event_trigger": _build_drop_event_trigger_plan_lazily,
    "drop_domain": _build_drop_domain_plan_lazily,
    "drop_database": _build_drop_database_plan_lazily,
    "drop_conversion": _build_drop_conversion_plan_lazily,
    "drop_cast": _build_drop_cast_plan_lazily,
    "drop_aggregate": _build_drop_aggregate_plan_lazily,
    "drop_access_method": _build_drop_access_method_plan_lazily,
    "do": _build_do_plan_lazily,
    "discard": _build_discard_plan_lazily,
    "delete": _build_delete_plan_lazily,
    "deallocate": _build_deallocate_plan_lazily,
    "create_view": _build_create_view_plan_lazily,
    "create_user_mapping": _build_create_user_mapping_plan_lazily,
    "create_user": _build_create_user_plan_lazily,
    "create_type": _build_create_type_plan_lazily,
    "create_trigger": _build_create_trigger_plan_lazily,
    "create_transform": _build_create_transform_plan_lazily,
    "create_text_search_template": _build_create_text_search_template_plan_lazily,
    "create_table_as": _build_create_table_as_plan_lazily,
    "create_text_search_parser": _build_create_text_search_parser_plan_lazily,
    "create_text_search_dictionary": _build_create_text_search_dictionary_plan_lazily,
    "create_text_search_configuration": _build_create_text_search_configuration_plan_lazily,
    "create_tablespace": _build_create_tablespace_plan_lazily,
    "create_table": _build_create_table_plan_lazily,
    "create_subscription": _build_create_subscription_plan_lazily,
    "create_statistics": _build_create_statistics_plan_lazily,
    "create_server": _build_create_server_plan_lazily,
    "create_sequence": _build_create_sequence_plan_lazily,
    "create_schema": _build_create_schema_plan_lazily,
    "create_rule": _build_create_rule_plan_lazily,
    "create_role": _build_create_role_plan_lazily,
    "create_publication": _build_create_publication_plan_lazily,
    "create_procedure": _build_create_procedure_plan_lazily,
    "create_policy": _build_create_policy_plan_lazily,
    "create_operator_family": _build_create_operator_family_plan_lazily,
    "create_operator_class": _build_create_operator_class_plan_lazily,
    "create_operator": _build_create_operator_plan_lazily,
    "create_materialized_view": _build_create_materialized_view_plan_lazily,
    "create_language": _build_create_language_plan_lazily,
    "create_index": _build_create_index_plan_lazily,
    "create_group": _build_create_group_plan_lazily,
    "create_function": _build_create_function_plan_lazily,
    "create_foreign_table": _build_create_foreign_table_plan_lazily,
    "create_foreign_data_wrapper": _build_create_foreign_data_wrapper_plan_lazily,
    "create_extension": _build_create_extension_plan_lazily,
    "create_event_trigger": _build_create_event_trigger_plan_lazily,
    "create_domain": _build_create_domain_plan_lazily,
    "create_database": _build_create_database_plan_lazily,
    "create_conversion": _build_create_conversion_plan_lazily,
    "create_collation": _build_create_collation_plan_lazily,
    "create_cast": _build_create_cast_plan_lazily,
    "create_aggregate": _build_create_aggregate_plan_lazily,
    "create_access_method": _build_create_access_method_plan_lazily,
    "copy": _build_copy_plan_lazily,
    "commit_prepared": _build_commit_prepared_plan_lazily,
    "commit": _build_commit_plan_lazily,
    "comment": _build_comment_plan_lazily,
    "cluster": _build_cluster_plan_lazily,
    "checkpoint": _build_checkpoint_plan_lazily,
    "call": _build_call_plan_lazily,
    "begin": _build_begin_plan_lazily,
    "analyze": _build_analyze_plan_lazily,
    "alter_view": _build_alter_view_plan_lazily,
    "alter_user_mapping": _build_alter_user_mapping_plan_lazily,
    "alter_user": _build_alter_user_plan_lazily,
    "alter_type": _build_alter_type_plan_lazily,
    "alter_trigger": _build_alter_trigger_plan_lazily,
    "alter_text_search_template": _build_alter_text_search_template_plan_lazily,
    "alter_text_search_parser": _build_alter_text_search_parser_plan_lazily,
    "alter_text_search_dictionary": _build_alter_text_search_dictionary_plan_lazily,
    "alter_text_search_configuration": _build_alter_text_search_configuration_plan_lazily,
    "alter_tablespace": _build_alter_tablespace_plan_lazily,
    "alter_system": _build_alter_system_plan_lazily,
    "alter_subscription": _build_alter_subscription_plan_lazily,
    "alter_statistics": _build_alter_statistics_plan_lazily,
    "alter_server": _build_alter_server_plan_lazily,
    "alter_publication": _build_alter_publication_plan_lazily,
    "alter_sequence": _build_alter_sequence_plan_lazily,
    "alter_table": _build_alter_table_plan_lazily,
    "alter_group": _build_alter_group_plan_lazily,
    "alter_role": _build_alter_role_plan_lazily,
    "alter_index": _build_alter_index_plan_lazily,
    "drop_collation": _build_drop_collation_plan_lazily,
}


def build_statement_regress_plan(
    snapshot: StatementFactorCycleSnapshot,
    statement_key: str,
) -> StatementRegressPlan:
    """Build one explicit statement plan; never fall back to generic SQL."""

    entry = _entry(snapshot, statement_key)
    try:
        builder = _PLAN_BUILDERS[statement_key]
    except KeyError as exc:
        raise RemainingStatementRegressError(
            f"statement {statement_key} has no reviewed renderer"
        ) from exc
    plan = builder(snapshot, entry)
    expected_rows = {
        row_id for factor in entry.factors for row_id in factor.row_ids
    }
    actual_rows = {decision.row_id for decision in plan.factor_decisions}
    if len(actual_rows) != len(plan.factor_decisions) or actual_rows != expected_rows:
        raise RemainingStatementRegressError(
            f"{statement_key} factor dispositions do not conserve canonical rows"
        )
    return plan


def _trace_lines(plan: StatementRegressPlan, case: StatementRegressCase) -> list[str]:
    derived = ", ".join(
        f"{key}={value}" for key, value in sorted(case.derived_axes.items())
    )
    factors = ", ".join(case.factor_values)
    return [
        f"-- case_id: {case.case_id}",
        f"-- source_md: {plan.reference_path}",
        f"-- factor_md: {plan.matrix_path}",
        f"-- factor_values: {factors}",
        f"-- derived_axes: {derived}",
        f"-- combination_strategy: {case.combination_strategy}",
        f"-- session_count: 1",
        f"-- execution_profile: {case.execution_profile}",
        "-- outer_transaction: forbidden",
        f"-- expected_anchor: {case.expected_anchor}",
    ]


def _header(plan: StatementRegressPlan, case: StatementRegressCase) -> list[str]:
    header = render_huawei_sql_header(
        HuaweiSqlHeader(
            author=_AUTHOR,
            create_at=_CREATE_DATE,
            version=_VERSION,
            description=case.description,
            fe=_FE,
        )
    ).rstrip("\n")
    return header.splitlines() + _trace_lines(plan, case)


def _render_abort_core(
    plan: StatementRegressPlan, case: StatementRegressCase
) -> str:
    optional_keyword = case.derived_axes["optional_keyword"]
    chain_clause = case.derived_axes["chain_clause"]
    state = case.derived_axes["transaction_state"]
    target = _abort_sql(optional_keyword, chain_clause)
    prefix = case.object_prefix

    if state == "idle":
        lines = _header(plan, case) + [
            "SET client_min_messages TO warning;",
            r"\set ON_ERROR_STOP off",
            "",
            "-- 1. 保持会话处于事务外空闲状态，避免框架外层事务掩盖 ABORT 的事务外行为。",
            "SELECT true AS idle_state_ready;",
            "",
            (
                f"-- 2. 事务外执行 {target[:-1]}；"
                + (
                    "expected SQLSTATE 25P01，且不得启动 chained transaction。"
                    if chain_clause == "AND CHAIN"
                    else "expected WARNING 25P01，命令为无效果操作。"
                )
            ),
            target,
        ]
        if chain_clause == "AND CHAIN":
            lines.extend(
                [
                    r"\echo target_sqlstate :SQLSTATE",
                    "",
                    "-- 3. 归一化检查上一条目标错误；expected abort_outside_chain_rejected=true。",
                    "SELECT :'SQLSTATE' = '25P01' AS abort_outside_chain_rejected;",
                    "",
                    "-- 4. 错误后执行稳定查询；expected session_usable_after_idle_abort=true。",
                    "SELECT true AS session_usable_after_idle_abort;",
                ]
            )
        else:
            lines.extend(
                [
                    "",
                    "-- 3. WARNING 后执行稳定查询；expected session_usable_after_idle_abort=true。",
                    "SELECT true AS session_usable_after_idle_abort;",
                ]
            )
        return "\n".join(lines).rstrip() + "\n"

    data_table = f"public.{prefix}data"
    probe_table = f"{prefix}probe"
    drop = f"DROP TABLE IF EXISTS {probe_table}, {data_table};"
    lines = _header(plan, case) + [
        drop,
        "SET search_path TO public, pg_catalog;",
        "SET client_min_messages TO warning;",
        r"\set ON_ERROR_STOP off",
        f"CREATE TABLE {data_table} (",
        "    id integer PRIMARY KEY,",
        "    note text NOT NULL,",
        "    amount numeric(12, 2) NOT NULL CHECK (amount >= 0)",
        ");",
        f"CREATE TEMP TABLE {probe_table} (",
        "    marker integer PRIMARY KEY,",
        "    note text NOT NULL",
        ") ON COMMIT DELETE ROWS;",
        f"INSERT INTO {data_table}(id, note, amount)",
        "VALUES (0, 'committed_control', 10.00);",
        "",
        "-- 1. 创建完整数据表和 ON COMMIT 探针；id=0 是已提交控制行，其他行只能存在于待回滚事务。",
        f"SELECT id, note, amount FROM {data_table} ORDER BY id;",
        "",
        "-- 2. 建立目标事务状态并写入只允许被 ABORT 丢弃的数据。",
        "BEGIN;",
        f"INSERT INTO {data_table}(id, note, amount) VALUES (1, 'top_level_change', 20.00);",
    ]
    if state == "active_failed":
        lines.extend(
            [
                "SELECT 1 / 0;",
                r"\echo setup_sqlstate :SQLSTATE",
            ]
        )
    elif state == "active_with_nested_savepoints":
        lines.extend(
            [
                f"SAVEPOINT {prefix}released_sp;",
                f"RELEASE SAVEPOINT {prefix}released_sp;",
                f"SAVEPOINT {prefix}simple_sp;",
                f"INSERT INTO {data_table}(id, note, amount) VALUES (2, 'simple_savepoint_change', 30.00);",
                f'SAVEPOINT "{prefix}Quoted Savepoint";',
                f"INSERT INTO {data_table}(id, note, amount) VALUES (3, 'quoted_savepoint_change', 40.00);",
            ]
        )

    lines.extend(
        [
            "",
            f"-- 3. 执行目标 {target[:-1]}；expected 旧顶层事务及全部子事务修改均被丢弃。",
            target,
            "",
            "-- 4. 查询已提交控制行；expected 仅 id=0，证明 ABORT 没有退化为局部 savepoint 回滚。",
            f"SELECT id, note, amount FROM {data_table} ORDER BY id;",
        ]
    )
    if chain_clause == "AND CHAIN":
        lines.extend(
            [
                "",
                "-- 5. 在 chained transaction 写入探针；expected chained_transaction_active=true。",
                f"INSERT INTO {probe_table}(marker, note) VALUES (1, 'inside_chain');",
                f"SELECT count(*) = 1 AS chained_transaction_active FROM {probe_table};",
                "",
                "-- 6. 使用无 CHAIN 的 ABORT 关闭新事务；expected 探针行被 ON COMMIT 清空。",
                "ABORT;",
                f"SELECT count(*) = 0 AS chained_transaction_cleaned FROM {probe_table};",
            ]
        )
    else:
        lines.extend(
            [
                "",
                "-- 5. 在目标命令后写入探针；隐式事务提交应触发 ON COMMIT DELETE ROWS，expected no_chained_transaction=true。",
                f"INSERT INTO {probe_table}(marker, note) VALUES (1, 'implicit_transaction');",
                f"SELECT count(*) = 0 AS no_chained_transaction FROM {probe_table};",
            ]
        )
    if case.ordinal == 1:
        lines.extend(
            [
                "",
                "-- 6. 恢复本文件修改的 session 状态，覆盖 cleanup_mode=reset_state。",
                "RESET ALL;",
            ]
        )
    lines.extend(
        [
            "",
            "-- 7. 反向清理本文件的临时探针和持久数据表。",
            drop,
        ]
    )
    return "\n".join(lines).rstrip() + "\n"


def _expected_isolation(isolation: str) -> str:
    return isolation.lower()


def _render_abort_characteristics(
    plan: StatementRegressPlan, case: StatementRegressCase
) -> str:
    isolation = case.derived_axes["isolation"]
    access_mode = case.derived_axes["access_mode"]
    deferrability = case.derived_axes["deferrability"]
    prefix = case.object_prefix
    probe = f"{prefix}probe"
    drop = f"DROP TABLE IF EXISTS {probe};"
    read_only = "true" if access_mode == "READ ONLY" else "false"
    deferrable = "true" if deferrability == "DEFERRABLE" else "false"
    lines = _header(plan, case) + [
        drop,
        "SET client_min_messages TO warning;",
        f"CREATE TEMP TABLE {probe} (",
        "    marker integer PRIMARY KEY,",
        "    note text NOT NULL",
        ") ON COMMIT DELETE ROWS;",
        "",
        "-- 1. 创建 ON COMMIT 探针，用行是否仍存在稳定区分 chained transaction 是否保持打开。",
        f"SELECT count(*) = 0 AS probe_initially_empty FROM {probe};",
        "",
        "-- 2. 以完整事务特征组合启动事务，为 AND CHAIN 的继承行为建立前置状态。",
        f"BEGIN ISOLATION LEVEL {isolation}, {access_mode}, {deferrability};",
        "",
        "-- 3. 执行目标 ABORT AND CHAIN；旧事务结束后应立即建立具有相同特征的新事务。",
        "ABORT AND CHAIN;",
        "",
        "-- 4. 归一化比较三项设置；expected characteristics_preserved=true。",
        "SELECT",
        f"    current_setting('transaction_isolation') = '{_expected_isolation(isolation)}'",
        f"    AND current_setting('transaction_read_only')::boolean = {read_only}",
        f"    AND current_setting('transaction_deferrable')::boolean = {deferrable}",
        "    AS characteristics_preserved;",
        "",
        "-- 5. 在新事务写入探针；expected chained_transaction_active=true。",
        f"INSERT INTO {probe}(marker, note) VALUES (1, 'inside_chain');",
        f"SELECT count(*) = 1 AS chained_transaction_active FROM {probe};",
        "",
        "-- 6. 使用无 CHAIN 的 ABORT 关闭新事务；expected chained_transaction_cleaned=true。",
        "ABORT;",
        f"SELECT count(*) = 0 AS chained_transaction_cleaned FROM {probe};",
        "",
        "-- 7. 清理本文件的临时探针表。",
        drop,
    ]
    return "\n".join(lines).rstrip() + "\n"


def _render_abort_catalog(
    plan: StatementRegressPlan, case: StatementRegressCase
) -> str:
    table = f"public.{case.object_prefix}created_inside_abort"
    drop = f"DROP TABLE IF EXISTS {table};"
    lines = _header(plan, case) + [
        drop,
        "SET search_path TO public, pg_catalog;",
        "",
        "-- 1. 清除同名残留；目标表随后只在待回滚事务内创建。",
        "SELECT to_regclass('public."
        + case.object_prefix
        + "created_inside_abort') IS NULL AS object_absent_before_target;",
        "",
        "-- 2. 在事务中创建包含完整列结构和约束的表。",
        "BEGIN;",
        f"CREATE TABLE {table} (",
        "    id integer PRIMARY KEY,",
        "    note text NOT NULL,",
        "    amount numeric(12, 2) NOT NULL CHECK (amount >= 0)",
        ");",
        "",
        "-- 3. 执行目标 ABORT；事务性 DDL 应与事务一起被丢弃。",
        "ABORT;",
        "",
        "-- 4. 通过稳定 regclass 谓词验证；expected transactional_ddl_was_discarded=true。",
        "SELECT to_regclass('public."
        + case.object_prefix
        + "created_inside_abort') IS NULL AS transactional_ddl_was_discarded;",
        "",
        "-- 5. 幂等清理同名对象，保证文件可重复执行。",
        drop,
    ]
    return "\n".join(lines).rstrip() + "\n"


def _render_abort_syntax(
    plan: StatementRegressPlan, case: StatementRegressCase
) -> str:
    syntax_error = case.derived_axes["syntax_error"]
    target = {
        "missing_and": "ABORT CHAIN;",
        "missing_chain": "ABORT AND;",
        "incomplete_no_chain": "ABORT AND NO;",
        "duplicate_optional_keyword": "ABORT WORK TRANSACTION;",
        "target_operand": f"ABORT {case.object_prefix}target;",
    }[syntax_error]
    lines = _header(plan, case) + [
        "SET client_min_messages TO warning;",
        r"\set ON_ERROR_STOP off",
        "",
        "-- 1. 保持连接在事务外并允许预期语法错误后继续执行验证查询。",
        "SELECT true AS parser_boundary_ready;",
        "",
        f"-- 2. 执行唯一的 {syntax_error} 负例；expected SQLSTATE 42601。",
        target,
        r"\echo target_sqlstate :SQLSTATE",
        "",
        "-- 3. 归一化检查目标错误；expected syntax_boundary_rejected=true。",
        "SELECT :'SQLSTATE' = '42601' AS syntax_boundary_rejected;",
        "",
        "-- 4. 恢复 session 设置并确认连接仍可使用。",
        "RESET ALL;",
        "SELECT true AS session_usable_after_syntax_error;",
    ]
    return "\n".join(lines).rstrip() + "\n"


def _render_abort_prepared(
    plan: StatementRegressPlan, case: StatementRegressCase
) -> str:
    chain_clause = case.derived_axes["chain_clause"]
    target = _abort_sql("omitted", chain_clause)
    gid = f"{case.object_prefix}prepared"
    target_failure = chain_clause == "AND CHAIN"
    lines = _header(plan, case) + [
        "SET client_min_messages TO warning;",
        r"\set ON_ERROR_STOP off",
        "",
        "-- 1. 清理同名 prepared transaction 残留；本外部 fixture 要求 max_prepared_transactions > 0。",
        "SELECT format('ROLLBACK PREPARED %L;', gid)",
        "FROM pg_catalog.pg_prepared_xacts",
        f"WHERE gid = '{gid}'",
        "ORDER BY gid",
        r"\gexec",
        "",
        "-- 2. 分配事务 ID 并准备事务，使其脱离当前会话成为独立 prepared transaction。",
        "BEGIN;",
        "SELECT pg_catalog.pg_current_xact_id() IS NOT NULL AS xid_assigned;",
        f"PREPARE TRANSACTION '{gid}';",
        "",
        f"-- 3. 当前会话已空闲，执行目标 {target[:-1]}；prepared transaction 不应被 ABORT 消费。",
        target,
    ]
    if target_failure:
        lines.extend(
            [
                r"\echo target_sqlstate :SQLSTATE",
                "SELECT :'SQLSTATE' = '25P01' AS idle_chain_rejected;",
            ]
        )
    lines.extend(
        [
            "",
            "-- 4. 查询固定 GID；expected prepared_transaction_preserved=true。",
            "SELECT count(*) = 1 AS prepared_transaction_preserved",
            "FROM pg_catalog.pg_prepared_xacts",
            f"WHERE gid = '{gid}'",
            "ORDER BY prepared_transaction_preserved;",
            "",
            "-- 5. 显式回滚 prepared transaction 并验证清理完成，避免跨用例遗留。",
            f"ROLLBACK PREPARED '{gid}';",
            "SELECT count(*) = 0 AS prepared_transaction_cleaned",
            "FROM pg_catalog.pg_prepared_xacts",
            f"WHERE gid = '{gid}'",
            "ORDER BY prepared_transaction_cleaned;",
        ]
    )
    return "\n".join(lines).rstrip() + "\n"


def render_statement_regress_case(
    plan: StatementRegressPlan,
    case: StatementRegressCase,
    repository_root: Path | None = None,
) -> str:
    """Render a reviewed logical case; no generic statement fallback exists."""

    if case not in plan.cases:
        raise RemainingStatementRegressError("case does not belong to the plan")
    if plan.statement_key == "alter_foreign_table":
        from .alter_foreign_table_factor_render import (
            render_alter_foreign_table_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_foreign_table_factor_plan(str(root))
        matches = [row for row in factor_plan.cases if row.case_id == case.case_id]
        if len(matches) != 1:
            raise RemainingStatementRegressError(
                f"ALTER FOREIGN TABLE case mapping is not unique: {case.case_id}"
            )
        return render_alter_foreign_table_factor_case(
            factor_plan,
            matches[0],
            root,
        )
    if plan.statement_key == "alter_function":
        from .alter_function_factor_render import (
            render_alter_function_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_function_factor_plan(str(root))
        extension_plan = _alter_function_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER FUNCTION case mapping is not unique: {case.case_id}"
            )
        return render_alter_function_factor_case(found, root)
    if plan.statement_key == "alter_language":
        from .alter_language_factor_render import (
            render_alter_language_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_language_factor_plan(str(root))
        extension_plan = _alter_language_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER LANGUAGE case mapping is not unique: {case.case_id}"
            )
        return render_alter_language_factor_case(found, root)
    if plan.statement_key == "alter_materialized_view":
        from .alter_materialized_view_factor_render import (
            render_alter_materialized_view_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_materialized_view_factor_plan(str(root))
        extension_plan = _alter_materialized_view_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER MATERIALIZED VIEW case mapping is not unique: "
                f"{case.case_id}"
            )
        return render_alter_materialized_view_factor_case(found, root)
    if plan.statement_key == "alter_policy":
        from .alter_policy_factor_render import (
            render_alter_policy_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_policy_factor_plan(str(root))
        extension_plan = _alter_policy_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER POLICY case mapping is not unique: {case.case_id}"
            )
        return render_alter_policy_factor_case(found, root)
    if plan.statement_key == "alter_procedure":
        from .alter_procedure_factor_render import (
            render_alter_procedure_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_procedure_factor_plan(str(root))
        extension_plan = _alter_procedure_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER PROCEDURE case mapping is not unique: {case.case_id}"
            )
        return render_alter_procedure_factor_case(found, root)
    if plan.statement_key == "alter_routine":
        from .alter_routine_factor_render import (
            render_alter_routine_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_routine_factor_plan(str(root))
        extension_plan = _alter_routine_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER ROUTINE case mapping is not unique: {case.case_id}"
            )
        return render_alter_routine_factor_case(found, root)
    if plan.statement_key == "alter_rule":
        from .alter_rule_factor_render import (
            render_alter_rule_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_rule_factor_plan(str(root))
        extension_plan = _alter_rule_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER RULE case mapping is not unique: {case.case_id}"
            )
        return render_alter_rule_factor_case(found, root)
    if plan.statement_key == "alter_schema":
        from .alter_schema_factor_render import (
            render_alter_schema_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_schema_factor_plan(str(root))
        extension_plan = _alter_schema_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER SCHEMA case mapping is not unique: {case.case_id}"
            )
        return render_alter_schema_factor_case(found, root)
    if plan.statement_key == "alter_server":
        from .alter_server_factor_render import (
            render_alter_server_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_server_factor_plan(str(root))
        extension_plan = _alter_server_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER SERVER case mapping is not unique: {case.case_id}"
            )
        return render_alter_server_factor_case(found, root)

    if plan.statement_key == "alter_statistics":
        from .alter_statistics_factor_render import (
            render_alter_statistics_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_statistics_factor_plan(str(root))
        extension_plan = _alter_statistics_extension_plan(str(root))
        found = next(
            (
                c
                for c in factor_plan.cases
                if c.case_id == case.case_id
            ),
            None,
        )
        if found is None:
            found = next(
                (
                    c
                    for c in extension_plan.cases
                    if c.case_id == case.case_id
                ),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER STATISTICS case mapping is not unique: "
                f"{case.case_id}"
            )
        return render_alter_statistics_factor_case(found, root)

    if plan.statement_key == "alter_subscription":
        from .alter_subscription_factor_render import (
            render_alter_subscription_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_subscription_factor_plan(str(root))
        extension_plan = _alter_subscription_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER SUBSCRIPTION case mapping is not unique: {case.case_id}"
            )
        return render_alter_subscription_factor_case(found, root)

    if plan.statement_key == "alter_system":
        from .alter_system_factor_render import (
            render_alter_system_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_system_factor_plan(str(root))
        extension_plan = _alter_system_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER SYSTEM case mapping is not unique: {case.case_id}"
            )
        return render_alter_system_factor_case(found, root)

    if plan.statement_key == "alter_tablespace":
        from .alter_tablespace_factor_render import (
            render_alter_tablespace_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_tablespace_factor_plan(str(root))
        extension_plan = _alter_tablespace_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER TABLESPACE case mapping is not unique: {case.case_id}"
            )
        return render_alter_tablespace_factor_case(found, root)

    if plan.statement_key == "alter_text_search_configuration":
        from .alter_text_search_configuration_factor_render import (
            render_alter_text_search_configuration_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_text_search_configuration_factor_plan(str(root))
        extension_plan = _alter_text_search_configuration_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER TEXT SEARCH CONFIGURATION case mapping is not unique: {case.case_id}"
            )
        return render_alter_text_search_configuration_factor_case(found, root)

    if plan.statement_key == "alter_text_search_dictionary":
        from .alter_text_search_dictionary_factor_render import (
            render_alter_text_search_dictionary_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_text_search_dictionary_factor_plan(str(root))
        extension_plan = _alter_text_search_dictionary_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER TEXT SEARCH DICTIONARY case mapping is not unique: {case.case_id}"
            )
        return render_alter_text_search_dictionary_factor_case(found, root)

    if plan.statement_key == "alter_text_search_parser":
        from .alter_text_search_parser_factor_render import (
            render_alter_text_search_parser_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_text_search_parser_factor_plan(str(root))
        extension_plan = _alter_text_search_parser_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER TEXT SEARCH PARSER case mapping is not unique: {case.case_id}"
            )
        return render_alter_text_search_parser_factor_case(found, root)

    if plan.statement_key == "alter_text_search_template":
        from .alter_text_search_template_factor_render import (
            render_alter_text_search_template_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_text_search_template_factor_plan(str(root))
        extension_plan = _alter_text_search_template_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER TEXT SEARCH TEMPLATE case mapping is not unique: {case.case_id}"
            )
        return render_alter_text_search_template_factor_case(found, root)

    if plan.statement_key == "alter_trigger":
        from .alter_trigger_factor_render import (
            render_alter_trigger_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_trigger_factor_plan(str(root))
        extension_plan = _alter_trigger_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER TRIGGER case mapping is not unique: {case.case_id}"
            )
        return render_alter_trigger_factor_case(found, root)

    if plan.statement_key == "alter_type":
        from .alter_type_factor_render import (
            render_alter_type_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_type_factor_plan(str(root))
        extension_plan = _alter_type_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER TYPE case mapping is not unique: {case.case_id}"
            )
        return render_alter_type_factor_case(found, root)

    if plan.statement_key == "alter_user":
        from .alter_user_factor_render import (
            render_alter_user_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_user_factor_plan(str(root))
        extension_plan = _alter_user_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER USER case mapping is not unique: {case.case_id}"
            )
        return render_alter_user_factor_case(found, root)

    if plan.statement_key == "alter_user_mapping":
        from .alter_user_mapping_factor_render import (
            render_alter_user_mapping_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_user_mapping_factor_plan(str(root))
        extension_plan = _alter_user_mapping_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER USER MAPPING case mapping is not unique: {case.case_id}"
            )
        return render_alter_user_mapping_factor_case(found, root)

    if plan.statement_key == "alter_view":
        from .alter_view_factor_render import (
            render_alter_view_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_view_factor_plan(str(root))
        extension_plan = _alter_view_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER VIEW case mapping is not unique: {case.case_id}"
            )
        return render_alter_view_factor_case(found, root)

    if plan.statement_key == "analyze":
        from .analyze_factor_render import (
            render_analyze_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _analyze_factor_plan(str(root))
        extension_plan = _analyze_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ANALYZE case mapping is not unique: {case.case_id}"
            )
        return render_analyze_factor_case(found, root)

    if plan.statement_key == "begin":
        from .begin_factor_render import (
            render_begin_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _begin_factor_plan(str(root))
        extension_plan = _begin_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"BEGIN case mapping is not unique: {case.case_id}"
            )
        return render_begin_factor_case(found, root)

    if plan.statement_key == "call":
        from .call_factor_render import (
            render_call_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _call_factor_plan(str(root))
        extension_plan = _call_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CALL case mapping is not unique: {case.case_id}"
            )
        return render_call_factor_case(found, root)

    if plan.statement_key == "checkpoint":
        from .checkpoint_factor_render import (
            render_checkpoint_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _checkpoint_factor_plan(str(root))
        extension_plan = _checkpoint_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CHECKPOINT case mapping is not unique: {case.case_id}"
            )
        return render_checkpoint_factor_case(found, root)

    if plan.statement_key == "cluster":
        from .cluster_factor_render import (
            render_cluster_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _cluster_factor_plan(str(root))
        extension_plan = _cluster_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CLUSTER case mapping is not unique: {case.case_id}"
            )
        return render_cluster_factor_case(found, root)

    if plan.statement_key == "comment":
        from .comment_factor_render import (
            render_comment_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _comment_factor_plan(str(root))
        extension_plan = _comment_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"COMMENT case mapping is not unique: {case.case_id}"
            )
        return render_comment_factor_case(found, root)

    if plan.statement_key == "commit":
        from .commit_factor_render import (
            render_commit_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _commit_factor_plan(str(root))
        extension_plan = _commit_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"COMMIT case mapping is not unique: {case.case_id}"
            )
        return render_commit_factor_case(found, root)

    if plan.statement_key == "commit_prepared":
        from .commit_prepared_factor_render import (
            render_commit_prepared_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _commit_prepared_factor_plan(str(root))
        extension_plan = _commit_prepared_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"COMMIT PREPARED case mapping is not unique: {case.case_id}"
            )
        return render_commit_prepared_factor_case(found, root)

    if plan.statement_key == "copy":
        from .copy_factor_render import (
            render_copy_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _copy_factor_plan(str(root))
        extension_plan = _copy_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"COPY case mapping is not unique: {case.case_id}"
            )
        return render_copy_factor_case(found, root)

    if plan.statement_key == "create_access_method":
        from .create_access_method_factor_render import (
            render_create_access_method_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_access_method_factor_plan(str(root))
        extension_plan = _create_access_method_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE ACCESS METHOD case mapping is not unique: {case.case_id}"
            )
        return render_create_access_method_factor_case(found, root)

    if plan.statement_key == "create_aggregate":
        from .create_aggregate_factor_render import (
            render_create_aggregate_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_aggregate_factor_plan(str(root))
        extension_plan = _create_aggregate_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE AGGREGATE case mapping is not unique: {case.case_id}"
            )
        return render_create_aggregate_factor_case(found, root)

    if plan.statement_key == "create_cast":
        from .create_cast_factor_render import (
            render_create_cast_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_cast_factor_plan(str(root))
        extension_plan = _create_cast_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE CAST case mapping is not unique: {case.case_id}"
            )
        return render_create_cast_factor_case(found, root)

    if plan.statement_key == "create_collation":
        from .create_collation_factor_render import (
            render_create_collation_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_collation_factor_plan(str(root))
        extension_plan = _create_collation_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE COLLATION case mapping is not unique: {case.case_id}"
            )
        return render_create_collation_factor_case(found, root)

    if plan.statement_key == "create_conversion":
        from .create_conversion_factor_render import (
            render_create_conversion_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_conversion_factor_plan(str(root))
        extension_plan = _create_conversion_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE CONVERSION case mapping is not unique: {case.case_id}"
            )
        return render_create_conversion_factor_case(found, root)

    if plan.statement_key == "create_database":
        from .create_database_factor_render import (
            render_create_database_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_database_factor_plan(str(root))
        extension_plan = _create_database_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE DATABASE case mapping is not unique: {case.case_id}"
            )
        return render_create_database_factor_case(found, root)

    if plan.statement_key == "create_domain":
        from .create_domain_factor_render import (
            render_create_domain_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_domain_factor_plan(str(root))
        extension_plan = _create_domain_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE DOMAIN case mapping is not unique: {case.case_id}"
            )
        return render_create_domain_factor_case(found, root)

    if plan.statement_key == "create_event_trigger":
        from .create_event_trigger_factor_render import (
            render_create_event_trigger_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_event_trigger_factor_plan(str(root))
        extension_plan = _create_event_trigger_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE EVENT TRIGGER case mapping is not unique: {case.case_id}"
            )
        return render_create_event_trigger_factor_case(found, root)

    if plan.statement_key == "create_extension":
        from .create_extension_factor_render import (
            render_create_extension_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_extension_factor_plan(str(root))
        extension_plan = _create_extension_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE EXTENSION case mapping is not unique: {case.case_id}"
            )
        return render_create_extension_factor_case(found, root)

    if plan.statement_key == "create_foreign_data_wrapper":
        from .create_foreign_data_wrapper_factor_render import (
            render_create_foreign_data_wrapper_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_foreign_data_wrapper_factor_plan(str(root))
        extension_plan = _create_foreign_data_wrapper_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE FOREIGN DATA WRAPPER case mapping is not unique: {case.case_id}"
            )
        return render_create_foreign_data_wrapper_factor_case(found, root)

    if plan.statement_key == "create_foreign_table":
        from .create_foreign_table_factor_render import (
            render_create_foreign_table_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_foreign_table_factor_plan(
            str(root)
        )
        extension_plan = _create_foreign_table_extension_plan(
            str(root)
        )
        found = next(
            (
                c
                for c in factor_plan.cases
                if c.case_id == case.case_id
            ),
            None,
        )
        if found is None:
            found = next(
                (
                    c
                    for c in extension_plan.cases
                    if c.case_id == case.case_id
                ),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE FOREIGN TABLE case mapping is not "
                f"unique: {case.case_id}"
            )
        return render_create_foreign_table_factor_case(
            found, root
        )

    if plan.statement_key == "create_function":
        from .create_function_factor_render import (
            render_create_function_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_function_factor_plan(str(root))
        extension_plan = _create_function_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE FUNCTION case mapping is not unique: {case.case_id}"
            )
        return render_create_function_factor_case(found, root)

    if plan.statement_key == "create_group":
        from .create_group_factor_render import (
            render_create_group_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_group_factor_plan(str(root))
        extension_plan = _create_group_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE GROUP case mapping is not unique: {case.case_id}"
            )
        return render_create_group_factor_case(found, root)

    if plan.statement_key == "create_index":
        from .create_index_factor_render import (
            render_create_index_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_index_factor_plan(str(root))
        extension_plan = _create_index_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE INDEX case mapping is not unique: {case.case_id}"
            )
        return render_create_index_factor_case(found, root)

    if plan.statement_key == "create_language":
        from .create_language_factor_render import (
            render_create_language_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_language_factor_plan(str(root))
        extension_plan = _create_language_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE LANGUAGE case mapping is not unique: {case.case_id}"
            )
        return render_create_language_factor_case(found, root)

    if plan.statement_key == "create_materialized_view":
        from .create_materialized_view_factor_render import (
            render_create_materialized_view_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_materialized_view_factor_plan(str(root))
        extension_plan = _create_materialized_view_extension_plan(
            str(root)
        )
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id),
            None,
        )
        if found is None:
            found = next(
                (
                    c
                    for c in extension_plan.cases
                    if c.case_id == case.case_id
                ),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                "CREATE MATERIALIZED VIEW case mapping is not unique: "
                f"{case.case_id}"
            )
        return render_create_materialized_view_factor_case(found, root)

    if plan.statement_key == "create_operator":
        from .create_operator_factor_render import (
            render_create_operator_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_operator_factor_plan(str(root))
        extension_plan = _create_operator_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE OPERATOR case mapping is not unique: {case.case_id}"
            )
        return render_create_operator_factor_case(found, root)

    if plan.statement_key == "create_operator_class":
        from .create_operator_class_factor_render import (
            render_create_operator_class_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_operator_class_factor_plan(
            str(root)
        )
        extension_plan = _create_operator_class_extension_plan(
            str(root)
        )
        found = next(
            (
                c
                for c in factor_plan.cases
                if c.case_id == case.case_id
            ),
            None,
        )
        if found is None:
            found = next(
                (
                    c
                    for c in extension_plan.cases
                    if c.case_id == case.case_id
                ),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE OPERATOR CLASS case mapping is not unique: "
                f"{case.case_id}"
            )
        return render_create_operator_class_factor_case(found, root)

    if plan.statement_key == "create_operator_family":
        from .create_operator_family_factor_render import (
            render_create_operator_family_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_operator_family_factor_plan(str(root))
        extension_plan = _create_operator_family_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (
                    c
                    for c in extension_plan.cases
                    if c.case_id == case.case_id
                ),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE OPERATOR FAMILY case mapping is not unique: {case.case_id}"
            )
        return render_create_operator_family_factor_case(found, root)

    if plan.statement_key == "create_policy":
        from .create_policy_factor_render import (
            render_create_policy_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_policy_factor_plan(str(root))
        extension_plan = _create_policy_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE POLICY case mapping is not unique: {case.case_id}"
            )
        return render_create_policy_factor_case(found, root)

    if plan.statement_key == "create_procedure":
        from .create_procedure_factor_render import (
            render_create_procedure_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_procedure_factor_plan(str(root))
        extension_plan = _create_procedure_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE PROCEDURE case mapping is not unique: {case.case_id}"
            )
        return render_create_procedure_factor_case(found, root)

    if plan.statement_key == "create_publication":
        from .create_publication_factor_render import (
            render_create_publication_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_publication_factor_plan(str(root))
        extension_plan = _create_publication_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE PUBLICATION case mapping is not unique: {case.case_id}"
            )
        return render_create_publication_factor_case(found, root)

    if plan.statement_key == "create_role":
        from .create_role_factor_render import (
            render_create_role_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_role_factor_plan(str(root))
        extension_plan = _create_role_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE ROLE case mapping is not unique: {case.case_id}"
            )
        return render_create_role_factor_case(found, root)

    if plan.statement_key == "create_rule":
        from .create_rule_factor_render import (
            render_create_rule_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_rule_factor_plan(str(root))
        extension_plan = _create_rule_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE RULE case mapping is not unique: {case.case_id}"
            )
        return render_create_rule_factor_case(found, root)

    if plan.statement_key == "create_schema":
        from .create_schema_factor_render import (
            render_create_schema_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_schema_factor_plan(str(root))
        extension_plan = _create_schema_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE SCHEMA case mapping is not unique: {case.case_id}"
            )
        return render_create_schema_factor_case(found, root)

    if plan.statement_key == "create_sequence":
        from .create_sequence_factor_render import (
            render_create_sequence_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_sequence_factor_plan(str(root))
        extension_plan = _create_sequence_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE SEQUENCE case mapping is not unique: {case.case_id}"
            )
        return render_create_sequence_factor_case(found, root)

    if plan.statement_key == "create_server":
        from .create_server_factor_render import (
            render_create_server_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_server_factor_plan(str(root))
        extension_plan = _create_server_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE SERVER case mapping is not unique: {case.case_id}"
            )
        return render_create_server_factor_case(found, root)

    if plan.statement_key == "create_statistics":
        from .create_statistics_factor_render import (
            render_create_statistics_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_statistics_factor_plan(str(root))
        extension_plan = _create_statistics_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE STATISTICS case mapping is not unique: {case.case_id}"
            )
        return render_create_statistics_factor_case(found, root)

    if plan.statement_key == "create_subscription":
        from .create_subscription_factor_render import (
            render_create_subscription_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_subscription_factor_plan(str(root))
        extension_plan = _create_subscription_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE SUBSCRIPTION case mapping is not unique: {case.case_id}"
            )
        return render_create_subscription_factor_case(found, root)

    if plan.statement_key == "create_table":
        from .create_table_factor_render import (
            render_create_table_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_table_factor_plan(str(root))
        extension_plan = _create_table_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE TABLE case mapping is not unique: {case.case_id}"
            )
        return render_create_table_factor_case(found, root)

    if plan.statement_key == "create_tablespace":
        from .create_tablespace_factor_render import (
            render_create_tablespace_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_tablespace_factor_plan(str(root))
        extension_plan = _create_tablespace_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE TABLESPACE case mapping is not unique: {case.case_id}"
            )
        return render_create_tablespace_factor_case(found, root)

    if plan.statement_key == "create_text_search_configuration":
        from .create_text_search_configuration_factor_render import (
            render_create_text_search_configuration_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = (
            _create_text_search_configuration_factor_plan(
                str(root)
            )
        )
        extension_plan = (
            _create_text_search_configuration_extension_plan(
                str(root)
            )
        )
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id),
            None,
        )
        if found is None:
            found = next(
                (
                    c
                    for c in extension_plan.cases
                    if c.case_id == case.case_id
                ),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                "CREATE TEXT SEARCH CONFIGURATION case mapping "
                f"is not unique: {case.case_id}"
            )
        return render_create_text_search_configuration_factor_case(
            found, root
        )

    if plan.statement_key == "create_text_search_dictionary":
        from .create_text_search_dictionary_factor_render import (
            render_create_text_search_dictionary_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_text_search_dictionary_factor_plan(str(root))
        extension_plan = _create_text_search_dictionary_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE TEXT SEARCH DICTIONARY case mapping is not unique: {case.case_id}"
            )
        return render_create_text_search_dictionary_factor_case(found, root)

    if plan.statement_key == "create_text_search_parser":
        from .create_text_search_parser_factor_render import (
            render_create_text_search_parser_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_text_search_parser_factor_plan(str(root))
        extension_plan = _create_text_search_parser_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE TEXT SEARCH PARSER case mapping is not unique: {case.case_id}"
            )
        return render_create_text_search_parser_factor_case(found, root)

    if plan.statement_key == "create_table_as":
        from .create_table_as_factor_render import (
            render_create_table_as_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_table_as_factor_plan(str(root))
        extension_plan = _create_table_as_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE TABLE AS case mapping is not unique: {case.case_id}"
            )
        return render_create_table_as_factor_case(found, root)

    if plan.statement_key == "create_text_search_template":
        from .create_text_search_template_factor_render import (
            render_create_text_search_template_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_text_search_template_factor_plan(str(root))
        extension_plan = _create_text_search_template_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE TEXT SEARCH TEMPLATE case mapping is not unique: {case.case_id}"
            )
        return render_create_text_search_template_factor_case(found, root)

    if plan.statement_key == "create_transform":
        from .create_transform_factor_render import (
            render_create_transform_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_transform_factor_plan(str(root))
        extension_plan = _create_transform_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE TRANSFORM case mapping is not unique: {case.case_id}"
            )
        return render_create_transform_factor_case(found, root)

    if plan.statement_key == "create_trigger":
        from .create_trigger_factor_render import (
            render_create_trigger_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_trigger_factor_plan(str(root))
        extension_plan = _create_trigger_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE TRIGGER case mapping is not unique: {case.case_id}"
            )
        return render_create_trigger_factor_case(found, root)

    if plan.statement_key == "create_type":
        from .create_type_factor_render import (
            render_create_type_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_type_factor_plan(str(root))
        extension_plan = _create_type_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE TYPE case mapping is not unique: {case.case_id}"
            )
        return render_create_type_factor_case(found, root)

    if plan.statement_key == "create_user":
        from .create_user_factor_render import (
            render_create_user_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_user_factor_plan(str(root))
        extension_plan = _create_user_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE USER case mapping is not unique: {case.case_id}"
            )
        return render_create_user_factor_case(found, root)

    if plan.statement_key == "create_user_mapping":
        from .create_user_mapping_factor_render import (
            render_create_user_mapping_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_user_mapping_factor_plan(str(root))
        extension_plan = _create_user_mapping_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE USER MAPPING case mapping is not unique: {case.case_id}"
            )
        return render_create_user_mapping_factor_case(found, root)

    if plan.statement_key == "create_view":
        from .create_view_factor_render import (
            render_create_view_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_view_factor_plan(str(root))
        extension_plan = _create_view_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"CREATE VIEW case mapping is not unique: {case.case_id}"
            )
        return render_create_view_factor_case(found, root)

    if plan.statement_key == "deallocate":
        from .deallocate_factor_render import (
            render_deallocate_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _deallocate_factor_plan(str(root))
        extension_plan = _deallocate_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DEALLOCATE case mapping is not unique: {case.case_id}"
            )
        return render_deallocate_factor_case(found, root)

    if plan.statement_key == "delete":
        from .delete_factor_render import (
            render_delete_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _delete_factor_plan(str(root))
        extension_plan = _delete_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DELETE case mapping is not unique: {case.case_id}"
            )
        return render_delete_factor_case(found, root)

    if plan.statement_key == "discard":
        from .discard_factor_render import (
            render_discard_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _discard_factor_plan(str(root))
        extension_plan = _discard_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DISCARD case mapping is not unique: {case.case_id}"
            )
        return render_discard_factor_case(found, root)

    if plan.statement_key == "do":
        from .do_factor_render import (
            render_do_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _do_factor_plan(str(root))
        extension_plan = _do_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DO case mapping is not unique: {case.case_id}"
            )
        return render_do_factor_case(found, root)

    if plan.statement_key == "drop_access_method":
        from .drop_access_method_factor_render import (
            render_drop_access_method_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_access_method_factor_plan(str(root))
        extension_plan = _drop_access_method_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP ACCESS METHOD case mapping is not unique: {case.case_id}"
            )
        return render_drop_access_method_factor_case(found, root)

    if plan.statement_key == "drop_aggregate":
        from .drop_aggregate_factor_render import (
            render_drop_aggregate_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_aggregate_factor_plan(str(root))
        extension_plan = _drop_aggregate_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP AGGREGATE case mapping is not unique: {case.case_id}"
            )
        return render_drop_aggregate_factor_case(found, root)

    if plan.statement_key == "drop_cast":
        from .drop_cast_factor_render import (
            render_drop_cast_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_cast_factor_plan(str(root))
        extension_plan = _drop_cast_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP CAST case mapping is not unique: {case.case_id}"
            )
        return render_drop_cast_factor_case(found, root)

    if plan.statement_key == "drop_conversion":
        from .drop_conversion_factor_render import (
            render_drop_conversion_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_conversion_factor_plan(str(root))
        extension_plan = _drop_conversion_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP CONVERSION case mapping is not unique: {case.case_id}"
            )
        return render_drop_conversion_factor_case(found, root)

    if plan.statement_key == "drop_database":
        from .drop_database_factor_render import (
            render_drop_database_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_database_factor_plan(str(root))
        extension_plan = _drop_database_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP DATABASE case mapping is not unique: {case.case_id}"
            )
        return render_drop_database_factor_case(found, root)

    if plan.statement_key == "drop_domain":
        from .drop_domain_factor_render import (
            render_drop_domain_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_domain_factor_plan(str(root))
        extension_plan = _drop_domain_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP DOMAIN case mapping is not unique: {case.case_id}"
            )
        return render_drop_domain_factor_case(found, root)

    if plan.statement_key == "drop_event_trigger":
        from .drop_event_trigger_factor_render import (
            render_drop_event_trigger_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_event_trigger_factor_plan(str(root))
        extension_plan = _drop_event_trigger_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP EVENT TRIGGER case mapping is not unique: {case.case_id}"
            )
        return render_drop_event_trigger_factor_case(found, root)

    if plan.statement_key == "drop_extension":
        from .drop_extension_factor_render import (
            render_drop_extension_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_extension_factor_plan(str(root))
        extension_plan = _drop_extension_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP EXTENSION case mapping is not unique: {case.case_id}"
            )
        return render_drop_extension_factor_case(found, root)

    if plan.statement_key == "drop_foreign_data_wrapper":
        from .drop_foreign_data_wrapper_factor_render import (
            render_drop_foreign_data_wrapper_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_foreign_data_wrapper_factor_plan(str(root))
        extension_plan = _drop_foreign_data_wrapper_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP FOREIGN DATA WRAPPER case mapping is not unique: {case.case_id}"
            )
        return render_drop_foreign_data_wrapper_factor_case(found, root)

    if plan.statement_key == "drop_foreign_table":
        from .drop_foreign_table_factor_render import (
            render_drop_foreign_table_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_foreign_table_factor_plan(str(root))
        extension_plan = _drop_foreign_table_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP FOREIGN TABLE case mapping is not unique: {case.case_id}"
            )
        return render_drop_foreign_table_factor_case(found, root)

    if plan.statement_key == "drop_function":
        from .drop_function_factor_render import (
            render_drop_function_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_function_factor_plan(str(root))
        extension_plan = _drop_function_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP FUNCTION case mapping is not unique: {case.case_id}"
            )
        return render_drop_function_factor_case(found, root)

    if plan.statement_key == "drop_group":
        from .drop_group_factor_render import (
            render_drop_group_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_group_factor_plan(str(root))
        extension_plan = _drop_group_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP GROUP case mapping is not unique: {case.case_id}"
            )
        return render_drop_group_factor_case(found, root)

    if plan.statement_key == "drop_index":
        from .drop_index_factor_render import (
            render_drop_index_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_index_factor_plan(str(root))
        extension_plan = _drop_index_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP INDEX case mapping is not unique: {case.case_id}"
            )
        return render_drop_index_factor_case(found, root)

    if plan.statement_key == "drop_language":
        from .drop_language_factor_render import (
            render_drop_language_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_language_factor_plan(str(root))
        extension_plan = _drop_language_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP LANGUAGE case mapping is not unique: {case.case_id}"
            )
        return render_drop_language_factor_case(found, root)

    if plan.statement_key == "drop_materialized_view":
        from .drop_materialized_view_factor_render import (
            render_drop_materialized_view_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_materialized_view_factor_plan(str(root))
        extension_plan = _drop_materialized_view_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP MATERIALIZED VIEW case mapping is not unique: {case.case_id}"
            )
        return render_drop_materialized_view_factor_case(found, root)

    if plan.statement_key == "drop_operator":
        from .drop_operator_factor_render import (
            render_drop_operator_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_operator_factor_plan(str(root))
        extension_plan = _drop_operator_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP OPERATOR case mapping is not unique: {case.case_id}"
            )
        return render_drop_operator_factor_case(found, root)

    if plan.statement_key == "drop_operator_class":
        from .drop_operator_class_factor_render import (
            render_drop_operator_class_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_operator_class_factor_plan(str(root))
        extension_plan = _drop_operator_class_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP OPERATOR CLASS case mapping is not unique: {case.case_id}"
            )
        return render_drop_operator_class_factor_case(found, root)

    if plan.statement_key == "drop_operator_family":
        from .drop_operator_family_factor_render import (
            render_drop_operator_family_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_operator_family_factor_plan(str(root))
        extension_plan = _drop_operator_family_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP OPERATOR FAMILY case mapping is not unique: {case.case_id}"
            )
        return render_drop_operator_family_factor_case(found, root)

    if plan.statement_key == "drop_owned":
        from .drop_owned_factor_render import (
            render_drop_owned_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_owned_factor_plan(str(root))
        extension_plan = _drop_owned_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP OWNED case mapping is not unique: {case.case_id}"
            )
        return render_drop_owned_factor_case(found, root)

    if plan.statement_key == "drop_policy":
        from .drop_policy_factor_render import (
            render_drop_policy_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_policy_factor_plan(str(root))
        extension_plan = _drop_policy_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP POLICY case mapping is not unique: {case.case_id}"
            )
        return render_drop_policy_factor_case(found, root)

    if plan.statement_key == "drop_procedure":
        from .drop_procedure_factor_render import (
            render_drop_procedure_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_procedure_factor_plan(str(root))
        extension_plan = _drop_procedure_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP PROCEDURE case mapping is not unique: {case.case_id}"
            )
        return render_drop_procedure_factor_case(found, root)

    if plan.statement_key == "drop_publication":
        from .drop_publication_factor_render import (
            render_drop_publication_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_publication_factor_plan(str(root))
        extension_plan = _drop_publication_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP PUBLICATION case mapping is not unique: {case.case_id}"
            )
        return render_drop_publication_factor_case(found, root)

    if plan.statement_key == "drop_role":
        from .drop_role_factor_render import (
            render_drop_role_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_role_factor_plan(str(root))
        extension_plan = _drop_role_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP ROLE case mapping is not unique: {case.case_id}"
            )
        return render_drop_role_factor_case(found, root)

    if plan.statement_key == "drop_routine":
        from .drop_routine_factor_render import (
            render_drop_routine_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_routine_factor_plan(str(root))
        extension_plan = _drop_routine_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP ROUTINE case mapping is not unique: {case.case_id}"
            )
        return render_drop_routine_factor_case(found, root)

    if plan.statement_key == "drop_rule":
        from .drop_rule_factor_render import (
            render_drop_rule_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_rule_factor_plan(str(root))
        extension_plan = _drop_rule_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP RULE case mapping is not unique: {case.case_id}"
            )
        return render_drop_rule_factor_case(found, root)

    if plan.statement_key == "drop_schema":
        from .drop_schema_factor_render import (
            render_drop_schema_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_schema_factor_plan(str(root))
        extension_plan = _drop_schema_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP SCHEMA case mapping is not unique: {case.case_id}"
            )
        return render_drop_schema_factor_case(found, root)

    if plan.statement_key == "drop_sequence":
        from .drop_sequence_factor_render import (
            render_drop_sequence_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_sequence_factor_plan(str(root))
        extension_plan = _drop_sequence_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP SEQUENCE case mapping is not unique: {case.case_id}"
            )
        return render_drop_sequence_factor_case(found, root)

    if plan.statement_key == "drop_server":
        from .drop_server_factor_render import (
            render_drop_server_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_server_factor_plan(str(root))
        extension_plan = _drop_server_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP SERVER case mapping is not unique: {case.case_id}"
            )
        return render_drop_server_factor_case(found, root)

    if plan.statement_key == "drop_statistics":
        from .drop_statistics_factor_render import (
            render_drop_statistics_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_statistics_factor_plan(str(root))
        extension_plan = _drop_statistics_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP STATISTICS case mapping is not unique: {case.case_id}"
            )
        return render_drop_statistics_factor_case(found, root)

    if plan.statement_key == "drop_subscription":
        from .drop_subscription_factor_render import (
            render_drop_subscription_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_subscription_factor_plan(str(root))
        extension_plan = _drop_subscription_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP SUBSCRIPTION case mapping is not unique: {case.case_id}"
            )
        return render_drop_subscription_factor_case(found, root)

    if plan.statement_key == "drop_table":
        from .drop_table_factor_render import (
            render_drop_table_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_table_factor_plan(str(root))
        extension_plan = _drop_table_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP TABLE case mapping is not unique: {case.case_id}"
            )
        return render_drop_table_factor_case(found, root)

    if plan.statement_key == "drop_tablespace":
        from .drop_tablespace_factor_render import (
            render_drop_tablespace_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_tablespace_factor_plan(str(root))
        extension_plan = _drop_tablespace_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP TABLESPACE case mapping is not unique: {case.case_id}"
            )
        return render_drop_tablespace_factor_case(found, root)

    if plan.statement_key == "drop_text_search_configuration":
        from .drop_text_search_configuration_factor_render import (
            render_drop_text_search_configuration_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_text_search_configuration_factor_plan(str(root))
        extension_plan = _drop_text_search_configuration_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP TEXT SEARCH CONFIGURATION case mapping is not unique: {case.case_id}"
            )
        return render_drop_text_search_configuration_factor_case(found, root)

    if plan.statement_key == "drop_text_search_dictionary":
        from .drop_text_search_dictionary_factor_render import (
            render_drop_text_search_dictionary_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_text_search_dictionary_factor_plan(str(root))
        extension_plan = _drop_text_search_dictionary_extension_plan(
            str(root)
        )
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (
                    c
                    for c in extension_plan.cases
                    if c.case_id == case.case_id
                ),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP TEXT SEARCH DICTIONARY case mapping is not unique: "
                f"{case.case_id}"
            )
        return render_drop_text_search_dictionary_factor_case(found, root)

    if plan.statement_key == "drop_text_search_parser":
        from .drop_text_search_parser_factor_render import (
            render_drop_text_search_parser_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_text_search_parser_factor_plan(str(root))
        extension_plan = _drop_text_search_parser_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP TEXT SEARCH PARSER case mapping is not unique: {case.case_id}"
            )
        return render_drop_text_search_parser_factor_case(found, root)

    if plan.statement_key == "drop_text_search_template":
        from .drop_text_search_template_factor_render import (
            render_drop_text_search_template_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_text_search_template_factor_plan(str(root))
        extension_plan = _drop_text_search_template_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP TEXT SEARCH TEMPLATE case mapping is not unique: {case.case_id}"
            )
        return render_drop_text_search_template_factor_case(found, root)

    if plan.statement_key == "drop_transform":
        from .drop_transform_factor_render import (
            render_drop_transform_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_transform_factor_plan(str(root))
        extension_plan = _drop_transform_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP TRANSFORM case mapping is not unique: {case.case_id}"
            )
        return render_drop_transform_factor_case(found, root)

    if plan.statement_key == "drop_trigger":
        from .drop_trigger_factor_render import (
            render_drop_trigger_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_trigger_factor_plan(str(root))
        extension_plan = _drop_trigger_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP TRIGGER case mapping is not unique: {case.case_id}"
            )
        return render_drop_trigger_factor_case(found, root)

    if plan.statement_key == "drop_type":
        from .drop_type_factor_render import (
            render_drop_type_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_type_factor_plan(str(root))
        extension_plan = _drop_type_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP TYPE case mapping is not unique: {case.case_id}"
            )
        return render_drop_type_factor_case(found, root)

    if plan.statement_key == "drop_user":
        from .drop_user_factor_render import (
            render_drop_user_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_user_factor_plan(str(root))
        extension_plan = _drop_user_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP USER case mapping is not unique: {case.case_id}"
            )
        return render_drop_user_factor_case(found, root)

    if plan.statement_key == "alter_publication":
        from .alter_publication_factor_render import (
            render_alter_publication_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_publication_factor_plan(str(root))
        extension_plan = _alter_publication_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER PUBLICATION case mapping is not unique: {case.case_id}"
            )
        return render_alter_publication_factor_case(found, root)
    if plan.statement_key == "alter_sequence":
        from .alter_sequence_factor_render import (
            render_alter_sequence_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_sequence_factor_plan(str(root))
        extension_plan = _alter_sequence_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER SEQUENCE case mapping is not unique: {case.case_id}"
            )
        return render_alter_sequence_factor_case(found, root)
    if plan.statement_key == "alter_table":
        from .alter_table_factor_render import (
            render_alter_table_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_table_factor_plan(str(root))
        extension_plan = _alter_table_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER TABLE case mapping is not unique: {case.case_id}"
            )
        return render_alter_table_factor_case(found, root)
    if plan.statement_key == "alter_large_object":
        from .alter_large_object_factor_render import (
            render_alter_large_object_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_large_object_factor_plan(str(root))
        extension_plan = _alter_large_object_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER LARGE OBJECT case mapping is not unique: {case.case_id}"
            )
        return render_alter_large_object_factor_case(found, root)
    if plan.statement_key == "alter_operator":
        from .alter_operator_factor_render import (
            render_alter_operator_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_operator_factor_plan(str(root))
        extension_plan = _alter_operator_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER OPERATOR case mapping is not unique: {case.case_id}"
            )
        return render_alter_operator_factor_case(found, root)
    if plan.statement_key == "alter_operator_class":
        from .alter_operator_class_factor_render import (
            render_alter_operator_class_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_operator_class_factor_plan(str(root))
        extension_plan = _alter_operator_class_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER OPERATOR CLASS case mapping is not unique: {case.case_id}"
            )
        return render_alter_operator_class_factor_case(found, root)
    if plan.statement_key == "alter_operator_family":
        from .alter_operator_family_factor_render import (
            render_alter_operator_family_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_operator_family_factor_plan(str(root))
        extension_plan = _alter_operator_family_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER OPERATOR FAMILY case mapping is not unique: {case.case_id}"
            )
        return render_alter_operator_family_factor_case(found, root)
    if plan.statement_key == "alter_group":
        from .alter_group_factor_render import render_alter_group_factor_case

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_group_factor_plan(str(root))
        extension_plan = _alter_group_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER GROUP case mapping is not unique: {case.case_id}"
            )
        return render_alter_group_factor_case(found, root)
    if plan.statement_key == "alter_role":
        from .alter_role_factor_render import render_alter_role_factor_case

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_role_factor_plan(str(root))
        extension_plan = _alter_role_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER ROLE case mapping is not unique: {case.case_id}"
            )
        return render_alter_role_factor_case(found, root)
    if plan.statement_key == "alter_index":
        from .alter_index_factor_render import (
            render_alter_index_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_index_factor_plan(str(root))
        extension_plan = _alter_index_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"ALTER INDEX case mapping is not unique: {case.case_id}"
            )
        return render_alter_index_factor_case(found, root)
    if plan.statement_key == "drop_collation":
        from .drop_collation_factor_render import (
            render_drop_collation_factor_case,
        )

        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_collation_factor_plan(str(root))
        extension_plan = _drop_collation_extension_plan(str(root))
        found = next(
            (c for c in factor_plan.cases if c.case_id == case.case_id), None
        )
        if found is None:
            found = next(
                (c for c in extension_plan.cases if c.case_id == case.case_id),
                None,
            )
        if found is None:
            raise RemainingStatementRegressError(
                f"DROP COLLATION case mapping is not unique: {case.case_id}"
            )
        return render_drop_collation_factor_case(found, root)
    if plan.statement_key == "alter_aggregate":
        from .alter_aggregate_regress import render_alter_aggregate_case

        return render_alter_aggregate_case(plan, case)
    if plan.statement_key == "alter_collation":
        from .alter_collation_regress import render_alter_collation_case

        return render_alter_collation_case(plan, case)
    if plan.statement_key == "alter_conversion":
        from .alter_conversion_regress import render_alter_conversion_case

        return render_alter_conversion_case(plan, case)
    if plan.statement_key == "alter_database":
        from .alter_database_regress import render_alter_database_case

        return render_alter_database_case(plan, case)
    if plan.statement_key == "alter_default_privileges":
        from .alter_default_privileges_regress import (
            render_alter_default_privileges_case,
        )

        return render_alter_default_privileges_case(plan, case)
    if plan.statement_key == "alter_domain":
        from .alter_domain_regress import render_alter_domain_case

        return render_alter_domain_case(plan, case)
    if plan.statement_key == "alter_event_trigger":
        from .alter_event_trigger_regress import render_alter_event_trigger_case

        return render_alter_event_trigger_case(plan, case)
    if plan.statement_key == "alter_extension":
        from .alter_extension_regress import render_alter_extension_case

        return render_alter_extension_case(plan, case)
    if plan.statement_key == "alter_foreign_data_wrapper":
        from .alter_foreign_data_wrapper_regress import (
            render_alter_foreign_data_wrapper_case,
        )

        return render_alter_foreign_data_wrapper_case(plan, case)
    if plan.statement_key != "abort":
        raise RemainingStatementRegressError(
            f"statement {plan.statement_key} has no SQL renderer"
        )
    renderers = {
        "legal_syntax_state_product": _render_abort_core,
        "chain_characteristics_product": _render_abort_characteristics,
        "transactional_ddl": _render_abort_catalog,
        "syntax_boundary": _render_abort_syntax,
        "prepared_transaction_noninterference": _render_abort_prepared,
    }
    try:
        renderer = renderers[case.case_group]
    except KeyError as exc:
        raise RemainingStatementRegressError(
            f"ABORT case group {case.case_group} has no renderer"
        ) from exc
    return renderer(plan, case)


def _sha256(payload: bytes) -> str:
    return hashlib.sha256(payload).hexdigest()


def _json_bytes(document: Mapping[str, Any]) -> bytes:
    return (
        json.dumps(document, ensure_ascii=False, indent=2, sort_keys=False) + "\n"
    ).encode("utf-8")


def _write_bytes(path: Path, payload: bytes) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_bytes(payload)


def _schedule(cases: tuple[StatementRegressCase, ...]) -> bytes:
    return "".join(f"test: {case.case_id}\n" for case in cases).encode("utf-8")


def _expected_output_files(
    plan: StatementRegressPlan,
    repository_root: Path | None = None,
) -> dict[str, bytes]:
    files = {
        case.sql_filename: render_statement_regress_case(
            plan,
            case,
            repository_root,
        ).encode("utf-8")
        for case in plan.cases
    }
    files["serial_schedule"] = _schedule(plan.serial_cases)
    files["external_schedule"] = _schedule(plan.external_cases)
    return files


def _coverage_document(
    plan: StatementRegressPlan,
    repository_root: Path | None = None,
) -> dict[str, Any]:
    document: dict[str, Any] = {
        "schema_version": 1,
        "statement_key": plan.statement_key,
        "cycle_fingerprint": plan.cycle_fingerprint,
        "factor_value_count": len(plan.factor_decisions),
        "factor_disposition_counts": plan.factor_disposition_counts,
        "missing_row_ids": [],
        "duplicate_row_ids": [],
        "decisions": [decision.to_dict() for decision in plan.factor_decisions],
    }
    if plan.statement_key == "alter_foreign_table":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_foreign_table_factor_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_function":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_function_factor_plan(str(root))
        extension_plan = _alter_function_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_language":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_language_factor_plan(str(root))
        extension_plan = _alter_language_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_materialized_view":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_materialized_view_factor_plan(str(root))
        extension_plan = _alter_materialized_view_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_policy":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_policy_factor_plan(str(root))
        extension_plan = _alter_policy_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_procedure":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_procedure_factor_plan(str(root))
        extension_plan = _alter_procedure_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_routine":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_routine_factor_plan(str(root))
        extension_plan = _alter_routine_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_rule":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_rule_factor_plan(str(root))
        extension_plan = _alter_rule_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_schema":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_schema_factor_plan(str(root))
        extension_plan = _alter_schema_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_server":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_server_factor_plan(str(root))
        extension_plan = _alter_server_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "alter_statistics":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_statistics_factor_plan(str(root))
        extension_plan = _alter_statistics_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "alter_subscription":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_subscription_factor_plan(str(root))
        extension_plan = _alter_subscription_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "alter_system":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_system_factor_plan(str(root))
        extension_plan = _alter_system_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "alter_tablespace":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_tablespace_factor_plan(str(root))
        extension_plan = _alter_tablespace_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "alter_text_search_configuration":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_text_search_configuration_factor_plan(str(root))
        extension_plan = _alter_text_search_configuration_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "alter_text_search_dictionary":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_text_search_dictionary_factor_plan(str(root))
        extension_plan = _alter_text_search_dictionary_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "alter_text_search_parser":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_text_search_parser_factor_plan(str(root))
        extension_plan = _alter_text_search_parser_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "alter_text_search_template":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_text_search_template_factor_plan(str(root))
        extension_plan = _alter_text_search_template_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "alter_trigger":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_trigger_factor_plan(str(root))
        extension_plan = _alter_trigger_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "alter_type":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_type_factor_plan(str(root))
        extension_plan = _alter_type_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "alter_user":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_user_factor_plan(str(root))
        extension_plan = _alter_user_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "alter_user_mapping":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_user_mapping_factor_plan(str(root))
        extension_plan = _alter_user_mapping_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "alter_view":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_view_factor_plan(str(root))
        extension_plan = _alter_view_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "analyze":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _analyze_factor_plan(str(root))
        extension_plan = _analyze_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "begin":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _begin_factor_plan(str(root))
        extension_plan = _begin_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "call":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _call_factor_plan(str(root))
        extension_plan = _call_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "checkpoint":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _checkpoint_factor_plan(str(root))
        extension_plan = _checkpoint_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "cluster":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _cluster_factor_plan(str(root))
        extension_plan = _cluster_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "comment":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _comment_factor_plan(str(root))
        extension_plan = _comment_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "commit":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _commit_factor_plan(str(root))
        extension_plan = _commit_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "commit_prepared":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _commit_prepared_factor_plan(str(root))
        extension_plan = _commit_prepared_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "copy":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _copy_factor_plan(str(root))
        extension_plan = _copy_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_access_method":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_access_method_factor_plan(str(root))
        extension_plan = _create_access_method_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_aggregate":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_aggregate_factor_plan(str(root))
        extension_plan = _create_aggregate_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_cast":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_cast_factor_plan(str(root))
        extension_plan = _create_cast_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_collation":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_collation_factor_plan(str(root))
        extension_plan = _create_collation_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_conversion":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_conversion_factor_plan(str(root))
        extension_plan = _create_conversion_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_database":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_database_factor_plan(str(root))
        extension_plan = _create_database_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_domain":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_domain_factor_plan(str(root))
        extension_plan = _create_domain_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_event_trigger":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_event_trigger_factor_plan(str(root))
        extension_plan = _create_event_trigger_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_extension":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_extension_factor_plan(str(root))
        extension_plan = _create_extension_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_foreign_data_wrapper":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_foreign_data_wrapper_factor_plan(str(root))
        extension_plan = _create_foreign_data_wrapper_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_foreign_table":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_foreign_table_factor_plan(
            str(root)
        )
        extension_plan = _create_foreign_table_extension_plan(
            str(root)
        )
        document.update(
            {
                "generation_mode": (
                    "factor_value_independent_loop_v1"
                ),
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(
                    factor_plan.cases
                ),
                "extension_case_count": len(
                    extension_plan.cases
                ),
                "delegated_decision_count": len(
                    factor_plan.delegated
                ),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_function":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_function_factor_plan(str(root))
        extension_plan = _create_function_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_group":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_group_factor_plan(str(root))
        extension_plan = _create_group_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_index":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_index_factor_plan(str(root))
        extension_plan = _create_index_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_language":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_language_factor_plan(str(root))
        extension_plan = _create_language_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_materialized_view":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_materialized_view_factor_plan(str(root))
        extension_plan = _create_materialized_view_extension_plan(
            str(root)
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_operator":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_operator_factor_plan(str(root))
        extension_plan = _create_operator_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_operator_class":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_operator_class_factor_plan(
            str(root)
        )
        extension_plan = _create_operator_class_extension_plan(
            str(root)
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(
                    factor_plan.cases
                ),
                "extension_case_count": len(
                    extension_plan.cases
                ),
                "delegated_decision_count": len(
                    factor_plan.delegated
                ),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_operator_family":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_operator_family_factor_plan(str(root))
        extension_plan = _create_operator_family_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_policy":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_policy_factor_plan(str(root))
        extension_plan = _create_policy_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_procedure":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_procedure_factor_plan(str(root))
        extension_plan = _create_procedure_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_publication":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_publication_factor_plan(str(root))
        extension_plan = _create_publication_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_role":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_role_factor_plan(str(root))
        extension_plan = _create_role_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_rule":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_rule_factor_plan(str(root))
        extension_plan = _create_rule_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_schema":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_schema_factor_plan(str(root))
        extension_plan = _create_schema_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_sequence":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_sequence_factor_plan(str(root))
        extension_plan = _create_sequence_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_server":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_server_factor_plan(str(root))
        extension_plan = _create_server_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_statistics":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_statistics_factor_plan(str(root))
        extension_plan = _create_statistics_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_subscription":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_subscription_factor_plan(str(root))
        extension_plan = _create_subscription_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_table":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_table_factor_plan(str(root))
        extension_plan = _create_table_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_tablespace":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_tablespace_factor_plan(str(root))
        extension_plan = _create_tablespace_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_text_search_configuration":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = (
            _create_text_search_configuration_factor_plan(
                str(root)
            )
        )
        extension_plan = (
            _create_text_search_configuration_extension_plan(
                str(root)
            )
        )
        document.update(
            {
                "generation_mode": (
                    "factor_value_independent_loop_v1"
                ),
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(
                    factor_plan.cases
                ),
                "extension_case_count": len(
                    extension_plan.cases
                ),
                "delegated_decision_count": len(
                    factor_plan.delegated
                ),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_text_search_dictionary":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_text_search_dictionary_factor_plan(str(root))
        extension_plan = _create_text_search_dictionary_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_text_search_parser":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_text_search_parser_factor_plan(str(root))
        extension_plan = _create_text_search_parser_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_table_as":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_table_as_factor_plan(str(root))
        extension_plan = _create_table_as_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_text_search_template":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_text_search_template_factor_plan(str(root))
        extension_plan = _create_text_search_template_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_transform":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_transform_factor_plan(str(root))
        extension_plan = _create_transform_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_trigger":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_trigger_factor_plan(str(root))
        extension_plan = _create_trigger_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_type":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_type_factor_plan(str(root))
        extension_plan = _create_type_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_user":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_user_factor_plan(str(root))
        extension_plan = _create_user_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_user_mapping":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_user_mapping_factor_plan(str(root))
        extension_plan = _create_user_mapping_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "create_view":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _create_view_factor_plan(str(root))
        extension_plan = _create_view_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "deallocate":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _deallocate_factor_plan(str(root))
        extension_plan = _deallocate_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "delete":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _delete_factor_plan(str(root))
        extension_plan = _delete_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "discard":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _discard_factor_plan(str(root))
        extension_plan = _discard_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "do":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _do_factor_plan(str(root))
        extension_plan = _do_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_access_method":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_access_method_factor_plan(str(root))
        extension_plan = _drop_access_method_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_aggregate":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_aggregate_factor_plan(str(root))
        extension_plan = _drop_aggregate_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_cast":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_cast_factor_plan(str(root))
        extension_plan = _drop_cast_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_conversion":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_conversion_factor_plan(str(root))
        extension_plan = _drop_conversion_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_database":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_database_factor_plan(str(root))
        extension_plan = _drop_database_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_domain":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_domain_factor_plan(str(root))
        extension_plan = _drop_domain_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_event_trigger":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_event_trigger_factor_plan(str(root))
        extension_plan = _drop_event_trigger_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_extension":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_extension_factor_plan(str(root))
        extension_plan = _drop_extension_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_foreign_data_wrapper":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_foreign_data_wrapper_factor_plan(str(root))
        extension_plan = _drop_foreign_data_wrapper_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_foreign_table":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_foreign_table_factor_plan(str(root))
        extension_plan = _drop_foreign_table_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_function":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_function_factor_plan(str(root))
        extension_plan = _drop_function_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_group":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_group_factor_plan(str(root))
        extension_plan = _drop_group_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_index":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_index_factor_plan(str(root))
        extension_plan = _drop_index_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_language":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_language_factor_plan(str(root))
        extension_plan = _drop_language_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_materialized_view":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_materialized_view_factor_plan(str(root))
        extension_plan = _drop_materialized_view_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_operator":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_operator_factor_plan(str(root))
        extension_plan = _drop_operator_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_operator_class":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_operator_class_factor_plan(str(root))
        extension_plan = _drop_operator_class_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_operator_family":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_operator_family_factor_plan(str(root))
        extension_plan = _drop_operator_family_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_owned":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_owned_factor_plan(str(root))
        extension_plan = _drop_owned_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_policy":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_policy_factor_plan(str(root))
        extension_plan = _drop_policy_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_procedure":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_procedure_factor_plan(str(root))
        extension_plan = _drop_procedure_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_publication":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_publication_factor_plan(str(root))
        extension_plan = _drop_publication_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_role":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_role_factor_plan(str(root))
        extension_plan = _drop_role_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_routine":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_routine_factor_plan(str(root))
        extension_plan = _drop_routine_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_rule":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_rule_factor_plan(str(root))
        extension_plan = _drop_rule_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_schema":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_schema_factor_plan(str(root))
        extension_plan = _drop_schema_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_sequence":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_sequence_factor_plan(str(root))
        extension_plan = _drop_sequence_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_server":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_server_factor_plan(str(root))
        extension_plan = _drop_server_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_statistics":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_statistics_factor_plan(str(root))
        extension_plan = _drop_statistics_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_subscription":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_subscription_factor_plan(str(root))
        extension_plan = _drop_subscription_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_table":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_table_factor_plan(str(root))
        extension_plan = _drop_table_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_tablespace":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_tablespace_factor_plan(str(root))
        extension_plan = _drop_tablespace_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_text_search_configuration":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_text_search_configuration_factor_plan(str(root))
        extension_plan = _drop_text_search_configuration_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_text_search_dictionary":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_text_search_dictionary_factor_plan(str(root))
        extension_plan = _drop_text_search_dictionary_extension_plan(
            str(root)
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_text_search_parser":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_text_search_parser_factor_plan(str(root))
        extension_plan = _drop_text_search_parser_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_text_search_template":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_text_search_template_factor_plan(str(root))
        extension_plan = _drop_text_search_template_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_transform":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_transform_factor_plan(str(root))
        extension_plan = _drop_transform_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_trigger":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_trigger_factor_plan(str(root))
        extension_plan = _drop_trigger_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_type":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_type_factor_plan(str(root))
        extension_plan = _drop_type_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "drop_user":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_user_factor_plan(str(root))
        extension_plan = _drop_user_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )

    elif plan.statement_key == "alter_publication":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_publication_factor_plan(str(root))
        extension_plan = _alter_publication_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_sequence":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_sequence_factor_plan(str(root))
        extension_plan = _alter_sequence_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_table":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_table_factor_plan(str(root))
        extension_plan = _alter_table_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_large_object":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_large_object_factor_plan(str(root))
        extension_plan = _alter_large_object_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_operator":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_operator_factor_plan(str(root))
        extension_plan = _alter_operator_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_operator_class":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_operator_class_factor_plan(str(root))
        extension_plan = _alter_operator_class_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_operator_family":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_operator_family_factor_plan(str(root))
        extension_plan = _alter_operator_family_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_group":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_group_factor_plan(str(root))
        extension_plan = _alter_group_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_role":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_role_factor_plan(str(root))
        extension_plan = _alter_role_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "alter_index":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _alter_index_factor_plan(str(root))
        extension_plan = _alter_index_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    elif plan.statement_key == "drop_collation":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan = _drop_collation_factor_plan(str(root))
        extension_plan = _drop_collation_extension_plan(str(root))
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": len(factor_plan.obligations),
                "local_sql_decision_count": len(factor_plan.cases),
                "extension_case_count": len(extension_plan.cases),
                "delegated_decision_count": len(factor_plan.delegated),
                "obligation_multiset_sha256": (
                    factor_plan.obligation_multiset_sha256
                ),
            }
        )
    return document


def _alter_foreign_table_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_foreign_table_factor_validate import (
        validate_alter_foreign_table_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    factor_plan = _alter_foreign_table_factor_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in factor_plan.obligations
    ]
    cases = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [list(item) for item in row.baseline_assignments],
            "execution_profile": row.execution_profile,
        }
        for row in factor_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_foreign_table_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(factor_plan.obligations),
        "sql_file_count": len(factor_plan.cases),
        "delegated_count": len(factor_plan.delegated),
        "obligation_multiset_sha256": factor_plan.obligation_multiset_sha256,
        "obligations": obligations,
        "cases": cases,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_foreign_table_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(factor_plan.delegated),
        "owner_statement_key": "create_index",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_foreign_table_factor_programs(
        factor_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_function_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_function_factor_validate import (
        validate_alter_function_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_function_factor_plan(str(root))
    extension_plan = _alter_function_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_function_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_function_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_function",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_function_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_language_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_language_factor_validate import (
        validate_alter_language_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_language_factor_plan(str(root))
    extension_plan = _alter_language_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_language_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_language_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_language",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_language_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_large_object_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_large_object_factor_validate import (
        validate_alter_large_object_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_large_object_factor_plan(str(root))
    extension_plan = _alter_large_object_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_large_object_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_large_object_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        # Large objects are created via the lo_create() function, not a
        # CREATE LARGE OBJECT DDL; the handoff ledger is empty (no delegated
        # obligations), so this label is informational only.
        "owner_statement_key": "lo_create",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_large_object_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_operator_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_operator_factor_validate import (
        validate_alter_operator_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_operator_factor_plan(str(root))
    extension_plan = _alter_operator_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_operator_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_operator_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_operator",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_operator_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_operator_class_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_operator_class_factor_validate import (
        validate_alter_operator_class_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_operator_class_factor_plan(str(root))
    extension_plan = _alter_operator_class_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_operator_class_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_operator_class_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_operator_class",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_operator_class_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_materialized_view_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_materialized_view_factor_validate import (
        validate_alter_materialized_view_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_materialized_view_factor_plan(str(root))
    extension_plan = _alter_materialized_view_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_materialized_view_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_materialized_view_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_materialized_view",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_materialized_view_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_policy_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_policy_factor_validate import (
        validate_alter_policy_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_policy_factor_plan(str(root))
    extension_plan = _alter_policy_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_policy_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_policy_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_policy",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_policy_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_procedure_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_procedure_factor_validate import (
        validate_alter_procedure_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_procedure_factor_plan(str(root))
    extension_plan = _alter_procedure_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_procedure_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_procedure_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_procedure",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_procedure_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_routine_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_routine_factor_validate import (
        validate_alter_routine_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_routine_factor_plan(str(root))
    extension_plan = _alter_routine_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_routine_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_routine_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_routine",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_routine_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_rule_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_rule_factor_validate import (
        validate_alter_rule_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_rule_factor_plan(str(root))
    extension_plan = _alter_rule_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_rule_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_rule_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_rule",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_rule_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_schema_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_schema_factor_validate import (
        validate_alter_schema_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_schema_factor_plan(str(root))
    extension_plan = _alter_schema_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_schema_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_schema_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_schema",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_schema_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_server_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_server_factor_validate import (
        validate_alter_server_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_server_factor_plan(str(root))
    extension_plan = _alter_server_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_server_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_server_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_server",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_server_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _alter_statistics_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_statistics_factor_validate import (
        validate_alter_statistics_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_statistics_factor_plan(str(root))
    extension_plan = _alter_statistics_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": (
                row.derived_from_combination_group
            ),
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_statistics_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": (
            len(baseline_case_rows) + len(extension_case_rows)
        ),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": (
            baseline_plan.obligation_multiset_sha256
        ),
        "extension_multiset_sha256": (
            extension_plan.extension_multiset_sha256
        ),
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_statistics_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_statistics",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_statistics_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _alter_subscription_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_subscription_factor_validate import (
        validate_alter_subscription_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_subscription_factor_plan(str(root))
    extension_plan = _alter_subscription_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_subscription_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_subscription_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_subscription",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_subscription_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _alter_system_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_system_factor_validate import (
        validate_alter_system_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_system_factor_plan(str(root))
    extension_plan = _alter_system_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_system_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_system_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_system",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_system_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _alter_tablespace_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_tablespace_factor_validate import (
        validate_alter_tablespace_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_tablespace_factor_plan(str(root))
    extension_plan = _alter_tablespace_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_tablespace_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_tablespace_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_tablespace",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_tablespace_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _alter_text_search_configuration_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_text_search_configuration_factor_validate import (
        validate_alter_text_search_configuration_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_text_search_configuration_factor_plan(str(root))
    extension_plan = _alter_text_search_configuration_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_text_search_configuration_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_text_search_configuration_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_text_search_configuration",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_text_search_configuration_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _alter_text_search_dictionary_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_text_search_dictionary_factor_validate import (
        validate_alter_text_search_dictionary_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_text_search_dictionary_factor_plan(str(root))
    extension_plan = _alter_text_search_dictionary_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_text_search_dictionary_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_text_search_dictionary_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_text_search_dictionary",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_text_search_dictionary_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _alter_text_search_parser_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_text_search_parser_factor_validate import (
        validate_alter_text_search_parser_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_text_search_parser_factor_plan(str(root))
    extension_plan = _alter_text_search_parser_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_text_search_parser_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_text_search_parser_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_text_search_parser",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_text_search_parser_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _alter_text_search_template_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_text_search_template_factor_validate import (
        validate_alter_text_search_template_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_text_search_template_factor_plan(str(root))
    extension_plan = _alter_text_search_template_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_text_search_template_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_text_search_template_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_text_search_template",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_text_search_template_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _alter_trigger_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_trigger_factor_validate import (
        validate_alter_trigger_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_trigger_factor_plan(str(root))
    extension_plan = _alter_trigger_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_trigger_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_trigger_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_trigger",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_trigger_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _alter_type_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_type_factor_validate import (
        validate_alter_type_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_type_factor_plan(str(root))
    extension_plan = _alter_type_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_type_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_type_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_type",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_type_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _alter_user_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_user_factor_validate import (
        validate_alter_user_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_user_factor_plan(str(root))
    extension_plan = _alter_user_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_user_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_user_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_user",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_user_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _alter_user_mapping_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_user_mapping_factor_validate import (
        validate_alter_user_mapping_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_user_mapping_factor_plan(str(root))
    extension_plan = _alter_user_mapping_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_user_mapping_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_user_mapping_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_user_mapping",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_user_mapping_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _alter_view_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_view_factor_validate import (
        validate_alter_view_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_view_factor_plan(str(root))
    extension_plan = _alter_view_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_view_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_view_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_view",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_view_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _analyze_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .analyze_factor_validate import (
        validate_analyze_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _analyze_factor_plan(str(root))
    extension_plan = _analyze_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "analyze_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "analyze_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "analyze",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_analyze_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _begin_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .begin_factor_validate import (
        validate_begin_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _begin_factor_plan(str(root))
    extension_plan = _begin_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "begin_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "begin_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "begin",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_begin_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _call_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .call_factor_validate import (
        validate_call_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _call_factor_plan(str(root))
    extension_plan = _call_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "call_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "call_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "call",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_call_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _checkpoint_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .checkpoint_factor_validate import (
        validate_checkpoint_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _checkpoint_factor_plan(str(root))
    extension_plan = _checkpoint_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "checkpoint_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "checkpoint_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "checkpoint",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_checkpoint_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _cluster_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .cluster_factor_validate import (
        validate_cluster_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _cluster_factor_plan(str(root))
    extension_plan = _cluster_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "cluster_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "cluster_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "cluster",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_cluster_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _comment_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .comment_factor_validate import (
        validate_comment_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _comment_factor_plan(str(root))
    extension_plan = _comment_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "comment_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "comment_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "comment",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_comment_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _commit_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .commit_factor_validate import (
        validate_commit_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _commit_factor_plan(str(root))
    extension_plan = _commit_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "commit_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "commit_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "commit",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_commit_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _commit_prepared_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .commit_prepared_factor_validate import (
        validate_commit_prepared_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _commit_prepared_factor_plan(str(root))
    extension_plan = _commit_prepared_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "commit_prepared_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "commit_prepared_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "commit_prepared",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_commit_prepared_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _copy_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .copy_factor_validate import (
        validate_copy_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _copy_factor_plan(str(root))
    extension_plan = _copy_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "copy_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "copy_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "copy",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_copy_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_access_method_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_access_method_factor_validate import (
        validate_create_access_method_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_access_method_factor_plan(str(root))
    extension_plan = _create_access_method_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_access_method_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_access_method_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_access_method",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_access_method_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_aggregate_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_aggregate_factor_validate import (
        validate_create_aggregate_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_aggregate_factor_plan(str(root))
    extension_plan = _create_aggregate_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_aggregate_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_aggregate_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_aggregate",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_aggregate_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_cast_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_cast_factor_validate import (
        validate_create_cast_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_cast_factor_plan(str(root))
    extension_plan = _create_cast_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_cast_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_cast_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_cast",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_cast_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_collation_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_collation_factor_validate import (
        validate_create_collation_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_collation_factor_plan(str(root))
    extension_plan = _create_collation_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_collation_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_collation_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_collation",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_collation_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_conversion_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_conversion_factor_validate import (
        validate_create_conversion_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_conversion_factor_plan(str(root))
    extension_plan = _create_conversion_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_conversion_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_conversion_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_conversion",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_conversion_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_database_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_database_factor_validate import (
        validate_create_database_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_database_factor_plan(str(root))
    extension_plan = _create_database_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_database_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_database_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_database",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_database_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_domain_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_domain_factor_validate import (
        validate_create_domain_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_domain_factor_plan(str(root))
    extension_plan = _create_domain_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_domain_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_domain_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_domain",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_domain_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_event_trigger_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_event_trigger_factor_validate import (
        validate_create_event_trigger_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_event_trigger_factor_plan(str(root))
    extension_plan = _create_event_trigger_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_event_trigger_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_event_trigger_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_event_trigger",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_event_trigger_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_extension_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_extension_factor_validate import (
        validate_create_extension_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_extension_factor_plan(str(root))
    extension_plan = _create_extension_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_extension_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_extension_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_extension",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_extension_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_foreign_data_wrapper_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_foreign_data_wrapper_factor_validate import (
        validate_create_foreign_data_wrapper_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_foreign_data_wrapper_factor_plan(str(root))
    extension_plan = _create_foreign_data_wrapper_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_foreign_data_wrapper_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_foreign_data_wrapper_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_foreign_data_wrapper",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_foreign_data_wrapper_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_foreign_table_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_foreign_table_factor_validate import (
        validate_create_foreign_table_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_foreign_table_factor_plan(
        str(root)
    )
    extension_plan = _create_foreign_table_extension_plan(
        str(root)
    )
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": (
                row.delegated_statement_key
            ),
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": (
                row.expected_failure_reason
            ),
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": (
                row.derived_from_combination_group
            ),
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": (
                row.expected_failure_reason
            ),
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": (
            "create_foreign_table_factor_value_loop_plan"
        ),
        "generation_mode": (
            "factor_value_independent_loop_v1"
        ),
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": (
            len(baseline_case_rows) + len(extension_case_rows)
        ),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": (
            baseline_plan.obligation_multiset_sha256
        ),
        "extension_multiset_sha256": (
            extension_plan.extension_multiset_sha256
        ),
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": (
            "create_foreign_table_factor_handoff_ledger"
        ),
        "generation_mode": (
            "factor_value_independent_loop_v1"
        ),
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_foreign_table",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = (
        validate_create_foreign_table_factor_programs(
            baseline_plan,
            extension_plan,
            {
                name: payload
                for name, payload in output_files.items()
                if name.endswith(".sql")
            },
            root,
        ).to_dict()
    )
    return factor_plan_document, handoff_document, actual_report

def _create_function_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_function_factor_validate import (
        validate_create_function_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_function_factor_plan(str(root))
    extension_plan = _create_function_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_function_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_function_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_function",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_function_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_group_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_group_factor_validate import (
        validate_create_group_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_group_factor_plan(str(root))
    extension_plan = _create_group_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_group_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_group_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_group",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_group_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_index_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_index_factor_validate import (
        validate_create_index_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_index_factor_plan(str(root))
    extension_plan = _create_index_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_index_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_index_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_index",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_index_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_language_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_language_factor_validate import (
        validate_create_language_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_language_factor_plan(str(root))
    extension_plan = _create_language_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_language_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_language_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_language",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_language_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_materialized_view_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_materialized_view_factor_validate import (
        validate_create_materialized_view_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_materialized_view_factor_plan(str(root))
    extension_plan = _create_materialized_view_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": (
                row.derived_from_combination_group
            ),
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_materialized_view_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": (
            len(baseline_case_rows) + len(extension_case_rows)
        ),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": (
            baseline_plan.obligation_multiset_sha256
        ),
        "extension_multiset_sha256": (
            extension_plan.extension_multiset_sha256
        ),
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_materialized_view_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_materialized_view",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_materialized_view_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_operator_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_operator_factor_validate import (
        validate_create_operator_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_operator_factor_plan(str(root))
    extension_plan = _create_operator_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_operator_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_operator_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_operator",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_operator_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_operator_class_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_operator_class_factor_validate import (
        validate_create_operator_class_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_operator_class_factor_plan(
        str(root)
    )
    extension_plan = _create_operator_class_extension_plan(
        str(root)
    )
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_operator_class_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows)
        + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_operator_class_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_operator_class",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_operator_class_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_operator_family_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_operator_family_factor_validate import (
        validate_create_operator_family_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_operator_family_factor_plan(str(root))
    extension_plan = _create_operator_family_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_operator_family_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_operator_family_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_operator_family",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_operator_family_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_policy_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_policy_factor_validate import (
        validate_create_policy_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_policy_factor_plan(str(root))
    extension_plan = _create_policy_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_policy_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_policy_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_policy",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_policy_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_procedure_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_procedure_factor_validate import (
        validate_create_procedure_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_procedure_factor_plan(str(root))
    extension_plan = _create_procedure_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_procedure_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_procedure_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_procedure",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_procedure_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_publication_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_publication_factor_validate import (
        validate_create_publication_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_publication_factor_plan(str(root))
    extension_plan = _create_publication_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_publication_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_publication_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_publication",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_publication_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_role_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_role_factor_validate import (
        validate_create_role_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_role_factor_plan(str(root))
    extension_plan = _create_role_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_role_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_role_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_role",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_role_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_rule_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_rule_factor_validate import (
        validate_create_rule_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_rule_factor_plan(str(root))
    extension_plan = _create_rule_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_rule_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_rule_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_rule",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_rule_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_schema_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_schema_factor_validate import (
        validate_create_schema_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_schema_factor_plan(str(root))
    extension_plan = _create_schema_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_schema_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_schema_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_schema",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_schema_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_sequence_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_sequence_factor_validate import (
        validate_create_sequence_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_sequence_factor_plan(str(root))
    extension_plan = _create_sequence_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_sequence_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_sequence_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_sequence",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_sequence_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_server_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_server_factor_validate import (
        validate_create_server_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_server_factor_plan(str(root))
    extension_plan = _create_server_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_server_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_server_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_server",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_server_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_statistics_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_statistics_factor_validate import (
        validate_create_statistics_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_statistics_factor_plan(str(root))
    extension_plan = _create_statistics_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_statistics_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_statistics_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_statistics",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_statistics_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_subscription_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_subscription_factor_validate import (
        validate_create_subscription_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_subscription_factor_plan(str(root))
    extension_plan = _create_subscription_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_subscription_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_subscription_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_subscription",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_subscription_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_table_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_table_factor_validate import (
        validate_create_table_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_table_factor_plan(str(root))
    extension_plan = _create_table_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_table_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_table_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_table",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_table_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_tablespace_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_tablespace_factor_validate import (
        validate_create_tablespace_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_tablespace_factor_plan(str(root))
    extension_plan = _create_tablespace_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_tablespace_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_tablespace_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_tablespace",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_tablespace_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_text_search_configuration_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_text_search_configuration_factor_validate import (
        validate_create_text_search_configuration_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = (
        _create_text_search_configuration_factor_plan(str(root))
    )
    extension_plan = (
        _create_text_search_configuration_extension_plan(
            str(root)
        )
    )
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": (
                row.delegated_statement_key
            ),
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": (
                row.expected_failure_reason
            ),
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": (
                row.derived_from_combination_group
            ),
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": (
                row.expected_failure_reason
            ),
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": (
            "create_text_search_configuration_"
            "factor_value_loop_plan"
        ),
        "generation_mode": (
            "factor_value_independent_loop_v1"
        ),
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": (
            len(baseline_case_rows)
            + len(extension_case_rows)
        ),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": (
            baseline_plan.obligation_multiset_sha256
        ),
        "extension_multiset_sha256": (
            extension_plan.extension_multiset_sha256
        ),
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": (
            "create_text_search_configuration_"
            "factor_handoff_ledger"
        ),
        "generation_mode": (
            "factor_value_independent_loop_v1"
        ),
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": (
            "create_text_search_configuration"
        ),
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = (
        validate_create_text_search_configuration_factor_programs(
            baseline_plan,
            extension_plan,
            {
                name: payload
                for name, payload in output_files.items()
                if name.endswith(".sql")
            },
            root,
        ).to_dict()
    )
    return factor_plan_document, handoff_document, actual_report

def _create_text_search_dictionary_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_text_search_dictionary_factor_validate import (
        validate_create_text_search_dictionary_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_text_search_dictionary_factor_plan(str(root))
    extension_plan = _create_text_search_dictionary_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_text_search_dictionary_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_text_search_dictionary_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_text_search_dictionary",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_text_search_dictionary_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_text_search_parser_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_text_search_parser_factor_validate import (
        validate_create_text_search_parser_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_text_search_parser_factor_plan(str(root))
    extension_plan = _create_text_search_parser_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_text_search_parser_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_text_search_parser_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_text_search_parser",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_text_search_parser_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_table_as_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_table_as_factor_validate import (
        validate_create_table_as_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_table_as_factor_plan(str(root))
    extension_plan = _create_table_as_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_table_as_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_table_as_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_table_as",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_table_as_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_text_search_template_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_text_search_template_factor_validate import (
        validate_create_text_search_template_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_text_search_template_factor_plan(str(root))
    extension_plan = _create_text_search_template_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_text_search_template_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_text_search_template_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_text_search_template",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_text_search_template_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_transform_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_transform_factor_validate import (
        validate_create_transform_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_transform_factor_plan(str(root))
    extension_plan = _create_transform_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_transform_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_transform_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_transform",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_transform_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_trigger_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_trigger_factor_validate import (
        validate_create_trigger_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_trigger_factor_plan(str(root))
    extension_plan = _create_trigger_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_trigger_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_trigger_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_trigger",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_trigger_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_type_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_type_factor_validate import (
        validate_create_type_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_type_factor_plan(str(root))
    extension_plan = _create_type_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_type_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_type_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_type",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_type_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_user_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_user_factor_validate import (
        validate_create_user_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_user_factor_plan(str(root))
    extension_plan = _create_user_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_user_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_user_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_user",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_user_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_user_mapping_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_user_mapping_factor_validate import (
        validate_create_user_mapping_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_user_mapping_factor_plan(str(root))
    extension_plan = _create_user_mapping_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_user_mapping_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_user_mapping_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_user_mapping",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_user_mapping_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _create_view_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .create_view_factor_validate import (
        validate_create_view_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _create_view_factor_plan(str(root))
    extension_plan = _create_view_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "create_view_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "create_view_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_view",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_create_view_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _deallocate_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .deallocate_factor_validate import (
        validate_deallocate_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _deallocate_factor_plan(str(root))
    extension_plan = _deallocate_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "deallocate_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "deallocate_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "deallocate",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_deallocate_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _delete_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .delete_factor_validate import (
        validate_delete_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _delete_factor_plan(str(root))
    extension_plan = _delete_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "delete_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "delete_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "delete",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_delete_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _discard_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .discard_factor_validate import (
        validate_discard_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _discard_factor_plan(str(root))
    extension_plan = _discard_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "discard_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "discard_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "discard",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_discard_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _do_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .do_factor_validate import (
        validate_do_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _do_factor_plan(str(root))
    extension_plan = _do_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "do_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "do_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "do",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_do_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_access_method_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_access_method_factor_validate import (
        validate_drop_access_method_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_access_method_factor_plan(str(root))
    extension_plan = _drop_access_method_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_access_method_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_access_method_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_access_method",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_access_method_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_aggregate_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_aggregate_factor_validate import (
        validate_drop_aggregate_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_aggregate_factor_plan(str(root))
    extension_plan = _drop_aggregate_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_aggregate_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_aggregate_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_aggregate",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_aggregate_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_cast_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_cast_factor_validate import (
        validate_drop_cast_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_cast_factor_plan(str(root))
    extension_plan = _drop_cast_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_cast_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_cast_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_cast",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_cast_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_conversion_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_conversion_factor_validate import (
        validate_drop_conversion_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_conversion_factor_plan(str(root))
    extension_plan = _drop_conversion_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_conversion_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_conversion_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_conversion",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_conversion_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_database_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_database_factor_validate import (
        validate_drop_database_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_database_factor_plan(str(root))
    extension_plan = _drop_database_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_database_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_database_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_database",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_database_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_domain_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_domain_factor_validate import (
        validate_drop_domain_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_domain_factor_plan(str(root))
    extension_plan = _drop_domain_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_domain_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_domain_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_domain",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_domain_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_event_trigger_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_event_trigger_factor_validate import (
        validate_drop_event_trigger_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_event_trigger_factor_plan(str(root))
    extension_plan = _drop_event_trigger_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_event_trigger_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_event_trigger_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_event_trigger",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_event_trigger_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_extension_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_extension_factor_validate import (
        validate_drop_extension_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_extension_factor_plan(str(root))
    extension_plan = _drop_extension_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_extension_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_extension_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_extension",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_extension_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_foreign_data_wrapper_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_foreign_data_wrapper_factor_validate import (
        validate_drop_foreign_data_wrapper_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_foreign_data_wrapper_factor_plan(str(root))
    extension_plan = _drop_foreign_data_wrapper_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_foreign_data_wrapper_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_foreign_data_wrapper_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_foreign_data_wrapper",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_foreign_data_wrapper_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_foreign_table_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_foreign_table_factor_validate import (
        validate_drop_foreign_table_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_foreign_table_factor_plan(str(root))
    extension_plan = _drop_foreign_table_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_foreign_table_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_foreign_table_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_foreign_table",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_foreign_table_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_function_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_function_factor_validate import (
        validate_drop_function_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_function_factor_plan(str(root))
    extension_plan = _drop_function_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_function_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_function_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_function",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_function_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_group_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_group_factor_validate import (
        validate_drop_group_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_group_factor_plan(str(root))
    extension_plan = _drop_group_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_group_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_group_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_group",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_group_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_index_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_index_factor_validate import (
        validate_drop_index_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_index_factor_plan(str(root))
    extension_plan = _drop_index_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_index_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_index_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_index",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_index_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_language_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_language_factor_validate import (
        validate_drop_language_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_language_factor_plan(str(root))
    extension_plan = _drop_language_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_language_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_language_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_language",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_language_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_materialized_view_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_materialized_view_factor_validate import (
        validate_drop_materialized_view_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_materialized_view_factor_plan(str(root))
    extension_plan = _drop_materialized_view_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_materialized_view_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_materialized_view_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_materialized_view",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_materialized_view_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_operator_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_operator_factor_validate import (
        validate_drop_operator_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_operator_factor_plan(str(root))
    extension_plan = _drop_operator_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_operator_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_operator_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_operator",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_operator_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_operator_class_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_operator_class_factor_validate import (
        validate_drop_operator_class_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_operator_class_factor_plan(str(root))
    extension_plan = _drop_operator_class_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_operator_class_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_operator_class_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_operator_class",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_operator_class_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_operator_family_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_operator_family import (
        validate_drop_operator_family_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_operator_family_factor_plan(str(root))
    extension_plan = _drop_operator_family_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_operator_family_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_operator_family_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_operator_family",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_operator_family_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_owned_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_owned_factor_validate import (
        validate_drop_owned_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_owned_factor_plan(str(root))
    extension_plan = _drop_owned_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_owned_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_owned_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_owned",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_owned_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_policy_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_policy_factor_validate import (
        validate_drop_policy_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_policy_factor_plan(str(root))
    extension_plan = _drop_policy_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_policy_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_policy_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_policy",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_policy_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_procedure_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_procedure import (
        validate_drop_procedure_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_procedure_factor_plan(str(root))
    extension_plan = _drop_procedure_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_procedure_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_procedure_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_procedure",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_procedure_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_publication_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_publication import (
        validate_drop_publication_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_publication_factor_plan(str(root))
    extension_plan = _drop_publication_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_publication_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_publication_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_publication",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_publication_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_role_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_role import (
        validate_drop_role_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_role_factor_plan(str(root))
    extension_plan = _drop_role_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_role_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_role_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_role",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_role_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_routine_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_routine import (
        validate_drop_routine_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_routine_factor_plan(str(root))
    extension_plan = _drop_routine_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_routine_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_routine_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_routine",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_routine_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_rule_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_rule import (
        validate_drop_rule_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_rule_factor_plan(str(root))
    extension_plan = _drop_rule_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_rule_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_rule_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_rule",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_rule_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_schema_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_schema import (
        validate_drop_schema_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_schema_factor_plan(str(root))
    extension_plan = _drop_schema_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_schema_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_schema_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_schema",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_schema_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_sequence_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_sequence import (
        validate_drop_sequence_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_sequence_factor_plan(str(root))
    extension_plan = _drop_sequence_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_sequence_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_sequence_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_sequence",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_sequence_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_server_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_server import (
        validate_drop_server_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_server_factor_plan(str(root))
    extension_plan = _drop_server_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_server_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_server_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_server",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_server_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_statistics_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_statistics import (
        validate_drop_statistics_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_statistics_factor_plan(str(root))
    extension_plan = _drop_statistics_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_statistics_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_statistics_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_statistics",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_statistics_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_subscription_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_subscription import (
        validate_drop_subscription_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_subscription_factor_plan(str(root))
    extension_plan = _drop_subscription_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_subscription_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_subscription_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_subscription",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_subscription_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_table_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_table import (
        validate_drop_table_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_table_factor_plan(str(root))
    extension_plan = _drop_table_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_table_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_table_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_table",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_table_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_tablespace_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_tablespace import (
        validate_drop_tablespace_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_tablespace_factor_plan(str(root))
    extension_plan = _drop_tablespace_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_tablespace_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_tablespace_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_tablespace",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_tablespace_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_text_search_configuration_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_text_search_configuration import (
        validate_drop_text_search_configuration_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_text_search_configuration_factor_plan(str(root))
    extension_plan = _drop_text_search_configuration_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_text_search_configuration_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_text_search_configuration_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_text_search_configuration",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_text_search_configuration_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_text_search_dictionary_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_text_search_dictionary import (
        validate_drop_text_search_dictionary_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_text_search_dictionary_factor_plan(str(root))
    extension_plan = _drop_text_search_dictionary_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_text_search_dictionary_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_text_search_dictionary_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_text_search_dictionary",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_text_search_dictionary_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_text_search_parser_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_text_search_parser import (
        validate_drop_text_search_parser_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_text_search_parser_factor_plan(str(root))
    extension_plan = _drop_text_search_parser_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_text_search_parser_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_text_search_parser_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_text_search_parser",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_text_search_parser_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_text_search_template_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_text_search_template import (
        validate_drop_text_search_template_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_text_search_template_factor_plan(str(root))
    extension_plan = _drop_text_search_template_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_text_search_template_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_text_search_template_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_text_search_template",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_text_search_template_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_transform_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_transform import (
        validate_drop_transform_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_transform_factor_plan(str(root))
    extension_plan = _drop_transform_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_transform_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_transform_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_transform",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_transform_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_trigger_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_trigger import (
        validate_drop_trigger_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_trigger_factor_plan(str(root))
    extension_plan = _drop_trigger_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_trigger_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_trigger_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_trigger",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_trigger_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_type_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_type import (
        validate_drop_type_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_type_factor_plan(str(root))
    extension_plan = _drop_type_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_type_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_type_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_type",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_type_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _drop_user_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .validate_drop_user import (
        validate_drop_user_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_user_factor_plan(str(root))
    extension_plan = _drop_user_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_user_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_user_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "drop_user",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_user_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report

def _alter_publication_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_publication_factor_validate import (
        validate_alter_publication_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_publication_factor_plan(str(root))
    extension_plan = _alter_publication_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_publication_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_publication_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_publication",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_publication_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_sequence_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_sequence_factor_validate import (
        validate_alter_sequence_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_sequence_factor_plan(str(root))
    extension_plan = _alter_sequence_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_sequence_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_sequence_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_sequence",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_sequence_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_table_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_table_factor_validate import (
        validate_alter_table_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_table_factor_plan(str(root))
    extension_plan = _alter_table_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_table_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_table_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_table",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_table_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_group_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_group_factor_validate import (
        validate_alter_group_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_group_factor_plan(str(root))
    extension_plan = _alter_group_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_group_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_group_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_role",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_group_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_role_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_role_factor_validate import (
        validate_alter_role_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_role_factor_plan(str(root))
    extension_plan = _alter_role_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_role_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_role_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "alter_role",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_role_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _alter_index_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .alter_index_factor_validate import (
        validate_alter_index_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _alter_index_factor_plan(str(root))
    extension_plan = _alter_index_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "alter_index_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "alter_index_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "owner_statement_key": "create_index",
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_alter_index_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _drop_collation_factor_documents(
    repository_root: Path,
    output_files: Mapping[str, bytes],
) -> tuple[dict[str, Any], dict[str, Any], dict[str, Any]]:
    from .drop_collation_factor_validate import (
        validate_drop_collation_factor_programs,
    )

    root = Path(repository_root).resolve(strict=True)
    baseline_plan = _drop_collation_factor_plan(str(root))
    extension_plan = _drop_collation_extension_plan(str(root))
    obligations = [
        {
            "ordinal": row.ordinal,
            "obligation_id": row.obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "value": row.value,
            "consumer_action_id": row.consumer_action_id,
            "disposition": row.disposition,
            "source_locator": row.source_locator,
            "delegated_statement_key": row.delegated_statement_key,
        }
        for row in baseline_plan.obligations
    ]
    baseline_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "primary_obligation_id": row.primary_obligation_id,
            "kind": row.kind,
            "factor_key": row.factor_key,
            "factor_value": row.factor_value,
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
            "baseline_assignments": [
                list(item) for item in row.baseline_assignments
            ],
            "execution_profile": row.execution_profile,
        }
        for row in baseline_plan.cases
    ]
    extension_case_rows = [
        {
            "ordinal": row.ordinal,
            "case_id": row.case_id,
            "sql_filename": row.sql_filename,
            "object_prefix": row.object_prefix,
            "derivation_id": row.derivation_id,
            "derived_from_combination_group": row.derived_from_combination_group,
            "derivation_reason": row.derivation_reason,
            "factor_assignment": [
                list(item) for item in row.factor_assignment
            ],
            "consumer_action_id": row.consumer_action_id,
            "outcome": row.outcome,
            "expected_sqlstate": row.expected_sqlstate,
            "expected_failure_reason": row.expected_failure_reason,
        }
        for row in extension_plan.cases
    ]
    factor_plan_document = {
        "schema_version": 1,
        "kind": "drop_collation_factor_value_loop_plan",
        "generation_mode": "factor_value_independent_loop_v1",
        "decision_count": len(baseline_plan.obligations),
        "sql_file_count": len(baseline_case_rows) + len(extension_case_rows),
        "delegated_count": len(baseline_plan.delegated),
        "obligation_multiset_sha256": baseline_plan.obligation_multiset_sha256,
        "extension_multiset_sha256": extension_plan.extension_multiset_sha256,
        "obligations": obligations,
        "cases": baseline_case_rows + extension_case_rows,
    }
    handoff_document = {
        "schema_version": 1,
        "kind": "drop_collation_factor_handoff_ledger",
        "generation_mode": "factor_value_independent_loop_v1",
        "delegated_count": len(baseline_plan.delegated),
        "decisions": [
            row
            for row in obligations
            if row["disposition"] == "delegated"
        ],
    }
    actual_report = validate_drop_collation_factor_programs(
        baseline_plan,
        extension_plan,
        {
            name: payload
            for name, payload in output_files.items()
            if name.endswith(".sql")
        },
        root,
    ).to_dict()
    return factor_plan_document, handoff_document, actual_report


def _package_document(
    plan: StatementRegressPlan,
    output_files: Mapping[str, bytes],
    repository_root: Path | None = None,
) -> dict[str, Any]:
    sql_hashes = {
        name: _sha256(payload)
        for name, payload in sorted(output_files.items())
        if name.endswith(".sql")
    }
    document: dict[str, Any] = {
        "schema_version": 1,
        "kind": "statement_factor_regress_package",
        "statement_key": plan.statement_key,
        "cycle_fingerprint": plan.cycle_fingerprint,
        "input_sha256": {
            "matrix": plan.matrix_sha256,
            "reference": plan.reference_sha256,
            "applicability_universe_semantic": plan.universe_semantic_sha256,
        },
        "renderer": {
            "id": f"explicit-{plan.statement_key.replace('_', '-')}-pg18-v1",
            "version": 1,
            "generic_fallback": False,
        },
        "case_count": len(plan.cases),
        "sql_file_count": len(plan.cases),
        "serial_case_count": len(plan.serial_cases),
        "external_case_count": len(plan.external_cases),
        "factor_value_count": len(plan.factor_decisions),
        "case_group_counts": plan.case_group_counts,
        "factor_disposition_counts": plan.factor_disposition_counts,
        "sql_sha256": sql_hashes,
        "schedule_sha256": {
            "serial_schedule": _sha256(output_files["serial_schedule"]),
            "external_schedule": _sha256(output_files["external_schedule"]),
        },
        "runtime_status": "not_run_static_sql_only",
    }
    if plan.statement_key == "abort":
        document["official_syntax_product"] = {
            "optional_keyword": 3,
            "chain_clause": 3,
            "transaction_state": 4,
            "expected_product": 36,
            "actual_product": plan.case_group_counts["legal_syntax_state_product"],
        }
        document["characteristics_product"] = {
            "isolation": 4,
            "access_mode": 2,
            "deferrability": 2,
            "expected_product": 16,
            "actual_product": plan.case_group_counts[
                "chain_characteristics_product"
            ],
        }
    elif plan.statement_key == "alter_aggregate":
        document["official_syntax_products"] = {
            "success_rename": {
                "signature_binding": 14,
                "aggregate_name_shape": 3,
                "new_name_shape": 3,
                "expected_product": 126,
                "actual_product": plan.case_group_counts["success_rename_product"],
            },
            "success_owner": {
                "signature_binding": 14,
                "aggregate_name_shape": 3,
                "owner_target": 4,
                "expected_product": 168,
                "actual_product": plan.case_group_counts["success_owner_product"],
            },
            "success_set_schema": {
                "signature_binding": 14,
                "aggregate_name_shape": 3,
                "expected_product": 42,
                "actual_product": plan.case_group_counts["success_set_schema_product"],
            },
            "branch_state_failures": {
                "expected_product": 336,
                "actual_product": (
                    plan.case_group_counts["failure_rename_conflict_product"]
                    + plan.case_group_counts["failure_owner_unavailable_product"]
                    + plan.case_group_counts["failure_set_schema_state_product"]
                ),
            },
        }
    elif plan.statement_key == "alter_collation":
        document["official_syntax_products"] = {
            "success_main_products": {
                "expected_product": 81,
                "actual_product": sum(
                    plan.case_group_counts[name]
                    for name in (
                        "success_refresh_product",
                        "success_rename_product",
                        "success_owner_product",
                        "success_set_schema_product",
                    )
                ),
            },
            "branch_state_failures": {
                "expected_product": 33,
                "actual_product": sum(
                    plan.case_group_counts[name]
                    for name in (
                        "missing_object_product",
                        "rename_duplicate_product",
                        "owner_missing_role_product",
                        "set_schema_missing_product",
                        "set_schema_conflict_product",
                    )
                ),
            },
            "privilege_truth_tables": {
                "expected_product": 27,
                "actual_product": sum(
                    plan.case_group_counts[name]
                    for name in (
                        "refresh_privilege_truth_product",
                        "rename_privilege_truth_product",
                        "owner_privilege_truth_product",
                        "set_schema_privilege_truth_product",
                    )
                ),
            },
            "suite_total": {
                "expected_product": 174,
                "actual_product": len(plan.cases),
            },
        }
    elif plan.statement_key == "alter_conversion":
        document["official_syntax_products"] = {
            "success_main_products": {
                "expected_product": 102,
                "actual_product": sum(
                    plan.case_group_counts[name]
                    for name in (
                        "success_rename_product",
                        "success_owner_product",
                        "success_set_schema_product",
                    )
                ),
            },
            "branch_state_products": {
                "expected_product": 31,
                "actual_product": sum(
                    plan.case_group_counts[name]
                    for name in (
                        "missing_conversion_product",
                        "rename_conflict_product",
                        "owner_missing_role_product",
                        "set_schema_missing_product",
                        "set_schema_conflict_product",
                        "default_pair_move_product",
                    )
                ),
            },
            "privilege_truth_tables": {
                "expected_product": 30,
                "actual_product": sum(
                    plan.case_group_counts[name]
                    for name in (
                        "rename_privilege_truth_product",
                        "owner_privilege_truth_product",
                        "set_schema_privilege_truth_product",
                    )
                ),
            },
            "suite_total": {
                "expected_product": 216,
                "actual_product": len(plan.cases),
            },
        }
    elif plan.statement_key == "alter_foreign_table":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_foreign_table_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_function":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_function_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_language":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_language_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_materialized_view":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_materialized_view_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_policy":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_policy_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_procedure":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_procedure_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_routine":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_routine_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_rule":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_rule_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_schema":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_schema_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_server":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_server_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "alter_statistics":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = (
            _alter_statistics_factor_documents(root, output_files)
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(
                    _json_bytes(factor_plan)
                ),
                "handoff_ledger_sha256": _sha256(
                    _json_bytes(handoff)
                ),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "alter_subscription":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_subscription_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "alter_system":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_system_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "alter_tablespace":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_tablespace_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "alter_text_search_configuration":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_text_search_configuration_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "alter_text_search_dictionary":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_text_search_dictionary_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "alter_text_search_parser":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_text_search_parser_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "alter_text_search_template":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_text_search_template_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "alter_trigger":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_trigger_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "alter_type":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_type_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "alter_user":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_user_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "alter_user_mapping":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_user_mapping_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "alter_view":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_view_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "analyze":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _analyze_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "begin":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _begin_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "call":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _call_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "checkpoint":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _checkpoint_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "cluster":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _cluster_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "comment":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _comment_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "commit":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _commit_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "commit_prepared":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _commit_prepared_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "copy":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _copy_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_access_method":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_access_method_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_aggregate":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_aggregate_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_cast":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_cast_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_collation":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_collation_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_conversion":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_conversion_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_database":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_database_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_domain":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_domain_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_event_trigger":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_event_trigger_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_extension":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_extension_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_foreign_data_wrapper":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_foreign_data_wrapper_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_foreign_table":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = (
            _create_foreign_table_factor_documents(
                root,
                output_files,
            )
        )
        document.update(
            {
                "generation_mode": (
                    "factor_value_independent_loop_v1"
                ),
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(
                    _json_bytes(factor_plan)
                ),
                "handoff_ledger_sha256": _sha256(
                    _json_bytes(handoff)
                ),
                "actual_factor_witness_report_sha256": (
                    _sha256(_json_bytes(actual))
                ),
                "actual_factor_witness_passed": actual[
                    "passed"
                ],
            }
        )

    elif plan.statement_key == "create_function":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_function_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_group":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_group_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_index":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_index_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_language":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_language_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_materialized_view":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = (
            _create_materialized_view_factor_documents(root, output_files)
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(
                    _json_bytes(factor_plan)
                ),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_operator":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_operator_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_operator_class":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = (
            _create_operator_class_factor_documents(
                root, output_files
            )
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(
                    _json_bytes(factor_plan)
                ),
                "handoff_ledger_sha256": _sha256(
                    _json_bytes(handoff)
                ),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_operator_family":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = (
            _create_operator_family_factor_documents(
                root,
                output_files,
            )
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_policy":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_policy_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_procedure":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_procedure_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_publication":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_publication_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_role":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_role_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_rule":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_rule_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_schema":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_schema_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_sequence":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_sequence_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_server":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_server_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_statistics":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_statistics_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_subscription":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_subscription_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_table":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_table_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_tablespace":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_tablespace_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_text_search_configuration":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = (
            _create_text_search_configuration_factor_documents(
                root,
                output_files,
            )
        )
        document.update(
            {
                "generation_mode": (
                    "factor_value_independent_loop_v1"
                ),
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(
                    _json_bytes(factor_plan)
                ),
                "handoff_ledger_sha256": _sha256(
                    _json_bytes(handoff)
                ),
                "actual_factor_witness_report_sha256": (
                    _sha256(_json_bytes(actual))
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_text_search_dictionary":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_text_search_dictionary_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_text_search_parser":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_text_search_parser_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_table_as":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_table_as_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_text_search_template":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_text_search_template_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_transform":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_transform_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_trigger":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_trigger_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_type":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_type_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_user":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_user_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_user_mapping":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_user_mapping_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "create_view":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _create_view_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "deallocate":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _deallocate_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "delete":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _delete_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "discard":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _discard_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "do":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _do_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_access_method":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_access_method_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_aggregate":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_aggregate_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_cast":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_cast_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_conversion":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_conversion_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_database":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_database_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_domain":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_domain_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_event_trigger":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_event_trigger_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_extension":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_extension_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_foreign_data_wrapper":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_foreign_data_wrapper_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_foreign_table":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_foreign_table_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_function":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_function_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_group":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_group_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_index":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_index_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_language":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_language_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_materialized_view":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_materialized_view_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_operator":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_operator_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_operator_class":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_operator_class_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_operator_family":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_operator_family_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_owned":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_owned_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_policy":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_policy_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_procedure":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_procedure_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_publication":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_publication_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_role":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_role_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_routine":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_routine_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_rule":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_rule_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_schema":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_schema_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_sequence":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_sequence_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_server":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_server_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_statistics":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_statistics_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_subscription":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_subscription_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_table":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_table_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_tablespace":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_tablespace_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_text_search_configuration":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_text_search_configuration_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_text_search_dictionary":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_text_search_dictionary_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_text_search_parser":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_text_search_parser_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_text_search_template":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_text_search_template_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_transform":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_transform_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_trigger":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_trigger_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_type":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_type_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "drop_user":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_user_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )

    elif plan.statement_key == "alter_publication":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_publication_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_sequence":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_sequence_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_table":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_table_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_large_object":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_large_object_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_operator":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_operator_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_operator_class":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_operator_class_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_operator_family":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_operator_family_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_group":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_group_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_role":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_role_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "alter_index":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _alter_index_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    elif plan.statement_key == "drop_collation":
        root = (
            Path(repository_root).resolve(strict=True)
            if repository_root is not None
            else Path(__file__).resolve().parents[2]
        )
        factor_plan, handoff, actual = _drop_collation_factor_documents(
            root,
            output_files,
        )
        document.update(
            {
                "generation_mode": "factor_value_independent_loop_v1",
                "decision_count": factor_plan["decision_count"],
                "delegated_count": handoff["delegated_count"],
                "obligation_multiset_sha256": factor_plan[
                    "obligation_multiset_sha256"
                ],
                "extension_multiset_sha256": factor_plan[
                    "extension_multiset_sha256"
                ],
                "factor_loop_plan_sha256": _sha256(_json_bytes(factor_plan)),
                "handoff_ledger_sha256": _sha256(_json_bytes(handoff)),
                "actual_factor_witness_report_sha256": _sha256(
                    _json_bytes(actual)
                ),
                "actual_factor_witness_passed": actual["passed"],
            }
        )
    return document


def _validate_rendered_output(
    plan: StatementRegressPlan,
    output_dir: Path,
    repository_root: Path,
    package: Mapping[str, Any] | None = None,
) -> StatementRegressValidation:
    issues: list[str] = []
    expected_names = {case.sql_filename for case in plan.cases}
    actual_paths = sorted(output_dir.glob(f"{plan.file_prefix}*.sql"))
    actual_names = {path.name for path in actual_paths}
    if actual_names != expected_names:
        missing = sorted(expected_names - actual_names)
        unexpected = sorted(actual_names - expected_names)
        if missing:
            issues.append("missing SQL files: " + ", ".join(missing))
        if unexpected:
            issues.append("unexpected SQL files: " + ", ".join(unexpected))

    case_by_filename = {case.sql_filename: case for case in plan.cases}
    sql_hashes: dict[str, str] = {}
    target_patterns = {
        "abort": r"(?m)^ABORT(?:\s|;)",
        "alter_aggregate": r"(?m)^ALTER\s+AGGREGATE(?:\s|;)",
        "alter_collation": r"(?m)^ALTER\s+COLLATION(?:\s|;)",
        "alter_conversion": r"(?m)^ALTER\s+CONVERSION(?:\s|;)",
        "alter_database": r"(?m)^ALTER\s+DATABASE(?:\s|;)",
        "alter_default_privileges": (
            r"(?m)^ALTER\s+DEFAULT\s+PRIVILEGES(?:\s|;|$)"
        ),
        "alter_domain": r"(?m)^ALTER\s+DOMAIN(?:\s|;|$)",
        "alter_event_trigger": r"(?m)^ALTER\s+EVENT\s+TRIGGER(?:\s|;|$)",
        "alter_extension": r"(?m)^ALTER\s+EXTENSION(?:\s|;|$)",
        "alter_foreign_data_wrapper": (
            r"(?m)^ALTER\s+FOREIGN\s+DATA\s+WRAPPER(?:\s|;|$)"
        ),
        "alter_foreign_table": (
            r"(?m)^ALTER\s+FOREIGN\s+TABLE(?:\s|;|$)"
        ),
        "alter_function": (
            r"(?m)^ALTER\s+FUNCTION(?:\s|;|$)"
        ),
        "alter_language": (
            r"(?m)^ALTER\s+LANGUAGE(?:\s|;|$)"
        ),
        "alter_large_object": (
            r"(?m)^ALTER\s+LARGE\s+OBJECT(?:\s|;|$)"
        ),
        "alter_materialized_view": (
            r"(?m)^ALTER\s+MATERIALIZED\s+VIEW(?:\s|;|$)"
        ),
        "alter_operator": (
            r"(?m)^ALTER\s+OPERATOR(?:\s|;|$)"
        ),
        "alter_operator_class": (
            r"(?m)^ALTER\s+OPERATOR\s+CLASS(?:\s|;|$)"
        ),
        "alter_operator_family": (
            r"(?m)^ALTER\s+OPERATOR\s+FAMILY(?:\s|;|$)"
        ),
        "alter_policy": (
            r"(?m)^ALTER\s+POLICY(?:\s|;|$)"
        ),
        "alter_procedure": (
            r"(?m)^ALTER\s+PROCEDURE(?:\s|;|$)"
        ),
        "alter_routine": (
            r"(?m)^ALTER\s+ROUTINE(?:\s|;|$)"
        ),
        "alter_rule": (
            r"(?m)^ALTER\s+RULE(?:\s|;|$)"
        ),
        "alter_schema": (
            r"(?m)^ALTER\s+SCHEMA(?:\s|;|$)"
        ),
        "alter_publication": (
            r"(?m)^ALTER\s+PUBLICATION(?:\s|;|$)"
        ),
        "alter_sequence": (
            r"(?m)^ALTER\s+SEQUENCE(?:\s|;|$)"
        ),
        "alter_table": (
            r"(?m)^ALTER\s+TABLE(?:\s|;|$)"
        ),
        "alter_group": r"(?m)^ALTER\s+GROUP(?:\s|;|$)",
        "alter_role": r"(?m)^ALTER\s+ROLE(?:\s|;|$)",
        "alter_index": r"(?m)^ALTER\s+INDEX(?:\s|;|$)",
        "alter_server": r"(?m)^ALTER\s+SERVER(?:\s|;|$)",
        "alter_statistics": r"(?m)^ALTER\s+STATISTICS(?:\s|;|$)",
        "alter_subscription": r"(?m)^ALTER\s+SUBSCRIPTION(?:\s|;|$)",
        "alter_system": r"(?m)^ALTER\s+SYSTEM(?:\s|;|$)",
        "drop_user": r"(?m)^DROP\s+USER(?:\s|;|$)",
        "drop_type": r"(?m)^DROP\s+TYPE(?:\s|;|$)",
        "drop_trigger": r"(?m)^DROP\s+TRIGGER(?:\s|;|$)",
        "drop_transform": r"(?m)^DROP\s+TRANSFORM(?:\s|;|$)",
        "drop_text_search_template": r"(?m)^DROP\s+TEXT\s+SEARCH\s+TEMPLATE(?:\s|;|$)",
        "drop_text_search_parser": r"(?m)^DROP\s+TEXT\s+SEARCH\s+PARSER(?:\s|;|$)",
        "drop_text_search_dictionary": r"(?m)^DROP\s+TEXT\s+SEARCH\s+DICTIONARY(?:\s|;|$)",
        "drop_text_search_configuration": r"(?m)^DROP\s+TEXT\s+SEARCH\s+CONFIGURATION(?:\s|;|$)",
        "drop_tablespace": r"(?m)^DROP\s+TABLESPACE(?:\s|;|$)",
        "drop_table": r"(?m)^DROP\s+TABLE(?:\s|;|$)",
        "drop_subscription": r"(?m)^DROP\s+SUBSCRIPTION(?:\s|;|$)",
        "drop_statistics": r"(?m)^DROP\s+STATISTICS(?:\s|;|$)",
        "drop_server": r"(?m)^DROP\s+SERVER(?:\s|;|$)",
        "drop_sequence": r"(?m)^DROP\s+SEQUENCE(?:\s|;|$)",
        "drop_schema": r"(?m)^DROP\s+SCHEMA(?:\s|;|$)",
        "drop_rule": r"(?m)^DROP\s+RULE(?:\s|;|$)",
        "drop_routine": r"(?m)^DROP\s+ROUTINE(?:\s|;|$)",
        "drop_role": r"(?m)^DROP\s+ROLE(?:\s|;|$)",
        "drop_publication": r"(?m)^DROP\s+PUBLICATION(?:\s|;|$)",
        "drop_procedure": r"(?m)^DROP\s+PROCEDURE(?:\s|;|$)",
        "drop_policy": r"(?m)^DROP\s+POLICY(?:\s|;|$)",
        "drop_owned": r"(?m)^DROP\s+OWNED(?:\s|;|$)",
        "drop_operator_family": r"(?m)^DROP\s+OPERATOR\s+FAMILY(?:\s|;|$)",
        "drop_operator_class": r"(?m)^DROP\s+OPERATOR\s+CLASS(?:\s|;|$)",
        "drop_operator": r"(?m)^DROP\s+OPERATOR(?:\s|;|$)",
        "drop_materialized_view": r"(?m)^DROP\s+MATERIALIZED\s+VIEW(?:\s|;|$)",
        "drop_language": r"(?m)^DROP\s+LANGUAGE(?:\s|;|$)",
        "drop_index": r"(?m)^DROP\s+INDEX(?:\s|;|$)",
        "drop_group": r"(?m)^DROP\s+GROUP(?:\s|;|$)",
        "drop_function": r"(?m)^DROP\s+FUNCTION(?:\s|;|$)",
        "drop_foreign_table": r"(?m)^DROP\s+FOREIGN\s+TABLE(?:\s|;|$)",
        "drop_foreign_data_wrapper": r"(?m)^DROP\s+FOREIGN\s+DATA\s+WRAPPER(?:\s|;|$)",
        "drop_extension": r"(?m)^DROP\s+EXTENSION(?:\s|;|$)",
        "drop_event_trigger": r"(?m)^DROP\s+EVENT\s+TRIGGER(?:\s|;|$)",
        "drop_domain": r"(?m)^DROP\s+DOMAIN(?:\s|;|$)",
        "drop_database": r"(?m)^DROP\s+DATABASE(?:\s|;|$)",
        "drop_conversion": r"(?m)^DROP\s+CONVERSION(?:\s|;|$)",
        "drop_cast": r"(?m)^DROP\s+CAST(?:\s|;|$)",
        "drop_aggregate": r"(?m)^DROP\s+AGGREGATE(?:\s|;|$)",
        "drop_access_method": r"(?m)^DROP\s+ACCESS\s+METHOD(?:\s|;|$)",
        "do": r"(?m)^DO(?:\s|;|$)",
        "discard": r"(?m)^DISCARD(?:\s|;|$)",
        "delete": r"(?m)^DELETE(?:\s|;|$)",
        "deallocate": r"(?m)^DEALLOCATE(?:\s|;|$)",
        "create_view": r"(?m)^CREATE\s+(?:OR\s+REPLACE\s+)?(?:TEMP(?:ORARY)?\s+)?(?:RECURSIVE\s+)?VIEW(?:\s|;|$)",
        "create_user_mapping": r"(?m)^CREATE\s+USER\s+MAPPING(?:\s|;|$)",
        "create_user": r"(?m)^CREATE\s+USER(?:\s|;|$)",
        "create_type": r"(?m)^CREATE\s+TYPE(?:\s|;|$)",
        "create_trigger": r"(?m)^CREATE\s+(?:OR\s+REPLACE\s+)?(?:CONSTRAINT\s+)?TRIGGER(?:\s|;|$)",
        "create_transform": r"(?m)^CREATE\s+(?:OR\s+REPLACE\s+)?TRANSFORM(?:\s|;|$)",
        "create_text_search_template": r"(?m)^CREATE\s+TEXT\s+SEARCH\s+TEMPLATE(?:\s|;|$)",
        "create_table_as": r"(?m)^CREATE\s+(?:(?:GLOBAL|LOCAL)\s+)?(?:TEMP(?:ORARY)?\s+)?(?:UNLOGGED\s+)?TABLE(?:\s|;|$)",
        "create_text_search_parser": r"(?m)^CREATE\s+TEXT\s+SEARCH\s+PARSER(?:\s|;|$)",
        "create_text_search_dictionary": r"(?m)^CREATE\s+TEXT\s+SEARCH\s+DICTIONARY(?:\s|;|$)",
        "create_text_search_configuration": r"(?m)^CREATE\s+TEXT\s+SEARCH\s+CONFIGURATION(?:\s|;|$)",
        "create_tablespace": r"(?m)^CREATE\s+TABLESPACE(?:\s|;|$)",
        "create_table": r"(?m)^CREATE\s+(?:(?:GLOBAL|LOCAL)\s+)?(?:TEMP(?:ORARY)?\s+)?(?:UNLOGGED\s+)?TABLE(?:\s|;|$)",
        "create_subscription": r"(?m)^CREATE\s+SUBSCRIPTION(?:\s|;|$)",
        "create_statistics": r"(?m)^CREATE\s+STATISTICS(?:\s|;|$)",
        "create_server": r"(?m)^CREATE\s+SERVER(?:\s|;|$)",
        "create_sequence": r"(?m)^CREATE\s+(?:(?:TEMP(?:ORARY)?|UNLOGGED)\s+)*SEQUENCE(?:\s|;|$)",
        "create_schema": r"(?m)^CREATE\s+SCHEMA(?:\s|;|$)",
        "create_rule": r"(?m)^CREATE\s+(?:OR\s+REPLACE\s+)?RULE(?:\s|;|$)",
        "create_role": r"(?m)^CREATE\s+ROLE(?:\s|;|$)",
        "create_publication": r"(?m)^CREATE\s+PUBLICATION(?:\s|;|$)",
        "create_procedure": r"(?m)^CREATE\s+(?:OR\s+REPLACE\s+)?PROCEDURE(?:\s|;|$)",
        "create_policy": r"(?m)^CREATE\s+POLICY(?:\s|;|$)",
        "create_operator_family": r"(?m)^CREATE\s+OPERATOR\s+FAMILY(?:\s|;|$)",
        "create_operator_class": r"(?m)^CREATE\s+OPERATOR\s+CLASS(?:\s|;|$)",
        "create_operator": r"(?m)^CREATE\s+OPERATOR(?:\s|;|$)",
        "create_materialized_view": r"(?m)^CREATE\s+MATERIALIZED\s+VIEW(?:\s|;|$)",
        "create_language": r"(?m)^CREATE\s+(?:OR\s+REPLACE\s+)?(?:TRUSTED\s+)?(?:PROCEDURAL\s+)?LANGUAGE(?:\s|;|$)",
        "create_index": r"(?m)^CREATE\s+(?:UNIQUE\s+)?INDEX(?:\s|;|$)",
        "create_group": r"(?m)^CREATE\s+GROUP(?:\s|;|$)",
        "create_function": r"(?m)^CREATE\s+(?:OR\s+REPLACE\s+)?FUNCTION(?:\s|;|$)",
        "create_foreign_table": r"(?m)^CREATE\s+FOREIGN\s+TABLE(?:\s|;|$)",
        "create_foreign_data_wrapper": r"(?m)^CREATE\s+FOREIGN\s+DATA\s+WRAPPER(?:\s|;|$)",
        "create_extension": r"(?m)^CREATE\s+EXTENSION(?:\s|;|$)",
        "create_event_trigger": r"(?m)^CREATE\s+EVENT\s+TRIGGER(?:\s|;|$)",
        "create_domain": r"(?m)^CREATE\s+DOMAIN(?:\s|;|$)",
        "create_database": r"(?m)^CREATE\s+DATABASE(?:\s|;|$)",
        "create_conversion": r"(?m)^CREATE\s+(?:DEFAULT\s+)?CONVERSION(?:\s|;|$)",
        "create_collation": r"(?m)^CREATE\s+COLLATION(?:\s|;|$)",
        "create_cast": r"(?m)^CREATE\s+CAST(?:\s|;|$)",
        "create_aggregate": r"(?m)^CREATE\s+(?:OR\s+REPLACE\s+)?AGGREGATE(?:\s|;|$)",
        "create_access_method": r"(?m)^CREATE\s+ACCESS\s+METHOD(?:\s|;|$)",
        "copy": r"(?m)^COPY(?:\s|;|$)",
        "commit_prepared": r"(?m)^COMMIT\s+PREPARED(?:\s|;|$)",
        "commit": r"(?m)^COMMIT(?:\s|;|$)",
        "comment": r"(?m)^COMMENT(?:\s|;|$)",
        "cluster": r"(?m)^CLUSTER(?:\s|;|$)",
        "checkpoint": r"(?m)^CHECKPOINT(?:\s|;|$)",
        "call": r"(?m)^CALL(?:\s|;|$)",
        "begin": r"(?m)^BEGIN(?:\s|;|$)",
        "analyze": r"(?m)^ANALYZE(?:\s|;|$)",
        "alter_view": r"(?m)^ALTER\s+VIEW(?:\s|;|$)",
        "alter_user_mapping": r"(?m)^ALTER\s+USER\s+MAPPING(?:\s|;|$)",
        "alter_user": r"(?m)^ALTER\s+USER(?:\s|;|$)",
        "alter_type": r"(?m)^ALTER\s+TYPE(?:\s|;|$)",
        "alter_trigger": r"(?m)^ALTER\s+TRIGGER(?:\s|;|$)",
        "alter_text_search_template": r"(?m)^ALTER\s+TEXT\s+SEARCH\s+TEMPLATE(?:\s|;|$)",
        "alter_text_search_parser": r"(?m)^ALTER\s+TEXT\s+SEARCH\s+PARSER(?:\s|;|$)",
        "alter_text_search_dictionary": r"(?m)^ALTER\s+TEXT\s+SEARCH\s+DICTIONARY(?:\s|;|$)",
        "alter_text_search_configuration": r"(?m)^ALTER\s+TEXT\s+SEARCH\s+CONFIGURATION(?:\s|;|$)",
        "alter_tablespace": r"(?m)^ALTER\s+TABLESPACE(?:\s|;|$)",
        "drop_collation": r"(?m)^DROP\s+COLLATION(?:\s|;|$)",
    }
    target_pattern = re.compile(
        target_patterns.get(plan.statement_key, r"(?!)"), re.IGNORECASE
    )
    for path in actual_paths:
        payload = path.read_bytes()
        sql_hashes[path.name] = _sha256(payload)
        case = case_by_filename.get(path.name)
        if case is None:
            continue
        try:
            sql = payload.decode("utf-8")
        except UnicodeDecodeError:
            issues.append(f"{path.name}: SQL is not UTF-8")
            continue
        try:
            validate_huawei_sql_header(sql)
        except ValueError as exc:
            issues.append(f"{path.name}: invalid Huawei header: {exc}")
        if f"-- case_id: {case.case_id}" not in sql:
            issues.append(f"{path.name}: missing case trace")
        if f"-- source_md: {plan.reference_path}" not in sql:
            issues.append(f"{path.name}: missing source reference trace")
        if f"-- factor_md: {plan.matrix_path}" not in sql:
            issues.append(f"{path.name}: missing factor reference trace")
        if len(re.findall(r"(?m)^-- \d+\. ", sql)) < 3:
            issues.append(f"{path.name}: fewer than three numbered stages")
        if not target_pattern.search(sql):
            issues.append(f"{path.name}: no real {plan.statement_key} statement")
        if re.search(r"\{[A-Za-z_][A-Za-z0-9_]*\}", sql):
            issues.append(f"{path.name}: unresolved SQL placeholder")
        if plan.statement_key == "abort" and "[ WORK | TRANSACTION ]" in sql:
            issues.append(f"{path.name}: unresolved ABORT EBNF")
        if re.search(r"\bpg_sleep\s*\(", sql, re.IGNORECASE):
            issues.append(f"{path.name}: pg_sleep is forbidden")
        if r"\!" in sql:
            issues.append(f"{path.name}: shell escape is forbidden")
        if case.outcome == "expected_failure" and "SQLSTATE" not in sql:
            issues.append(f"{path.name}: expected failure lacks a SQLSTATE anchor")
        if contains_create_table_statement(sql):
            table_audit = audit_complete_table_script(
                sql,
                expected_object_prefix=case.object_prefix,
            )
            if not table_audit.passed:
                issues.extend(
                    f"{path.name}: {issue}" for issue in table_audit.issues
                )
        catalog_audit = audit_catalog_observability(sql)
        if not catalog_audit.passed and case.execution_profile != "external_isolated":
            issues.extend(
                f"{path.name}: catalog audit: {issue}"
                for issue in catalog_audit.parser_issues
            )
            for query in catalog_audit.queries:
                issues.extend(
                    f"{path.name}: catalog audit: {issue}" for issue in query.issues
                )

    serial_path = output_dir / "serial_schedule"
    external_path = output_dir / "external_schedule"
    expected_serial = _schedule(plan.serial_cases)
    expected_external = _schedule(plan.external_cases)
    if not serial_path.is_file() or serial_path.read_bytes() != expected_serial:
        issues.append("serial_schedule does not exactly match same-session cases")
    if not external_path.is_file() or external_path.read_bytes() != expected_external:
        issues.append("external_schedule does not exactly match external cases")

    style_validator_path = (
        repository_root
        / "skills/regress-output-script-style/scripts/validate_regress_sql_style.py"
    )
    if not style_validator_path.is_file():
        issues.append("regress output style validator is missing")
    else:
        module_name = "_pg_case_factory_regress_output_style_validator"
        specification = importlib.util.spec_from_file_location(
            module_name, style_validator_path
        )
        if specification is None or specification.loader is None:
            issues.append("cannot load regress output style validator")
        else:
            module = importlib.util.module_from_spec(specification)
            sys.modules[module_name] = module
            try:
                specification.loader.exec_module(module)
                _, style_issues = module.validate_directory(
                    output_dir, plan.file_prefix
                )
            except Exception as exc:  # fail closed at the external style boundary
                issues.append(f"regress output style validator failed: {exc}")
            else:
                issues.extend(
                    f"{item.file}: {item.message}" for item in style_issues
                )

    row_ids = [decision.row_id for decision in plan.factor_decisions]
    if len(row_ids) != len(set(row_ids)):
        issues.append("factor disposition ledger contains duplicate canonical rows")
    for decision in plan.factor_decisions:
        if decision.disposition != "justified_na":
            missing_cases = sorted(set(decision.case_ids) - {case.case_id for case in plan.cases})
            if missing_cases:
                issues.append(
                    f"factor decision {decision.row_id} references unknown cases: "
                    + ", ".join(missing_cases)
                )

    if package is not None:
        if package.get("statement_key") != plan.statement_key:
            issues.append("package statement_key does not match")
        if package.get("cycle_fingerprint") != plan.cycle_fingerprint:
            issues.append("package cycle fingerprint does not match")
        if package.get("sql_file_count") != len(plan.cases):
            issues.append("package SQL file count does not match")
        if package.get("sql_sha256") != sql_hashes:
            issues.append("package SQL SHA-256 map does not match real files")

    return StatementRegressValidation(
        statement_key=plan.statement_key,
        cycle_fingerprint=plan.cycle_fingerprint,
        passed=not issues,
        issues=tuple(dict.fromkeys(issues)),
        sql_file_count=len(actual_paths),
        factor_value_count=len(plan.factor_decisions),
        sql_sha256=sql_hashes,
        input_sha256={
            "matrix": plan.matrix_sha256,
            "reference": plan.reference_sha256,
            "applicability_universe_semantic": plan.universe_semantic_sha256,
        },
        package_sha256=_sha256(_json_bytes(package)) if package is not None else "",
    )


def _publish_directory(target: Path, files: Mapping[str, bytes]) -> None:
    if target.exists() or target.is_symlink():
        raise RemainingStatementRegressError(
            f"output directory already exists: {target}"
        )
    target.parent.mkdir(parents=True, exist_ok=True)
    staging = Path(
        tempfile.mkdtemp(prefix=f".{target.name}.staging.", dir=target.parent)
    )
    try:
        for relative, payload in files.items():
            _write_bytes(staging / relative, payload)
        os.replace(staging, target)
    except BaseException:
        shutil.rmtree(staging, ignore_errors=True)
        raise


def generate_statement_regress_package(
    repository_root: Path,
    statement_key: str,
    *,
    output_dir: Path,
    evidence_dir: Path,
) -> StatementRegressValidation:
    """Generate one immutable statement leaf and its independent evidence bundle."""

    if output_dir.exists() or output_dir.is_symlink():
        raise RemainingStatementRegressError(
            f"output directory already exists: {output_dir}"
        )
    if evidence_dir.exists() or evidence_dir.is_symlink():
        raise RemainingStatementRegressError(
            f"evidence directory already exists: {evidence_dir}"
        )
    from .statement_factor_cycle import discover_statement_factor_cycle

    resolved_root = Path(repository_root).resolve(strict=True)
    snapshot = discover_statement_factor_cycle(resolved_root)
    plan = build_statement_regress_plan(snapshot, statement_key)
    output_files = _expected_output_files(plan, resolved_root)
    _publish_directory(output_dir, output_files)

    factor_documents: tuple[dict[str, Any], dict[str, Any], dict[str, Any]] | None = None
    if statement_key == "alter_foreign_table":
        factor_documents = _alter_foreign_table_factor_documents(
            resolved_root,
            output_files,
        )
    elif statement_key == "alter_function":
        factor_documents = _alter_function_factor_documents(
            resolved_root,
            output_files,
        )
    elif statement_key == "alter_language":
        factor_documents = _alter_language_factor_documents(
            resolved_root,
            output_files,
        )
    elif statement_key == "alter_materialized_view":
        factor_documents = _alter_materialized_view_factor_documents(
            resolved_root,
            output_files,
        )
    elif statement_key == "alter_large_object":
        factor_documents = _alter_large_object_factor_documents(
            resolved_root,
            output_files,
        )
    elif statement_key == "alter_operator":
        factor_documents = _alter_operator_factor_documents(
            resolved_root,
            output_files,
        )
    elif statement_key == "alter_operator_class":
        factor_documents = _alter_operator_class_factor_documents(
            resolved_root,
            output_files,
        )
    elif statement_key == "alter_operator_family":
        factor_documents = _alter_operator_family_factor_documents(
            resolved_root,
            output_files,
        )
    elif statement_key == "alter_group":
        factor_documents = _alter_group_factor_documents(
            resolved_root,
            output_files,
        )
    elif statement_key == "alter_role":
        factor_documents = _alter_role_factor_documents(
            resolved_root,
            output_files,
        )
    elif statement_key == "alter_index":
        factor_documents = _alter_index_factor_documents(
            resolved_root,
            output_files,
        )
    elif statement_key == "drop_collation":
        factor_documents = _drop_collation_factor_documents(
            resolved_root,
            output_files,
        )
    package = _package_document(plan, output_files, resolved_root)
    validation = _validate_rendered_output(
        plan, output_dir, resolved_root, package
    )
    if factor_documents is not None and not factor_documents[2]["passed"]:
        custom_issues = tuple(
            f"actual factor witness: {issue}"
            for issue in factor_documents[2]["issues"]
        )
        validation = StatementRegressValidation(
            statement_key=validation.statement_key,
            cycle_fingerprint=validation.cycle_fingerprint,
            passed=False,
            issues=tuple(dict.fromkeys([*validation.issues, *custom_issues])),
            sql_file_count=validation.sql_file_count,
            factor_value_count=validation.factor_value_count,
            sql_sha256=validation.sql_sha256,
            input_sha256=validation.input_sha256,
            package_sha256=validation.package_sha256,
        )
    evidence_files = {
        "plan.json": _json_bytes(plan.to_dict()),
        "coverage.json": _json_bytes(_coverage_document(plan, resolved_root)),
        "package.json": _json_bytes(package),
        "validation.json": _json_bytes(validation.to_dict()),
    }
    if factor_documents is not None:
        factor_plan, handoff, actual = factor_documents
        evidence_files.update(
            {
                "factor-loop-plan.json": _json_bytes(factor_plan),
                "handoff.json": _json_bytes(handoff),
                "actual-factor-witness-report.json": _json_bytes(actual),
            }
        )
    _publish_directory(evidence_dir, evidence_files)
    if not validation.passed:
        raise RemainingStatementRegressError(
            f"generated {statement_key} package failed validation: "
            + "; ".join(validation.issues)
        )
    return validation


def _load_json(path: Path, label: str) -> dict[str, Any]:
    if path.is_symlink() or not path.is_file():
        raise RemainingStatementRegressError(f"{label} is missing: {path}")
    try:
        document = json.loads(path.read_text(encoding="utf-8"))
    except (OSError, UnicodeError, json.JSONDecodeError) as exc:
        raise RemainingStatementRegressError(f"cannot read {label}: {exc}") from exc
    if not isinstance(document, dict):
        raise RemainingStatementRegressError(f"{label} must contain a JSON object")
    return document


def validate_statement_regress_package(
    repository_root: Path,
    statement_key: str,
    *,
    output_dir: Path,
    evidence_dir: Path,
) -> StatementRegressValidation:
    """Recompute every static statement gate against the real published files."""

    from .statement_factor_cycle import discover_statement_factor_cycle

    resolved_root = Path(repository_root).resolve(strict=True)
    snapshot = discover_statement_factor_cycle(resolved_root)
    plan = build_statement_regress_plan(snapshot, statement_key)
    expected_plan = _json_bytes(plan.to_dict())
    expected_coverage = _json_bytes(_coverage_document(plan, resolved_root))
    issues: list[str] = []
    try:
        if (evidence_dir / "plan.json").read_bytes() != expected_plan:
            issues.append("persisted plan.json differs from the reviewed plan")
        if (evidence_dir / "coverage.json").read_bytes() != expected_coverage:
            issues.append("persisted coverage.json differs from canonical decisions")
    except OSError as exc:
        issues.append(f"cannot read persisted plan/coverage evidence: {exc}")
    package = _load_json(evidence_dir / "package.json", "statement package evidence")
    validation = _validate_rendered_output(
        plan, output_dir, resolved_root, package
    )
    if statement_key == "alter_foreign_table":
        actual_output_files: dict[str, bytes] = {}
        for case in plan.cases:
            path = output_dir / case.sql_filename
            if path.is_file() and not path.is_symlink():
                actual_output_files[case.sql_filename] = path.read_bytes()
        for schedule_name in ("serial_schedule", "external_schedule"):
            path = output_dir / schedule_name
            if path.is_file() and not path.is_symlink():
                actual_output_files[schedule_name] = path.read_bytes()
        factor_plan, handoff, actual = _alter_foreign_table_factor_documents(
            resolved_root,
            actual_output_files,
        )
        expected_factor_evidence = {
            "factor-loop-plan.json": _json_bytes(factor_plan),
            "handoff.json": _json_bytes(handoff),
            "actual-factor-witness-report.json": _json_bytes(actual),
        }
        for name, expected in expected_factor_evidence.items():
            try:
                if (evidence_dir / name).read_bytes() != expected:
                    issues.append(f"persisted {name} differs from actual bytes")
            except OSError as exc:
                issues.append(f"cannot read persisted {name}: {exc}")
        if not actual["passed"]:
            issues.extend(
                f"actual factor witness: {item}" for item in actual["issues"]
            )
        if {"serial_schedule", "external_schedule"} <= set(actual_output_files):
            expected_package = _package_document(
                plan,
                actual_output_files,
                resolved_root,
            )
            if package != expected_package:
                issues.append(
                    "package evidence differs from the actual factor-loop package"
                )
    elif statement_key == "alter_function":
        actual_output_files: dict[str, bytes] = {}
        for case in plan.cases:
            path = output_dir / case.sql_filename
            if path.is_file() and not path.is_symlink():
                actual_output_files[case.sql_filename] = path.read_bytes()
        for schedule_name in ("serial_schedule", "external_schedule"):
            path = output_dir / schedule_name
            if path.is_file() and not path.is_symlink():
                actual_output_files[schedule_name] = path.read_bytes()
        factor_plan, handoff, actual = _alter_function_factor_documents(
            resolved_root,
            actual_output_files,
        )
        expected_factor_evidence = {
            "factor-loop-plan.json": _json_bytes(factor_plan),
            "handoff.json": _json_bytes(handoff),
            "actual-factor-witness-report.json": _json_bytes(actual),
        }
        for name, expected in expected_factor_evidence.items():
            try:
                if (evidence_dir / name).read_bytes() != expected:
                    issues.append(f"persisted {name} differs from actual bytes")
            except OSError as exc:
                issues.append(f"cannot read persisted {name}: {exc}")
        if not actual["passed"]:
            issues.extend(
                f"actual factor witness: {item}" for item in actual["issues"]
            )
        if {"serial_schedule", "external_schedule"} <= set(actual_output_files):
            expected_package = _package_document(
                plan,
                actual_output_files,
                resolved_root,
            )
            if package != expected_package:
                issues.append(
                    "package evidence differs from the actual factor-loop package"
                )
    elif statement_key == "alter_language":
        actual_output_files: dict[str, bytes] = {}
        for case in plan.cases:
            path = output_dir / case.sql_filename
            if path.is_file() and not path.is_symlink():
                actual_output_files[case.sql_filename] = path.read_bytes()
        for schedule_name in ("serial_schedule", "external_schedule"):
            path = output_dir / schedule_name
            if path.is_file() and not path.is_symlink():
                actual_output_files[schedule_name] = path.read_bytes()
        factor_plan, handoff, actual = _alter_language_factor_documents(
            resolved_root,
            actual_output_files,
        )
        expected_factor_evidence = {
            "factor-loop-plan.json": _json_bytes(factor_plan),
            "handoff.json": _json_bytes(handoff),
            "actual-factor-witness-report.json": _json_bytes(actual),
        }
        for name, expected in expected_factor_evidence.items():
            try:
                if (evidence_dir / name).read_bytes() != expected:
                    issues.append(f"persisted {name} differs from actual bytes")
            except OSError as exc:
                issues.append(f"cannot read persisted {name}: {exc}")
        if not actual["passed"]:
            issues.extend(
                f"actual factor witness: {item}" for item in actual["issues"]
            )
        if {"serial_schedule", "external_schedule"} <= set(actual_output_files):
            expected_package = _package_document(
                plan,
                actual_output_files,
                resolved_root,
            )
            if package != expected_package:
                issues.append(
                    "package evidence differs from the actual factor-loop package"
                )
    elif statement_key == "alter_materialized_view":
        actual_output_files: dict[str, bytes] = {}
        for case in plan.cases:
            path = output_dir / case.sql_filename
            if path.is_file() and not path.is_symlink():
                actual_output_files[case.sql_filename] = path.read_bytes()
        for schedule_name in ("serial_schedule", "external_schedule"):
            path = output_dir / schedule_name
            if path.is_file() and not path.is_symlink():
                actual_output_files[schedule_name] = path.read_bytes()
        factor_plan, handoff, actual = _alter_materialized_view_factor_documents(
            resolved_root,
            actual_output_files,
        )
        expected_factor_evidence = {
            "factor-loop-plan.json": _json_bytes(factor_plan),
            "handoff.json": _json_bytes(handoff),
            "actual-factor-witness-report.json": _json_bytes(actual),
        }
        for name, expected in expected_factor_evidence.items():
            try:
                if (evidence_dir / name).read_bytes() != expected:
                    issues.append(f"persisted {name} differs from actual bytes")
            except OSError as exc:
                issues.append(f"cannot read persisted {name}: {exc}")
        if not actual["passed"]:
            issues.extend(
                f"actual factor witness: {item}" for item in actual["issues"]
            )
        if {"serial_schedule", "external_schedule"} <= set(actual_output_files):
            expected_package = _package_document(
                plan,
                actual_output_files,
                resolved_root,
            )
            if package != expected_package:
                issues.append(
                    "package evidence differs from the actual factor-loop package"
                )
    elif statement_key == "alter_large_object":
        actual_output_files: dict[str, bytes] = {}
        for case in plan.cases:
            path = output_dir / case.sql_filename
            if path.is_file() and not path.is_symlink():
                actual_output_files[case.sql_filename] = path.read_bytes()
        for schedule_name in ("serial_schedule", "external_schedule"):
            path = output_dir / schedule_name
            if path.is_file() and not path.is_symlink():
                actual_output_files[schedule_name] = path.read_bytes()
        factor_plan, handoff, actual = _alter_large_object_factor_documents(
            resolved_root,
            actual_output_files,
        )
        expected_factor_evidence = {
            "factor-loop-plan.json": _json_bytes(factor_plan),
            "handoff.json": _json_bytes(handoff),
            "actual-factor-witness-report.json": _json_bytes(actual),
        }
        for name, expected in expected_factor_evidence.items():
            try:
                if (evidence_dir / name).read_bytes() != expected:
                    issues.append(f"persisted {name} differs from actual bytes")
            except OSError as exc:
                issues.append(f"cannot read persisted {name}: {exc}")
        if not actual["passed"]:
            issues.extend(
                f"actual factor witness: {item}" for item in actual["issues"]
            )
        if {"serial_schedule", "external_schedule"} <= set(actual_output_files):
            expected_package = _package_document(
                plan,
                actual_output_files,
                resolved_root,
            )
            if package != expected_package:
                issues.append(
                    "package evidence differs from the actual factor-loop package"
                )
    elif statement_key == "alter_group":
        actual_output_files: dict[str, bytes] = {}
        for case in plan.cases:
            path = output_dir / case.sql_filename
            if path.is_file() and not path.is_symlink():
                actual_output_files[case.sql_filename] = path.read_bytes()
        for schedule_name in ("serial_schedule", "external_schedule"):
            path = output_dir / schedule_name
            if path.is_file() and not path.is_symlink():
                actual_output_files[schedule_name] = path.read_bytes()
        factor_plan, handoff, actual = _alter_group_factor_documents(
            resolved_root,
            actual_output_files,
        )
        expected_factor_evidence = {
            "factor-loop-plan.json": _json_bytes(factor_plan),
            "handoff.json": _json_bytes(handoff),
            "actual-factor-witness-report.json": _json_bytes(actual),
        }
        for name, expected in expected_factor_evidence.items():
            try:
                if (evidence_dir / name).read_bytes() != expected:
                    issues.append(f"persisted {name} differs from actual bytes")
            except OSError as exc:
                issues.append(f"cannot read persisted {name}: {exc}")
        if not actual["passed"]:
            issues.extend(
                f"actual factor witness: {item}" for item in actual["issues"]
            )
    elif statement_key == "alter_role":
        actual_output_files: dict[str, bytes] = {}
        for case in plan.cases:
            path = output_dir / case.sql_filename
            if path.is_file() and not path.is_symlink():
                actual_output_files[case.sql_filename] = path.read_bytes()
        for schedule_name in ("serial_schedule", "external_schedule"):
            path = output_dir / schedule_name
            if path.is_file() and not path.is_symlink():
                actual_output_files[schedule_name] = path.read_bytes()
        factor_plan, handoff, actual = _alter_role_factor_documents(
            resolved_root,
            actual_output_files,
        )
        expected_factor_evidence = {
            "factor-loop-plan.json": _json_bytes(factor_plan),
            "handoff.json": _json_bytes(handoff),
            "actual-factor-witness-report.json": _json_bytes(actual),
        }
        for name, expected in expected_factor_evidence.items():
            try:
                if (evidence_dir / name).read_bytes() != expected:
                    issues.append(f"persisted {name} differs from actual bytes")
            except OSError as exc:
                issues.append(f"cannot read persisted {name}: {exc}")
        if not actual["passed"]:
            issues.extend(
                f"actual factor witness: {item}" for item in actual["issues"]
            )
    elif statement_key == "alter_index":
        actual_output_files: dict[str, bytes] = {}
        for case in plan.cases:
            path = output_dir / case.sql_filename
            if path.is_file() and not path.is_symlink():
                actual_output_files[case.sql_filename] = path.read_bytes()
        for schedule_name in ("serial_schedule", "external_schedule"):
            path = output_dir / schedule_name
            if path.is_file() and not path.is_symlink():
                actual_output_files[schedule_name] = path.read_bytes()
        factor_plan, handoff, actual = _alter_index_factor_documents(
            resolved_root,
            actual_output_files,
        )
        expected_factor_evidence = {
            "factor-loop-plan.json": _json_bytes(factor_plan),
            "handoff.json": _json_bytes(handoff),
            "actual-factor-witness-report.json": _json_bytes(actual),
        }
        for name, expected in expected_factor_evidence.items():
            try:
                if (evidence_dir / name).read_bytes() != expected:
                    issues.append(f"persisted {name} differs from actual bytes")
            except OSError as exc:
                issues.append(f"cannot read persisted {name}: {exc}")
        if not actual["passed"]:
            issues.extend(
                f"actual factor witness: {item}" for item in actual["issues"]
            )
        if {"serial_schedule", "external_schedule"} <= set(actual_output_files):
            expected_package = _package_document(
                plan,
                actual_output_files,
                resolved_root,
            )
            if package != expected_package:
                issues.append(
                    "package evidence differs from the actual factor-loop package"
                )
    elif statement_key == "drop_collation":
        actual_output_files: dict[str, bytes] = {}
        for case in plan.cases:
            path = output_dir / case.sql_filename
            if path.is_file() and not path.is_symlink():
                actual_output_files[case.sql_filename] = path.read_bytes()
        for schedule_name in ("serial_schedule", "external_schedule"):
            path = output_dir / schedule_name
            if path.is_file() and not path.is_symlink():
                actual_output_files[schedule_name] = path.read_bytes()
        factor_plan, handoff, actual = _drop_collation_factor_documents(
            resolved_root,
            actual_output_files,
        )
        expected_factor_evidence = {
            "factor-loop-plan.json": _json_bytes(factor_plan),
            "handoff.json": _json_bytes(handoff),
            "actual-factor-witness-report.json": _json_bytes(actual),
        }
        for name, expected in expected_factor_evidence.items():
            try:
                if (evidence_dir / name).read_bytes() != expected:
                    issues.append(f"persisted {name} differs from actual bytes")
            except OSError as exc:
                issues.append(f"cannot read persisted {name}: {exc}")
        if not actual["passed"]:
            issues.extend(
                f"actual factor witness: {item}" for item in actual["issues"]
            )
        if {"serial_schedule", "external_schedule"} <= set(actual_output_files):
            expected_package = _package_document(
                plan,
                actual_output_files,
                resolved_root,
            )
            if package != expected_package:
                issues.append(
                    "package evidence differs from the actual factor-loop package"
                )
    combined = tuple(dict.fromkeys([*issues, *validation.issues]))
    result = StatementRegressValidation(
        statement_key=statement_key,
        cycle_fingerprint=plan.cycle_fingerprint,
        passed=not combined,
        issues=combined,
        sql_file_count=validation.sql_file_count,
        factor_value_count=validation.factor_value_count,
        sql_sha256=validation.sql_sha256,
        input_sha256=validation.input_sha256,
        package_sha256=validation.package_sha256,
    )
    _write_bytes(evidence_dir / "validation.json", _json_bytes(result.to_dict()))
    return result
